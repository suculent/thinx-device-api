---
phase: 15-fs-finder-removal
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - lib/thinx/builder.js
  - lib/thinx/platform.js
  - lib/thinx/plugins/arduino/plugin.js
  - spec/jasmine/XBuilderSpec.js
  - spec/jasmine/PlatformSpec.js
  - spec/jasmine/PluginSpec.js
autonomous: true
requirements:
  - REFACTOR-06

must_haves:
  truths:
    - "builder.js has no require('fs-finder') and no finder.from/finder.in calls"
    - "platform.js has no require('fs-finder') and no finder.from calls"
    - "plugins/arduino/plugin.js has no require('fs-finder') and no finder.from calls"
    - "builder.cleanupSecrets still removes environment.json, environment.h, thinx.yml from a build dir"
    - "builder header-file search (HEADER_FILE_NAME) still finds files recursively via findFilesSync"
    - "platform.getPlatformFromPath still returns a platform string when thinx.yml is present"
    - "arduino plugin.check still returns 'arduino' for a dir with .ino files"
    - "XBuilderSpec, PlatformSpec, and PluginSpec all pass"
  artifacts:
    - path: "lib/thinx/builder.js"
      provides: "builder.js with all 4 fs-finder calls replaced"
      contains: "findFilesSync"
    - path: "lib/thinx/platform.js"
      provides: "platform.js with 1 fs-finder call replaced"
      contains: "findFilesSync"
    - path: "lib/thinx/plugins/arduino/plugin.js"
      provides: "arduino plugin with 1 fs-finder call replaced"
      contains: "findFilesSync"
    - path: "spec/jasmine/XBuilderSpec.js"
      provides: "Builder spec with behavior-locking finder assertions for cleanupSecrets and header search"
    - path: "spec/jasmine/PlatformSpec.js"
      provides: "Platform spec with assertion that thinx.yml is found recursively"
    - path: "spec/jasmine/PluginSpec.js"
      provides: "Plugin spec with assertion that *.ino glob works recursively"
  key_links:
    - from: "lib/thinx/builder.js"
      to: "lib/thinx/finder.js"
      via: "require('./finder')"
      pattern: "require.*finder"
    - from: "lib/thinx/platform.js"
      to: "lib/thinx/finder.js"
      via: "require('./finder')"
      pattern: "require.*finder"
    - from: "lib/thinx/plugins/arduino/plugin.js"
      to: "../../finder"
      via: "require('../../finder')"
      pattern: "require.*finder"
---

<objective>
Sweep the remaining 3 modules — `lib/thinx/builder.js` (4 call sites), `lib/thinx/platform.js` (1 call site), and `lib/thinx/plugins/arduino/plugin.js` (1 call site) — replacing all `fs-finder` usage with `findFilesSync` from `lib/thinx/finder.js`. Extend the three existing specs with behavior-locking assertions.

Purpose: Closes the last 6 of 12 call sites, making all production code free of `fs-finder` so Plan 04 can safely drop the dependency.

Output: Three modules free of `fs-finder`; three specs extended with temp-dir assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fs-finder-removal/15-CONTEXT.md
@.planning/phases/15-fs-finder-removal/15-01-SUMMARY.md

<interfaces>
<!-- Exact call sites being replaced — from CONTEXT.md table. -->

builder.js:17  — remove:   const finder = require("fs-finder");
builder.js:837 — replace:  finder.from(XBUILD_PATH).findFiles(HEADER_FILE_NAME)
                 → findFilesSync(XBUILD_PATH, HEADER_FILE_NAME, true)
                 (recursive=true; HEADER_FILE_NAME is an exact-name string like "thinx.h")
builder.js:943 — replace:  finder.in(cpath).findFiles("environment.json")
                 → findFilesSync(cpath, "environment.json", false)
                 (non-recursive; cpath is the build working dir)
builder.js:949 — replace:  finder.in(cpath).findFiles("environment.h")
                 → findFilesSync(cpath, "environment.h", false)
                 (non-recursive)
builder.js:955 — replace:  finder.in(cpath).findFiles("thinx.yml")
                 → findFilesSync(cpath, "thinx.yml", false)
                 (non-recursive)

New require in builder.js: const { findFilesSync } = require('./finder');
(builder.js already has: const { readdirSync } = require('fs'); — add finder import separately)

platform.js:1  — remove:  var finder = require("fs-finder");
platform.js:47 — replace: finder.from(local_path).findFiles('thinx.yml')
                 → findFilesSync(local_path, 'thinx.yml', true)
                 (recursive=true)

New require in platform.js: const { findFilesSync } = require('./finder');
(Add after the fs-extra import on line 2)

plugins/arduino/plugin.js:7  — remove:  var finder = require("fs-finder");
plugins/arduino/plugin.js:15 — replace: finder.from(path).findFiles('*.ino')
                                → findFilesSync(path, '*.ino', true)
                                (recursive=true; glob mask)

New require in plugins/arduino/plugin.js: const { findFilesSync } = require('../../finder');
(plugin.js is at lib/thinx/plugins/arduino/ — two levels up to reach lib/thinx/finder.js)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fs-finder in builder.js (4 call sites)</name>
  <files>lib/thinx/builder.js</files>
  <read_first>
    - lib/thinx/builder.js (read lines 1-25 for imports; lines 830-860 for the HEADER_FILE_NAME search; lines 938-960 for cleanupSecrets — understand each variable name and how the return value is used before touching anything)
    - lib/thinx/finder.js (confirm findFilesSync signature)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (call-site table — builder has both recursive and non-recursive sites; get each flag right)
  </read_first>
  <action>
    In builder.js:
    1. Remove line 17: `const finder = require("fs-finder");`
    2. Add near line 17 in its place (keep import block clean): `const { findFilesSync } = require('./finder');`
    3. Line 837: replace `finder.from(XBUILD_PATH).findFiles(HEADER_FILE_NAME)` with `findFilesSync(XBUILD_PATH, HEADER_FILE_NAME, true)`. Variable stays `h_file`. The surrounding try/catch and the null check on h_file[0] stay untouched.
    4. Line 943: replace `finder.in(cpath).findFiles("environment.json")` with `findFilesSync(cpath, "environment.json", false)`. Variable stays `env_files`. The forEach loop calling fs.unlink stays untouched.
    5. Line 949: replace `finder.in(cpath).findFiles("environment.h")` with `findFilesSync(cpath, "environment.h", false)`. Variable stays `h_files`.
    6. Line 955: replace `finder.in(cpath).findFiles("thinx.yml")` with `findFilesSync(cpath, "thinx.yml", false)`. Variable stays `yml_files`.
    No other changes. Do NOT touch async logic, callback chains, or unrelated lines.
  </action>
  <verify>
    <automated>grep -c "fs-finder\|finder\.in\|finder\.from\|finder\.findFiles" lib/thinx/builder.js</automated>
  </verify>
  <done>grep returns 0. File has one `require('./finder')` import and four `findFilesSync` calls.</done>
</task>

<task type="auto">
  <name>Task 2: Replace fs-finder in platform.js and plugins/arduino/plugin.js (1 call site each)</name>
  <files>lib/thinx/platform.js, lib/thinx/plugins/arduino/plugin.js</files>
  <read_first>
    - lib/thinx/platform.js (full file — line 1 is the finder import, line 47 is the only call site; understand the ymls variable usage at line 49-53 to confirm the replacement is drop-in)
    - lib/thinx/plugins/arduino/plugin.js (full file — line 7 is the import, line 15 is the only call site; understand how inos[] is filtered for the /lib/ exclusion below)
    - lib/thinx/finder.js (confirm findFilesSync signature)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (both are recursive .from() calls with different glob semantics)
  </read_first>
  <action>
    platform.js:
    1. Remove line 1: `var finder = require("fs-finder");`
    2. Add as the new first line (before the fs-extra require): `const { findFilesSync } = require('./finder');`
    3. Line 47: replace `finder.from(local_path).findFiles('thinx.yml')` with `findFilesSync(local_path, 'thinx.yml', true)`. Variable stays `ymls`. Downstream code (typeof ymls, ymls.length, ymls[0]) is untouched.

    plugins/arduino/plugin.js:
    1. Remove line 7: `var finder = require("fs-finder");`
    2. Add as replacement: `const { findFilesSync } = require('../../finder');` — path is two directories up because plugin.js lives at lib/thinx/plugins/arduino/.
    3. Line 15: replace `finder.from(path).findFiles('*.ino')` with `findFilesSync(path, '*.ino', true)`. Variable stays `inos`. The /lib/ filter loop below stays untouched.
    No other changes to either file.
  </action>
  <verify>
    <automated>grep -c "fs-finder\|finder\.in\|finder\.from\|finder\.findFiles" lib/thinx/platform.js lib/thinx/plugins/arduino/plugin.js</automated>
  </verify>
  <done>grep returns 0 across both files. Each file has exactly one `require` pointing to finder.js and one `findFilesSync` call.</done>
</task>

<task type="auto">
  <name>Task 3: Extend XBuilderSpec, PlatformSpec, and PluginSpec with behavior-locking assertions</name>
  <files>spec/jasmine/XBuilderSpec.js, spec/jasmine/PlatformSpec.js, spec/jasmine/PluginSpec.js</files>
  <read_first>
    - spec/jasmine/XBuilderSpec.js (full file — understand the Redis-dependent describe block; new finder tests must NOT require Redis; append separate self-contained describe block or inject new it() after existing ones where safe)
    - spec/jasmine/PlatformSpec.js (full file — it already calls Platform.getPlatformFromPath indirectly via getPlatform; new case directly tests recursive thinx.yml detection)
    - spec/jasmine/PluginSpec.js (full file — it already has arduino path test at line ~39; new case exercises *.ino glob recursion with a temp dir)
    - lib/thinx/finder.js (for the direct import in new tests)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (verification section — test recursive vs non-recursive, full absolute paths, glob masks)
  </read_first>
  <action>
    XBuilderSpec.js — append a new self-contained describe("Builder finder helpers") block (does NOT use Redis; uses os, fs, path, findFilesSync from '../../lib/thinx/finder') with two cases:
      Case A: "cleanupSecrets non-recursive: finds environment.json, environment.h, thinx.yml only at top level" — create a temp dir with those three files at root and the same three files in a subdir; call findFilesSync(root, 'environment.json', false), findFilesSync(root, 'environment.h', false), findFilesSync(root, 'thinx.yml', false); each must return exactly 1 result (the root-level file), not 2; paths must be absolute.
      Case B: "HEADER_FILE_NAME recursive search finds file in subdirectory" — create temp dir with sub/myheader.h inside; call findFilesSync(root, 'myheader.h', true); result must include the absolute path to sub/myheader.h.

    PlatformSpec.js — append one new it() case:
      Case C: "getPlatformFromPath finds thinx.yml recursively" — create a temp dir with sub/thinx.yml inside (with minimal platform: arduino content); call Platform.getPlatformFromPath(root) directly (it is a static method); assert result is not null. Clean up temp dir. This exercises the finder.from→findFilesSync recursive path in production code.

    PluginSpec.js — append one new it() case inside the existing describe("Plugins") block:
      Case D: "arduino plugin.check finds *.ino recursively and returns platform string" — use the existing './spec/test_repositories/arduino' path (already has src/src.ino); call plugins.arduino.check('./spec/test_repositories/arduino'); assert result equals 'arduino'. This validates that the *.ino glob replacement works end-to-end.

    Clean up temp dirs in afterAll or try/finally. Do NOT modify existing test cases.
  </action>
  <verify>
    <automated>npx jasmine spec/jasmine/XBuilderSpec.js spec/jasmine/PlatformSpec.js spec/jasmine/PluginSpec.js --no-color 2>&1 | tail -10</automated>
  </verify>
  <done>All three spec files exit 0. Pre-existing and new cases pass. Tail output shows no failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → builder.cleanupSecrets(cpath) | cpath is the build sandbox path — operator-controlled, not HTTP user input |
| caller → platform.getPlatformFromPath(local_path) | local_path is the checked-out repo path — CI-controlled |
| caller → plugin.check(path) | path is the checked-out repo path — CI-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | builder.js XBUILD_PATH recursive walk | accept | XBUILD_PATH is a server-side build sandbox; contents are checked-out firmware repos. No user-supplied path traversal angle — same surface as existing fs-finder call. |
| T-15-05 | Information Disclosure | cleanupSecrets reads env file list before unlink | accept | The findFilesSync call only discovers paths; it does not read file contents. The unlink calls that follow are unchanged. |
| T-15-06 | Tampering | plugins/arduino require path traversal ('../../finder') | accept | Relative require paths are resolved at server startup, not at request time. No external input can influence module resolution. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
1. `grep -rE "require.*fs-finder|finder\.(in|from|findFiles|findDirectories|showSystem)" lib/thinx/builder.js lib/thinx/platform.js lib/thinx/plugins/arduino/plugin.js` returns no matches.
2. `npx jasmine spec/jasmine/XBuilderSpec.js spec/jasmine/PlatformSpec.js spec/jasmine/PluginSpec.js` exits 0.
3. Each of the three files has exactly one `require(.*finder)` line pointing to lib/thinx/finder.js.
</verification>

<success_criteria>
builder.js, platform.js, and plugins/arduino/plugin.js contain zero fs-finder references. All three use findFilesSync from lib/thinx/finder.js. XBuilderSpec, PlatformSpec, and PluginSpec all pass with the new behavior-locking assertions. npm test suite is not broken.
</success_criteria>

<output>
Create `.planning/phases/15-fs-finder-removal/15-03-SUMMARY.md` when done.
</output>
