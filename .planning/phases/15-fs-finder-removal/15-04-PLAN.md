---
phase: 15-fs-finder-removal
plan: 04
type: execute
wave: 3
depends_on:
  - 15-02
  - 15-03
files_modified:
  - package.json
autonomous: true
requirements:
  - REFACTOR-06
  - REFACTOR-07

must_haves:
  truths:
    - "package.json dependencies no longer contains an fs-finder entry"
    - "grep -rE 'fs-finder|FsFinder|finder\\.(from|in|findFiles|findDirectories)' lib/ returns no matches"
    - "npm ls fs-finder resolves to nothing after npm install"
    - "npm test (full Jasmine suite) is green with no regressions"
  artifacts:
    - path: "package.json"
      provides: "package.json without fs-finder dependency"
      contains: "fs-extra"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "fs-finder.*github:"
---

<objective>
Drop `fs-finder` from `package.json` `dependencies`, run `npm install` to purge it from `node_modules`, and run the full Jasmine suite to confirm zero regressions. This is REFACTOR-07 and must land LAST — after every call site is confirmed clean by Plans 02 and 03.

Purpose: Completes the excision. After this plan the package is gone from the runtime dependency tree and the build will fail cleanly if any missed call site tries to require("fs-finder").

Output: `package.json` without the `fs-finder` line; a passing `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fs-finder-removal/15-CONTEXT.md
@.planning/phases/15-fs-finder-removal/15-02-SUMMARY.md
@.planning/phases/15-fs-finder-removal/15-03-SUMMARY.md

<interfaces>
<!-- The single production line being removed. -->

package.json line 55 (as of planning):
  "fs-finder": "github:suculent/Node-FsFinder#master",

This is the ONLY change to package.json. Do not touch any other dependency.
fs-extra (line 54) stays — it is still used by multiple lib/ modules.

Precondition check before editing package.json:
  grep -rE "require.*fs-finder|finder\.(in|from|findFiles|findDirectories)" lib/
  → must return 0 lines (confirmed by Plans 02 + 03 before this wave starts).

If the grep is non-empty, STOP and report — do not remove the package before all call sites are gone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify call-site sweep is complete, then remove fs-finder from package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (read lines 50-60 to see the exact fs-finder entry syntax before editing)
    - .planning/phases/15-fs-finder-removal/15-02-SUMMARY.md (confirm Plans 02 completed without issues)
    - .planning/phases/15-fs-finder-removal/15-03-SUMMARY.md (confirm Plans 03 completed without issues)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (REFACTOR-07 definition and the package.json:55 anchor)
  </read_first>
  <action>
    Step 1 — Precondition gate: run `grep -rE "require.*fs-finder|finder\.(in|from|findFiles|findDirectories)" lib/`. If any matches are found, STOP and report the file:line so the missed call site can be fixed before proceeding. Do not continue past this check if grep returns output.

    Step 2 — Edit package.json: remove the single line `"fs-finder": "github:suculent/Node-FsFinder#master",` from the `dependencies` block. Ensure the surrounding JSON remains valid (check that the preceding dependency line ends with a comma and the removed line did not leave a trailing comma on the line above). Do not modify any other field.

    Step 3 — Re-validate JSON: `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` must exit 0.
  </action>
  <verify>
    <automated>node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('fs-finder' in p.dependencies);" | grep -c "false"</automated>
  </verify>
  <done>The node one-liner prints "false" (fs-finder not in dependencies). package.json is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: npm install to purge fs-finder, then run the full test suite</name>
  <files></files>
  <read_first>
    - package.json (confirm the just-edited state is correct before running npm install)
    - AGENTS.md:93-107 (chai-http v4 lock — npm install must not bump chai-http; verify with npm ls chai-http after install)
  </read_first>
  <action>
    Step 1 — Run `npm install` (no flags). This rebuilds node_modules and removes fs-finder from the resolved tree. If npm install exits non-zero, report the error and do not proceed.

    Step 2 — Verify removal: `npm ls fs-finder 2>&1 | grep -c "fs-finder"`. Expect 0 (empty = not in tree). Note: output may contain an error line about the package not being found — that is expected and means removal succeeded.

    Step 3 — Verify chai-http lock: `npm ls chai-http 2>&1 | grep "chai-http"`. Confirm the version shown is ^4.x (not 5.x). If 5.x appears, stop and report.

    Step 4 — Run full test suite: `npm test`. Report the exit code and any failing spec names. All pre-existing specs plus the five new behavior-locking specs (FinderSpec, DeploymentSpec, RepositorySpec, XBuilderSpec/PlatformSpec/PluginSpec additions) must pass.

    Step 5 — Source-level final check: `grep -rE "fs-finder|FsFinder|finder\.(from|in|findFiles|findDirectories)" lib/`. Must return nothing.
  </action>
  <verify>
    <automated>grep -rE "fs-finder|FsFinder|finder\.(from|in|findFiles|findDirectories)" lib/ | wc -l | tr -d ' '</automated>
  </verify>
  <done>grep returns 0 lines. npm test exits 0. npm ls fs-finder shows the package is not in the resolved dependency tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Reads package.json and resolves the dependency tree from the npm registry and GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | npm install re-resolving transitive deps | mitigate | package-lock.json governs the resolved tree; removing fs-finder only drops that entry. Run `npm ls` diff before/after to verify no unexpected version bumps. |
| T-15-08 | Denial of Service | Removing fs-finder before all call sites cleared | mitigate | Precondition grep gate in Task 1 Step 1 blocks proceeding if any call site remains. The dependency removal will cause a MODULE_NOT_FOUND crash at runtime if a require("fs-finder") survives. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages are being installed — this plan only removes one entry. |
</threat_model>

<verification>
Phase 15 success criteria (from ROADMAP.md):
1. `grep -r "fs-finder\|FsFinder\|finder\.from\|finder\.in\|finder\.findFiles" lib/` — returns no matches.
2. `npm ls fs-finder` — resolves to nothing (package not in resolved dependency tree).
3. `package.json` dependencies does not contain an `fs-finder` entry.
4. Each of the 5 touched modules (builder.js, deployment.js, platform.js, repository.js, plugins/arduino/plugin.js) has a behavior-locking spec (FinderSpec, DeploymentSpec additions, RepositorySpec additions, XBuilderSpec additions, PlatformSpec additions, PluginSpec additions) that passes.
5. `npm test` (full Jasmine suite) is green.
</verification>

<success_criteria>
REFACTOR-06 and REFACTOR-07 are both complete: all 12 call sites replaced, all 5 touched modules have behavior-locking specs, fs-finder is gone from package.json and node_modules, and npm test is green.
</success_criteria>

<output>
Create `.planning/phases/15-fs-finder-removal/15-04-SUMMARY.md` when done.
</output>
