---
phase: 15-fs-finder-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - lib/thinx/deployment.js
  - lib/thinx/repository.js
  - spec/jasmine/DeploymentSpec.js
  - spec/jasmine/RepositorySpec.js
autonomous: true
requirements:
  - REFACTOR-06

must_haves:
  truths:
    - "deployment.js has no require('fs-finder') and no finder.in/finder.from calls"
    - "repository.js has no require('fs-finder') and no finder.from/showSystemFiles calls"
    - "deployment.latestFirmwarePath still returns a file path for a real device deploy dir"
    - "deployment.latestFirmwareArtifact still returns the newest *.zip path"
    - "repository.findAllRepositories returns full absolute paths to .git directories including hidden entries"
    - "Repository.findAllRepositories() called directly returns .git paths (dotfile flag survives into production code)"
    - "DeploymentSpec and RepositorySpec both pass"
  artifacts:
    - path: "lib/thinx/deployment.js"
      provides: "deployment.js with fs-finder calls replaced"
      contains: "findFilesSync"
    - path: "lib/thinx/repository.js"
      provides: "repository.js with fs-finder calls replaced"
      contains: "findDirsSync"
    - path: "spec/jasmine/DeploymentSpec.js"
      provides: "Deployment spec with behavior-locking finder assertions"
    - path: "spec/jasmine/RepositorySpec.js"
      provides: "Repository spec with behavior-locking finder assertions including production-entry-point sub-case"
  key_links:
    - from: "lib/thinx/deployment.js"
      to: "lib/thinx/finder.js"
      via: "require('./finder')"
      pattern: "require.*finder"
    - from: "lib/thinx/repository.js"
      to: "lib/thinx/finder.js"
      via: "require('./finder')"
      pattern: "require.*finder"
---

<objective>
Sweep `lib/thinx/deployment.js` and `lib/thinx/repository.js` — replace all `fs-finder` call sites with `findFilesSync` / `findDirsSync` from `lib/thinx/finder.js`, remove the `require("fs-finder")` imports, and extend the existing specs with behavior-locking assertions.

Purpose: These two modules account for 3 of the 9 call sites. They are the smallest sweep and serve as the template for Plan 03.

Output: Both modules free of `fs-finder`; both specs extended with temp-dir assertions proving non-recursive and recursive+dotfile semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-fs-finder-removal/15-CONTEXT.md
@.planning/phases/15-fs-finder-removal/15-01-SUMMARY.md

<interfaces>
<!-- Exact call sites being replaced — from CONTEXT.md table. -->

deployment.js:10  — remove:  var finder = require("fs-finder");
deployment.js:301 — replace: finder.in(dpath).findFiles(extension)
                    → findFilesSync(dpath, extension, false)
                    (non-recursive; extension is a runtime var like ".bin" or "*.bin")
deployment.js:315 — replace: finder.in(dpath).findFiles("*.zip")
                    → findFilesSync(dpath, "*.zip", false)
                    (non-recursive; the latestFirmwareArtifact method)

repository.js:7   — remove:  var finder = require("fs-finder");
repository.js:29  — replace: finder.from(repositories_path).showSystemFiles().findDirectories(".git")
                    → findDirsSync(repositories_path, ".git", true, true)
                    (recursive=true; includeDotfiles=true — showSystemFiles() effect)

New require line in each file (add near top with other requires):
  const { findFilesSync, findDirsSync } = require('./finder');

deployment.js:301 context — the call is inside a for-loop over extensions; result is used as:
  var files = findFilesSync(dpath, extension, false);
  if (files.length === 0) continue;

deployment.js:315 context — latestFirmwareArtifact method:
  var files = findFilesSync(dpath, "*.zip", false);
  return this.latestFile(files);

repository.js:29 context — findAllRepositories static method (no Redis, no instance state):
  var repo_gits = findDirsSync(repositories_path, ".git", true, true);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fs-finder in deployment.js (2 call sites)</name>
  <files>lib/thinx/deployment.js</files>
  <read_first>
    - lib/thinx/deployment.js (read fully — focus on lines 10, 295-310, 313-317; understand latestFile helper signature and how files[] is consumed)
    - lib/thinx/finder.js (confirm exported function names and signature before writing the replacement lines)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (call-site mapping table — non-negotiable behavior notes)
  </read_first>
  <action>
    In deployment.js:
    1. Remove line 10: `var finder = require("fs-finder");`
    2. Add near the top (with the other const/var requires): `const { findFilesSync } = require('./finder');` — import only what this file needs.
    3. Line 301: replace `finder.in(dpath).findFiles(extension)` with `findFilesSync(dpath, extension, false)`. The variable name stays `files` and downstream usage (files.length, files[0], this.latestFile(files)) stays untouched.
    4. Line 315: replace `finder.in(dpath).findFiles("*.zip")` with `findFilesSync(dpath, "*.zip", false)`. The variable name stays `files` and `return this.latestFile(files)` stays untouched.
    No other changes. Do NOT modify any async logic, callback shapes, or unrelated lines.
  </action>
  <verify>
    <automated>grep -c "fs-finder\|finder\.in\|finder\.from\|finder\.findFiles" lib/thinx/deployment.js</automated>
  </verify>
  <done>grep returns 0 (no fs-finder references remain). The file still has `require('./finder')` and two `findFilesSync` calls.</done>
</task>

<task type="auto">
  <name>Task 2: Replace fs-finder in repository.js (1 call site + dotfile requirement)</name>
  <files>lib/thinx/repository.js</files>
  <read_first>
    - lib/thinx/repository.js (read fully — focus on lines 7, 27-32; understand that findAllRepositories is a static method with no Redis dependency, returning repo_gits array used downstream in findAllRepositoriesWithFullname)
    - lib/thinx/finder.js (confirm findDirsSync signature: root, mask, recursive, includeDotfiles)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (showSystemFiles() note — THIS is the only call site that requires includeDotfiles=true; get it wrong and repository scan silently returns 0 repos)
  </read_first>
  <action>
    In repository.js:
    1. Remove line 7: `var finder = require("fs-finder");`
    2. Add near the top (with the other const/var requires): `const { findDirsSync } = require('./finder');`
    3. Line 29: replace `finder.from(repositories_path).showSystemFiles().findDirectories(".git")` with `findDirsSync(repositories_path, ".git", true, true)`. The fourth argument `true` (includeDotfiles) is MANDATORY — it replicates showSystemFiles() behavior. Variable name stays `repo_gits`.
    No other changes.
  </action>
  <verify>
    <automated>grep -c "fs-finder\|finder\.from\|finder\.in\|showSystemFiles\|finder\.findDir" lib/thinx/repository.js</automated>
  </verify>
  <done>grep returns 0 (no fs-finder references remain). The file has `require('./finder')` with findDirsSync and the replacement call passes includeDotfiles=true.</done>
</task>

<task type="auto">
  <name>Task 3: Extend DeploymentSpec and RepositorySpec with behavior-locking assertions</name>
  <files>spec/jasmine/DeploymentSpec.js, spec/jasmine/RepositorySpec.js</files>
  <read_first>
    - spec/jasmine/DeploymentSpec.js (full file — identify the describe block structure and existing it() tests to know where to append new tests without conflict)
    - spec/jasmine/RepositorySpec.js (full file — same reason; note that Repository.findAllRepositories already has a test at line ~39; the new sub-case validates finder semantics by calling the static method directly, which requires no Redis)
    - lib/thinx/repository.js (lines 27-32 — confirm findAllRepositories is static and reads repositories_path from app_config, NOT from an instance; the test must stub or temporarily reassign app_config.data_root + app_config.build_root to point to the temp dir)
    - lib/thinx/finder.js (confirm exported API used in new tests)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (verification section — the spec must assert: non-recursive results correct, full absolute paths returned, dotfile flag survives into production code path)
  </read_first>
  <action>
    DeploymentSpec.js — append two new it() cases inside the existing describe("Deployer") block:
      Case A: "findFilesSync non-recursive: does not descend subdirs" — create a temp dir with os.tmpdir()+fs.mkdtempSync, write one file at root level and one in a subdirectory, call findFilesSync(root, '*', false) from '../../lib/thinx/finder', assert result contains only the root-level file (length 1, path is absolute).
      Case B: "findFilesSync returns [] for nonexistent path without throwing" — call findFilesSync('/nonexistent-deployment-test-dir', '*.zip', false), assert result is an array with length 0.
    Clean up temp dir in afterAll or inside each test with a try/finally.

    RepositorySpec.js — append two new it() cases inside the existing describe("Repository") block:

      Case C (direct finder call): "findDirsSync finds .git directories including dotfiles" — create a temp dir, mkdir -p tmp/myrepo/.git inside it, call findDirsSync(tmp, '.git', true, true) from '../../lib/thinx/finder', assert result has length >= 1 and result[0] ends with '/.git'. Clean up temp dir.

      Case C2 (production entry point): "Repository.findAllRepositories returns .git paths (dotfile flag survives into production)" — create a temp dir with a myrepo/.git/ subdirectory inside it; save the original app_config.data_root and app_config.build_root values; temporarily set app_config.data_root to the temp dir root and app_config.build_root to '' (empty string so the concatenated path equals the temp dir); call Repository.findAllRepositories() (static — no Redis required); assert the returned array has length >= 1 and at least one entry ends with '/.git'; restore the original app_config values in a try/finally to avoid polluting other tests; clean up the temp dir. This sub-case catches an accidental flip of includeDotfiles to false inside repository.js — such a flip would cause findAllRepositories() to return [] even though Case C (the direct finder call) still passes.

    Do NOT import Redis, Messenger, or any network dep in these new blocks — import only os, fs, path, { findFilesSync, findDirsSync } from finder, Repository from '../../lib/thinx/repository', and app_config from '../../lib/thinx/app_config' (or however it is imported in the existing RepositorySpec.js — read the file first to match the existing require path).
    Do NOT modify existing test cases.
  </action>
  <verify>
    <automated>npx jasmine spec/jasmine/DeploymentSpec.js spec/jasmine/RepositorySpec.js --no-color 2>&1 | tail -8</automated>
  </verify>
  <done>Both spec files exit 0, all pre-existing and new tests pass (Cases A, B, C, C2). The tail output shows no failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → findFilesSync/findDirsSync | root args are operator-controlled paths (deploy dir, repo root) — not HTTP input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02a | Information Disclosure | deployment.js dpath walk | accept | dpath is constructed from Filez.deployPathForDevice(owner, udid) — server-side; owner/udid already validated upstream before this method is reached |
| T-15-02b | Information Disclosure | repository.js repositories_path walk | accept | repositories_path = app_config.data_root + app_config.build_root — operator config, not user input |
| T-15-03 | Tampering | includeDotfiles flag on repository.js call | mitigate | The includeDotfiles=true on the .git search is an explicit positional argument; code review plus spec Case C (direct call) and Case C2 (production entry point) both confirm it is set. If accidentally omitted, repo scan returns 0 repos — a loud failure, not a silent security issue. Case C2 specifically catches a flip inside repository.js that Case C alone would miss. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — this plan only edits existing .js files |
</threat_model>

<verification>
1. `grep -rE "require.*fs-finder|finder\.(in|from|findFiles|findDirectories|showSystem)" lib/thinx/deployment.js lib/thinx/repository.js` returns no matches.
2. `grep -c "require.*finder" lib/thinx/deployment.js` is 1 (the new finder.js import).
3. `grep -c "require.*finder" lib/thinx/repository.js` is 1.
4. `npx jasmine spec/jasmine/DeploymentSpec.js spec/jasmine/RepositorySpec.js` exits 0 (Cases A, B, C, C2 all pass).
</verification>

<success_criteria>
deployment.js and repository.js contain zero fs-finder references. Both use findFilesSync / findDirsSync from ./finder. Repository's .git search passes includeDotfiles=true. Spec cases A, B, C, C2 pass — notably C2 exercises Repository.findAllRepositories() directly and proves the dotfile flag survives from repository.js into the production static method. npm test suite is not broken.
</success_criteria>

<output>
Create `.planning/phases/15-fs-finder-removal/15-02-SUMMARY.md` when done.
</output>
