---
phase: 15-fs-finder-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/thinx/finder.js
  - spec/jasmine/FinderSpec.js
autonomous: true
requirements:
  - REFACTOR-06

must_haves:
  truths:
    - "A synchronous findFilesSync(root, mask, recursive, includeDotfiles) function exists and is exported"
    - "A synchronous findDirsSync(root, mask, recursive, includeDotfiles) function exists and is exported"
    - "findFilesSync with recursive=false returns only direct-child files (not grandchildren)"
    - "findFilesSync with recursive=true descends all subdirs and returns full absolute paths"
    - "findDirsSync with includeDotfiles=true returns hidden directories (e.g. .git)"
    - "glob masks (*.ino, *.zip) work correctly"
    - "exact-name masks (thinx.yml, environment.json) work correctly"
    - "empty match returns [] not an exception"
    - "FinderSpec exits 0 on npm test"
  artifacts:
    - path: "lib/thinx/finder.js"
      provides: "Shared sync file-walk primitives"
      exports: ["findFilesSync", "findDirsSync"]
    - path: "spec/jasmine/FinderSpec.js"
      provides: "Behavior-locking spec for finder helpers"
      contains: "temp-dir fixtures"
  key_links:
    - from: "lib/thinx/finder.js"
      to: "lib/thinx/builder.js"
      via: "require('./finder')"
      pattern: "require.*finder"
    - from: "lib/thinx/finder.js"
      to: "lib/thinx/deployment.js"
      via: "require('./finder')"
      pattern: "require.*finder"
---

<objective>
Create `lib/thinx/finder.js` — a single module exporting two synchronous file-walk primitives that will replace all 12 `fs-finder` call sites in Wave 2.

Purpose: Centralise the walk logic in one place so Wave 2 module sweeps are surgical one-liners, and a single spec proves all semantic invariants (recursive vs non-recursive, dotfiles, glob, full-path) before any production call site is touched.

Output: `lib/thinx/finder.js` (two exported functions) + `spec/jasmine/FinderSpec.js` (temp-dir fixture spec).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-fs-finder-removal/15-CONTEXT.md

<interfaces>
<!-- Semantics being replaced — extracted from CONTEXT.md and confirmed against source. -->
<!-- Executor must implement these contracts EXACTLY. -->

fs-finder semantics (from lib/node_modules/fs-finder/lib/Finder.js behavior, confirmed in CONTEXT.md):

finder.in(path)        → non-recursive (single directory level)
finder.from(path)      → recursive (all subdirectories)
.findFiles(mask)       → returns array of full/absolute file paths; [] if none
.findDirectories(mask) → returns array of full/absolute dir paths; [] if none
.showSystemFiles()     → include dotfiles/hidden entries (required for .git search)

mask examples in production:
  "*.zip"              glob — all zip files
  "*.ino"              glob — all Arduino sketches
  'thinx.yml'          exact name
  'environment.json'   exact name
  'environment.h'      exact name
  '.git'               exact name (dotfile/hidden dir)
  HEADER_FILE_NAME     runtime variable (exact name at call time)
  extension            runtime variable (caller supplies e.g. ".bin")

All callers use return value INLINE and SYNCHRONOUSLY — no Promises permitted.

Node engine: >=19.x (v22 in CI); fs.readdirSync(path, { recursive: true }) is available (Node 18.17+).
fs.readdirSync(path, { recursive: true }) returns relative path strings from root — must be joined with root to form absolute paths.
fs.readdirSync(path, { withFileTypes: true }) returns Dirent objects — use for non-recursive to filter by isFile()/isDirectory().

Existing lib/thinx/util.js is web-request utilities only — do NOT add walk helpers there.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/thinx/finder.js with findFilesSync and findDirsSync</name>
  <files>lib/thinx/finder.js</files>
  <read_first>
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (section: "The 12 call sites" table + behavior-preservation constraints — the exact semantics contract)
    - lib/thinx/util.js (negative example: do NOT model finder.js after this class; finder.js exports plain functions, not a class)
    - lib/thinx/builder.js:17 (how the import will look: const { findFilesSync, findDirsSync } = require('./finder');)
  </read_first>
  <behavior>
    - findFilesSync(root, mask, recursive=false, includeDotfiles=false): returns string[] of absolute paths to files matching mask under root.
    - findDirsSync(root, mask, recursive=false, includeDotfiles=false): returns string[] of absolute paths to directories matching mask under root.
    - mask is either a glob pattern containing '*' or an exact filename string.
    - Glob: only '*' wildcard supported (fs-finder used simple prefix/suffix matching, not full minimatch). Implement as: split on '*', check startsWith(prefix) && endsWith(suffix) on the basename.
    - Exact: basename === mask.
    - recursive=false: read only direct children of root using fs.readdirSync(root, { withFileTypes: true }). Do NOT descend into subdirs.
    - recursive=true: use fs.readdirSync(root, { recursive: true }) to get all relative paths, then filter by type and mask. Reconstruct absolute path as path.join(root, relPath).
    - includeDotfiles=false (default): skip entries whose basename starts with '.'.
    - includeDotfiles=true: include hidden entries (required for .git directory search in repository.js).
    - If root does not exist or is not a directory, return [] (do not throw — fs-finder returned [] on missing dir; callers depend on this).
    - Export as module.exports = { findFilesSync, findDirsSync } (plain object, not a class).
  </behavior>
  <action>
    Create lib/thinx/finder.js. Import only 'fs' (core) and 'path' (core) — no third-party deps. Export two named functions: findFilesSync and findDirsSync with the signatures and semantics described in the behavior block. Add a brief JSDoc comment on each function documenting the mask convention (glob '*' or exact name) and the recursive/includeDotfiles flags. Guard the root-not-a-directory case with fs.existsSync + fs.statSync().isDirectory() before readdirSync; return [] if the guard fails. For recursive=true, the built-in Node 18.17+ fs.readdirSync(root, { recursive: true }) returns relative string paths; filter them further with path.basename() for mask matching and fs.statSync(absPath).isFile() / isDirectory() for type discrimination. Do not use any dynamic require() or eval().
  </action>
  <verify>
    <automated>node -e "const f = require('./lib/thinx/finder'); console.log(typeof f.findFilesSync, typeof f.findDirsSync);" | grep -c "function function"</automated>
  </verify>
  <done>lib/thinx/finder.js exists, exports findFilesSync and findDirsSync as functions, and the one-liner node check exits 0 printing "function function".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavior-locking spec FinderSpec.js with temp-dir fixtures</name>
  <files>spec/jasmine/FinderSpec.js</files>
  <read_first>
    - lib/thinx/finder.js (just created — spec must cover every exported function and every code path)
    - spec/jasmine/XBuilderSpec.js (structure pattern: describe/beforeAll/afterAll, chai expect, require path)
    - spec/jasmine/PluginSpec.js (simpler structure pattern for non-Redis specs)
    - .planning/phases/15-fs-finder-removal/15-CONTEXT.md (call-site table: the spec must exercise all distinct semantics: non-recursive findFiles, recursive findFiles, recursive findDirs with dotfiles, glob mask, exact-name mask, empty result)
  </read_first>
  <behavior>
    - beforeAll: create a fresh temp dir with os.tmpdir() + fs.mkdtempSync; build this layout inside it:
        root/
          alpha.ino
          bravo.zip
          thinx.yml
          environment.json
          environment.h
          sub/
            deep.ino
            deeper/
              .git/       (hidden dir — must be found by findDirsSync with includeDotfiles=true)
    - afterAll: fs.rmSync(tmpRoot, { recursive: true, force: true }) to clean up.
    - Test cases (all using the single temp fixture):
      1. findFilesSync(root, '*.ino', false) returns exactly [path.join(root,'alpha.ino')] (non-recursive: sub/deep.ino excluded).
      2. findFilesSync(root, '*.ino', true) returns two paths including sub/deep.ino (recursive).
      3. findFilesSync(root, '*.zip', false) returns [path.join(root,'bravo.zip')].
      4. findFilesSync(root, 'thinx.yml', false) returns [path.join(root,'thinx.yml')].
      5. findFilesSync(root, 'environment.json', false) returns [path.join(root,'environment.json')].
      6. findFilesSync(root, 'environment.h', false) returns [path.join(root,'environment.h')].
      7. findDirsSync(root, '.git', true, true) returns [path.join(root,'sub','deeper','.git')] — dotfile dir found recursively.
      8. findDirsSync(root, '.git', true, false) returns [] — dotfiles excluded when includeDotfiles=false.
      9. findFilesSync('/nonexistent/path', '*.ino', false) returns [] without throwing.
      10. findFilesSync(root, '*.nomatch', false) returns [].
    - All returned paths must be absolute (start with '/').
  </behavior>
  <action>
    Create spec/jasmine/FinderSpec.js. Use CommonJS require() (chai-http v4 constraint — all specs stay CJS). Import: os, fs, path from Node core; expect from 'chai'; findFilesSync and findDirsSync from '../../lib/thinx/finder'. In beforeAll build the fixture tree with fs.mkdirSync (recursive: true) for sub/deeper/.git/ and fs.writeFileSync for the leaf files. Each test case maps to one it() block. Use expect(result).to.be.an('array') + expect(result).to.have.lengthOf(N) + expect(result[0]).to.equal(expectedAbsPath) or expect(result).to.deep.include(path). Do NOT require Redis, Messenger, or any network-dependent module — this spec must be self-contained and fast.
  </action>
  <verify>
    <automated>npx jasmine spec/jasmine/FinderSpec.js --no-color 2>&1 | tail -5</automated>
  </verify>
  <done>FinderSpec.js exits 0, all 10 test cases pass, temp dir is cleaned up after the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → findFilesSync/findDirsSync | root argument comes from caller-supplied config paths (e.g. app_config.data_root); not from user HTTP input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | findFilesSync root parameter | accept | root is sourced from server-side app_config / build paths set by operators, not from HTTP request bodies. No new external attack surface vs. the fs-finder calls being replaced. |
| T-15-02 | Information Disclosure | Recursive walk escaping intended subtree via symlink | mitigate | Use fs.readdirSync with built-in Node recursion (does not follow symlinks by default); add a note in JSDoc that symlinks are not followed. Do not use fs.realpathSync — this preserves fs-finder's existing behaviour. |
| T-15-03 | Denial of Service | Unbounded recursion on deep directory trees | accept | fs.readdirSync({recursive:true}) is bounded by OS limits; identical exposure to fs-finder which also walked entire trees. Scope is internal build paths not writable by API callers. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — lib/thinx/finder.js uses only Node core (fs, path). |
</threat_model>

<verification>
1. `node -e "const f = require('./lib/thinx/finder'); console.log(typeof f.findFilesSync === 'function' && typeof f.findDirsSync === 'function');"` prints `true`.
2. `npx jasmine spec/jasmine/FinderSpec.js` exits 0, all 10 cases pass.
3. `grep -n "require\|import" lib/thinx/finder.js` shows only 'fs' and 'path' — no third-party deps.
</verification>

<success_criteria>
lib/thinx/finder.js exists with findFilesSync and findDirsSync exported. FinderSpec.js passes all 10 behavior-locking cases covering: non-recursive file find, recursive file find, recursive dotfile directory find, glob mask, exact-name mask, and empty-result safety. npm test suite is not broken.
</success_criteria>

<output>
Create `.planning/phases/15-fs-finder-removal/15-01-SUMMARY.md` when done.
</output>
