---
phase: 22-ci-sast-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/codeql-analysis.yml
  - .circleci/config.yml
  - .planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md
  - .planning/phases/22-ci-sast-baseline/22-CODEQL-ALERTS.json
autonomous: true
requirements: [CI-01, CI-02]
tags: [ci, codeql, circleci, sast]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A push to thinx-staging runs the rewritten CodeQL workflow (github/codeql-action init and analyze at v4, actions/checkout at v7, languages javascript-typescript, build-mode none, queries security-extended) to a success conclusion, and its analysis for the pushed commit is listed by the code-scanning analyses API under refs/heads/thinx-staging (CI-01, D-04, D-05)"
    - "The workflow triggers on push to main and thinx-staging, on pull_request to main, and on the weekly schedule; it has exactly three steps, none of them a run step, and keeps the least-privilege permissions block (contents read at top level; contents read, security-events write, actions read on the job) (D-04)"
    - "paths-ignore covers node_modules, minified JS, the vendored builders/lua-inspect tree, spec/ and the three test-harness scripts, so no baseline alert path starts with spec/, builders/lua-inspect/ or node_modules/ (D-06)"
    - "GitHub CodeQL default setup reads not-configured before the first push and again after the first analysis (CI-01, D-08)"
    - "The D-01 pre-check evidence is recorded before the edit: docker-compose.test.yml, Dockerfile.test, services/broker/Dockerfile.test, the worker builder script and the last green test job log reference no image on registry.thinx.cloud:5000 (D-01)"
    - "The CircleCI test job has no direct private-registry login, no docker login anywhere in .circleci/config.yml passes a password as an argv flag, and the test job for the CI-02 commit on thinx-staging finishes with status success (CI-02, D-01)"
    - "The two docker.io logins at the Hub-publish steps and the dhi.io login in the test job are unchanged and still read the password from stdin; the registry-login command gets no new parameters (D-02)"
    - "The CI-02 commit adds nothing to .circleci/config.yml beyond the D-01 fallback step: no docker push retry and no CircleCI serialization group (D-03)"
    - "Adjacency edge (CI-02): the CI-02 commit's numstat for .circleci/config.yml is exactly 0 added / 1 deleted (delete path) or 2 added / 1 deleted (fallback path), so the adjacent dhi.io login and its comment block stay byte-identical"
    - "Empty edge (CI-02): the count of direct private-registry docker login lines outside the registry-login command definition is 0, and the count of argv-password logins is 0"
    - "Ordering edge (CI-02): in the test job's Starting Support Services step, the dhi.io login still precedes the compose start of mosquitto, transformer, worker and couchdb; on the fallback path the registry-login step precedes that run step"
    - "22-CODEQL-BASELINE.md records the analysis id, commit SHA, run URL, query suite, totals by severity, counts by rule, counts by file (both sorted by count descending, then name ascending), a lib/thinx/git.js and lib/thinx/builder.js section (with an explicit 'none' when empty), and a trigger-evidence table. Its 'Total open alerts' equals the length of 22-CODEQL-ALERTS.json (D-07)"
    - "No CodeQL alert on refs/heads/thinx-staging is dismissed during this phase: the dismissed-alert count stays 0 (D-07)"
  artifacts:
    - path: ".github/workflows/codeql-analysis.yml"
      provides: "CodeQL advanced-setup workflow for javascript-typescript on main/thinx-staging pushes and PRs to main"
      contains: "github/codeql-action/init@v4"
    - path: ".circleci/config.yml"
      provides: "Test job without the raw private-registry login"
      contains: "--password-stdin dhi.io"
    - path: ".planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md"
      provides: "Phase 23 'before' evidence: CodeQL alert counts by severity, rule and file, plus the git.js/builder.js sinks"
      contains: "Total open alerts:"
    - path: ".planning/phases/22-ci-sast-baseline/22-CODEQL-ALERTS.json"
      provides: "Reduced alert list (number, rule_id, severity, security_severity_level, path, start_line, state)"
  key_links:
    - from: ".github/workflows/codeql-analysis.yml"
      to: "GitHub code scanning (analyses API, refs/heads/thinx-staging)"
      via: "github/codeql-action/analyze@v4 SARIF upload with security-events: write"
      pattern: "codeql-action/analyze@v4"
    - from: ".circleci/config.yml test job"
      to: "docker-compose.test.yml services started via the remote Docker engine"
      via: "the Starting Support Services run step (dhi.io login, then compose up)"
      pattern: "docker compose up -d mosquitto transformer worker couchdb"
    - from: ".planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md"
      to: "Phase 23 sink-hardening verification"
      via: "the lib/thinx/git.js and lib/thinx/builder.js alert section (alert numbers and lines)"
      pattern: "## lib/thinx/git.js and lib/thinx/builder.js"
  prohibitions:
    - statement: "MUST NOT dismiss, suppress or fix any CodeQL alert in this phase, or widen paths-ignore beyond vendored/minified assets and tests to make the baseline smaller"
      status: resolved
      verification: judgment
    - statement: "MUST NOT put alert message text, code snippets, alert html_url values or exploitation notes into the committed baseline files; this repository is public, so the baseline carries only alert numbers, rule ids, severities, paths and line numbers"
      status: resolved
      verification: judgment
    - statement: "MUST NOT push to main; every push in this plan goes to thinx-staging"
      status: resolved
      verification: judgment
---

<objective>
Land the two CI-config requirements on `thinx-staging` and capture the CodeQL "before" baseline.

1. **CI-01 (the phase tracer).** Rewrite `.github/workflows/codeql-analysis.yml` per D-04, D-05 and D-06, recheck that default setup is off (D-08), push to `thinx-staging`, and prove end to end that the analysis appears in code scanning.
2. **CI-02.** Run the D-01 pre-check, then delete the raw private-registry login at `.circleci/config.yml:767`. Use the `registry-login` fallback only if the pre-check finds a private-registry pull. D-02 and D-03 are honoured. Push, and prove the CircleCI `test` job is green for that commit.
3. **D-07.** Record the alert baseline in `22-CODEQL-BASELINE.md` plus the reduced `22-CODEQL-ALERTS.json`. Phase 23 consumes them as its "before" evidence.

Purpose: CI gives trustworthy signals before any v1.14 code change lands. The rest of the phase is ordered after this plan (22-02 pushes to the same branch, 22-03 opens the PR).
Output: the rewritten workflow, the edited CircleCI config, and the two baseline files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-ci-sast-baseline/22-CONTEXT.md
@.planning/phases/22-ci-sast-baseline/22-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@AGENTS.md
@.github/workflows/codeql-analysis.yml
@.circleci/config.yml

Facts verified at planning time (2026-09-25; re-verify, do not trust blindly):
- `gh` is authenticated as `suculent`. The repo is **public**. The Actions policy is `allowed_actions: selected` with `github_owned_allowed: true` and `verified_allowed: false`, so GitHub-owned actions only. The tags `actions/checkout` `v7` (v7.0.1) and `github/codeql-action` `v4` both exist. `init@v4` accepts the inputs `languages`, `build-mode`, `queries` and `config`.
- Default setup: `state: not-configured`. The only CodeQL analyses on record are old ones on `refs/heads/master`. The dismissed CodeQL alert count is 0.
- `origin/thinx-staging` is `115acffb`. Local `thinx-staging` is 6 planning-doc commits ahead, and they ride along with the first push. `origin/main` is 17 commits behind staging.
- **PATTERNS.md drift:** the CircleCI `test` job renames `docker-compose.test.yml` to `docker-compose.yml` (step "Switch to Test Dockerfiles", config.yml around line 700). The compose file the test job actually runs is therefore `docker-compose.test.yml`, not the root `docker-compose.yml` that PATTERNS inspected. Its images are `dhi.io/couchdb:3`, `thinxcloud/redis`, `thinxcloud/transformer`, `thinxcloud/worker:latest`, `influxdb:1.8`, and builds from `services/broker/Dockerfile.test` (`FROM thinxcloud/mosquitto`) and the root `Dockerfile.test` (`FROM golang:1.26.8-alpine3.24`, `FROM thinxcloud/base:latest`). The worker pulls only `suculent/*-docker-build` images (`services/worker/builder`). The public CircleCI API shows 0 occurrences of `registry.thinx.cloud` in every step output of the last green `test` job (build 15389).
- The CircleCI v1.1 API is readable without a token for this project. Use `https://circleci.com/api/v1.1/project/github/suculent/thinx-device-api/tree/thinx-staging?limit=N`. Each item has `build_num`, `vcs_revision`, `status`, `lifecycle` and `workflows.job_name`. No CircleCI token exists on this machine.
- In this shell `grep` is a ugrep wrapper that honours ignore files during recursive search. Use `git grep` for repo-wide searches. Plain `grep` on named files or on piped input behaves normally.
- PyYAML loads the workflow key `on` as the boolean `True`. Checks read `d.get("on", d.get(True))`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): CodeQL end to end on thinx-staging (rewritten workflow, push, analysis visible in code scanning)</name>
  <precondition>`gh api repos/suculent/thinx-device-api/code-scanning/default-setup --jq .state` prints `not-configured` (gh is authenticated with admin on the repo)</precondition>
  <reversibility rating="reversible">An advanced-setup workflow on a non-required check. Reverting the file or deleting the analyses undoes it, and nothing else reads its output yet.</reversibility>
  <files>.github/workflows/codeql-analysis.yml</files>
  <read_first>
    - .github/workflows/codeql-analysis.yml (the whole 67-line file being replaced; keep lines 12-26 verbatim)
    - .planning/phases/22-ci-sast-baseline/22-CONTEXT.md (D-04, D-05, D-06, D-08 and Claude's Discretion)
    - .planning/phases/22-ci-sast-baseline/22-PATTERNS.md (section on the codeql-analysis.yml rewrite)
    - .planning/research/PITFALLS.md (Pitfall 17)
    - .planning/research/STACK.md (codeql-action v4 / checkout v7 rows, GitHub-owned-actions-only policy)
  </read_first>
  <action>
Recheck D-08 first. Run `gh api repos/suculent/thinx-device-api/code-scanning/default-setup --jq .state`. If it prints anything other than `not-configured`, STOP: an advanced workflow's SARIF upload is rejected while default setup is on. Report this rather than turning default setup off.

Rewrite `.github/workflows/codeql-analysis.yml` in place per D-04. It must contain exactly the following and nothing from the old job body:
- `name: "CodeQL"`.
- A short header comment, 3 lines at most. It says this is advanced setup, that GitHub default setup must stay off because the two conflict on SARIF upload, and that the check is informational and not a required status check.
- Triggers under `on`:
  - `push` with `branches: [main, thinx-staging]`
  - `pull_request` with `branches: [main]`
  - `schedule` with the existing cron `'0 18 * * 5'`, kept per D-04; the exact time is Claude's discretion and stays unchanged
- The existing least-privilege block, copied verbatim from current lines 12-26:
  - the 3-line comment
  - top-level `permissions: contents: read`
  - `jobs: analyze:` with `name: Analyze` and `runs-on: ubuntu-latest`
  - job-level `permissions` of `contents: read`, `security-events: write` and `actions: read`, with their inline comments
- No `strategy`/matrix key. Claude's discretion: collapse to a single job.
- Exactly three steps:
  1. `name: Checkout repository`, `uses: actions/checkout@v7`, with no `with:` block. The default `submodules: false` is what keeps `services/console` and the other submodules out of the scan, so no paths rule is needed for them (D-06).
  2. `name: Initialize CodeQL`, `uses: github/codeql-action/init@v4`, with these inputs:
     - `languages: javascript-typescript`
     - `build-mode: none`
     - `queries: security-extended` (D-05)
     - a literal-block `config: |` holding a `paths-ignore:` list with exactly these entries: `node_modules`, `'**/*.min.js'`, `'builders/lua-inspect/**'` (the vendored Lua inspector and its jquery-1.4.2.min.js), `'spec/**'`, `'scripts/test-*.js'`, `test_cert_probe_runner.js`, `verify_path_traversal_fix.js`. This is the D-06 intent: vendored and minified assets plus tests. `lib/`, the root modules, the other `scripts/` and `static/` stay scanned.
  3. `name: Perform CodeQL Analysis`, `uses: github/codeql-action/analyze@v4`, with `category: "/language:javascript-typescript"`.

Do not add any `run:` step. JavaScript extraction with `build-mode: none` needs no dependency install and no PR-head checkout. Do not add any non-GitHub-owned action, because repo policy rejects them at queue time.

Validate locally with the first automated check below. Commit it alone as `ci(22): rewrite CodeQL workflow for main/thinx-staging (codeql-action v4, checkout v7)`. Push to the staging branch only, with `git push origin thinx-staging`, per AGENTS.md and the push-staging-only rule. Never push to `main`.

Then follow the run:
- Find the run with `gh run list --workflow codeql-analysis.yml --branch thinx-staging --event push --limit 10 --json databaseId,headSha,status,conclusion,url`.
- Its `headSha` equals `git log -1 --format=%H -- .github/workflows/codeql-analysis.yml`. Poll every 20 s until the run appears, up to 5 min.
- Run `gh run watch {databaseId} --interval 30 --exit-status`.
- If the run fails, read `gh run view {databaseId} --log-failed`, fix the workflow, and re-push. It must finish green.
- After success, record the run URL and the analysis id, `commit_sha` and `results_count` from the analyses API for the SUMMARY and for Task 3. Recheck that default setup still reads `not-configured`.
  </action>
  <verify>
    <automated>python3 -c 'import yaml,sys; d=yaml.safe_load(open(sys.argv[1])); on=d.get("on", d.get(True)); assert on["push"]["branches"]==["main","thinx-staging"], on; assert on["pull_request"]["branches"]==["main"]; assert on["schedule"][0]["cron"]; assert d["permissions"]=={"contents":"read"}; j=d["jobs"]["analyze"]; assert j["permissions"]=={"contents":"read","security-events":"write","actions":"read"}; assert "strategy" not in j; s=j["steps"]; assert len(s)==3 and not any("run" in x for x in s); assert s[0]["uses"]=="actions/checkout@v7"; w=s[1]["with"]; assert s[1]["uses"]=="github/codeql-action/init@v4" and w["languages"]=="javascript-typescript" and w["build-mode"]=="none" and w["queries"]=="security-extended"; pi=yaml.safe_load(w["config"])["paths-ignore"]; assert {"node_modules","**/*.min.js","builders/lua-inspect/**","spec/**"}.issubset(set(pi)), pi; assert s[2]["uses"]=="github/codeql-action/analyze@v4"; print("WF-OK")' .github/workflows/codeql-analysis.yml</automated>
    <fails_when>non-zero exit (AssertionError or a YAML parse error in the traceback), or stdout lacks WF-OK</fails_when>
    <automated>S=$(git log -1 --format=%H -- .github/workflows/codeql-analysis.yml) && test -n "$S" && gh run list --workflow codeql-analysis.yml --branch thinx-staging --event push --limit 20 --json headSha,conclusion | jq -r --arg s "$S" '[.[] | select(.headSha==$s)][0].conclusion'</automated>
    <fails_when>output is anything other than exactly `success` (`null` means no push run exists for the workflow commit; `failure` or `cancelled` means the run did not complete green)</fails_when>
    <automated>S=$(git log -1 --format=%H -- .github/workflows/codeql-analysis.yml) && test -n "$S" && N=$(gh api 'repos/suculent/thinx-device-api/code-scanning/analyses?ref=refs/heads/thinx-staging&tool_name=CodeQL&per_page=30' | jq --arg s "$S" '[.[] | select(.commit_sha==$s and .error=="")] | length'); [ "$N" -ge 1 ] && echo "ANALYSIS-VISIBLE $N"</automated>
    <fails_when>no `ANALYSIS-VISIBLE` line printed (N is 0: the SARIF upload did not reach code scanning for the workflow commit, or it carries an error)</fails_when>
    <automated>gh api repos/suculent/thinx-device-api/code-scanning/default-setup --jq .state</automated>
    <fails_when>output is anything other than exactly `not-configured`</fails_when>
  </verify>
  <acceptance_criteria>
    - The python structural check prints `WF-OK`.
    - `gh run list … --json headSha,conclusion` for the workflow commit's SHA prints `success`.
    - The analyses API lists at least one error-free CodeQL analysis for that SHA under `refs/heads/thinx-staging`. The check prints `ANALYSIS-VISIBLE {n}` with n ≥ 1.
    - The default-setup state prints `not-configured` both before the push and after the run.
    - `git log origin/main -1 --format=%H` is unchanged by this task, because nothing was pushed to main.
    - The SUMMARY records the run URL, the analysis id, the `results_count` and the commit SHA.
  </acceptance_criteria>
  <done>A thinx-staging push produced a green CodeQL javascript-typescript analysis (v4, checkout v7, build-mode none, security-extended). It is visible in code scanning for the pushed commit, and default setup is still off.</done>
</task>

<task type="auto">
  <name>Task 2: CI-02 — D-01 pre-check, then remove the raw private-registry login from the test job; prove the test job green</name>
  <files>.circleci/config.yml</files>
  <read_first>
    - .circleci/config.yml (lines 9-46 the registry-login command; lines 615-790 the test job, especially the "Switch to Test Dockerfiles" and "Starting Support Services" steps; lines 225-240 and 300-312 the docker.io logins)
    - docker-compose.test.yml (every image: and build: key; this is the compose file the test job actually runs)
    - Dockerfile.test (FROM lines 5 and 31)
    - services/broker/Dockerfile.test (FROM line 1)
    - services/worker/builder (docker pull lines; builder images the worker pulls through the socket)
    - .env.dist (line 3 sets REGISTRY; confirm nothing in the test path consumes it)
    - .planning/phases/22-ci-sast-baseline/22-CONTEXT.md (D-01, D-02, D-03)
    - .planning/research/PITFALLS.md (Pitfall 18)
  </read_first>
  <action>
**D-01 pre-check** (read-only; record every result in the SUMMARY):
- (a) `git grep -n 'registry\.thinx' -- ':!.planning' ':!.circleci/config.yml'` must list only `.env.dist:3`, the `REGISTRY=` default.
- (b) `git grep -nw REGISTRY -- docker-compose.test.yml Dockerfile.test lib spec thinx-core.js thinx.js` must print nothing, so the `.env` value is never consumed on the test path.
- (c) `grep -n -E 'image:|^FROM' docker-compose.test.yml Dockerfile.test services/broker/Dockerfile.test` and `grep -n 'docker pull' services/worker/builder` must show only Docker Hub (`thinxcloud/*`, `suculent/*`, `influxdb`, `golang`) and `dhi.io` references.
- (d) Find the newest `test` job with status `success` via the CircleCI v1.1 tree endpoint for thinx-staging. Fetch `https://circleci.com/api/v1.1/project/github/suculent/thinx-device-api/{build_num}`. For every step's `actions[0].output_url`, download the output and count lines matching `registry.thinx.cloud` in the `.[].message` text. The total must be 0.

If all four hold, take the **delete path**. Delete exactly line 767 of `.circleci/config.yml`: the raw login to the private registry that passes the password as a command-line flag, in the test job's "Starting Support Services" step. Nothing else changes: the comment block at lines 768-770, the dhi.io stdin login at 771 and the compose start line stay byte-identical.

If any pre-check finds a pull from `registry.thinx.cloud:5000`, take the **fallback path (D-01)**. Delete the same line and insert a new step `- registry-login:` with the parameter `registry: registry.thinx.cloud:5000`, at the test job's step indentation (4 spaces before the dash, parameter at 8), immediately **before** the `- run:` whose name is `Starting Support Services`. It is a CircleCI command, so it cannot live inside the run's shell text. The test job's `thinx-docker-repo` context already supplies DOCKER_LOGIN/DOCKER_PASSWORD, and the command's preflight fails fast if they are empty.

Per D-02, leave the docker.io logins (around lines 235 and 309) and the dhi.io login untouched, and do not add parameters to `registry-login`. Per D-03, add no push retry wrapper and no CircleCI serialization group.

Validate with the YAML-parse and gate checks below, then commit alone as `ci(22): drop raw private-registry login from the CircleCI test job (CI-02)`.

Before pushing, wait until Task 1's CircleCI pipeline has drained. Push contention on the private registry is the known flake cause (Pitfall 18). Poll the thinx-staging tree endpoint (`limit=30`) every 30 s, up to 30 min, until the count of items whose `lifecycle` is not `finished` is 0. Then push with `git push origin thinx-staging`.

Follow the `test` job whose `vcs_revision` equals the pushed SHA until its `lifecycle` is `finished`. Poll every 30 s, up to 40 min. It must end `success`. If it fails, read the failing step's output_url. If the failure is registry-related, switch to the fallback path and re-push; otherwise diagnose. Record the build number and URL.
  </action>
  <verify>
    <automated>python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1])); print("YAML-OK")' .circleci/config.yml</automated>
    <fails_when>non-zero exit or a yaml ScannerError/ParserError traceback; YAML-OK absent</fails_when>
    <automated>A=$(grep -v '^[[:space:]]*#' .circleci/config.yml | grep -cE 'docker login[^|]*--password[ =]'); B=$(grep -v '^[[:space:]]*#' .circleci/config.yml | grep -cE 'docker login.*registry\.thinx\.cloud'); echo "argv=$A direct=$B"; [ "$A" = "0" ] && [ "$B" = "0" ] && echo CI02-GATES-OK</automated>
    <fails_when>CI02-GATES-OK absent; `argv=` or `direct=` shows a non-zero count</fails_when>
    <automated>git show --numstat --format= "$(git log -1 --format=%H -- .circleci/config.yml)" -- .circleci/config.yml</automated>
    <fails_when>the numstat line is neither `0	1	.circleci/config.yml` (delete path) nor `2	1	.circleci/config.yml` (fallback path)</fails_when>
    <automated>D=$(grep -nF -- '--password-stdin dhi.io' .circleci/config.yml | cut -d: -f1); U=$(grep -nF 'docker compose up -d mosquitto transformer worker couchdb' .circleci/config.yml | cut -d: -f1); H=$(grep -cF -- '--password-stdin docker.io' .circleci/config.yml); echo "dhi=$D up=$U hub=$H"; [ -n "$D" ] && [ -n "$U" ] && [ "$D" -lt "$U" ] && [ "$H" = "2" ] && echo ORDER-OK</automated>
    <fails_when>ORDER-OK absent: the dhi.io login line is missing, no longer precedes the compose start, or the docker.io stdin login count is not 2</fails_when>
    <automated>S=$(git log -1 --format=%H -- .circleci/config.yml) && test -n "$S" && curl -s 'https://circleci.com/api/v1.1/project/github/suculent/thinx-device-api/tree/thinx-staging?limit=60' | jq -r --arg s "$S" '[.[] | select(.vcs_revision==$s and .workflows.job_name=="test")][0].status'</automated>
    <fails_when>output is anything other than exactly `success` (`null` = no test job found for the CI-02 commit; `failed`/`running`/`queued` = not green yet)</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY lists the four D-01 pre-check results. (a) returns only `.env.dist:3:REGISTRY=registry.thinx.cloud:5000`. (b) is empty. (c) shows only Docker Hub and dhi.io references. (d) prints a total of 0 `registry.thinx.cloud` lines across the last green test job's step outputs.
    - `YAML-OK` and `CI02-GATES-OK` print, with `argv=0 direct=0`.
    - The CI-02 commit numstat is `0	1	.circleci/config.yml`, or `2	1` on the fallback path, and the SUMMARY states which path was taken and why.
    - `ORDER-OK` prints: the dhi.io login precedes the compose start and `hub=2`.
    - On the fallback path, `grep -n 'registry: registry.thinx.cloud:5000' .circleci/config.yml` shows a match whose line number is lower than the line of `name: Starting Support Services`.
    - The CircleCI `test` job for the CI-02 commit prints `success`.
    - Nothing was pushed to main.
  </acceptance_criteria>
  <done>No docker login in .circleci/config.yml passes a password on argv, and no direct private-registry login remains. The test job for that commit is green on thinx-staging. The docker.io and dhi.io logins and the registry-login command are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: D-07 CodeQL baseline artifact (22-CODEQL-BASELINE.md + reduced 22-CODEQL-ALERTS.json)</name>
  <files>.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md, .planning/phases/22-ci-sast-baseline/22-CODEQL-ALERTS.json</files>
  <read_first>
    - .planning/phases/22-ci-sast-baseline/22-CONTEXT.md (D-07 and the specifics on Phase 23 "before" evidence)
    - .planning/phases/22-ci-sast-baseline/22-PATTERNS.md (the 22-CODEQL-BASELINE.md suggested sections)
    - .planning/ROADMAP.md (Phase 23 success criterion 5, which consumes this list)
    - lib/thinx/git.js and lib/thinx/builder.js (only to confirm alert line numbers land on real sinks; do not edit)
  </read_first>
  <action>
Wait until the CodeQL push run for the current `origin/thinx-staging` HEAD (the Task 2 commit) has concluded `success`, using the same `gh run list` / `gh run watch` pattern as Task 1. Then read the analyses list for `ref=refs/heads/thinx-staging&tool_name=CodeQL`.

Record for both Task 1's and Task 2's commits: the analysis `id`, `commit_sha`, `created_at`, `results_count`, `rules_count` and `tool.version`. Phase 22 changes no JavaScript, so the two `results_count` values should match. If they differ, record both and explain the difference in the doc. Do not pick one silently.

Fetch the open alerts on that ref with `gh api --paginate --slurp 'repos/suculent/thinx-device-api/code-scanning/alerts?ref=refs/heads/thinx-staging&tool_name=CodeQL&state=open&per_page=100'`. Pipe through jq using `add // []`, then map each alert to exactly these keys:
- `number`
- `rule_id` (from `.rule.id`)
- `severity` (from `.rule.severity`)
- `security_severity_level` (from `.rule.security_severity_level`, null allowed)
- `path` (from `.most_recent_instance.location.path`)
- `start_line` (from `.most_recent_instance.location.start_line`)
- `state`

Sort by `path`, then `start_line`, then `number`, and write the result to `22-CODEQL-ALERTS.json`. Leave out alert messages, `html_url` and code snippets: this repo is public, so the file carries identifiers only. An empty result is valid and is written as `[]`.

Write `22-CODEQL-BASELINE.md`. The format is Claude's discretion within D-07; use these sections in this order:
- **Header table:** repository, ref `refs/heads/thinx-staging`, the baseline analysis id and commit SHA (the latest analysis), the workflow run URL, date (UTC), tool version, and query suite `security-extended`. Also a line `Total open alerts: N`, where N is the JSON array length.
- `## Totals by severity`: one table by `security_severity_level` (critical, high, medium, low, none for null) and one by rule `severity` (error, warning, note). Always print all rows, with a 0 where a bucket is empty.
- `## Counts by rule`: rule id and count, sorted by count descending then rule id ascending.
- `## Counts by file`: path and count, same sort.
- `## lib/thinx/git.js and lib/thinx/builder.js`: one row per alert in those two files (alert number, rule id, path, start line). This is Phase 23's "before" list for the git.js execSync sink and the builder readFileSync/lstatSync sinks. If there are no rows, write the literal line `none — CodeQL security-extended raised no alert in these files at this commit`.
- `## Trigger evidence`: a table with the rows staging push (Task 1 SHA, run URL, analysis id), staging push CI-02 (Task 2 SHA, run URL, analysis id, results_count equality), PR to main (`pending — filled by 22-03`) and push to main (`pending — trigger present in YAML; recorded when the user merges`).
- `## Data`: the exact gh/jq commands used, and a note that no alert was dismissed or fixed in Phase 22 (D-07).

Run the verification checks below. Commit both files together as `docs(22): record CodeQL security-extended baseline (D-07)`. Do not push for this commit alone: it rides the next thinx-staging push in 22-02, so a docs-only change does not trigger an extra CircleCI deploy.
  </action>
  <verify>
    <automated>jq -e 'type=="array"' .planning/phases/22-ci-sast-baseline/22-CODEQL-ALERTS.json</automated>
    <fails_when>non-zero exit (file missing, not JSON, or not an array)</fails_when>
    <automated>F=.planning/phases/22-ci-sast-baseline; N=$(jq length $F/22-CODEQL-ALERTS.json); grep -qF "Total open alerts: $N" $F/22-CODEQL-BASELINE.md && echo "COUNT-MATCH $N"</automated>
    <fails_when>COUNT-MATCH absent (the md total differs from the JSON length, or the line is missing)</fails_when>
    <automated>M=.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md; for h in '## Totals by severity' '## Counts by rule' '## Counts by file' '## lib/thinx/git.js and lib/thinx/builder.js' '## Trigger evidence' '## Data'; do grep -qF "$h" $M || echo "MISSING $h"; done; echo HEADINGS-CHECKED</automated>
    <fails_when>any `MISSING` line is printed</fails_when>
    <automated>jq '[.[] | select((.path|test("^(spec/|builders/lua-inspect/|node_modules/)")) or has("message") or has("html_url"))] | length' .planning/phases/22-ci-sast-baseline/22-CODEQL-ALERTS.json</automated>
    <fails_when>output is not `0` (an ignored path leaked into the results, or message/html_url fields were committed)</fails_when>
    <automated>gh api 'repos/suculent/thinx-device-api/code-scanning/alerts?ref=refs/heads/thinx-staging&tool_name=CodeQL&state=dismissed&per_page=100' | jq length</automated>
    <fails_when>output is not `0` (an alert was dismissed during the phase)</fails_when>
  </verify>
  <acceptance_criteria>
    - `22-CODEQL-ALERTS.json` is a JSON array. Every element has exactly the keys number, rule_id, severity, security_severity_level, path, start_line and state, which `jq -e 'all(.[]; keys == ["number","path","rule_id","security_severity_level","severity","start_line","state"])'` confirms (exit 0).
    - `COUNT-MATCH {N}` prints, and no `MISSING` heading line prints.
    - The ignored-path, message and html_url leak count is `0`, and the dismissed-alert count is `0`.
    - The git.js/builder.js section has either one row per alert in those files or the literal `none — …` line.
    - The trigger-evidence table has the Task 1 and Task 2 analysis ids plus the two `pending` rows.
    - The commit contains only the two baseline files.
  </acceptance_criteria>
  <done>Phase 23 has a committed, reproducible "before" list: counts by severity, rule and file, plus the git.js/builder.js alerts, all taken from the thinx-staging CodeQL security-extended analysis. No alert was dismissed or fixed.</done>
</task>

</tasks>

<flagged_assumptions>
- **A-01 (CI-01, edge probe row `unclassified`, unresolved).** The spec-less edge probe could not classify CI-01, so no probe-derived edge predicate exists for it. The explicit truths above cover the requirement's own criteria. Two edges are handled by construction but were not probe-derived: (a) a push that changes no JavaScript still produces an analysis, and Task 3 compares Task 1's and Task 2's `results_count` instead of assuming they are equal; (b) an empty alert set is written as `[]` and `none — …` rather than being omitted. Flagged for manual review.
- **A-04 (CI-02 scope).** `services/console/.circleci/config.yml` (a submodule repo) still logs in to `registry.thinx.cloud:5000` with the orb's `docker/check` and no retry. It runs only on the dormant `thinx-console` branch. CONTEXT scopes CI-02 to the parent `.circleci/config.yml`, so this plan does not touch it. It is recorded here so the gap is visible and not silently dropped.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CircleCI job → remote Docker engine / registries | Registry credentials from CircleCI contexts are used for docker login |
| GitHub Actions runner → code scanning API | GITHUB_TOKEN with security-events: write uploads SARIF |
| Repo (public) → anyone | Committed planning artifacts are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information disclosure | `.circleci/config.yml` test job login (line 767) | medium | mitigate | Task 2 deletes the argv-password login (or replaces it with `registry-login`, which uses `--password-stdin`). The gate `argv=0 direct=0` proves it. |
| T-22-02 | Elevation of privilege | CodeQL workflow GITHUB_TOKEN | medium | mitigate | Keep the top-level `contents: read` and the job-scoped `security-events: write` / `actions: read` verbatim. The Task 1 python check asserts both permission maps exactly. |
| T-22-SC | Tampering | GitHub Actions supply chain (`actions/checkout@v7`, `github/codeql-action/*@v4`) | medium | mitigate | GitHub-owned actions only (repo policy `github_owned_allowed: true`, `verified_allowed: false`). No third-party action and no `run:` steps, which the Task 1 check asserts. No package-manager installs in this plan, so the legitimacy gate does not apply. |
| T-22-03 | Information disclosure | Committed CodeQL baseline in a public repo | low | accept | The source is public, so anyone can reproduce these CodeQL results, and ROADMAP Phase 23 already names the sinks. The committed files hold only numbers, rule ids, severities, paths and lines; the Task 3 check asserts no message or html_url fields. |
| T-22-04 | Denial of service | Private registry during concurrent pushes | low | mitigate | Task 2 pushes only after the thinx-staging CircleCI queue drains (no job with a lifecycle other than finished). Push retry and serial-group stay deferred per D-03. |
</threat_model>

<verification>
- The CodeQL push run for the workflow commit is `success`, and its analysis is visible under `refs/heads/thinx-staging`.
- Default setup reads `not-configured` before and after.
- `.circleci/config.yml` has `argv=0 direct=0`, the numstat matches the path taken, `ORDER-OK` prints, and the test job for the CI-02 commit is `success`.
- The baseline files exist and are consistent (`COUNT-MATCH`, no MISSING headings, 0 leaks, 0 dismissed).
</verification>

<success_criteria>
- CI-01 (partial; 22-03 completes the PR-run evidence): the push trigger on thinx-staging is proven by a green run and a visible analysis. The main-push and PR triggers are present in the YAML. The check is not required, and default setup is off.
- CI-02: every CircleCI login to `registry.thinx.cloud:5000` in `.circleci/config.yml` goes through `registry-login` or no longer exists, no login passes a password on argv, and the test job is green.
- D-07: the baseline is committed for Phase 23.
</success_criteria>

## Source Coverage (phase-wide audit)

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | — | CI gives trustworthy signals: CodeQL on the production branches, registry logins stop flaking, Vue links point at the Vue console | 01, 02, 03 | COVERED |
| REQ | CI-01 | CodeQL v4/checkout v7, javascript-typescript, build-mode none; push main+staging, PR to main; non-required; default setup off | 01 (T1), 03 (T2) | COVERED |
| REQ | CI-02 | No direct private-registry login; no argv password | 01 (T2) | COVERED |
| REQ | CI-03 | Vue links resolve to the Vue console host in the live bundle; VUE_WEB_HOSTNAME traced; dead env removed | 02 | COVERED |
| CONTEXT | D-01 | Pre-check, then delete :767; registry-login fallback | 01 (T2) | COVERED |
| CONTEXT | D-02 | Leave docker.io/dhi.io logins; no registry-login params | 01 (T2) | COVERED |
| CONTEXT | D-03 | No push retry or serial-group | 01 (T2) | COVERED |
| CONTEXT | D-04 | In-place rewrite, triggers + cron, no PR-head step, no install step, keep permissions | 01 (T1) | COVERED |
| CONTEXT | D-05 | security-extended | 01 (T1) | COVERED |
| CONTEXT | D-06 | paths-ignore for vendored/minified assets and tests | 01 (T1) | COVERED |
| CONTEXT | D-07 | Baseline only, artifact in the phase dir | 01 (T3), 03 (T2) | COVERED |
| CONTEXT | D-08 | Staging push run + PR run, no merge, non-required, recheck default setup | 01 (T1), 03 | COVERED |
| CONTEXT | D-09 | Target https://console.thinx.cloud | 02 (T1) | COVERED |
| CONTEXT | D-10 | Var → build arg → Dockerfile → bundle → three links | 02 (T1) | COVERED |
| CONTEXT | D-11 | Fix the var or the chain, never WEB_HOSTNAME | 02 (T1) | COVERED |
| CONTEXT | D-12 | The env sits on the classic console service | 02 (T1) | COVERED |
| CONTEXT | D-13 | Repo + gluster + live env-rm, no restart.sh or stack deploy | 02 (T1, T2, T3) | COVERED |
| RESEARCH | Pitfall 17 | Default-vs-advanced conflict, v4 not v3, alert flood, non-required, staging pushes scanned | 01, 03 | COVERED (the alert flood is handled by baseline-only per D-07) |
| RESEARCH | Pitfall 18 | Retry exists, route :767, push contention | 01 (T2) | COVERED (push retry and serial-group deferred per D-03) |
| RESEARCH | Pitfall 19 | Var set, scheme, ARG/ENV before build, bundle grep, click links, never WEB_HOSTNAME | 02 (T1) | COVERED |
| RESEARCH | Gaps | VUE_WEB_HOSTNAME + console repo CI wiring; default setup recheck | 02 (T1), 01 (T1), 03 (T2) | COVERED |
| RESEARCH | SUMMARY "optionally influxdb:1.8 in CI compose" | — | — | EXCLUDED (ROADMAP Phase 22 note: dropped) |
| RESEARCH | STACK optional dependabot.yml / rename VUE_WEB_HOSTNAME | — | — | EXCLUDED (marked optional in STACK, not in the Phase 22 Delivers list or in CONTEXT) |

## Artifacts this phase produces (plan 01)

- `.github/workflows/codeql-analysis.yml` (rewritten): job `analyze` (name `Analyze`), steps `Checkout repository`, `Initialize CodeQL`, `Perform CodeQL Analysis`, analysis category `/language:javascript-typescript`.
- `.circleci/config.yml` (edited): test job step `Starting Support Services` without the raw private-registry login. On the fallback path only, a new `registry-login` step with `registry: registry.thinx.cloud:5000` in the test job.
- New file `.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md`.
- New file `.planning/phases/22-ci-sast-baseline/22-CODEQL-ALERTS.json` (array of `{number, rule_id, severity, security_severity_level, path, start_line, state}`).
- No new code symbols, CLI flags or functions.

<output>
Create `.planning/phases/22-ci-sast-baseline/22-01-SUMMARY.md` when done
</output>
