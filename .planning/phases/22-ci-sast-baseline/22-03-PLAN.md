---
phase: 22-ci-sast-baseline
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - .planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md
autonomous: false
requirements: [CI-01]
tags: [ci, codeql, pull-request]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An open pull request thinx-staging → main exists, and its CodeQL pull_request run for the PR head commit concluded success (CI-01, D-08)"
    - "The code-scanning analyses API lists at least one error-free CodeQL analysis under refs/pull/{PR number}/merge (CI-01, D-08)"
    - "The CodeQL check is not required on main: neither branch protection required_status_checks (contexts or checks) nor any ruleset rule of type required_status_checks names Analyze or CodeQL (CI-01, D-08)"
    - "The pull request is still OPEN at the end of the phase. It is not merged, and nothing was pushed to main by this phase; the main-push run is recorded later, when the user merges at the normal release cadence (D-08)"
    - "GitHub CodeQL default setup still reads not-configured after the PR run (CI-01, D-08)"
    - "The push trigger for main is present in .github/workflows/codeql-analysis.yml (D-04, D-08)"
    - "22-CODEQL-BASELINE.md's trigger-evidence table records the PR run (PR number, head SHA, run URL, analysis id) and marks the main push as trigger present, run pending the user's merge (D-07, D-08)"
  artifacts:
    - path: ".planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md"
      provides: "Trigger evidence completed with the PR-to-main run"
      contains: "refs/pull/"
  key_links:
    - from: "pull request thinx-staging → main"
      to: ".github/workflows/codeql-analysis.yml pull_request trigger"
      via: "GitHub pull_request event (branches: [main])"
      pattern: "pull_request"
    - from: "PR CodeQL run"
      to: "code scanning analyses under refs/pull/{PR number}/merge"
      via: "github/codeql-action/analyze@v4 SARIF upload"
      pattern: "refs/pull/"
  prohibitions:
    - statement: "MUST NOT put secrets, swarm node IPs, ssh host/port/key details or internal registry credentials into the PR title or body; the repository and its PRs are public"
      status: resolved
      verification: judgment
    - statement: "MUST NOT merge the PR, enable auto-merge, add the CodeQL check to required status checks, or push to main as part of this phase"
      status: resolved
      verification: judgment
---

<objective>
Complete the CI-01 trigger evidence per D-08. Behind a blocking human gate, open a PR `thinx-staging` → `main`. Prove that its CodeQL `pull_request` run completes and appears in code scanning, that the check is not required, and that default setup is still off. Record the evidence in the baseline doc. Do not merge. The merge, and with it the main-push run, belongs to the user's release cadence.

The phase tracer lives in 22-01 Task 1. This plan opens with a decision gate because opening the release PR is outward-facing.

Purpose: CI-01's "PR to main produces an analysis" and "non-required" criteria are proven against GitHub, not asserted from YAML.
Output: an open PR, a green PR CodeQL run, and the updated trigger-evidence section of `22-CODEQL-BASELINE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-ci-sast-baseline/22-CONTEXT.md
@.planning/phases/22-ci-sast-baseline/22-01-SUMMARY.md
@.planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md
@.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md
@.github/workflows/codeql-analysis.yml
@AGENTS.md

Facts verified at planning time (2026-09-25; re-verify):
- There was no open PR with head `thinx-staging` and base `main`. `origin/main` was 17 commits behind `origin/thinx-staging` before this phase started, so the PR carries those commits plus every Phase 22 commit.
- `main` branch protection exists, with `required_status_checks.contexts` and `.checks` both empty. `gh api repos/suculent/thinx-device-api/rules/branches/main` returns no rules. No `.coderabbit.yaml` exists, so no external reviewer fires automatically when the PR opens.
- Memory rule: a direct push to `main` is accepted despite the PR rule ("Bypassed rule violations"), so nothing mechanical stops a mistaken push. This plan never pushes to `main` and never merges.
- For `pull_request` events the code-scanning ref is `refs/pull/{number}/merge`, and `gh run list --event pull_request --branch thinx-staging` lists the PR runs (headBranch is the PR head).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve opening the release PR thinx-staging → main (evidence only; no merge)</name>
  <read_first>
    - .planning/phases/22-ci-sast-baseline/22-CONTEXT.md (D-08)
    - .planning/phases/22-ci-sast-baseline/22-01-SUMMARY.md and 22-02-SUMMARY.md (what the PR will carry)
  </read_first>
  <decision>Open a pull request thinx-staging → main now so its CodeQL pull_request run serves as CI-01 evidence (D-08)?</decision>
  <context>
Before presenting, run `git fetch -q origin` and show the user:
- `git rev-list --count origin/main..origin/thinx-staging`
- `git log --oneline origin/main..origin/thinx-staging | head -40`

The PR carries every staging commit not yet on main, not only Phase 22. Opening it:
- triggers the CodeQL `pull_request` run
- makes the PR visible publicly
- does not merge anything
- is reversible with `gh pr close`

The check will not be required. Merging stays with the user at the normal release cadence, and only then does the main-push CodeQL run happen.
  </context>
  <options>
    <option id="open-pr">
      <name>Open the PR now (recommended by D-08)</name>
      <pros>Proves CI-01's PR trigger and the non-required status inside this phase. The PR doubles as the next release PR.</pros>
      <cons>The PR is public and lists every pending staging commit. It stays open until the user merges or closes it.</cons>
    </option>
    <option id="hold">
      <name>Hold the PR</name>
      <pros>No public PR yet.</pros>
      <cons>CI-01's "PR to main produces an analysis" stays unproven. The SUMMARY must list it as open, and verification cannot pass that criterion.</cons>
    </option>
  </options>
  <resume-signal>Type "open-pr" or "hold"</resume-signal>
  <acceptance_criteria>
    - The SUMMARY records the choice together with the commit count shown to the user.
    - On "hold", Task 2 is skipped, and the SUMMARY lists the PR-run evidence as open with the exact Task 2 commands.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Open the PR, prove the PR CodeQL run and non-required status, record the evidence</name>
  <precondition>Task 1 resumed with "open-pr"; `gh auth status` shows an active account with push rights on suculent/thinx-device-api</precondition>
  <reversibility rating="reversible">An open PR can be closed with gh pr close. No merge happens and required checks are not changed.</reversibility>
  <files>.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md</files>
  <read_first>
    - .planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md (the trigger-evidence table written by 22-01 Task 3)
    - .github/workflows/codeql-analysis.yml (triggers)
    - .planning/phases/22-ci-sast-baseline/22-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
1. Recheck D-08. `gh api repos/suculent/thinx-device-api/code-scanning/default-setup --jq .state` must print `not-configured`; otherwise STOP and report. Also confirm `git fetch -q origin` leaves local `thinx-staging` equal to `origin/thinx-staging`. If local commits are unpushed, they are docs from 22-01/22-02. Leave them unpushed, because pushing them would add a pipeline, and note it.
2. If an open PR with head `thinx-staging` and base `main` already exists, reuse it. Otherwise create one with `gh pr create --base main --head thinx-staging --title "Release: thinx-staging to main (includes v1.14 Phase 22 CI and SAST baseline)" --body-file {scratch file}`.
   - The body lists the Phase 22 changes: the CodeQL workflow rewrite, the removed test-job registry login, and the dead Vue env removed from docker-swarm.yml.
   - It states that the CodeQL check is informational, not required, and that this PR is merged at the maintainer's release cadence.
   - It contains no hostnames beyond the public thinx.cloud names, no IPs, no ssh details and no credentials.
   - If the executor session's own instructions require a PR attribution line, end the body with it.
   - Do not pass `--draft`, since draft PRs still run `pull_request` workflows but the user wants a normal release PR. Never run `gh pr merge`, and never enable auto-merge.
3. Get the PR number and head SHA from `gh pr view {N} --json number,headRefOid,url`. Poll `gh run list --workflow codeql-analysis.yml --event pull_request --branch thinx-staging --limit 10 --json databaseId,headSha,status,conclusion,url` every 20 s, up to 5 min, until a run with `headSha` equal to the head SHA appears. Then run `gh run watch {databaseId} --interval 30 --exit-status`. On failure, read `--log-failed`, fix the workflow on thinx-staging (a new push updates the PR), and repeat.
4. Confirm the analysis is visible under `refs/pull/{N}/merge`. Confirm the check is not required: the branch-protection contexts and checks contain no Analyze/CodeQL entry, and no ruleset rule of type `required_status_checks` applies to main. Confirm the PR state is `OPEN`. Recheck the default setup state.
5. In `22-CODEQL-BASELINE.md`, replace the PR-to-main row's pending placeholder written by 22-01 with: PR number and URL, head SHA, run URL, analysis id, and `results_count`. Set the main-push row to `trigger present in YAML (push: branches [main, thinx-staging]); run recorded when the user merges PR #{N}`. Change nothing else in the doc. Commit it alone as `docs(22): record CodeQL PR-run evidence (D-08)`. Do not push only for this docs commit.
  </action>
  <verify>
    <automated>N=$(gh pr list --head thinx-staging --base main --state open --json number --jq '.[0].number') && test -n "$N" && gh pr view "$N" --json state,baseRefName,headRefName --jq '[.state,.baseRefName,.headRefName] | join(" ")'</automated>
    <fails_when>output is not exactly `OPEN main thinx-staging` (empty = no open PR; `MERGED`/`CLOSED` = the PR was merged or closed)</fails_when>
    <automated>N=$(gh pr list --head thinx-staging --base main --state open --json number --jq '.[0].number') && test -n "$N" && H=$(gh pr view "$N" --json headRefOid --jq .headRefOid) && gh run list --workflow codeql-analysis.yml --event pull_request --branch thinx-staging --limit 20 --json headSha,conclusion | jq -r --arg h "$H" '[.[] | select(.headSha==$h)][0].conclusion'</automated>
    <fails_when>output is anything other than exactly `success` (`null` = no pull_request run for the PR head)</fails_when>
    <automated>N=$(gh pr list --head thinx-staging --base main --state open --json number --jq '.[0].number') && test -n "$N" && C=$(gh api "repos/suculent/thinx-device-api/code-scanning/analyses?ref=refs/pull/$N/merge&tool_name=CodeQL&per_page=10" | jq '[.[] | select(.error=="")] | length') && echo "pr=$N analyses=$C" && [ "$C" -ge 1 ] && echo PR-ANALYSIS-VISIBLE</automated>
    <fails_when>PR-ANALYSIS-VISIBLE absent (analyses=0: no SARIF upload for the PR merge ref)</fails_when>
    <automated>P=$(gh api repos/suculent/thinx-device-api/branches/main/protection --jq '[.required_status_checks.contexts[]?, .required_status_checks.checks[]?.context] | map(select(test("Analyze|CodeQL"))) | length') && R=$(gh api repos/suculent/thinx-device-api/rules/branches/main --jq '[.[] | select(.type=="required_status_checks")] | length') && D=$(gh api repos/suculent/thinx-device-api/code-scanning/default-setup --jq .state) && echo "protection=$P rulesets=$R default_setup=$D"</automated>
    <fails_when>output is not exactly `protection=0 rulesets=0 default_setup=not-configured`</fails_when>
    <automated>python3 -c 'import yaml,sys; d=yaml.safe_load(open(sys.argv[1])); on=d.get("on", d.get(True)); assert "main" in on["push"]["branches"] and on["pull_request"]["branches"]==["main"]; print("MAIN-TRIGGER-OK")' .github/workflows/codeql-analysis.yml</automated>
    <fails_when>non-zero exit or MAIN-TRIGGER-OK absent</fails_when>
    <automated>M=.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md; echo "pr_rows=$(grep -c 'refs/pull/' $M) placeholder=$(grep -c 'filled by 22-03' $M) main_row=$(grep -c 'run recorded when the user merges' $M)"</automated>
    <fails_when>output is not `pr_rows=` with a value of 1 or more, `placeholder=0`, and `main_row=1`</fails_when>
  </verify>
  <acceptance_criteria>
    - The PR check prints `OPEN main thinx-staging`.
    - The PR head's CodeQL `pull_request` run concluded `success`.
    - `PR-ANALYSIS-VISIBLE` prints, with analyses ≥ 1 for `refs/pull/{N}/merge`.
    - The check prints `protection=0 rulesets=0 default_setup=not-configured`.
    - `MAIN-TRIGGER-OK` prints.
    - The baseline doc prints `pr_rows` of 1 or more, `placeholder=0` and `main_row=1`.
    - `git log origin/main -1 --format=%H` is the same as before the phase, because nothing was pushed to or merged into main by this phase.
    - The SUMMARY records the PR URL, head SHA, run URL and analysis id.
  </acceptance_criteria>
  <done>A thinx-staging → main PR is open with a green, non-required CodeQL analysis under its merge ref. Default setup is still off, the main-push trigger is in the YAML, and the evidence is recorded in the baseline doc. Nothing was merged.</done>
</task>

</tasks>

<flagged_assumptions>
- A-01 from 22-01 applies here as well: CI-01 is an `unclassified` edge-probe row. Two cases are handled explicitly but were not probe-derived: an already-open staging PR is reused rather than duplicated, and a PR run that fails because of the workflow is fixed on thinx-staging, which updates the PR.
- The main-push CodeQL run (D-08) is intentionally **not** produced in this phase. It is accepted as "trigger present in YAML" plus a run recorded when the user merges. Verification must not mark CI-01 failed for its absence.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer workstation → GitHub (gh token) | Creates a public PR and reads the protection and code-scanning settings |
| Public → PR page | PR title, body and checks are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Tampering / Elevation of privilege | Accidental merge or direct push to main (the protection bypass is possible) | high | mitigate | The Task 1 blocking gate comes first. The plan never runs `gh pr merge`, auto-merge or a push to main. The checks require the PR state `OPEN`, and `origin/main` is unchanged. |
| T-22-11 | Information disclosure | PR body contents | medium | mitigate | The body lists changes only: no IPs, ssh details, credentials or internal hostnames beyond the public thinx.cloud names (see prohibitions). |
| T-22-12 | Denial of service | CodeQL made a required check that blocks merges when slow or failing | low | mitigate | Nothing adds it to protection or rulesets. The check requires `protection=0 rulesets=0`. |
| T-22-13 | Elevation of privilege | `pull_request` workflow token | low | accept | Top-level `contents: read`, and the job has only `security-events: write`/`actions: read`. The PR comes from a same-repo branch, and fork PRs get a read-only token without secrets. The workflow has no `run:` steps. |
</threat_model>

<verification>
- The PR is OPEN with base main and head thinx-staging.
- The PR CodeQL run is `success`, and an analysis exists under `refs/pull/{N}/merge`.
- The check prints `protection=0 rulesets=0 default_setup=not-configured`.
- The main-push trigger is present in the YAML, and the baseline doc's trigger evidence is complete except for the main-push run, which waits for the user's merge.
</verification>

<success_criteria>
- CI-01: a push to `thinx-staging` (22-01) and a PR to `main` (this plan) each produced a CodeQL javascript-typescript analysis visible in code scanning. The push to `main` is wired in the YAML and recorded when the user merges. The check is not required, and default setup stays off.
</success_criteria>

## Artifacts this phase produces (plan 03)

- `.planning/phases/22-ci-sast-baseline/22-CODEQL-BASELINE.md` (edited): trigger-evidence rows for the PR run and the main push.
- GitHub (not a repo file): an open pull request with head `thinx-staging` and base `main`, and its CodeQL analysis under `refs/pull/{N}/merge`.
- No new code symbols, CLI flags or functions.

<output>
Create `.planning/phases/22-ci-sast-baseline/22-03-SUMMARY.md` when done
</output>
