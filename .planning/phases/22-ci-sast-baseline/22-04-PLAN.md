---
phase: 22-ci-sast-baseline
plan: 04
type: execute
wave: 4
depends_on: ["22-02", "22-03"]
files_modified:
  - services/console/vue/src/components/Layout/Layout.vue
  - services/console/vue/tests/unit/footer-hostnames.cjs
  - services/console/vue/package.json
  - services/console
  - .planning/REQUIREMENTS.md
  - .planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [CI-03]
tags: [vue-console, submodule, hostname, gap-closure, deploy]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "In the live Vue bundle, the 'THiNX Console' links (layout, login, password reset) point at the Vue console host. In a logged-in session the /app footer 'THiNX Console' anchor has href https://console.thinx.cloud and 'THiNX Cloud' has href https://thinx.cloud. /#/login and /#/password-reset still render href=https://console.thinx.cloud (ROADMAP SC3, CI-03, D-09, D-10.4)"
    - "services/console/vue/src/components/Layout/Layout.vue imports hostnameMixin from '@/mixins/hostnames' and declares mixins: [hostnameMixin]. The Layout.vue diff against a0e86707 is 2 added lines and 0 removed, so the footer template is byte-identical. services/console/vue/src/main.js is unchanged (D-09, D-11)"
    - "Every file under services/console/vue/src that reads $hostnames declares the hostnames mixin: Layout, Login, PasswordReset, OAuthReturn and Visits. Layout was the only one missing it"
    - "npm --prefix services/console/vue run test:unit exits 0. With VUE_APP_CONSOLE_HOSTNAME=console.thinx.cloud it renders the real footers of Layout, Login and PasswordReset to https://console.thinx.cloud and https://thinx.cloud, and it prints the fallback line proving an unset var yields the serving origin, not undefined. The same test run before the Layout edit failed on the Layout lines, and that RED output is recorded in the SUMMARY"
    - "The served https://console.thinx.cloud/js/app.js has a buildHash that resolves to a commit on origin/thinx-staging. That commit's services/console gitlink contains the Layout fix, the compiled Layout script module references mixins/hostnames, and consoleHostname still compiles to console.thinx.cloud (D-10.3)"
    - "The image serving the fix was built by a green vue-console-registry CircleCI job on thinx-staging for the pointer-bump commit. .circleci/config.yml is not modified, so the 22-02 build-arg chain is unchanged (D-10, D-11)"
    - "Nothing was pushed to any main branch. Parent origin/main stays 033ea946 and submodule origin/main stays a0e86707. PR #569 stays open and unmerged"
    - "REQUIREMENTS.md marks CI-03 [x] and Complete only after the logged-in check returns 'approved'. This plan leaves the CI-01 and CI-02 rows alone. 22-02-SUMMARY.md gains a short correction note and nothing else changes in it"
  artifacts:
    - path: "services/console/vue/src/components/Layout/Layout.vue"
      provides: "Authenticated layout whose footer links get their href from the hostnames mixin"
      contains: "mixins: [hostnameMixin],"
    - path: "services/console/vue/tests/unit/footer-hostnames.cjs"
      provides: "Plain-node regression test: renders the real footers with each component's real mixins, and sweeps src for $hostnames readers without the mixin"
      contains: "mixins/hostnames"
    - path: "services/console/vue/package.json"
      provides: "test:unit runs env-json.cjs and footer-hostnames.cjs"
      contains: "tests/unit/footer-hostnames.cjs"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CI-03 marked complete after the logged-in verification"
      contains: "- [x] **CI-03**"
  key_links:
    - from: "services/console/vue/src/mixins/hostnames.js (data CONSOLE/LANDING, created() assigns this.$hostnames)"
      to: "services/console/vue/src/components/Layout/Layout.vue:12 footer anchors"
      via: "mixins: [hostnameMixin] in the Layout component options"
      pattern: "mixins: \\[hostnameMixin\\]"
    - from: "services/console submodule commit on origin/thinx-staging"
      to: "parent gitlink services/console on origin/thinx-staging"
      via: "chore(22) pointer-bump commit pushed to thinx-staging"
      pattern: "services/console"
    - from: "parent thinx-staging push"
      to: "served https://console.thinx.cloud/js/app.js (thinx_vue)"
      via: "CircleCI vue-console-registry (VUE_APP_BUILD_HASH = CIRCLE_SHA1[:7]) then Swarmpit autoredeploy of thinx_vue"
      pattern: "buildHash"
  prohibitions:
    - requirement_id: CI-03
      statement: "MUST NOT hardcode the console or landing host into Layout.vue or any other footer, and MUST NOT alter or drop the VUE_APP_CONSOLE_HOSTNAME build arg. The hrefs must keep flowing from the build-time VUE_APP_* values through the hostnames mixin (D-09, D-11)"
      status: resolved
      verification: judgment
    - requirement_id: CI-03
      statement: "MUST NOT push to main in either repo or merge, approve, or auto-merge PR #569. Recovery MUST NOT use restart.sh, docker stack deploy, or any service update other than the swarm-autopull-recovery rung-1 restart of swarmpit_app"
      status: resolved
      verification: judgment
    - requirement_id: CI-03
      statement: "MUST NOT mark CI-03 complete, or claim the Layout link is fixed, before the logged-in human check has returned 'approved'. This is the overclaim that produced the gap"
      status: resolved
      verification: judgment
    - requirement_id: CI-03
      statement: "MUST NOT print environment values, CircleCI step output beyond step names and the required-vars success line, service env values, or ssh host, port, or key details into logs, the SUMMARY, or commits. Key names only"
      status: resolved
      verification: judgment
---

<!-- planner-discipline-allow: VUE_APP_CONSOLE_HOSTNAME -->

<objective>
Close the one CI-03 gap from 22-VERIFICATION.md. The authenticated Layout footer renders "THiNX Console" and "THiNX Cloud" with no href. `Layout.vue` binds `this.$hostnames.CONSOLE` and `.LANDING` but never declares the hostnames mixin, so it gets the empty `Vue.prototype.$hostnames = {}` that `main.js:23` sets.

Fix it with the established per-component pattern that Login.vue and PasswordReset.vue already use (D-09). Guard it with a plain-node unit test that follows the existing `tests/unit/*.cjs` pattern, so no test framework is added. Ship it through the submodule flow to thinx-staging only, prove it in the served bundle, and confirm it in a logged-in session. Only then mark CI-03 complete and correct the 22-02-SUMMARY overclaim.

Purpose: ROADMAP SC3 names the layout link explicitly. The two public-page links already work. This plan makes the third one work and proves it, without widening scope.
Output: a submodule commit (Layout.vue, a new footer test, and the package.json test:unit chain) on `thinx-cloud/console` thinx-staging, a parent pointer-bump commit on thinx-staging, a rebuilt `thinx/console:vue` rolled out to `thinx_vue`, REQUIREMENTS.md CI-03 marked complete, and a correction note in 22-02-SUMMARY.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-ci-sast-baseline/22-VERIFICATION.md
@.planning/phases/22-ci-sast-baseline/22-CONTEXT.md
@.planning/phases/22-ci-sast-baseline/22-02-PLAN.md
@.planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md
@AGENTS.md

Facts verified at planning time (2026-09-25). Re-verify them; do not trust them blindly.

- **Submodule state.** `services/console` is checked out on its local `main` at `a0e86707d6c1ddf81c7ac225107dfe3392645715`, with a clean tree. `origin/main` and `origin/thinx-staging` are both `a0e86707`, and the parent gitlink pins the same commit. The local `thinx-staging` branch is stale at `98ff250`, an ancestor of origin, so it fast-forwards. The remote is `git@github.com:thinx-cloud/console.git`. Commits go through a husky `commit-msg` hook running commitlint `config-conventional`, and console history uses `fix(scope): …` headers.
- **Defect class sweep.** `grep -rn '\$hostnames' services/console/vue/src` finds these readers: Layout.vue, Login.vue, PasswordReset.vue, OAuthReturn.vue and Visits.vue, plus `main.js:23` (the empty prototype) and `mixins/hostnames.js:27` (the assignment). The four pages import `@/mixins/hostnames` and declare `mixins: [hostnameMixin]`. Login has the import at line 101 and the mixin at 107; PasswordReset has them at 105 and 111. **Layout.vue is the only reader without the mixin.** The global `Vue.mixin(layoutMixin)` (`mixins/layout.js`) has only `appConfig` data and `decodeHtml`, so nothing collides with the hostnames mixin's `hostnames` data or `fixUrlProtocol` method.
- **Unit-test precedent, no framework.** `services/console/vue/package.json` `test:unit` is `node tests/unit/env-json.cjs`, a plain-node script that prints `ok   …` / `FAIL …` lines and exits 1 on any failure. It passes today. The app's only other runner is cypress. `test:unit` does not run in CI: the Vue Dockerfile runs `yarn build` and `yarn test:csp:dist` only. It is a local pre-deploy guard. `node_modules` is present with `vue` 2.7.16, `vue-template-compiler` 2.7.16 and `vuex`, so nothing needs installing.
- **The test approach was prototyped.** It renders the real `<footer>` of each SFC with the component's own `mixins`, the real `hostnames.js`, and `Vue.prototype.$hostnames = {}` mirrored from main.js. The env is `VUE_APP_CONSOLE_HOSTNAME=console.thinx.cloud` and `VUE_APP_LANDING_HOSTNAME=https://thinx.cloud`, matching the live bundle's literals, and `window.location.origin` is stubbed to `https://served-origin.invalid`.
  - Current source prints Layout `console=undefined landing=undefined`, while Login and PasswordReset print `https://console.thinx.cloud` / `https://thinx.cloud`.
  - With the two-line Layout fix, all three print the correct values, and the unset-var fallback prints `https://served-origin.invalid`.
  - Two traps the prototype hit: a component with no `data` option must not get `data: undefined` passed in (Vue then warns "data functions should return an object" and the render throws), and an unfixed Layout makes the render throw unless it is wrapped in try/catch.
- **Live bundle today.** `https://console.thinx.cloud/js/app.js` has buildHash `9ccf9f1`. It compiles `consoleHostname = "console.thinx.cloud"` and `landingHostname = "https://thinx.cloud"`. It is a dev-mode webpack build with eval'd modules and escaped quotes. The awk in the Task 1 verify isolates the compiled Layout script module, which references `mixins/hostnames` 0 times today; Login's module references it once.
- **buildHash is the parent commit.** `.circleci/config.yml:196` passes `VUE_APP_BUILD_HASH=$(echo $CIRCLE_SHA1 | cut -c -7)`, where CIRCLE_SHA1 is the pushed parent head. `vue-console-registry` (lines 888-898) builds on `thinx-staging` and publishes the private-registry image that `thinx_vue` pulls. The submodule's own CircleCI runs `Test Vue console` (cypress) on every branch and publishes only from branch `thinx-console`. It is not the deploy path, and its result is not a gate here. Record it only.
- **Parent state.** `thinx-staging` is 7 commits ahead of `origin/thinx-staging` (`9ccf9f18`), all `.planning` docs, and the orchestrator's plan commit will add one more. `origin/main` is `033ea946828991ec0b62bfcd7992ce65b8370439`. PR #569 (thinx-staging to main) is open. Pushing thinx-staging moves its head and triggers a PR CodeQL run. That is expected, and the PR must not be merged. Untracked `.claude/`, `.gsd/`, `.planning/milestone.lock` and `.planning/state.json` must never be staged.
- **Grounded read-only commands (2026-09-25).**
  - The CircleCI v1.1 tree endpoint (public, no token) resolved `job=15413 status=success` for `9ccf9f18`.
  - The queue-drain count was `0`.
  - Headless Chrome with `timeout --foreground 40` writing `--dump-dom` to a file (Chrome 154 does not exit on its own) printed both footer anchors on `/#/login` and `/#/password-reset`.
  - No CircleCI API token exists locally, so a failed job cannot be rerun from here.
- **SSH (fallback only).** `micro` is a shell alias in `~/.aliases`, not an ssh Host. Load it as 22-02 did: `set -- $(sed -n "s/^alias micro=[\"']\(.*\)[\"']$/\1/p" ~/.aliases) && test "$1" = ssh`, then run `"$@" -o BatchMode=yes -o ConnectTimeout=10 '{remote cmd}'`. Never copy the host, port or key into a committed file, because this repo is public. `docker service ls` is unreliable under load, so query services by name.
- **REQUIREMENTS.md already reverted.** Commit `1cf4b401` set CI-01, CI-02 and CI-03 to `[ ]` / `Gaps Found`, so the "revert CI-03 until verified" step is already done. This plan flips only CI-03, and only after the logged-in check. The phase re-verification restores CI-01 and CI-02, which the verifier found satisfied.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): Layout footer gets the hostnames mixin, guarded by a plain-node footer test, and ships submodule to parent pointer to vue-console-registry to thinx_vue, proven in the served bundle</name>
  <precondition>`git -C services/console status --porcelain` prints nothing; after `git -C services/console fetch origin`, `git -C services/console rev-parse origin/thinx-staging` equals `git rev-parse HEAD:services/console` (a0e86707 at planning time); the parent is on branch thinx-staging; `.planning/REQUIREMENTS.md` still shows `- [ ] **CI-03**`</precondition>
  <reversibility rating="reversible">Pushing to thinx-staging deploys the Vue console. To undo it, git revert the submodule commit and the pointer bump and push thinx-staging again, which rebuilds the previous bundle. `docker service rollback thinx_vue` restores the prior task immediately.</reversibility>
  <files>services/console/vue/src/components/Layout/Layout.vue, services/console/vue/tests/unit/footer-hostnames.cjs, services/console/vue/package.json, services/console</files>
  <read_first>
    - services/console/vue/src/components/Layout/Layout.vue (whole file: footer at line 12, imports at lines 18-26, component options at lines 28-50)
    - services/console/vue/src/mixins/hostnames.js (whole file: data() reads the VUE_APP_* values when the instance is created; created() applies fixUrlProtocol and assigns this.$hostnames)
    - services/console/vue/src/main.js (line 23 prototype fallback, line 13 layoutMixin import; read-only, do not edit)
    - services/console/vue/src/pages/Login/Login.vue (lines 86-110: footer plus the working pattern, import at 101, mixins at 107)
    - services/console/vue/src/pages/PasswordReset/PasswordReset.vue (lines 94-112: footer plus the working pattern, import at 105, mixins at 111)
    - services/console/vue/tests/unit/env-json.cjs (the plain-node unit test pattern to copy: check(), ok/FAIL lines, failures counter, process.exit)
    - services/console/vue/package.json (scripts, test:unit at line 17)
    - .circleci/config.yml (lines 144-200 build-vue-console, VUE_APP_BUILD_HASH at 196; lines 888-898 vue-console-registry branch filter)
    - .planning/phases/22-ci-sast-baseline/22-VERIFICATION.md (gaps block: truth, reason, missing)
    - .planning/phases/22-ci-sast-baseline/22-CONTEXT.md (D-09, D-10, D-11)
    - AGENTS.md (Deployment flow)
  </read_first>
  <behavior>
    - Env VUE_APP_CONSOLE_HOSTNAME=console.thinx.cloud and VUE_APP_LANDING_HOSTNAME=https://thinx.cloud. Rendering the real footer of Layout.vue with Layout's own mixins gives THiNX Console -> https://console.thinx.cloud and THiNX Cloud -> https://thinx.cloud. RED before the fix: both are undefined, or the render throws.
    - The same holds for Login.vue and PasswordReset.vue. They are already green; this is a regression guard.
    - With VUE_APP_CONSOLE_HOSTNAME unset, Layout's THiNX Console href equals the stubbed window.location.origin (https://served-origin.invalid). That is the documented fallback, and it is never undefined.
    - Every .vue or .js file under vue/src that reads $hostnames, except src/main.js and src/mixins/hostnames.js, imports '@/mixins/hostnames' and lists hostnameMixin in its mixins array. There are 5 such files after the fix.
  </behavior>
  <action>
Implements the gap's "missing" items 1-3 for CI-03 (D-09, D-10, D-11). Run everything from the parent repo root. Never print environment values. Write scratch files (bundle downloads, Chrome dumps, test output) to mktemp paths, never into either repo.

**A. Preconditions and branch.**
1. Check the precondition line above. If the submodule's origin/thinx-staging has moved past the parent gitlink, STOP and report, because the bump would ship console commits nobody reviewed.
2. Run `git -C services/console checkout thinx-staging`, then `git -C services/console merge --ff-only origin/thinx-staging`. The submodule HEAD must now be a0e86707.
3. Re-run the defect-class grep for `$hostnames` over `services/console/vue/src` and confirm that Layout.vue is the only reader without the mixin. If another reader also lacks it, fix it the same way in this commit, list it in the SUMMARY, and expect the count in the unit-test verify to grow by one per file.

**B. RED: write the test first.** Create `services/console/vue/tests/unit/footer-hostnames.cjs` in the style of `env-json.cjs`: a header comment, `check(name, cond)` printing `ok   ` or `FAIL ` lines, a failures counter, and `process.exit(failures ? 1 : 0)`. Resolve the vue root as two directories above `__dirname`, then:
1. **Load the libraries.** `require('vue/dist/vue.runtime.common.js')`, `vue-template-compiler` and `vuex`. All three are present in `vue/node_modules`. If any is missing, STOP; do not install packages.
2. **Stub the environment.**
   - Set `global.window` to an object whose `location.origin` is `https://served-origin.invalid`.
   - Set `process.env`: VUE_APP_CONSOLE_HOSTNAME to `console.thinx.cloud` (scheme-less, as the live bundle compiles it), VUE_APP_LANDING_HOSTNAME to `https://thinx.cloud`, and VUE_APP_API_HOSTNAME to `https://app.thinx.cloud`.
   - Set `Vue.config.productionTip` and `Vue.config.devtools` to false.
   - Set `Vue.prototype.$hostnames` to an empty object, with a comment saying it mirrors `src/main.js:23`.
3. **Module loader.** Write a small ESM-to-function loader for a source string:
   - Rewrite named imports (`import { a, b as c } from 'x'`) to a destructuring `const` from `__import('x')`, turning `as` into `:`.
   - Rewrite default imports to `__import('x').default`.
   - Drop side-effect-only imports such as the `.scss` line.
   - Replace `export default` with an assignment to `module.exports.default`.
   - Evaluate with `new Function('module', '__import', code)`, and put the same eslint-disable comment that `env-json.cjs` uses above it.
4. **Import resolution.** The `__import` resolver maps:
   - `@/mixins/hostnames` to the real `src/mixins/hostnames.js`, loaded through the same loader
   - `vuex` to the real package
   - every other specifier to a Proxy whose `default` is `{}` and whose other properties are no-op functions (child components, utils)
5. **Footer render helper.** For a given SFC path:
   - Call `compiler.parseComponent`, take the first footer element (from the opening footer tag through its closing tag) out of `template.content`, and call `compiler.compileToFunctions` on it.
   - Load the component options from `script.content`.
   - Create `new Vue` with `mixins` set to `options.mixins || []` plus the compiled render and staticRenderFns. Add `data` only when `options.data` is a function; passing `data: undefined` breaks the render.
   - Call `vm._render()` inside try/catch; a throw counts as a failure that carries the message. Walk the VNode tree and map each `a` element's trimmed text to `data.attrs.href`.
6. **Printed checks.** The exact line texts matter, because the verify counts them:
   - For each of `src/components/Layout/Layout.vue`, `src/pages/Login/Login.vue` and `src/pages/PasswordReset/PasswordReset.vue` (component name taken from the file), print `{Name} footer THiNX Console -> {href}` expecting `https://console.thinx.cloud`, and `{Name} footer THiNX Cloud -> {href}` expecting `https://thinx.cloud`.
   - Then delete VUE_APP_CONSOLE_HOSTNAME, re-render Layout, and print `Layout footer falls back to the serving origin when VUE_APP_CONSOLE_HOSTNAME is unset -> {href}` expecting `https://served-origin.invalid`. Restore the var afterwards.
   - Walk `src` recursively over `.vue` and `.js` files, skipping `src/main.js` and `src/mixins/hostnames.js`. For each file whose text contains `$hostnames`, print `hostnames mixin declared: {path relative to vue/}`. It passes only when the file imports hostnameMixin from `@/mixins/hostnames` and has a `mixins:` array containing `hostnameMixin`.
7. **Chain the script.** In `services/console/vue/package.json`, set `test:unit` to run `node tests/unit/env-json.cjs && node tests/unit/footer-hostnames.cjs`. Change no other key.
8. **Run RED.** Run `npm --prefix services/console/vue run test:unit` BEFORE touching Layout.vue. Expected: non-zero exit, the two Layout footer checks and Layout's mixin-declared check failing, and Login and PasswordReset passing. Save the output for the SUMMARY as RED evidence.

**C. GREEN: the fix (D-09; the verifier's preferred minimal option).**
1. In `services/console/vue/src/components/Layout/Layout.vue`, add `import hostnameMixin from '@/mixins/hostnames';` on the line directly after the ImpersonationBanner import. Use single quotes, as Layout's other imports do.
2. Add `mixins: [hostnameMixin],` on the line directly after `components: { Sidebar, Header, ImpersonationBanner },`, with two-space indentation.
3. Leave the template, including line 12, and every other line byte-identical.
4. Do not edit `main.js`. The verifier's alternative, populating `Vue.prototype.$hostnames` once or registering the mixin with `Vue.mixin`, is rejected: a global mixin runs the hostnames `data()` and `created()` on every component instance, library components included, and four pages already use the per-component pattern.
5. Re-run `npm --prefix services/console/vue run test:unit`. It must exit 0.

**D. Submodule commit and push.**
1. Stage exactly the three submodule files.
2. Commit with the header `fix(vue): give Layout the hostnames mixin so the footer links get an href`. Use it verbatim, because the verifies find the fix commit by this header. The body names the phase 22 gap (CI-03) and the new footer test. The commitlint hook enforces conventional format; if it rejects the message, fix the message, not the hook.
3. Run `git -C services/console push origin thinx-staging`.
4. Never push the submodule's main, and never force-push.

**E. Parent pointer bump.**
1. From the repo root, run `git add services/console` and stage nothing else. Never use `git add -A` or `git add .`.
2. Commit `chore(22): bump console submodule for the Layout footer hostnames mixin (CI-03)`. The commit must contain only the gitlink.

**F. Push the parent to thinx-staging only.**
1. Wait until the thinx-staging CircleCI queue has drained. Poll the public v1.1 tree endpoint (`limit=30`) every 30 s, up to 30 min, until the count of items whose lifecycle is not `finished` is 0. This avoids the registry push contention described in Pitfall 18.
2. Run the outgoing-diff credential scan from the verify block below. If it reports any hit, STOP.
   - The range also carries the pending `.planning` docs commits.
   - `22-REVIEW.md` quotes the ssh line that `AGENTS.md` already publishes on both remote branches. That is accepted, and WR-02 is out of scope.
3. Record `PRE_HASH`, the served buildHash before the push.
4. Run `git push origin thinx-staging`, and record `PUSHED=$(git rev-parse HEAD)`.
5. Never push to main. The push moves PR #569's head and starts a PR CodeQL run; leave the PR alone.

**G. Wait for the build.** Poll the tree endpoint (`limit=100`) every 30 s, up to 40 min, for the `vue-console-registry` job whose `vcs_revision` equals PUSHED.
- On `success`, continue.
- On `failed`, read only the name of the failing step.
  - If the failure is a registry login or push timeout, you cannot rerun the job without a token. Return a `checkpoint:human-action` asking the user to rerun the failed job in the CircleCI UI, then resume polling.
  - If the failure is `yarn build` or a compile error caused by this change, fix it forward once: a new submodule commit, push, bump and push. If it fails again, STOP and report.

**H. Wait for the rollout.** Poll the served bundle, cache-busted, every 30 s for up to 15 min after the job goes green, until its buildHash differs from PRE_HASH.
If it is still stale, use the swarm-autopull-recovery skill as the fallback. Load the micro ssh command as described in context. Stop at rung 1:
1. Read-only diagnosis: `thinx_vue` UpdatedAt, UpdateStatus and image through `docker service inspect thinx_vue` with a format string; the running task and node through `docker service ps thinx_vue --filter desired-state=running`; the swarmpit.thinx.cloud HTTP code.
2. Rung 1 only: `docker service update --force swarmpit_app`. Wait 90 s, confirm Swarmpit returns 200, then keep polling the bundle for another 10 min.
3. If the bundle is still stale, STOP and return a `checkpoint:human-action`. Rungs 2-4 need operator approval.
4. Never run restart.sh or docker stack deploy, and never update any service other than swarmpit_app.
5. If a permission classifier denies an ssh command, do not route around it. Return a `checkpoint:human-action` carrying the exact command.

**I. Prove the deploy.**
1. Run the served-bundle verify and the public-page headless Chrome verify.
2. Record in the SUMMARY:
   - the RED and GREEN test output
   - the submodule fix SHA, PUSHED, and the vue-console-registry job number
   - PRE_HASH and the served buildHash, plus the `layout_mixin` count
   - the public-page hrefs
   - whether recovery was needed
   - the submodule `Test Vue console` status, informational only
   - that the submodule's main now trails its thinx-staging by this one commit
  </action>
  <verify>
    <automated>O=$(mktemp) && npm --prefix services/console/vue run test:unit > "$O" 2>&1; rc=$?; echo "rc=$rc decl=$(grep -c '^ok   hostnames mixin declared: ' "$O") footer=$(grep -cE '^ok   (Layout|Login|PasswordReset) footer THiNX (Console|Cloud) -> https://' "$O") fallback=$(grep -c '^ok   Layout footer falls back to the serving origin' "$O") envjson=$(grep -c '^ok   both consoles agree' "$O")"</automated>
    <fails_when>output is not exactly `rc=0 decl=5 footer=6 fallback=1 envjson=1`. A non-zero rc means a check failed or the script threw. `footer=0` or `decl=0` means the test did not run or the line texts drifted from the spec. decl above 5 is acceptable only when step A.3 found and fixed another reader, as recorded in the SUMMARY</fails_when>
    <automated>N=$(mktemp) && M=$(mktemp) && git -C services/console diff --name-only a0e86707 HEAD > "$N" && git -C services/console diff --numstat a0e86707 HEAD -- vue/src/components/Layout/Layout.vue > "$M" && echo "files=$(sort "$N" | tr '\n' ' ')" && echo "layout=$(cut -f1,2 "$M" | tr '\t' '/') import=$(grep -cF "import hostnameMixin from '@/mixins/hostnames';" services/console/vue/src/components/Layout/Layout.vue) mixins=$(grep -cE '^  mixins: \[hostnameMixin\],$' services/console/vue/src/components/Layout/Layout.vue)"; git -C services/console diff --quiet a0e86707 HEAD -- vue/src/main.js && echo MAIN-JS-UNCHANGED</automated>
    <fails_when>the `files=` line is absent (a git diff failed) or is not exactly `vue/package.json vue/src/components/Layout/Layout.vue vue/tests/unit/footer-hostnames.cjs ` (plus only a documented A.3 file); `layout=` is not `2/0`; `import=` or `mixins=` is not 1; or MAIN-JS-UNCHANGED is absent</fails_when>
    <automated>D=$(mktemp) && git diff origin/thinx-staging..HEAD > "$D" && echo "outgoing_lines=$(grep -c '^+' "$D") secret_hits=$(grep '^+' "$D" | grep -cE 'BEGIN [A-Z ]*PRIVATE KEY|ghp_[A-Za-z0-9]{20,}|github_pat_|AKIA[0-9A-Z]{16}|xox[abpr]-|sk_live_')"</automated>
    <fails_when>no output line (the git diff failed, so the scan did not run), `outgoing_lines=0` before the push (the range is empty, so the scan checked nothing), or `secret_hits=` anything other than 0. Run it before the parent push</fails_when>
    <automated>git -C services/console fetch -q origin && git -C services/console merge-base --is-ancestor HEAD origin/thinx-staging && echo SUB-STAGING-HAS-FIX; SM=$(git -C services/console rev-parse --short=8 origin/main) && echo "sub_main=$SM"; git fetch -q origin && F=$(git -C services/console log -1 --format=%H --grep='^fix(vue): give Layout the hostnames mixin' thinx-staging) && test -n "$F" && G=$(git rev-parse -q --verify origin/thinx-staging:services/console) && git -C services/console merge-base --is-ancestor "$F" "$G" && echo PARENT-STAGING-POINTER-HAS-FIX; PM=$(git rev-parse --short=8 origin/main) && B=$(git log -1 --format=%H origin/thinx-staging -- services/console) && X=$(mktemp) && git show --name-only --format= "$B" > "$X" && echo "parent_main=$PM bump_files=$(tr '\n' ' ' < "$X")"</automated>
    <fails_when>SUB-STAGING-HAS-FIX or PARENT-STAGING-POINTER-HAS-FIX is absent; the `sub_main=` or `parent_main=` line is absent; `sub_main` is not `a0e86707`; `parent_main` is not `033ea946` (something was pushed to a main branch); or `bump_files` is not exactly `services/console `</fails_when>
    <automated>P=$(git log -1 --format=%H origin/thinx-staging -- services/console) && curl -sf 'https://circleci.com/api/v1.1/project/github/suculent/thinx-device-api/tree/thinx-staging?limit=100' | jq -r --arg s "$P" '[.[] | select(.vcs_revision==$s and .workflows.job_name=="vue-console-registry")][0] | "job=\(.build_num) status=\(.status)"'</automated>
    <fails_when>output is not `job={number} status=success`. `job=null status=null` means no vue-console-registry job ran for the pointer-bump commit; any status other than `success` means the build did not publish</fails_when>
    <automated>git fetch -q origin thinx-staging && T=$(mktemp) && curl -sf -H 'Cache-Control: no-cache' "https://console.thinx.cloud/js/app.js?cb=$(date +%s)" -o "$T"; H=$(grep -oE 'buildHash: \W*[0-9a-f]{7}' "$T" | grep -oE '[0-9a-f]{7}$' | head -1); L=$(awk '/^\/\*\*\*\/ ".*babel-loader.*src\/components\/Layout\/Layout\.vue\?vue&type=script&lang=js":$/{f=1;next} f&&/^\/\*\*\*\/ "/{exit} f' "$T" | grep -c 'mixins/hostnames'); C=$(grep -oE 'consoleHostname = \W*[a-z:/.]+' "$T" | head -1 | grep -oE '[a-z:/.]+$'); G=$(git rev-parse -q --verify "$H:services/console"); F=$(git -C services/console log -1 --format=%H --grep='^fix(vue): give Layout the hostnames mixin' thinx-staging) && test -n "$F"; echo "hash=$H layout_mixin=$L console=$C"; test -n "$G" && git merge-base --is-ancestor "$H" origin/thinx-staging && git -C services/console merge-base --is-ancestor "$F" "$G" && [ "$L" -ge 1 ] && [ "$C" = console.thinx.cloud ] && echo SERVED-BUNDLE-HAS-FIX</automated>
    <fails_when>SERVED-BUNDLE-HAS-FIX absent. `hash=9ccf9f1` (or the recorded PRE_HASH) means thinx_vue has not rolled out yet; `layout_mixin=0` with a new hash means the compiled Layout module lacks the mixin, or the bundle is no longer a dev-mode eval build and the module boundaries moved, in which case record that and rely on the Task 2 check; `console=` other than `console.thinx.cloud` means the build-arg chain broke (D-11)</fails_when>
    <automated>C="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"; D=$(mktemp -d); for r in login password-reset; do F=$(mktemp); timeout --foreground 40 "$C" --headless=new --disable-gpu --no-first-run --user-data-dir="$D" --virtual-time-budget=15000 --dump-dom "https://console.thinx.cloud/#/$r" > "$F" 2>/dev/null; echo "[$r] $(grep -oE 'href="[^"]*" target="_blank">THiNX (Console|Cloud)' "$F" | sort -u | tr '\n' ' ')"; done</automated>
    <fails_when>either line lacks `href="https://console.thinx.cloud" target="_blank">THiNX Console` or `href="https://thinx.cloud" target="_blank">THiNX Cloud` (an empty bracket line means Chrome timed out before rendering: re-run once before treating it as a regression)</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY holds the RED run: non-zero exit, with the Layout footer checks and Layout's mixin-declared check failing, taken before Layout.vue was edited.
    - The unit-test verify prints `rc=0 decl=5 footer=6 fallback=1 envjson=1`.
    - The diff-shape verify prints `files=vue/package.json vue/src/components/Layout/Layout.vue vue/tests/unit/footer-hostnames.cjs`, `layout=2/0 import=1 mixins=1`, and `MAIN-JS-UNCHANGED`.
    - `secret_hits=0` was printed before the parent push.
    - The push verify prints `SUB-STAGING-HAS-FIX`, `sub_main=a0e86707`, `PARENT-STAGING-POINTER-HAS-FIX`, `parent_main=033ea946`, and `bump_files=services/console`.
    - The CircleCI verify prints `job={number} status=success`.
    - The served-bundle verify prints `SERVED-BUNDLE-HAS-FIX`, with a hash different from PRE_HASH, `layout_mixin` of 1 or more, and `console=console.thinx.cloud`.
    - Headless Chrome prints both anchors on both `[login]` and `[password-reset]`.
    - `.circleci/config.yml` is unchanged: `git diff --quiet 9ccf9f18 origin/thinx-staging -- .circleci/config.yml && echo CI-CONFIG-UNCHANGED` prints `CI-CONFIG-UNCHANGED`.
  </acceptance_criteria>
  <done>Layout.vue declares the hostnames mixin and a plain-node test guards all three footers plus the defect class. The fix is on thinx-staging in both repos, and nothing reached main. A green vue-console-registry build of the pointer-bump commit is what console.thinx.cloud serves, and its compiled Layout module now pulls in the hostnames mixin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Logged-in check of the /app footer links on console.thinx.cloud</name>
  <read_first>
    - services/console/vue/src/components/Layout/Layout.vue (the footer at line 12 and the new mixins line)
    - .planning/phases/22-ci-sast-baseline/22-VERIFICATION.md (the gap's third "missing" item: the logged-in re-check)
  </read_first>
  <what-built>Layout.vue now gets its footer hrefs from the hostnames mixin. The fix was built by a green vue-console-registry job and is served at https://console.thinx.cloud; the served buildHash is the one recorded in Task 1. The public login and password-reset footers were re-checked headless. Only the authenticated /app footer needs a logged-in browser. If a logged-in Chrome tab is already available through the claude-in-chrome skill, the executor may read the two hrefs first and show them here, but the user's answer is what closes the check.</what-built>
  <how-to-verify>
    1. Open https://console.thinx.cloud/#/login and hard-reload with Cmd+Shift+R. The small build id under the login footer must equal the served buildHash recorded in Task 1, or you are on a cached bundle.
    2. Log in.
    3. On any /app page, for example https://console.thinx.cloud/#/app/dashboard, find the footer "THiNX Console by THiNX Cloud". Hover or inspect both links, or run this in the DevTools console: Array.from(document.querySelectorAll('footer.contentFooter a')).map(a => a.textContent + ' -> ' + a.getAttribute('href'))
    4. Expected: "THiNX Console -> https://console.thinx.cloud" and "THiNX Cloud -> https://thinx.cloud". Neither link may be missing its href.
    5. Click "THiNX Console". A new tab should open on console.thinx.cloud.
  </how-to-verify>
  <resume-signal>Type "approved", or paste the two hrefs you saw if they differ</resume-signal>
  <acceptance_criteria>
    - The SUMMARY records the user's answer verbatim, and the two hrefs if the user or the executor observed them.
    - On "approved", Task 3 runs.
    - On any other answer, Task 3 is skipped, CI-03 stays unchecked, the SUMMARY lists the observed hrefs as an open gap, and the plan ends without claiming the fix.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mark CI-03 complete and add a correction note to 22-02-SUMMARY (only after "approved")</name>
  <precondition>Task 2 resumed with "approved"</precondition>
  <files>.planning/REQUIREMENTS.md, .planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 14 CI-03 checkbox; traceability table rows at lines 84-86)
    - .planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md (frontmatter requirements-completed and coverage D2; bold summary paragraph at line 96; "## Performance" at line 98; "Decisions Made" bullet on CI-03)
  </read_first>
  <action>
1. **REQUIREMENTS.md.** Make exactly two edits:
   - Change the CI-03 bullet's checkbox from unchecked to checked and keep the requirement text as it is.
   - In the traceability table, change the CI-03 row's status cell from `Gaps Found` to `Complete`.

   Leave the CI-01 and CI-02 lines and rows alone. Commit `1cf4b401` reverted them wholesale when the phase had gaps. The verifier found both satisfied, and the phase re-verification that runs after this plan is what restores them. This plan must not pre-empt that step.
2. **22-02-SUMMARY.md.** Insert a new section headed `## Correction (22-04 gap closure)` right after the bold summary paragraph and before `## Performance`. Write 3 to 5 plain sentences that say:
   - The summary's claim that all three THiNX Console links resolve to https://console.thinx.cloud was premature. So were the `requirements-completed: [CI-03]` frontmatter and the "CI-03 is marked complete" decision.
   - The logged-in Layout footer check (coverage D2, human_judgment) was never confirmed. The link in fact rendered with no href, because Layout.vue lacked the hostnames mixin. That defect predates phase 22 and was found by 22-VERIFICATION.
   - It was fixed in console commit {short SHA} and deployed as buildHash {7 hex}. The logged-in check was approved on {UTC date}. See 22-04-SUMMARY.

   Change nothing else in the file, frontmatter included. It stays as the historical record, and the note corrects it.
  </action>
  <verify>
    <automated>S=$(git diff --numstat 1cf4b401 -- .planning/REQUIREMENTS.md) && echo "req=$(grep -c '^- \[x\] \*\*CI-03\*\*' .planning/REQUIREMENTS.md) row=$(grep -c '^| CI-03 | Phase 22 | Complete |$' .planning/REQUIREMENTS.md) numstat=$(printf '%s\n' "$S" | cut -f1,2 | tr '\t' '/')"</automated>
    <fails_when>output is not exactly `req=1 row=1 numstat=2/2`. `numstat` above 2/2 means a CI-01 or CI-02 line, or other text, was changed as well</fails_when>
    <automated>S=$(git diff --numstat 6cd8a5f6 -- .planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md) && echo "note=$(grep -c '^## Correction (22-04 gap closure)$' .planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md) removed=$(printf '%s\n' "$S" | cut -f2) order=$(awk '/^## Correction \(22-04 gap closure\)$/{c=NR} /^## Performance$/{p=NR} END{print (c && p && c<p) ? "before-performance" : "misplaced"}' .planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md)"</automated>
    <fails_when>output is not exactly `note=1 removed=0 order=before-performance`. `removed` above 0 means existing summary text was rewritten instead of annotated</fails_when>
  </verify>
  <acceptance_criteria>
    - The REQUIREMENTS verify prints `req=1 row=1 numstat=2/2`.
    - The 22-02-SUMMARY verify prints `note=1 removed=0 order=before-performance`.
    - The correction note names the console fix SHA, the served buildHash, and the approval date, and it contains no host, port, key, or environment value.
  </acceptance_criteria>
  <done>CI-03 is checked and Complete in REQUIREMENTS.md, backed by the logged-in approval. CI-01 and CI-02 are left for the phase re-verification. 22-02-SUMMARY carries a short, additive correction of its overclaim.</done>
</task>

</tasks>

<flagged_assumptions>
- **A-22-04-1 (CI-03 edge-probe row: unclassified, unresolved).** The spec-less edge probe could not classify CI-03, so this row stays unresolved and is flagged for manual review. The plan handles five edges explicitly, but none of them was derived from the probe:
  - (a) An unset var falls back to `window.location.origin`. The unit test's fallback line asserts this.
  - (b) A cached `app.js` in the browser or at the edge. Verification uses a cache-busting query, and the human check requires a hard reload and a build-id match.
  - (c) Swarmpit may not redeploy `thinx_vue`. The fallback is swarm-autopull-recovery rung 1, then STOP.
  - (d) A later push can change buildHash. The bundle check uses commit ancestry plus a gitlink check rather than equality with PUSHED.
  - (e) A future production-mode (minified) build would move the module boundaries the awk relies on, giving `layout_mixin=0` with a new hash. In that case the logged-in check is the authority.
- **A-22-04-2 (submodule main not pushed).** The orchestrator scoped all pushes to thinx-staging. Project memory records a prior practice of keeping the console submodule's `main` and `thinx-staging` equal. After this plan, the submodule's `main` trails by the fix commit. Syncing it is the user's call, and the SUMMARY must say so.
- **A-22-04-3 (PR #569 head moves).** Pushing parent thinx-staging updates the open PR #569 and starts a new PR CodeQL run. 22-03's evidence (run 36139870033, head 9ccf9f18) stays valid as the historical record. The PR stays open and unmerged.
- **A-22-04-4 (docs commits go public).** The parent push publishes the pending `.planning` docs commits. `22-REVIEW.md` quotes the ssh line that `AGENTS.md` already publishes on both remote branches, so nothing new is disclosed. WR-02 stays out of scope.
- **Out of scope (not planned):** review WR-01/WR-02, A-04, IN-01..IN-06, the main-push CodeQL row (pending PR #569), and the logged-in rtm classic-console retest (a separate human item in 22-VERIFICATION).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation to GitHub remotes | Pushes to thinx-staging in two repos. thinx-staging is the production deploy path, and main is PR-gated but bypassable |
| CircleCI build to private registry to swarm | Build args carry the console, landing and API hosts; Swarmpit autoredeploys `thinx_vue` from the registry |
| Public internet to console.thinx.cloud | Every authenticated user sees the footer links, and a wrong href sends them elsewhere |
| Repo (public) to anyone | Committed plan, summary, test and commit text are world-readable |
| Operator workstation to swarm manager (fallback only) | Root ssh; only the rung-1 swarmpit_app restart is allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-14 | Spoofing | Layout footer anchors (the href target) | low | mitigate | The href comes only from the build-time `VUE_APP_CONSOLE_HOSTNAME` / `VUE_APP_LANDING_HOSTNAME` through the hostnames mixin, and `fixUrlProtocol` adds https. No route, query or user input reaches it. The unit test pins https://console.thinx.cloud and https://thinx.cloud. The template stays byte-identical (`layout=2/0`), so nothing is hardcoded and the build arg is untouched (D-11). |
| T-22-15 | Tampering / Elevation of privilege | Pushes to main in either repo; PR #569 | medium | mitigate | Explicit `push origin thinx-staging` refspecs in both repos, and no `--force`. The verify asserts `parent_main=033ea946` and `sub_main=a0e86707` after the push. PR #569 is never merged or approved. |
| T-22-16 | Denial of service | A broken Vue bundle deployed to console.thinx.cloud | low | mitigate | The unit test runs before the push. CircleCI `yarn build` fails the job before publishing if the code does not compile. The public-page headless check and the logged-in check run after the deploy. Rollback is a revert plus a push, or `docker service rollback thinx_vue`. |
| T-22-17 | Information disclosure | CircleCI output, service env, ssh details, commit and summary text | medium | mitigate | Only step names and the required-vars success line are read. Service inspection uses format strings for UpdatedAt, UpdateStatus and image, never env values. The ssh command is loaded from `~/.aliases` at run time. A credential-pattern scan of the outgoing diff must print `secret_hits=0` before the parent push. |
| T-22-18 | Elevation of privilege / Repudiation | Stack-wide redeploy resetting the chronograf credentials during rollout recovery | high | mitigate | Recovery is limited to swarm-autopull-recovery rung 1 (`docker service update --force swarmpit_app`). restart.sh, docker stack deploy and updates to any other service are forbidden, and rungs 2-4 STOP for operator approval. The SUMMARY records whether recovery ran. |
| T-22-19 | Information disclosure | Pending `.planning` docs published by the parent push (22-REVIEW.md quotes the AGENTS.md ssh line) | low | accept | Nothing new is disclosed: the same line is already public in `AGENTS.md` on origin/main and origin/thinx-staging. WR-02 (removing it) is tracked separately and is out of scope here. The token scan still guards against actual credentials. |
| T-22-20 | Tampering | Test harness evaluating source text with `new Function` | low | accept | It is test-only and evaluates only the repo's own `src` files, locally, following the existing `env-json.cjs` eval precedent. `tests/` is outside the webpack entry and the nginx runtime stage, so nothing in it ships. |
</threat_model>

<verification>
- Pre-deploy: `npm --prefix services/console/vue run test:unit` goes RED on the unfixed Layout and GREEN (`rc=0 decl=5 footer=6 fallback=1 envjson=1`) after the two-line fix. The diff shape is `layout=2/0` and main.js is unchanged.
- Ship: submodule and parent thinx-staging both carry the fix, both main branches are unchanged, and the bump commit touches only the gitlink.
- Build and serve: vue-console-registry is `success` for the bump commit. The served bundle prints `SERVED-BUNDLE-HAS-FIX`, and the public pages still render both anchors.
- Logged in: the user approves the /app footer hrefs https://console.thinx.cloud and https://thinx.cloud.
- Bookkeeping: CI-03 is `[x]` and Complete (`numstat=2/2`), and the 22-02-SUMMARY correction is additive (`removed=0`).
</verification>

<success_criteria>
- ROADMAP SC3 / CI-03 holds in full: in the live Vue bundle the layout, login and password-reset "THiNX Console" links all point at https://console.thinx.cloud, and the layout's "THiNX Cloud" link has its landing href back. This is proven by the served bundle, headless Chrome on the public pages, and a logged-in check.
- The defect class is closed and guarded: every `$hostnames` reader declares the mixin, and `test:unit` fails if a future component forgets it.
- No push to main, no PR merge, no stack-wide redeploy. CI-03 is marked complete only after the logged-in approval.
</success_criteria>

## Artifacts this phase produces (plan 04)

- `services/console/vue/src/components/Layout/Layout.vue` (edited, console submodule): imports `hostnameMixin` from `@/mixins/hostnames` and declares `mixins: [hostnameMixin]`. The template is unchanged.
- `services/console/vue/tests/unit/footer-hostnames.cjs` (new, console submodule): a plain-node unit test that renders the real footers of Layout, Login and PasswordReset with their real mixins, checks the unset-var fallback, and sweeps `src` for `$hostnames` readers that lack the mixin.
- `services/console/vue/package.json` (edited, console submodule): `test:unit` chains `env-json.cjs` and `footer-hostnames.cjs`. No dependency changes.
- `services/console` gitlink (parent): bumped to the console fix commit.
- `.planning/REQUIREMENTS.md` (edited): CI-03 is `[x]` and `Complete`.
- `.planning/phases/22-ci-sast-baseline/22-02-SUMMARY.md` (edited): an additive `## Correction (22-04 gap closure)` section.
- Remote, not files:
  - `thinx-cloud/console` thinx-staging and `thinx-device-api` thinx-staging both advance.
  - A new private-registry `thinx/console:vue` image.
  - A new `thinx_vue` task serving the fixed bundle.
- No new CLI flags or runtime config, and no dependency changes. The only new function-level code is inside the test file.

<output>
Create `.planning/phases/22-ci-sast-baseline/22-04-SUMMARY.md` when done
</output>
