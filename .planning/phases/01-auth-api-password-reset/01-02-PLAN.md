---
phase: 01-auth-api-password-reset
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - spec/jasmine/ZZ-RouterPasswordResetSpec.js
autonomous: false
requirements:
  - AUTH-API-01

must_haves:
  truths:
    - "A new chai-http v4 regression spec at `spec/jasmine/ZZ-RouterPasswordResetSpec.js` covers AUTH-API-01 (c): unauthenticated POST with `Authorization: Bearer null` returns 200; identical for `Bearer undefined` / empty Bearer; legacy alias parity; unregistered email no-enum 200."
    - "The new spec runs green locally on the just-fixed branch and red against pre-fix `main` (proves it would have caught the regression)."
    - "`curl -X POST https://rtm.thinx.cloud/api/v2/password/reset -H 'Authorization: Bearer null' -H 'Content-Type: application/json' -d '{\"email\":\"x@y\"}'` returns HTTP 200 after the rtm deploy lands (was 403 before)."
    - "The Vue console 'Forgot password?' flow on rtm completes the full email → reset_key → set-password round-trip (UAT criterion AUTH-API-01 (b))."
    - "Root cause + reversion plan documented in the phase SUMMARY/close-out (ROADMAP Phase 1 success criterion 5)."
  artifacts:
    - path: "spec/jasmine/ZZ-RouterPasswordResetSpec.js"
      provides: "Regression coverage for the `Bearer null` / `Bearer undefined` / empty-Bearer / unregistered-email no-enum / legacy-alias-parity cases"
      min_lines: 50
      contains: "Router Password Reset (G8 regression)"
  key_links:
    - from: "spec/jasmine/ZZ-RouterPasswordResetSpec.js"
      to: "bootstrap.thx + chai.request(thx.app)"
      via: "chai-http v4 `chai.request(thx.app).post(...).set('Authorization', 'Bearer null').end(...)`"
      pattern: "chai\\.request\\(thx\\.app\\)\\s*\\.post\\('/api/v2/password/reset'\\)"
    - from: "rtm.thinx.cloud Vue console"
      to: "Node API `POST /api/v2/password/reset`"
      via: "Browser fetch with `Authorization: Bearer null` (from Vue `core/api.js:53-57`) — must now return 200, not 403"
      pattern: "Bearer null"
---

<objective>
Ship the regression coverage and the end-to-end verification that closes AUTH-API-01:

1. **New chai-http v4 regression spec** at `spec/jasmine/ZZ-RouterPasswordResetSpec.js` that explicitly exercises the `Authorization: Bearer null` request shape (the exact rtm-observed bug), plus symmetric coverage for `Bearer undefined`, empty Bearer, legacy alias parity, and the no-enum 200 contract for unregistered emails. The existing v2 reset spec at `ZZ-AppSessionUserV2DeleteSpec.js:85-97` does NOT cover the `Bearer null` case (it omits the header) and would not have caught the regression.

2. **Local reproduction + verification** — confirm the spec fails on pre-fix `main` and passes after plan 01-01 lands. This is the "would-have-caught-it" gate.

3. **Push + CI + deploy** — push `thinx-staging`, wait for CircleCI green, run `./scripts/stack-deploy` manually because `OPS-swarmpull` is broken (per MEMORY.md / RESEARCH.md Pitfall 4 — incident 2026-05-25 14:44 CET; ops fix is Phase 3, not this phase).

4. **rtm curl reproduction post-deploy** — confirm `curl -X POST https://rtm.thinx.cloud/api/v2/password/reset -H 'Authorization: Bearer null' ...` returns 200, not 403. This is ROADMAP Phase 1 success criterion 1.

5. **Vue console "Forgot password?" UAT** — human verifies the round-trip (email submission → email received → reset_key consumed → new password works on `/login`). This is ROADMAP Phase 1 success criterion 2 (`AUTH-API-01 (b)`).

Purpose: Lock in regression coverage so any future code change that re-introduces the JWT-branch-fires-on-header-presence bug will fail in CI before it ships, and confirm on the live host that the bug is actually closed for real users.

Output:
- One new spec file (`ZZ-RouterPasswordResetSpec.js`) committed atomically.
- A passing CircleCI build on `thinx-staging`.
- A clean `./scripts/stack-deploy` run with verified image rollover on rtm.
- A green human-verified Vue UAT walk on rtm.
- A close-out `01-SUMMARY.md` documenting the root cause, the smallest-change rationale, and the reversion plan (per ROADMAP Phase 1 success criterion 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auth-api-password-reset/01-CONTEXT.md
@.planning/phases/01-auth-api-password-reset/01-RESEARCH.md
@.planning/phases/01-auth-api-password-reset/01-01-PLAN.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

@spec/jasmine/ZZ-AppSessionUserV2DeleteSpec.js
@spec/jasmine/ZZ-AppSessionUserSpec.js
@spec/_envi.json
@spec/helpers/bootstrap.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From spec/helpers/bootstrap.js (the canonical ZZ-* spec bootstrap):
- `module.exports = state` where `state.thx` is a constructed-and-initialized `THiNX` instance (set in a global `beforeAll` that runs once per integration suite).
- New ZZ-* specs `require('../helpers/bootstrap')` and read `bootstrap.thx` inside their own `beforeAll`. Do NOT construct a second `THiNX()` — `Messenger.getInstance(...)` is a singleton and a double-init breaks the suite.

From spec/_envi.json (test fixture seeded by `02-OwnerSpec.js`):
- `envi.dynamic.owner` / `envi.dynamic.email` — registered user used by reset success-path tests. Already consumed by `ZZ-AppSessionUserSpec.js:182-196`. Available because `02-` runs before `ZZ-`.
- `envi.dynamic2.owner` / `envi.dynamic2.email` — alternate registered user, used by `ZZ-AppSessionUserV2DeleteSpec.js:85-97`.

From spec/jasmine/ZZ-AppSessionUserV2DeleteSpec.js (template the new spec adapts):
- Uses `chai.request(thx.app).post('/api/v2/password/reset').send({ email: envi.dynamic2.email }).end((_err, res) => { ... })` — the v4 chai-http idiom (per AGENTS.md L83-92 lock).
- Every `it(...)` ends with `}, 30000);` — explicit 30s per-spec timeout (CI cold-start sometimes exceeds 10s).
- `beforeAll` emits `🚸 [chai] >>> running <Name> spec`; `afterAll` emits `🚸 [chai] <<< completed <Name> spec`.

From RESEARCH.md §"Verified pattern — adding a regression spec (chai-http v4)":
The exact spec body to write is documented in RESEARCH.md L391-486. Use it as the implementation template, adjusted to match the conventions above.

From CONVENTIONS.md:
- `chai-http` pinned at `^4.3.0`. MUST NOT bump to v5. Use `chai.request(thx.app)`, NOT `request.execute(thx.app)`.
- CommonJS only (`require` / `module.exports`), not ESM.
- Emoji-prefixed log markers in `beforeAll`/`afterAll` (`🚸 [chai] >>> running ... spec`).

From TESTING.md "CI Execution":
- Local full-suite command: `npm test` (or, if the local node env lacks the docker services, `docker compose -f docker-compose.test.yml up --build api | tee ./test.log` and grep for `specs, 0 failures`).
- Single-spec filter: `npm run jasmine -- --filter='Router Password Reset'` works because Jasmine 5 supports a name-substring filter via the runtime CLI.

From AGENTS.md L8-19 (deploy):
- Push `thinx-staging` to trigger CircleCI. Do NOT wait for the user to do this manually (L11).
- Swarm host: `ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020`; swarm path `/mnt/gluster/deployment/swarm`.
- Manual redeploy: `./scripts/stack-deploy` (because OPS-swarmpull is broken, per MEMORY.md / RESEARCH.md Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create chai-http v4 regression spec ZZ-RouterPasswordResetSpec.js</name>
  <files>spec/jasmine/ZZ-RouterPasswordResetSpec.js</files>
  <behavior>
    A new Jasmine spec file that asserts the AUTH-API-01 contract under the exact request shapes that triggered the rtm regression. Each `it(...)` block is one behavior:
    - Test 1 (the original G8 trigger): `POST /api/v2/password/reset` with header `Authorization: Bearer null` and body `{email: envi.dynamic.email}` (registered) returns HTTP 200. Pre-fix this would 403 at `lib/router.js:132`. Post-fix the guard from plan 01-01 falls through and the route handler responds 200 with the standard envelope.
    - Test 2 (symmetric): `POST /api/v2/password/reset` with header `Authorization: Bearer undefined` and body `{email: envi.dynamic.email}` returns HTTP 200.
    - Test 3 (symmetric, edge): `POST /api/v2/password/reset` with header `Authorization: Bearer ` (empty token after the Bearer prefix) and body `{email: envi.dynamic.email}` returns HTTP 200.
    - Test 4 (no-enum, unregistered): `POST /api/v2/password/reset` with NO Authorization header and body `{email: 'nobody-known-' + Date.now() + '@example.invalid'}` returns HTTP 200 (was 401 before plan 01-01). Confirms AUTH-API-01 (b) status normalization.
    - Test 5 (no-enum, empty body): `POST /api/v2/password/reset` with empty body `{}` returns HTTP 200 (was 401 before plan 01-01).
    - Test 6 (legacy alias parity, D-01): `POST /api/user/password/reset` with `Authorization: Bearer null` and body `{email: envi.dynamic.email}` returns HTTP 200.
    - Test 7 (baseline regression-safety): `POST /api/v2/password/reset` with NO Authorization header and body `{email: envi.dynamic.email}` returns HTTP 200 (already passes today; locked in here to detect any future widening of the JWT branch that would break the existing baseline).

    Each test asserts `res.status === 200` and `typeof res.text === 'string'`. Body content is intentionally NOT locked beyond envelope shape (`success`/`response` properties present) because (a) the test env returns the reset_key on the registered-success path, which is sensitive to upstream changes in `lib/thinx/owner.js` that are outside this phase's scope, and (b) per the no-enum contract, the body's exact text MAY differ between registered and unregistered while the status MUST NOT.
  </behavior>
  <action>
Create a new file `spec/jasmine/ZZ-RouterPasswordResetSpec.js` following the exact pattern documented in RESEARCH.md §"Verified pattern — adding a regression spec (chai-http v4)" (L391-486). Implementation requirements:

**File-level structure:**
- CommonJS `require`s at the top (NO `import`). Follow `ZZ-AppSessionUserV2DeleteSpec.js` import order: `bootstrap`, `chai`, `chai.expect`, `chai-http` + `chai.use(chaiHttp)`, `_envi.json`.
- Single top-level `describe("Router Password Reset (G8 regression)", function () { ... })`.
- `beforeAll((done) => { console.log("🚸 [chai] >>> running Router Password Reset spec"); thx = bootstrap.thx; done(); })`.
- `afterAll((done) => { console.log("🚸 [chai] <<< completed Router Password Reset spec"); done(); })`.

**Per-test structure:** Each `it(...)` ends with `}, 30000);` (30s timeout — per TESTING.md "Patterns enforced"). Use `chai.request(thx.app).post(...).set('Authorization', '...').send({...}).end((_err, res) => { ... })` — NOT `.execute(...)` (that is chai-http v5). Use `_err` (underscore-prefixed) per the ESLint `no-unused-vars` rule (`argsIgnorePattern: "^_"`).

**The seven tests** (mapping the behavior block above):

1. `"POST /api/v2/password/reset (Authorization: Bearer null) — does not 403 (G8 regression)"` — sets `Authorization: 'Bearer null'`, sends `{ email: envi.dynamic.email }`, expects `res.status === 200`.
2. `"POST /api/v2/password/reset (Authorization: Bearer undefined) — does not 403"` — sets `Authorization: 'Bearer undefined'`, same body, expects 200.
3. `"POST /api/v2/password/reset (Authorization: Bearer ) — does not 403"` — sets `Authorization: 'Bearer '` (note trailing space, empty token), same body, expects 200.
4. `"POST /api/v2/password/reset (no Auth, unregistered email) — 200 no-enum"` — no `.set('Authorization', ...)` call, sends `{ email: 'nobody-known-' + Date.now() + '@example.invalid' }`, expects 200.
5. `"POST /api/v2/password/reset (no Auth, empty body) — 200 no-enum"` — no Authorization, `.send({})`, expects 200.
6. `"POST /api/user/password/reset (Authorization: Bearer null, legacy alias) — does not 403"` — legacy v1 alias URL, same `Bearer null` header, registered email, expects 200.
7. `"POST /api/v2/password/reset (no Auth, registered email) — 200 baseline"` — no Authorization, registered email, expects 200 (the existing-baseline contract).

For each test the assertion block MUST include:
```
expect(res.status).to.equal(200);
expect(res.text).to.be.a('string');
let body = JSON.parse(res.text);
expect(body).to.have.property('success');
expect(body).to.have.property('response');
done();
```

DO NOT assert on `body.success === true` or on the exact `body.response` string — per the no-enum contract, body content may differ across the seven cases while the envelope shape and status code MUST be uniform.

**Order of `it(...)` blocks:** Match the seven-test enumeration above (numbered 1-7). Do NOT consume the `reset_key` from any test in this spec — round-trip continuity is covered by `ZZ-AppSessionUserV2DeleteSpec.js:99-108` (`GET /api/v2/password/reset?owner_id=...&reset_key=...`) which is unaffected by this plan.

**ESLint:**
- The spec MUST pass `npm run lint -- spec/jasmine/ZZ-RouterPasswordResetSpec.js`.
- Unused error callbacks MUST use `_err`.
- Match the surrounding style (no Prettier; semicolons; double or single quotes per file consistency — `ZZ-AppSessionUserV2DeleteSpec.js` uses single quotes for URLs and double quotes for header values; copy that pattern).

**Atomic commit:** `test(spec): G8 regression — Authorization: Bearer null on /api/v2/password/reset` (per D-04 atomic-commit-per-fix convention).

**Anti-regression check before committing:** Run the spec in isolation against the just-fixed branch (`npm run jasmine -- --filter='Router Password Reset'`); all seven tests MUST pass. Then OPTIONAL but recommended for the SUMMARY's "would-have-caught-it" claim — stash the plan 01-01 changes (`git stash`), re-run the same filter, observe at least tests 1, 2, 3, 4, 5, 6 FAIL (with status 403 for tests 1/2/3/6, status 401 for tests 4/5), unstash, re-run, observe all pass. Record the pre-fix failure counts in the SUMMARY. If git-stash is impractical because of overlapping context, document the would-have-caught-it claim by referencing RESEARCH.md Ranked Candidate #1 evidence instead.
  </action>
  <verify>
<automated>node -c spec/jasmine/ZZ-RouterPasswordResetSpec.js &amp;&amp; npm run lint -- spec/jasmine/ZZ-RouterPasswordResetSpec.js &amp;&amp; grep -c "Router Password Reset (G8 regression)" spec/jasmine/ZZ-RouterPasswordResetSpec.js | (read n; [ "$n" -eq 1 ] || exit 1) &amp;&amp; grep -c "Bearer null" spec/jasmine/ZZ-RouterPasswordResetSpec.js | (read n; [ "$n" -ge 2 ] || exit 1) &amp;&amp; grep -c "}, 30000);" spec/jasmine/ZZ-RouterPasswordResetSpec.js | (read n; [ "$n" -ge 7 ] || exit 1) &amp;&amp; grep -c "chai\\.request(thx\\.app)" spec/jasmine/ZZ-RouterPasswordResetSpec.js | (read n; [ "$n" -ge 7 ] || exit 1) &amp;&amp; ! grep -q "request\\.execute" spec/jasmine/ZZ-RouterPasswordResetSpec.js &amp;&amp; ! grep -q "^import" spec/jasmine/ZZ-RouterPasswordResetSpec.js</automated>

Static-gate breakdown:
1. File parses (`node -c`).
2. ESLint clean.
3. The `describe("Router Password Reset (G8 regression)" ...)` header is present exactly once.
4. The string `Bearer null` appears at least twice (Test 1 v2 + Test 6 legacy alias).
5. At least 7 `it(...)` blocks with the 30000 ms timeout (one per test in the behavior section).
6. At least 7 `chai.request(thx.app)` call sites.
7. No `request.execute` (that is chai-http v5, banned per AGENTS.md L83-92).
8. No `import` keyword at line start (no ESM, CommonJS only per CONVENTIONS.md).

Functional gate (mandatory before commit): `npm run jasmine -- --filter='Router Password Reset'` runs and reports 7/7 specs passing AND `npm test` (or docker-compose equivalent) full-suite still reports `specs, 0 failures`. The new spec must NOT regress any sibling test (especially `ZZ-AppSessionUserSpec.js`, `ZZ-AppSessionUserV2DeleteSpec.js`, `ZZ-RouterAPIKeySpec.js`, `ZZ-AppSession.js`).
  </verify>
  <done>
    - `spec/jasmine/ZZ-RouterPasswordResetSpec.js` exists with 7 `it(...)` blocks covering the cases enumerated in the behavior block.
    - Each test uses chai-http v4 `chai.request(thx.app).post(...).set(...).send(...).end(...)` style.
    - No `import` statements; no `request.execute` calls; no chai-http v5 idioms.
    - `npm run jasmine -- --filter='Router Password Reset'` reports 7 passing.
    - `npm test` reports `specs, 0 failures` for the full suite.
    - One atomic commit landed on `thinx-staging` with message starting `test(spec): G8 regression`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Push thinx-staging, wait for CircleCI green, run manual stack-deploy</name>
  <files>(none — this is the deploy task; no source files change)</files>
  <action>
**Sub-step 2a — Push the branch:**

Per AGENTS.md L11 ("push the commit to the current branch to trigger CircleCI instead of waiting for the user to do it manually"), push `thinx-staging` to origin. Three commits should be present (two from plan 01-01, one from this plan's Task 1).

```bash
git push origin thinx-staging
```

If the push is rejected because of remote divergence, STOP and surface the conflict to the user — do NOT force-push (per the GSD planner safety protocol). Resolving remote divergence in a hotfix branch is a human decision.

**Sub-step 2b — Watch CircleCI:**

CircleCI runs the test job from `.circleci/config.yml:129-316` inside `docker-compose.test.yml`. The success gate (per TESTING.md "CI Execution") is `grep "specs, 0 failures" ./test.log`. Watch the job at the CircleCI dashboard URL (the project root README or CircleCI default URL for `suculent/thinx-device-api` workflow). Acceptable wait time: ~10-15 minutes for a cold pipeline.

If CI fails:
- If failure is in `ZZ-RouterPasswordResetSpec.js` (the new spec): diagnose locally first; do NOT push a quick-fix commit without re-running the full suite locally.
- If failure is in an unrelated spec that was green pre-push: surface to the user — could be flaky test or environment drift, not necessarily caused by this plan.
- If both `lib/router.js` lint and `lib/router.user.js` lint passed locally but CircleCI's `npm run lint` fails: surface — likely an ESLint version skew.

Do NOT proceed to sub-step 2c until CI is green on the most recent thinx-staging SHA.

**Sub-step 2c — Manual stack-deploy (OPS-swarmpull workaround):**

Per RESEARCH.md Pitfall 4 + MEMORY.md `swarmpit auto-pull broken since 2026-05-25 14:44 CET`, Swarmpit will NOT auto-redeploy on rtm even after CircleCI publishes the new `thinxcloud/api:latest` and `registry.thinx.cloud:5000/thinx/console:vue` images. Run the manual fallback:

```bash
ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020 'cd /mnt/gluster/deployment/swarm && ./scripts/stack-deploy'
```

(Adapt the command if `./scripts/stack-deploy` lives at a different path on the swarm host — `AGENTS.md:8-19` says swarm path is `/mnt/gluster/deployment/swarm`; the `scripts/stack-deploy` invocation is the same convention used in console Phase 11.)

Wait for the script to complete and verify the API service task picked up the new image:

```bash
ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020 'docker service ls | grep thinx_api'
```

The `IMAGE` column should show a fresh image hash (compare against the previous deploy SHA from MEMORY.md `session-handoff-2026-05-23` or whatever the last known state is). If the hash is unchanged after `stack-deploy` completes, surface to the user — could indicate registry-auth issue or that the parent submodule pointer didn't actually advance.

Sub-step 2c is "deploy-or-document": if ssh access is unavailable in this session, document the manual `stack-deploy` invocation in the SUMMARY and hand off the deploy step to the user. Do NOT skip the curl verification in Task 3 — defer Task 3 until the deploy is confirmed.
  </action>
  <verify>
<automated>git log --oneline -5 | head -5</automated>

Plus manual verification points (documented in SUMMARY):
- `git log --oneline origin/thinx-staging -5` matches the local last 5 commits (push succeeded).
- CircleCI job status = "success" on the latest `thinx-staging` SHA (visible in CircleCI dashboard / GitHub status check).
- `docker service ls | grep thinx_api` on rtm shows an updated image hash after `./scripts/stack-deploy`.
- (No bash automation can fully verify the remote deploy state from this session; this verification is partially manual by design.)
  </verify>
  <done>
    - `thinx-staging` pushed to origin with all three plan commits visible at the remote.
    - CircleCI green on the head of `thinx-staging`.
    - `./scripts/stack-deploy` run successfully on rtm (or invocation documented in the SUMMARY for human follow-up if ssh access is unavailable in this session).
    - `docker service ls` on rtm confirms the new image hash (or evidence documented in SUMMARY).
  </done>
</task>

<task type="auto">
  <name>Task 3: rtm curl reproduction — confirm Bearer null no longer 403s</name>
  <files>(none — this is the live-host verification task; no files modified)</files>
  <action>
Per RESEARCH.md §"Diagnostic Playbook" Step 2 and ROADMAP Phase 1 success criterion 1, reproduce the exact rtm-observed request shape from the host machine (not from inside the swarm — we want to verify the fix as a real browser would experience it). Two curl invocations:

**Pre-fix baseline reminder (informational, do NOT run if the deploy from Task 2 has already landed):**

```bash
# Pre-fix this returned HTTP/2 403 with Content-Length: 0 (per RESEARCH.md Step 2).
# Skipped because deploy has rolled the fix.
```

**Post-fix verification (RUN THIS):**

```bash
curl -sS -i -X POST https://rtm.thinx.cloud/api/v2/password/reset \
     -H 'Content-Type: application/json' \
     -H 'Origin: https://rtm.thinx.cloud' \
     -H 'Authorization: Bearer null' \
     --data '{"email":"someone-known-to-not-exist-' "$(date +%s)" '@example.invalid"}'
```

Expected response:
- `HTTP/2 200` (the fix landed).
- `Content-Type: application/json; charset=utf-8` (the route handler ran; `enforceACLHeaders` no longer skipped because the guard fell through).
- `Access-Control-Allow-Origin: https://rtm.thinx.cloud` (CORS reflection ran post-fallthrough, per `lib/router.js:32-56`).
- Body = `{"success":...,"response":"..."}` envelope. The `success` and `response` fields are non-empty strings/bools; exact body content is not asserted per the no-enum contract.

**Negative-control curl** (proves the JWT branch still works for real malformed tokens):

```bash
curl -sS -i -X POST https://rtm.thinx.cloud/api/v2/password/reset \
     -H 'Content-Type: application/json' \
     -H 'Origin: https://rtm.thinx.cloud' \
     -H 'Authorization: Bearer abc.def.ghi' \
     --data '{"email":"x@y.invalid"}'
```

Expected response:
- `HTTP/2 403` with `Content-Length: 0` — `Bearer abc.def.ghi` is a non-empty, non-`null`, non-`undefined` token, so the guard does NOT fire; `app.login.verify` dispatches; `jsonwebtoken` rejects the malformed JWT; `res.status(403).end()` at L132 fires (still — per the deliberate v1.x-deferred decision documented in CONCERNS.md and RESEARCH.md Pitfall 2). This confirms the guard is strictly narrowing, not widening.

**Save both curl outputs verbatim** (status line + headers + body) into the SUMMARY for the close-out audit trail.

If the post-fix curl returns 403 instead of 200, the fix did NOT land. Possible causes (in order of likelihood):
1. `./scripts/stack-deploy` did not actually roll the API task (re-check `docker service ls | grep thinx_api`; image hash unchanged).
2. CircleCI built but didn't push (check `registry.thinx.cloud:5000/thinx/console:vue` image manifest age vs. expected).
3. There's a host-level nginx outside the swarm that's caching the 403 (rare; per RESEARCH.md A5 assumption review — confirm via `ssh + docker exec` Diagnostic Step 5).

Do NOT proceed to Task 4 (Vue UAT checkpoint) until the post-fix curl returns 200.
  </action>
  <verify>
<automated># This is a live-host verification; the curl outputs must be captured manually into the SUMMARY.
# Confirm the curl binary is available locally:
which curl &amp;&amp; curl --version | head -1</automated>

Manual verification (REQUIRED — record in SUMMARY):
- Post-fix curl with `Authorization: Bearer null` returns `HTTP/2 200` against `https://rtm.thinx.cloud/api/v2/password/reset`. (✓ AUTH-API-01 (a) / ROADMAP Phase 1 success criterion 1.)
- Negative-control curl with `Authorization: Bearer abc.def.ghi` still returns `HTTP/2 403` (confirms guard is narrowing, not widening). (✓ Threat T-01-03 mitigation verified end-to-end.)
- Both curl outputs (raw, including headers) saved into `01-SUMMARY.md` under a "rtm verification" section.
  </verify>
  <done>
    - Post-fix curl returns 200 on rtm.
    - Negative-control curl still returns 403 on rtm.
    - Both curl outputs (status line, headers, body) verbatim in `01-SUMMARY.md`.
    - If either curl returns an unexpected status, SUMMARY contains a diagnostic note and the deploy-rollover analysis (image hash check via `docker service ls`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vue console "Forgot password?" UAT on rtm.thinx.cloud</name>
  <what-built>
    The Vue console's "Forgot password?" flow on rtm.thinx.cloud now exercises a backend that no longer 403s on `Authorization: Bearer null`, and the password-reset email round-trip is functional end-to-end.

    What changed under the hood:
    - `lib/router.js` carries a Bearer-null guard at the JWT branch (plan 01-01 Task 1) that prevents the literal string `Bearer null` from reaching `app.login.verify`. The Vue console at `services/console/vue/src/core/api.js:53-57` sends `Authorization: 'Bearer null'` whenever `this.refreshToken === null` (i.e., user is logged out) — that header now silently falls through to the cookie/no-auth path.
    - `lib/router.user.js postPasswordReset` (plan 01-01 Task 2) now returns 200 with the standard envelope for ALL inputs (no enumeration leak; AUTH-API-01 (b)).
    - A regression spec at `spec/jasmine/ZZ-RouterPasswordResetSpec.js` (plan 01-02 Task 1) locks both fixes into CI.
    - The fix was deployed via manual `./scripts/stack-deploy` (Task 2c) because OPS-swarmpull is broken (incident 2026-05-25 14:44 CET; Phase 3 owns the fix).
    - rtm curl with `Authorization: Bearer null` now returns 200 (Task 3 verified).
  </what-built>
  <how-to-verify>
    Goal: confirm the end-to-end "Forgot password?" round-trip works for a real user, in a real browser, against rtm.thinx.cloud. This is ROADMAP Phase 1 success criterion 2 (`AUTH-API-01 (b)`).

    Pre-conditions:
    - A working test mailbox you control (e.g., your `corpus.cz` or a sandbox address). You'll need access to read the reset email.
    - A registered THiNX account whose email matches the mailbox above. If you don't already have one on rtm, create one via the console signup or have an admin seed one — but the simplest is to use the dynamic user pattern from spec fixtures if you have admin access (otherwise just use your real registered account).
    - Chrome / Firefox / Safari — any modern browser. Use a clean profile or incognito to ensure no stale session cookie (`x-thx-core`) is in flight.

    Steps:
    1. Open https://rtm.thinx.cloud/ in a fresh incognito window. Confirm the Vue console loads. If you see a login page, you're in the right state (logged out).
    2. Open DevTools → Network tab → Preserve log. (Optional but recommended for diagnostics.) Filter to `password/reset` to watch the requests.
    3. Click "Forgot password?" on the login page.
    4. Enter the email address of your registered account.
    5. Submit the form.
    6. **Confirm in DevTools:** the `POST /api/v2/password/reset` request:
       - Carries `Authorization: Bearer null` as a request header (the Vue client always sends this when `refreshToken === null` — this is the original G8 trigger; we're confirming the backend now handles it).
       - Returns HTTP 200 (NOT 403). The response body shape is `{"success":...,"response":"..."}`.
       - Has `Access-Control-Allow-Origin: https://rtm.thinx.cloud` in the response headers (proves CORS ran post-fallthrough, not pre-403).
    7. Switch to the mailbox. The reset email should arrive within ~60 seconds. (If it doesn't, the Mailgun integration on rtm may be misconfigured — that's an ops issue, NOT a Phase 1 regression. Note it in the resume signal and proceed; AUTH-API-01 (a)/(c) status-code criteria can still be claimed independently.)
    8. Click the reset link in the email. Browser opens `https://rtm.thinx.cloud/api/v2/password/reset?owner_id=...&reset_key=...` (or the v1 alias). The page should render the "Enter your new password" form (per `ZZ-AppSessionUserV2DeleteSpec.js:99-108` contract).
    9. Enter a new password and submit. The form posts to `POST /api/v2/password/set`. Expect HTTP 200.
    10. Navigate to `/login`. Sign in with the email + the new password. Expect successful login → console main page.

    Negative-side checks (optional but valuable for the SUMMARY):
    11. Open another incognito window. Visit "Forgot password?" again. Submit a randomly-generated email that you KNOW is not registered (e.g., `nobody-known-<random>@example.invalid`). Confirm the POST returns HTTP 200 (NOT 401) and the response body shape is the same envelope. This confirms AUTH-API-01 (b) end-to-end (no enumeration leak visible in the network panel).
    12. Confirm no email arrives at that fake address (sanity check — registered behavior is unchanged, only the HTTP-level response shape was normalized).

    What to look for if something goes wrong:
    - "Forgot password?" form returns 403: deploy didn't actually roll; re-check `./scripts/stack-deploy` output and `docker service ls` on rtm.
    - "Forgot password?" form returns 200 but no email arrives: Mailgun config on rtm may be misconfigured. Document and surface; this is NOT a Phase 1 regression (rtm Mailgun was working at some prior point per the project history; check key rotation on the swarm host `/mnt/data/conf/config.json`).
    - Reset link from email opens but the GET round-trip returns 401: this would suggest the `getPasswordReset` handler at `lib/router.user.js:26-37` regressed. NOT touched by this plan, but if it surfaces here it's a real ops problem.
    - Login after reset fails: `postPasswordSet` regressed. Same — NOT touched by this plan; surface immediately.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — Vue UAT completed successfully: forgot-password form → email received → reset link consumed → new password works on login. AUTH-API-01 (b) verified end-to-end.
    - `approved-with-mailgun-note` — Status codes correct (200, no 403) but email did not arrive. AUTH-API-01 (a) verified; (b) round-trip partially blocked by Mailgun config (NOT a Phase 1 issue). Proceed to close-out.
    - `failed: <description>` — Specific failure mode. Phase 1 stays open; plan an iteration.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write phase close-out SUMMARY with root cause + reversion plan</name>
  <files>.planning/phases/01-auth-api-password-reset/01-02-SUMMARY.md, .planning/phases/01-auth-api-password-reset/01-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**Sub-step 5a — Write `01-02-SUMMARY.md` (this plan's individual summary):**

Follow the standard GSD SUMMARY template. Required sections:
- Phase / plan identifier.
- What shipped (the new spec file + the deploy + the verification).
- Files created: `spec/jasmine/ZZ-RouterPasswordResetSpec.js` (and commit SHA).
- Commands run (the `git push`, the `./scripts/stack-deploy`, the two curl probes from Task 3).
- Verbatim outputs of the post-fix curl + the negative-control curl from Task 3.
- Vue UAT outcome (from Task 4 resume-signal).
- Any open ops items (e.g., Mailgun config note if `approved-with-mailgun-note`).

**Sub-step 5b — Write `01-SUMMARY.md` (phase-level rollup):**

A phase-level close-out that consolidates plan 01-01 and plan 01-02 outcomes against the 5 ROADMAP Phase 1 success criteria:
1. **`curl -X POST ... returns HTTP 200`** — Status: ✓ / outcome from Task 3 curl output verbatim.
2. **Vue console round-trip works** — Status: ✓ / outcome from Task 4 UAT.
3. **Identical response for registered & unregistered (no enumeration)** — Status: ✓ / pointer to the regression spec Test 4 + plan 01-01 Task 2 traceability comment in `lib/router.user.js`.
4. **Regression spec exists** — Status: ✓ / pointer to `spec/jasmine/ZZ-RouterPasswordResetSpec.js` + the 7-test enumeration.
5. **Root cause documented + reversion plan** — Status: ✓ / the dedicated subsection below.

**Required subsection: "Root cause"**

One paragraph (3-5 sentences) summarizing:
- The Vue console at `services/console/vue/src/core/api.js:53-57` unconditionally sends `Authorization: 'Bearer ' + this.refreshToken`; when logged out, this serializes to the literal string `"Bearer null"`.
- `lib/router.js:103` (pre-fix) keyed the JWT branch on header *presence*, not header *validity*; `Bearer null` matched, `app.login.verify` failed to parse the literal `null`, and `res.status(403).end()` at L132 stamped the response.
- The legacy AngularJS console never sent `Authorization` when logged out, which is why the same backend code worked for years before the Vue port.
- Reference RESEARCH.md §"Summary" + §"Ranked Root-Cause Candidates" #1 for the full evidence trail.

**Required subsection: "Reversion plan"**

Per ROADMAP Phase 1 success criterion 5 (mandatory when an edge-config change is involved; conditionally documented here because the fix lives in middleware not edge — this is still good hygiene for the GA close-out).

If the fix needs to be reverted (e.g., it caused a regression we didn't catch):
1. **Quickest revert:** `git revert <SHA-of-router.js-commit> <SHA-of-router.user.js-commit>` on `thinx-staging`. The two commits are independent (different files), so they can be reverted together or individually.
2. **Re-deploy after revert:** Push, wait for CI, run `./scripts/stack-deploy` on rtm. Same flow as the original deploy.
3. **What to leave behind:** The regression spec at `spec/jasmine/ZZ-RouterPasswordResetSpec.js` can stay even after a revert — its 7 tests will fail on a pre-fix codebase, which is actually the desired state if we're consciously rolling back (it surfaces "this regression exists" rather than hiding it). If staying-with-failing-tests is unacceptable to CI green, comment out the 7 tests with a clearly-marked `xit` and a TODO referencing the revert SHA.
4. **What NOT to revert separately:** The `ZZ-AppSessionUserSpec.js:170-180` assertion update is bundled with the no-enum commit; if you revert the no-enum commit you ALSO revert that assertion (which is correct — they're an atomic pair).
5. **Blast radius of revert:** Reverting reinstates the rtm 403 bug for all "Forgot password?" users on the Vue console. Mailgun + CouchDB are untouched. Sibling phases (Phase 2 PII scrub, Phase 3 OPS-swarmpull, Phase 4 dep triage) are unaffected.

**Required subsection: "Out of scope / deferred follow-ups"**

Document items consciously NOT shipped in this phase (per CONTEXT.md `<deferred>`):
- JWT-403 → 401 status code fix at `lib/router.js:132` — v1.x deferred per CONCERNS.md (CONCERNS.md "Bare 403 inside JWT verify path"); the FIXME comment stays in place.
- Vue client cleanup to omit `Authorization` header when no token — cross-repo change with deploy blast radius; backend guard makes it unnecessary. v1.x candidate.
- PII redaction at `lib/thinx/owner.js:499` (`password_reset_init` error log emits raw email) — Phase 2 / SEC-PII-01.
- Refactor `password_reset_init` to async/await — REFACTOR-04 / v1.x.
- Per-route rate limiting on `/api/v2/password/reset` — v1.x deferred per CONCERNS.md "Global rate limit too high for auth routes".

**Sub-step 5c — Update STATE.md:**

Apply the following edits to `.planning/STATE.md`:
- "Active phase" line: change from `Phase 1 — AUTH API — Password Reset` to whatever the next active phase is (Phase 2 PII Logging Scrub per the roadmap dependency chain).
- "Phase status" for Phase 1: change `pending (active)` to `complete`.
- "Progress": bump `0/4 complete` to `1/4 complete` and update the ASCII progress bar.
- "Performance Metrics": increment Phases completed (`0 → 1`) and Plans completed (`0 → 2` — one per plan in this phase).
- "Accumulated Context" / "Decisions": add a 2026-05-26 row documenting the fix landed and the root cause locator.
- "Session Continuity / Stopped at": update to reflect "Phase 1 complete; Phase 2 (SEC-PII-01) next".

**Sub-step 5d — Update ROADMAP.md:**

In `.planning/ROADMAP.md`:
- Mark Phase 1 checkbox `[x]` in both the Phases list (top) and the Phase Details section header.
- In the "Progress" table, change `Phase 1` row: `Plans Complete` from `0/0` to `2/2`, `Status` from `Not started` to `Complete`, `Completed` from `-` to `2026-05-26`.
- In "Requirement Coverage" table, change `AUTH-API-01` row `Status` from `Pending` to `Complete`.

**Sub-step 5e — Atomic commits:**

Two commits, in this order:
1. `docs(phase-01): close-out SUMMARY + root cause + reversion plan` — commits `01-02-SUMMARY.md` + `01-SUMMARY.md`.
2. `docs(state): mark Phase 1 (AUTH-API-01 / G8) complete` — commits `.planning/STATE.md` + `.planning/ROADMAP.md`.

(Matches Phase 10 close-out commit pattern from MEMORY.md `session-handoff-2026-05-23` — `docs(phase-10): close out — live UAT accepted; ADMIN-01/02/03 Verified`.)
  </action>
  <verify>
<automated>test -f .planning/phases/01-auth-api-password-reset/01-02-SUMMARY.md &amp;&amp; test -f .planning/phases/01-auth-api-password-reset/01-SUMMARY.md &amp;&amp; grep -c "Root cause" .planning/phases/01-auth-api-password-reset/01-SUMMARY.md | (read n; [ "$n" -ge 1 ] || exit 1) &amp;&amp; grep -c "Reversion plan" .planning/phases/01-auth-api-password-reset/01-SUMMARY.md | (read n; [ "$n" -ge 1 ] || exit 1) &amp;&amp; grep -c "AUTH-API-01" .planning/ROADMAP.md | (read n; [ "$n" -ge 1 ] || exit 1) &amp;&amp; grep -q "\[x\].*Phase 1" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - `01-02-SUMMARY.md` + `01-SUMMARY.md` written with all required subsections (Root cause, Reversion plan, Out of scope).
    - `.planning/STATE.md` reflects Phase 1 complete; active phase advanced to Phase 2 (or marked "ready for /gsd:plan-phase 2" handoff).
    - `.planning/ROADMAP.md` Phase 1 row checked off `[x]`, Plans Complete `2/2`, AUTH-API-01 requirement marked Complete.
    - Two atomic commits landed on `thinx-staging`.
    - No unrelated edits in the commit (no churn in CONTEXT.md, RESEARCH.md, or codebase/ files — those stay as historical artifacts).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → CI (CircleCI) | Untrusted local push; CircleCI re-runs the suite against a clean image. Mitigation = the deploy is gated on `specs, 0 failures`. |
| CircleCI → registry (`thinxcloud/api:latest`, `registry.thinx.cloud:5000/thinx/console:vue`) | Image push; registry-side auth required. Not changed in this phase. |
| Operator → swarm host (`188.166.23.244`) | ssh on port 2020 with key auth. Existing convention. |
| Browser → rtm.thinx.cloud | UAT path; the actual user-visible verification surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | The new regression spec accidentally asserts the WRONG contract (e.g., locks `body.success === true` for the unregistered email, re-leaking enumeration via spec) | mitigate | Per Task 1 action, the spec asserts ONLY `res.status === 200` and envelope-shape properties (`body.success`/`body.response` present), NOT specific values. Matches the AUTH-API-01 (b) contract verbatim. |
| T-02-02 | Information Disclosure | Mailgun side-effects in the new spec cause flakiness or send real emails to real addresses | mitigate | The spec uses `envi.dynamic.email` (registered seeded user) or a generated `'@example.invalid'` (RFC 2606 reserved TLD — guaranteed undeliverable). In test env, `Owner.password_reset_init` returns the reset_key directly without calling `sendMail` (per `lib/thinx/owner.js:231`). No real email side-effects. |
| T-02-03 | Denial of Service | The spec hits 7 endpoints in series; combined with bootstrap's 60s init, total spec runtime could exceed the 30s per-test cap or the implicit suite timeout | accept | Each test is independent and uses the 30s per-test timeout (matches `ZZ-AppSessionUserV2DeleteSpec.js`). Suite total expected to add ~5-10s; full suite is ~5-10 minutes so the overhead is negligible. |
| T-02-04 | Repudiation | The post-fix curl in Task 3 is documented in the SUMMARY but could be faked / misread | accept | The SUMMARY captures verbatim `curl -i` output including status line and headers. Negative-control curl in Task 3 (the `Bearer abc.def.ghi` probe still 403s) provides anti-tampering evidence — if the operator wanted to fake a "fix landed" claim by curling against a stale environment, the negative control would also return 200 (because the stale environment would also pass that case), but the post-fix `Bearer null` 200 + negative-control `abc.def.ghi` 403 combination is only consistent with a real fix on a real deploy. |
| T-02-05 | Tampering (supply chain) | npm/pip/cargo installs | mitigate (vacuous) | Zero new package installs in this plan (the chai-http v4 / chai 4.5.0 / jasmine 5.x stack is already pinned in `package.json`). RESEARCH.md §"Package Legitimacy Audit" confirms no new packages. No SLOP/ASSUMED/SUS verdicts. No human checkpoint needed. |
| T-02-06 | Information Disclosure | The SUMMARY file (`01-SUMMARY.md`) accidentally captures PII (real email addresses, reset keys from curl output) | mitigate | Task 3 explicitly uses `someone-known-to-not-exist-$(date +%s)@example.invalid` (RFC 2606 invalid TLD, no real user). The negative-control curl also uses `x@y.invalid`. Task 4 UAT uses the operator's own mailbox — the SUMMARY records the UAT *outcome* (`approved`/`failed`), NOT the specific email address used. PII scrub guidance is reinforced by the Phase 2 SEC-PII-01 mandate. |
| T-02-07 | Spoofing | An attacker performs the rtm curl from a different origin and the response leaks the reset_key | accept | The rtm endpoint at `/api/v2/password/reset` is unauthenticated by design (you can't authenticate if you've forgotten your password). The reset_key returned in the response body is the test-env behavior (`owner.js:231` skips Mailgun); in prod, the response carries a generic envelope and the reset_key goes via email. Verify in Task 3 that the prod response body does NOT contain a reset_key — if it does, surface immediately (would indicate ENVIRONMENT env var is not "production" on rtm, which is a separate config concern). |
</threat_model>

<verification>
**Phase 1 Wave 2 exit gates (must all pass to close the phase):**

1. `spec/jasmine/ZZ-RouterPasswordResetSpec.js` exists and contains 7 `it(...)` blocks each ending in `}, 30000);`.
2. `npm run jasmine -- --filter='Router Password Reset'` reports 7 passing.
3. `npm test` (or docker-compose equivalent) reports `specs, 0 failures` for the full suite.
4. `git log origin/thinx-staging --oneline -5` matches local — push landed.
5. CircleCI green on the head of `thinx-staging`.
6. `./scripts/stack-deploy` ran on rtm AND `docker service ls | grep thinx_api` shows fresh image hash (or this step is documented as handed-off to the user in the SUMMARY).
7. Post-fix curl with `Authorization: Bearer null` returns 200 against rtm — captured verbatim in SUMMARY.
8. Negative-control curl with `Authorization: Bearer abc.def.ghi` still returns 403 against rtm — captured verbatim in SUMMARY.
9. Vue UAT outcome recorded (`approved` / `approved-with-mailgun-note` / `failed: ...`) in SUMMARY.
10. `01-SUMMARY.md` contains the three required subsections: Root cause, Reversion plan, Out of scope / deferred follow-ups.
11. `.planning/STATE.md` and `.planning/ROADMAP.md` updated to reflect Phase 1 complete; AUTH-API-01 marked Complete.

**Phase exit overall** (the 5 ROADMAP Phase 1 success criteria all green):
1. ✓ rtm curl returns 200.
2. ✓ Vue UAT round-trip works (or `approved-with-mailgun-note` if Mailgun config is broken — that's an ops issue, not an AUTH-API-01 regression).
3. ✓ No enumeration (regression spec Test 4 + handler edit traceability comment).
4. ✓ Regression spec exists (Task 1).
5. ✓ Root cause + reversion plan documented (Task 5 SUMMARY).
</verification>

<success_criteria>
- `spec/jasmine/ZZ-RouterPasswordResetSpec.js` written and green (7/7) on the just-fixed branch.
- Full Jasmine suite green locally and in CI.
- `thinx-staging` pushed, CircleCI green, `./scripts/stack-deploy` confirmed (or handed off).
- rtm curl returns 200 for `Bearer null`, 403 for malformed-but-non-empty JWT — both captured in SUMMARY.
- Vue console UAT on rtm completes the email → reset_key → set-password → login round-trip (or `approved-with-mailgun-note` if Mailgun config is the only blocker).
- `01-SUMMARY.md` contains Root cause + Reversion plan + Out of scope subsections.
- `.planning/STATE.md` + `.planning/ROADMAP.md` reflect Phase 1 complete; AUTH-API-01 marked Complete.
- No new npm packages. No edits to `lib/thinx/owner.js`. No edge config changes. No chai-http v5 migration.
</success_criteria>

<output>
The SUMMARY artifacts are themselves the output of this plan (see Task 5). Specifically:
- `.planning/phases/01-auth-api-password-reset/01-02-SUMMARY.md` — this plan's individual summary.
- `.planning/phases/01-auth-api-password-reset/01-SUMMARY.md` — phase-level rollup with Root cause + Reversion plan + Out of scope subsections.
- Updates to `.planning/STATE.md` and `.planning/ROADMAP.md` recording Phase 1 closed and AUTH-API-01 Complete.

After this plan executes, the operator can run `/gsd:plan-phase 2` to begin SEC-PII-01 work.
</output>