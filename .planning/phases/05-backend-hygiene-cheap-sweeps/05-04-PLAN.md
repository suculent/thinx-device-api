---
phase: 05-backend-hygiene-cheap-sweeps
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REFACTOR-05
tags:
  - docs
  - scope-amendment
  - roadmap
  - state
  - backlog

must_haves:
  truths:
    - "ROADMAP.md Phase 5 success criterion 3 is amended to reflect the REFACTOR-05 scope reduction (jshint moves to devDependencies; fs-finder stays — full removal deferred to v1.10)."
    - "REQUIREMENTS.md REFACTOR-05 entry has a `Scope amendment` annotation noting the deferral, so future verifier runs don't reject Phase 5 on literal-text grounds."
    - "STATE.md records the v1.10-candidate backlog entry `fs-finder removal sweep` so the deferred work is tracked and not lost."
    - "The phase-closeout verifier reading ROADMAP + REQUIREMENTS + STATE sees a consistent, traceable story for the partial REFACTOR-05 closure."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 5 success criterion 3 with the scope-reduction note."
      contains: "REFACTOR-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REFACTOR-05 entry annotated with the scope-amendment + deferral pointer."
      contains: "REFACTOR-05"
    - path: ".planning/STATE.md"
      provides: "v1.10-candidate backlog entry `fs-finder removal sweep`."
      contains: "fs-finder"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 5 success criterion 3)"
      to: ".planning/REQUIREMENTS.md REFACTOR-05 entry"
      via: "shared scope-amendment annotation"
      pattern: "fs-finder"
    - from: ".planning/STATE.md (v1.10 backlog)"
      to: "Deferred fs-finder removal sweep work"
      via: "explicit backlog entry"
      pattern: "fs-finder removal sweep"
---

<objective>
Record the REFACTOR-05 scope amendment in the canonical planning artifacts (ROADMAP.md, REQUIREMENTS.md, STATE.md) so the phase-closeout verifier doesn't reject Phase 5 on the literal-text criterion 3 ("jshint AND fs-finder are declared in devDependencies"). Without this plan, the verifier would see "fs-finder still in dependencies" and flag the phase as failing — even though the operator-approved scope reduction (per CONTEXT.md) is the correct, deliberate outcome.

Purpose: Close the gap between the literal roadmap text and the executed scope. The amendment is documented in three places so any future audit can trace the deferral end-to-end:
  - **ROADMAP.md** — the success criterion 3 text is amended (or a "Scope amendment" sub-note is added) so the source-of-truth phase entry reflects what shipped.
  - **REQUIREMENTS.md** — REFACTOR-05's entry gets a parenthetical or sub-bullet acknowledging the partial closure + deferral, with a pointer to the v1.10 candidate.
  - **STATE.md** — the v1.10 backlog (or todos/decisions section) gets a fresh entry for "fs-finder removal sweep" so the deferred work is tracked.

Output: Three edited planning files + one atomic commit: `docs(05): record REFACTOR-05 scope amendment (fs-finder removal deferred to v1.10)`.

This plan runs in Wave 2 AFTER plans 05-01, 05-02, and 05-03 have all landed. The Wave 2 doc-update reflects what actually shipped (jshint moved; fs-finder stayed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-01-PLAN.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-02-PLAN.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-03-PLAN.md

<interfaces>
<!-- The three doc files this plan edits, with their existing text-anchor points. Read each file in full before editing. -->

**.planning/ROADMAP.md (key anchor: Phase 5 success criterion 3)**
Current state (line 43):
```
  3. `jshint` and `fs-finder` are declared in `devDependencies`, the production Docker image still builds + reaches the `Server up at` log line, and no `require('jshint')` / `require('fs-finder')` remains in `lib/` or `thinx-core.js`.
```
Edit target: amend in place (preferred) OR add a "Scope amendment (2026-06-02)" sub-bullet immediately after criterion 4. Either is acceptable per CONTEXT.md "Open Questions for Researcher / Planner". The amend-in-place approach is preferred because it's the smallest diff and keeps the criterion list flat.

**.planning/REQUIREMENTS.md (key anchor: REFACTOR-05 bullet)**
Current state (line 20 — the REFACTOR-05 bullet):
```
- [ ] **REFACTOR-05**: Reclassify `jshint` and `fs-finder` from `dependencies` to `devDependencies` in `package.json`. Confirm production Docker image still builds (`Dockerfile:86 npm install --omit=dev`) and starts; no runtime require of either module from app code. Validated by: ...
```
Edit target: append a sub-bullet or parenthetical note inside the REFACTOR-05 entry recording the 2026-06-02 scope amendment and the v1.10 deferral pointer. Do NOT delete the existing literal text — keep it for audit traceability and ANNOTATE it.

**.planning/STATE.md (key anchor: Decisions section + Todos)**
Current Decisions section starts around line 44; current Todos section around line 53. Edit target: (a) add a Decisions entry dated 2026-06-02 recording the REFACTOR-05 scope amendment, (b) add a Todos / future-backlog entry for "fs-finder removal sweep — v1.10 candidate".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md Phase 5 success criterion 3 to reflect the REFACTOR-05 scope reduction</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file; specifically lines 36-45 — the Phase 5 entry)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-05 decision block — "Phase 5 scope amendment summary")
  </read_first>
  <action>
    Amend line 43 of `.planning/ROADMAP.md` (the Phase 5 success criterion 3) in place. The amended text MUST:

    1. State clearly that ONLY `jshint` moves to `devDependencies` (not `fs-finder`).
    2. State that `fs-finder` stays in `dependencies` with a referenced rationale (5 active runtime call sites in `lib/`).
    3. Reference the deferral target (v1.10) so the reader can trace what was deferred and where it will land.
    4. Preserve the production-build-and-`Server up at` portion of the criterion — that part is still in scope.
    5. Keep the criterion as a single numbered bullet under "Success Criteria"; do NOT add a separate bullet for the deferral (that goes in the "Notes" section if anywhere).

    Suggested amended text (use this verbatim unless minor copy-edits improve clarity):

    > 3. `jshint` is declared in `devDependencies` (scope-amended 2026-06-02: `fs-finder` stays in `dependencies` because of 5 active runtime call sites in `lib/`; full removal deferred to v1.10 — see REQUIREMENTS.md REFACTOR-05 annotation and STATE.md decisions). The production Docker image still builds + reaches the `Server up at` log line, and no `require('jshint')` remains in `lib/` or `thinx-core.js`.

    Additionally: append a single line to the "Notes" section at the bottom of ROADMAP.md noting the scope amendment, formatted as:

    > - **Phase 5 scope amendment (2026-06-02):** REFACTOR-05 reduced to `jshint`-only reclassification; `fs-finder` removal sweep deferred to a proposed v1.10 phase (see STATE.md decisions + REQUIREMENTS.md REFACTOR-05 annotation).

    Do NOT touch any other phase's success criteria. Do NOT touch the Requirement Coverage table (REFACTOR-05 is still mapped to Phase 5 and still pending until the merge lands). Do NOT renumber the success criteria.
  </action>
  <verify>
    <automated>grep -c "scope-amended 2026-06-02" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scope-amended 2026-06-02" .planning/ROADMAP.md` returns at least `1`.
    - `grep -c "fs-finder.*defer\|deferred.*v1.10" .planning/ROADMAP.md` returns at least `1` (the deferral pointer is in the file).
    - `grep -n "Phase 5: Backend Hygiene" .planning/ROADMAP.md` still returns a match at line 36 (the phase header is intact — we did not corrupt the document structure).
    - `grep -nE "^### Phase [0-9]" .planning/ROADMAP.md | wc -l` returns 7 (all 7 phases still present — we did not accidentally delete a phase).
  </acceptance_criteria>
  <done>
    - ROADMAP.md Phase 5 success criterion 3 amended in place with the scope-reduction wording.
    - Notes section has a Phase 5 scope-amendment entry pointing at REQUIREMENTS + STATE.
    - All other phases unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Annotate REFACTOR-05 in REQUIREMENTS.md with the scope-amendment + deferral pointer</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file; specifically line 20 — the REFACTOR-05 bullet)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-05 decision block)
  </read_first>
  <action>
    Annotate the REFACTOR-05 bullet in `.planning/REQUIREMENTS.md` (currently at line 20). Append a parenthetical OR a sub-bullet at the end of the existing bullet text — do NOT delete the existing literal text (it is the audit trail for what was originally asked).

    Use this exact appended text (after the existing bullet content, on a new indented sub-line):

    >   - **Scope amendment (2026-06-02, Phase 5):** Closed for `jshint` (moved to `devDependencies`); `fs-finder` portion deferred — fork is internally owned (`github:suculent/Node-FsFinder#master`) and has 5 active runtime call sites in `lib/` (`builder.js`, `deployment.js`, `platform.js`, `repository.js`, `plugins/arduino/plugin.js`), so reclassification would break production. Full `fs-finder` removal (replace with `fs-extra` glob helpers or native `fs.promises.readdir` recursion) deferred to a proposed v1.10 phase. See `.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md` (REFACTOR-05 decision block) and `.planning/STATE.md` (v1.10 backlog) for details.

    Do NOT touch any other requirement. Do NOT touch the Traceability table at the bottom of REQUIREMENTS.md (REFACTOR-05 still maps to Phase 5 and stays "Pending" until the merge to staging lands — the post-merge verification flip happens at execute-phase wrap-up, not in this plan).
  </action>
  <verify>
    <automated>grep -c "Scope amendment (2026-06-02, Phase 5)" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Scope amendment (2026-06-02, Phase 5)" .planning/REQUIREMENTS.md` returns exactly `1`.
    - `grep -A1 "REFACTOR-05" .planning/REQUIREMENTS.md | grep -c "Scope amendment"` returns at least `1` (the annotation is attached to the REFACTOR-05 entry, not floating elsewhere).
    - `grep -cE "^- \[ \] \*\*REFACTOR-(01|02|03|04|05)" .planning/REQUIREMENTS.md` returns exactly `5` (all 5 REFACTOR-NN entries still present — we did not accidentally delete or merge any).
    - `grep -nE "REFACTOR-05.*Phase 5" .planning/REQUIREMENTS.md` returns the Traceability table line — the requirement's phase mapping is intact.
  </acceptance_criteria>
  <done>
    - REFACTOR-05 bullet in REQUIREMENTS.md annotated with the 2026-06-02 scope amendment and the v1.10-deferral rationale.
    - Original bullet text preserved (annotation is additive).
    - Traceability table unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the scope amendment as a Decision and add the v1.10 backlog entry in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full file; specifically the `## Accumulated Context` → `### Decisions` and `### Todos` sub-sections, currently at lines 44 and 53)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-05 decision block + "Deferred Ideas" section)
  </read_first>
  <action>
    Two additions to `.planning/STATE.md`:

    **Addition 1 — Decisions entry** (add to the `### Decisions` section, immediately after the existing 2026-06-02 entries so chronological order is preserved):

    > - 2026-06-02 — Phase 5 scope amendment: REFACTOR-05 reduced to `jshint`-only reclassification (moved to `devDependencies`); `fs-finder` STAYS in `dependencies` because the internally-owned fork (`github:suculent/Node-FsFinder#master`) has 5 active runtime call sites in `lib/`. Full `fs-finder` removal sweep deferred to a proposed v1.10 phase. Amendment recorded in ROADMAP.md (Phase 5 success criterion 3 + Notes) and REQUIREMENTS.md (REFACTOR-05 sub-bullet); rationale in `.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md` REFACTOR-05 decision block.

    **Addition 2 — Future-backlog/Todos entry** (add to the `### Todos` section OR — preferred — to a new sub-section `### v1.10 Candidates` immediately after Todos. If `### v1.10 Candidates` does not yet exist, create it):

    > - **fs-finder removal sweep** (v1.10 candidate, deferred from v1.9 Phase 5 REFACTOR-05): replace `finder.from()` / `finder.in()` / `finder.findFiles()` calls in 5 modules (`lib/thinx/builder.js`, `lib/thinx/deployment.js`, `lib/thinx/platform.js`, `lib/thinx/repository.js`, `lib/thinx/plugins/arduino/plugin.js`) with `fs-extra` glob helpers (already a dep) OR native `fs.promises.readdir` recursion. After the sweep lands, `fs-finder` can be removed from `package.json` entirely. Estimated touch surface: ~10 call sites across ~5 files. Sequenced after v1.9 ships.

    Update the STATE.md frontmatter `last_updated` field to today's date (`2026-06-02`) and `last_activity` to `2026-06-02 — Phase 5 scope amendment recorded (REFACTOR-05 fs-finder deferral)`. Do NOT touch the `gsd_state_version`, `milestone`, `status`, or `stopped_at` fields. Do NOT touch the `progress` block — that updates when plans complete, not when scope is amended.

    Do NOT touch the `## Quick Tasks Completed` table or the `## Milestones` section. Do NOT touch the `## Cross-Project Touchpoints` section.
  </action>
  <verify>
    <automated>grep -c "fs-finder removal sweep" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fs-finder removal sweep" .planning/STATE.md` returns at least `1`.
    - `grep -c "Phase 5 scope amendment" .planning/STATE.md` returns at least `1`.
    - `grep -E "^last_updated:" .planning/STATE.md` returns a line containing `2026-06-02`.
    - `grep -cE "^### Decisions|^### Todos" .planning/STATE.md` returns at least `2` (both sub-sections still present).
    - `grep -cE "^---$" .planning/STATE.md` returns exactly `2` (frontmatter opens and closes correctly — we did not corrupt the YAML).
  </acceptance_criteria>
  <done>
    - STATE.md has a 2026-06-02 Decisions entry for the Phase 5 scope amendment.
    - STATE.md has a `fs-finder removal sweep` v1.10 backlog entry.
    - Frontmatter `last_updated` / `last_activity` reflect today's edit.
    - All other STATE.md sections preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Stage and commit the doc-update atomically</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (post-Task-1 state)
    - .planning/REQUIREMENTS.md (post-Task-2 state)
    - .planning/STATE.md (post-Task-3 state)
  </read_first>
  <action>
    Single atomic GPG-signed commit covering the three doc edits. Subject:

    `docs(05): record REFACTOR-05 scope amendment (fs-finder removal deferred to v1.10)`

    Body should reference: (a) the three artifacts edited (ROADMAP, REQUIREMENTS, STATE), (b) the rationale (5 active runtime call sites in `lib/`), (c) the v1.10 deferral pointer, (d) Phase 5 of the v1.9 milestone.

    Stage exactly three files: `git add .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`. Do NOT use `git add -A`. GPG-sign per project default.

    Do NOT push to remote in this plan — execute-phase wrap-up handles the staging-branch push after all phase commits are present.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -q "^docs(05): record REFACTOR-05 scope amendment (fs-finder removal deferred to v1.10)$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s'` returns exactly `docs(05): record REFACTOR-05 scope amendment (fs-finder removal deferred to v1.10)`.
    - `git log -1 --name-only --pretty=format: | sort` lists exactly three files: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` (alphabetical).
    - `git log -1 --format=%b | grep -iE "v1.10|deferred"` returns at least one match.
    - `git log -1 --show-signature 2>&1 | grep -iE "Good signature|gpg:"` confirms GPG-signed.
    - `git status --short` returns empty.
  </acceptance_criteria>
  <done>
    - Doc-update lands as one atomic GPG-signed commit touching exactly three .planning/ files.
    - Working tree is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts (ROADMAP/REQUIREMENTS/STATE) → phase-closeout verifier | The verifier reads these documents to flip requirement statuses and gate phase closure. Inconsistency between the literal text and the executed scope causes false-negative phase failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Repudiation | Future audit reads only the literal ROADMAP text and concludes Phase 5 failed (because fs-finder wasn't moved) | mitigate | Amend ROADMAP.md success criterion 3 in place AND annotate REQUIREMENTS.md REFACTOR-05 bullet AND record the decision in STATE.md. Three independent surfaces all carry the scope-amendment story, with cross-references so a reader landing on any one of them can find the other two. |
| T-05-13 | Information Disclosure | Scope-amendment commits in git history disclose deferred-work intent | accept | The deferral is operationally beneficial to disclose — it signals to future maintainers (including the v1.10 planner) that the fs-finder removal is queued, why, and what the touch surface is. No sensitive content. |
| T-05-14 | Tampering | Frontmatter corruption (the STATE.md YAML edit) | mitigate | Task 3 explicitly lists which frontmatter fields to touch (`last_updated`, `last_activity`) and which to leave alone (`gsd_state_version`, `milestone`, `status`, `stopped_at`, `progress`). The acceptance criteria include `grep -cE "^---$" .planning/STATE.md` returning exactly 2 to confirm the YAML envelope is intact. |

No HIGH severity items. This is a pure-docs plan with no source-code or supply-chain surface.
</threat_model>

<verification>
- `grep -c "scope-amended 2026-06-02" .planning/ROADMAP.md` returns at least 1.
- `grep -c "Scope amendment (2026-06-02, Phase 5)" .planning/REQUIREMENTS.md` returns exactly 1.
- `grep -c "fs-finder removal sweep" .planning/STATE.md` returns at least 1.
- `grep -c "Phase 5 scope amendment" .planning/STATE.md` returns at least 1.
- One atomic GPG-signed commit (`docs(05): record REFACTOR-05 scope amendment (fs-finder removal deferred to v1.10)`) touching exactly 3 files.
- Cross-references: each of the 3 docs references the OTHER two by name (`.planning/ROADMAP.md` cites REQUIREMENTS+STATE; `.planning/REQUIREMENTS.md` cites STATE+CONTEXT; `.planning/STATE.md` cites ROADMAP+REQUIREMENTS+CONTEXT). The doc set is internally consistent.
</verification>

<success_criteria>
- Goal-backward must_have #7 (scope-amendment artifact present so verification doesn't reject criterion 3 on literal-text grounds) satisfied. ✓
- Phase-closeout verifier reads ROADMAP+REQUIREMENTS+STATE and sees a consistent story for the partial REFACTOR-05 closure. ✓
- v1.10 backlog entry for `fs-finder removal sweep` exists in STATE.md so the deferred work is tracked. ✓
- The doc-update is the LAST commit of Phase 5 (Wave 2 after all three Wave 1 plans), so the staging-branch push after this commit reflects a complete, coherent Phase 5 state. ✓
</success_criteria>

<output>
Create `.planning/phases/05-backend-hygiene-cheap-sweeps/05-04-SUMMARY.md` when done.
</output>
