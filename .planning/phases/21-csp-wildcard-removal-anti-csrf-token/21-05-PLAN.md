---
phase: 21-csp-wildcard-removal-anti-csrf-token
plan: 05
type: execute
wave: 3
depends_on: ["21-04"]
files_modified:
  - .planning/runbooks/csp-csrf-hardening.md
autonomous: false
requirements: [SEC-CSP-01, SEC-CSRF-01]

must_haves:
  truths:
    - "CSRF enforcement is flipped ON in production (debug.csrf_enforce=true or CSRF_ENFORCE=true) only after Plan 21-04's fail-open functional verification passed"
    - "A HawkScan rescan of rtm.thinx.cloud reports 0 NEW plugin 10055-4 (CSP: Wildcard Directive) paths"
    - "A HawkScan rescan of rtm.thinx.cloud reports 0 NEW plugin 20012 (Anti-CSRF Tokens) paths"
    - "A login POST with no/forged X-XSRF-TOKEN is rejected (4xx) post-enforce, while a normal login through each console still succeeds"
    - "A TRUE COLD session (fresh incognito / cleared cookies) can still log in through both consoles AND submit the Vue password-reset form post-enforce — the priming GET /api/(v2/)csrf-token sets the cookie before the first POST (no cold-session lockout)"
    - "A documented rollback procedure exists and is proven (or at minimum dry-run reviewed) before the flip"
  artifacts:
    - path: ".planning/runbooks/csp-csrf-hardening.md"
      provides: "Operator runbook: enforce-flip steps, HawkScan rescan procedure, rollback procedure"
      contains: "## Rollback"
  key_links: []
---

<objective>
Flip CSRF enforcement from fail-open to hard-reject in the `rtm.thinx.cloud` production staging environment, then verify closure of both HawkScan findings (SEC-CSP-01, SEC-CSRF-01) via a rescan, with a pre-written rollback procedure ready before the flip is made.

Purpose: this is the final, highest-risk step of the phase — flipping enforcement is a deliberate, manual, operator-gated action (the flag lives in the swarm host's mounted `/mnt/data/conf/config.json`, which is NOT part of any git repo and cannot be edited via CI push) taken only after Plan 21-04 proved the console wiring works in the wild.
Output: `.planning/runbooks/csp-csrf-hardening.md` (new runbook, following the existing `.planning/runbooks/websocket-handshake.md` + `swarm-configs/README.md` convention), enforcement live, HawkScan rescan showing 0 NEW findings for both plugins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-csp-wildcard-removal-anti-csrf-token/21-CONTEXT.md
@.planning/runbooks/websocket-handshake.md
@.planning/runbooks/swarm-configs/README.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Write the runbook and flip CSRF enforcement on production</name>
  <action>Write .planning/runbooks/csp-csrf-hardening.md (Claude, automated), then hand off to the operator for the manual SSH flip — see how-to-verify below.</action>
  <what-built>
    A new runbook file `.planning/runbooks/csp-csrf-hardening.md` documenting: (1) the exact SSH + config-edit + service-restart steps to flip `debug.csrf_enforce` to `true` in the swarm host's `/mnt/data/conf/config.json` (or set `CSRF_ENFORCE=true` in the `thinx_api` service's environment via `docker service update`, whichever the operator prefers — both are supported by Plan 21-01's `isEnforced()` check), (2) the HawkScan rescan command/procedure, and (3) a rollback procedure (revert the config flag back to `false` / unset `CSRF_ENFORCE` + restart — no code revert needed since the fail-open code path is unconditionally present; a full `git revert` + re-push of Plans 21-01/21-02/21-03 is documented as the deeper fallback if the middleware itself needs removing).
  </what-built>
  <how-to-verify>
    This task requires the operator to physically perform the flip — Claude cannot SSH to the production swarm host. Steps for the operator (already written into the runbook by Claude in this task; the operator executes them):
    1. `ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020` (per `AGENTS.md`).
    2. Edit `/mnt/data/conf/config.json`'s `debug.csrf_enforce` key from `false`/absent to `true` (or set the `CSRF_ENFORCE=true` env var on the `thinx_api` service definition, per operator preference).
    3. `docker service update --force thinx_api` (matches the existing redeploy pattern for this micro-pinned service, per the `thinx-api-mosquitto-colocation` memory — stays on `micro`, no reschedule risk).
    4. Confirm the service restarted cleanly (`docker service ps thinx_api`, tail logs for startup errors).
  </how-to-verify>
  <resume-signal>Type "flipped" once the config change is live and the service has restarted cleanly, or "blocked" with the error if the restart failed (in which case do NOT proceed to the rescan task — revert the flag per the runbook's Rollback section first).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HawkScan rescan + normal-login parity verification</name>
  <action>Manual verification only — no further automation to run here. See how-to-verify below.</action>
  <what-built>Production CSRF enforcement is now live; this task verifies HawkScan closure and normal-login parity.</what-built>
  <how-to-verify>
    1. Run a HawkScan rescan of `rtm.thinx.cloud` (`hawk rescan` against the prior scan ID, or a fresh `hawk scan` if rescan is unavailable — per the existing scan/rescan pair referenced in `REQUIREMENTS.md`, scan `c5691244` / rescan `1f3ec1e7`).
    2. Confirm the rescan report shows **0 NEW** findings for plugin `10055-4` ("CSP: Wildcard Directive") and plugin `20012` ("Anti-CSRF Tokens").
    3. Manually confirm: `curl -X POST https://rtm.thinx.cloud/api/login -H 'Content-Type: application/json' -d '{"username":"x","password":"y"}'` (no `X-XSRF-TOKEN` header, no cookie jar) now returns a 403 with `{"success":false,"response":"csrf_token_invalid"}` instead of proceeding to credential validation.
    3b. COLD-SESSION test (plan-checker BLOCKER 1 — this is the case that MUST pass). Open a FRESH incognito window (or fully clear cookies for `.thinx.cloud`) so there is NO pre-existing `XSRF-TOKEN` cookie, then load each console's login page. Confirm in DevTools Network that the page fires `GET /api/csrf-token` (classic) / `GET /api/v2/csrf-token` (Vue) on load and Application > Cookies now shows an `XSRF-TOKEN` cookie. Then log in — it MUST succeed on this cold session (the priming GET set the cookie before the POST). Also cold-test the Vue password-reset entry page directly: open `https://console.thinx.cloud/#/password-reset` in a fresh incognito window, confirm it primes the cookie on mount, and submit the "send reset email" form — it MUST NOT 403. Do NOT rely on a reused/pre-warmed browser for this step; a warm browser hides exactly the lockout this test exists to catch.
    4. WARNING — RE-CONFIRM CONSOLE PLACEMENT (plan-checker warning): as in Plan 21-04 step 0, artifacts disagree on whether `rtm.thinx.cloud` is served by the legacy or the Vue container. Before trusting the "legacy at rtm / Vue at console" labels here, run `docker service ps thinx_console thinx_vue` (or check Traefik/Swarmpit) to confirm the actual host->container mapping and swap the labels below if reversed. Then log in through BOTH consoles (using whichever hostname the operator confirmed maps to each) with a real browser session that has the `XSRF-TOKEN` cookie — confirm normal login still succeeds in both.
    5. If ANY of the above fails (including the cold-session step 3b): immediately follow the Rollback section in `.planning/runbooks/csp-csrf-hardening.md` (flip `debug.csrf_enforce` back to `false` / unset `CSRF_ENFORCE`, `docker service update --force thinx_api`) and report the failure — do not leave production in a broken enforce state.
  </how-to-verify>
  <resume-signal>Type "verified — 0 NEW findings, cold-session login OK in both consoles + Vue password-reset" to close the phase, or describe the failure (which step, what response) to trigger rollback.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator SSH session -> production swarm host config | Manual, out-of-band change; not captured by any CI gate, so a runbook + rollback procedure is the only safety net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Denial of Service | Enforce flip breaking real production login | mitigate | Flip happens ONLY after Plan 21-04's fail-open functional verification passed with real traffic; rollback (flag revert, no redeploy needed) is documented and reviewed BEFORE the flip, not written after a failure |
| T-21-13 | Repudiation | No record of when/why enforcement was flipped | mitigate | `.planning/runbooks/csp-csrf-hardening.md` is a git-committed, timestamped audit trail of the flip + rescan result, following the same convention as `websocket-handshake.md` (SEC-WS-01) and `swarm-configs/README.md` |

No package-manager installs are part of this plan.
</threat_model>

<verification>
- HawkScan rescan: 0 NEW findings for plugin 10055-4 and plugin 20012.
- Forged/missing-token login POST returns 4xx post-enforce.
- Normal login succeeds in both consoles post-enforce.
- Rollback procedure documented and available before the flip.
</verification>

<success_criteria>
- SEC-CSP-01 and SEC-CSRF-01 both closed per the ROADMAP's Phase 21 success criteria: HawkScan rescan clean on both plugins, both consoles fully functional, one shared server-side CSRF scheme, byte-for-byte-equivalent (modulo placeholder) CSP across all three sources.
</success_criteria>

<output>
Create `.planning/phases/21-csp-wildcard-removal-anti-csrf-token/21-05-SUMMARY.md` when done
</output>
