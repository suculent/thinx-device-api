---
phase: 04-dependency-triage
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [SEC-DEP-01]

must_haves:
  truths:
    - "All 4 override-block edits land in a single atomic commit: REMOVE follow-redirects pin, CHANGE lodash 4.17.23->4.18.1, CHANGE minimatch 5.1.0->5.1.9, ADD ws 8.20.1."
    - "Lockfile (package-lock.json) is regenerated by exactly one `npm install` invocation and committed alongside the package.json edit."
    - "Full `npm test` (17 ZZ-* mocha specs + non-ZZ jasmine specs) passes locally pre-push."
    - "Commit is pushed to thinx-staging; CircleCI returns green; new docker image digest publishes."
    - "Swarmpit autoredeploy rolls the new thinx_api task on rtm.thinx.cloud within ~120s of CI green (Phase 3's fix is load-bearing here)."
    - "Phase 1 contract preserved post-deploy: `curl -X POST https://rtm.thinx.cloud/api/v2/password/reset` with `Authorization: Bearer null` returns 200."
    - "`npm audit --omit=dev` runtime-tree high count drops to 0 (the primary success metric)."
  artifacts:
    - path: "package.json"
      provides: "Overrides block (L97-L136) with 4 surgical edits applied; all other 34 entries untouched."
      contains: "lodash: 4.18.1; minimatch: 5.1.9; ws: 8.20.1; no follow-redirects override line"
    - path: "package-lock.json"
      provides: "Regenerated lockfile reflecting all 4 override changes propagated through every dependency path."
      contains: "follow-redirects ≥ 1.16.0; lodash 4.18.1 (all instances); minimatch 5.1.9 (all instances); ws 8.20.1 (all instances including nested under engine.io / engine.io-client / socket.io-adapter)"
  key_links:
    - from: "package.json overrides block"
      to: "package-lock.json resolved versions"
      via: "single `npm install` invocation"
      pattern: "lodash.*4\\.18\\.1|minimatch.*5\\.1\\.9|ws.*8\\.20\\.1"
    - from: "thinx-staging push"
      to: "Swarmpit autoredeploy on rtm.thinx.cloud"
      via: "CircleCI build-api-cloud -> docker hub digest change -> Swarmpit watcher fires"
      pattern: "thinx_api.*autoredeploy fired"
    - from: ".planning/dep-triage.md Section 2 (Fix log)"
      to: "this slice's commit SHA + the 7 GHSA IDs from Slice 1's blocker rows"
      via: "Task 5 appends fix-log row referencing commit SHA + alerts closed"
      pattern: "GHSA-r4q5-vmmm-2653.*GHSA-r5fr-rjxr-66jc.*GHSA-f23m-r3pf-42rh.*GHSA-7r86-cg39-jmmj.*GHSA-23c5-xmqv-rm74.*GHSA-3ppc-4f35-3m26"

must_not:
  - "Do NOT run `npm audit fix --force` — rewrites majors and would break the chai-http v4 lock per AGENTS.md."
  - "Do NOT bump chai-http to v5 — locked per AGENTS.md L82-92; no current alert forces this."
  - "Do NOT prune any of the other 34 entries in the overrides block — surgical edits only; block hygiene is a v1.x exercise."
  - "Do NOT restructure jshint or fs-finder from dependencies to devDependencies — out of scope (file as REFACTOR-05 in Slice 3, not here)."
  - "Do NOT touch services/console/ — sibling GSD project; its deps are SEC-DEP-02 scope, not this phase."
  - "Do NOT run `npm install` more than once in this slice — the single install after the 4 override edits is what regenerates the lockfile cleanly. Repeated runs may pick up unrelated transitive churn and pollute the diff."
  - "Do NOT bump axios direct dep further — `^1.16.0` already resolves to 1.16.1 (past all 15 stale axios alerts)."
  - "Do NOT add or modify any file under lib/, thinx-core.js, thinx.js, or spec/ — this slice's commit touches package.json + package-lock.json ONLY."
  - "Do NOT pin ws to 8.21.0 — research recommendation is 8.20.1 to match the top-level direct dep declaration in package.json L94."
  - "Do NOT push before `npm test` exits 0 locally — if specs fail, revert the override change and investigate; pushing red specs wastes CI cycles and deploy budget."
---

<objective>
Ship the 4 surgical override-block edits that close the 7 active blocker alerts identified by Slice 1's triage table. This is a single atomic commit: edit `package.json` overrides, run `npm install` once, run `npm test` once, commit both files together, push to `thinx-staging`, verify CI green, verify Swarmpit autoredeploy on rtm, verify Phase 1 + Phase 2 contracts still hold, capture provisional `npm audit --omit=dev` post-fix counts.

Purpose: This is the ONLY code-touching slice of Phase 4. The other three slices are documentation/bookkeeping/PR-creation. The success metric for the entire phase ("zero unaddressed high-severity in runtime tree") is measured on the post-deploy output of this commit.

Output: One commit on `thinx-staging` modifying exactly `package.json` + `package-lock.json`; a green CI run; a new docker image on rtm via Swarmpit autoredeploy; provisional post-fix audit counts captured for Slice 3 to formalize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/dep-triage.md
@.planning/phases/04-dependency-triage/04-RESEARCH.md
@.planning/phases/03-swarm-auto-pull/03-SUMMARY.md
@.planning/codebase/STACK.md
@AGENTS.md
@package.json
@Dockerfile

<interfaces>
<!-- The 4 surgical edits below are the load-bearing change set for this entire phase. -->
<!-- Source-of-truth rationale: 04-RESEARCH.md Section "Override block hygiene" (L112-L142) and Section "Override side-effects" (L201-L259). -->

Current overrides block (package.json L97-L136) — relevant pre-edit lines:
  L115: "follow-redirects": "1.15.6"        <-- REMOVE this line entirely
  L122: "lodash": "4.17.23"                 <-- CHANGE to "lodash": "4.18.1"
  L124: "minimatch": "5.1.0"                <-- CHANGE to "minimatch": "5.1.9"
  (no existing ws entry)                    <-- ADD "ws": "8.20.1"

Target post-edit state (4 edits applied):
  - follow-redirects override line is absent (axios's own `^1.16.0` declaration will resolve to ≥1.16.0)
  - lodash override pinned to 4.18.1
  - minimatch override pinned to 5.1.9
  - ws override pinned to 8.20.1 (NEW line)

Phase 1 verification contract (must survive Phase 4 deploy):
  curl -X POST https://rtm.thinx.cloud/api/v2/password/reset \
    -H 'Authorization: Bearer null' \
    -H 'Content-Type: application/json' \
    -d '{"email":"x@y"}'
  -> HTTP 200 + standard success body

Socket.io builder ⇄ worker channel (highest-risk override surface for ws):
  lib/thinx/queue.js:8 — socket.io-client outbound (engine.io-client > ws nested tilde-lock)
  lib/thinx/queue.js:94 — socket.io server on port 4000 (engine.io > ws nested tilde-lock)
  Pre-fix: ws 8.17.1 at all nested paths
  Post-fix: ws 8.20.1 at all nested paths (top-level direct dep was already 8.20.1)

CI integration:
  CircleCI pipeline: build-api-cloud
  Triggers on push to thinx-staging
  Runs: `npm install --omit=dev` (Dockerfile L86) + `npm test` (jasmine 17 ZZ-* + non-ZZ specs)
  Publishes: thinxcloud/api:latest with new sha256 digest
  Expected wall-clock: CI green ~15 min from push; new digest visible within 5 min of CI green

Swarmpit autoredeploy (Phase 3's load-bearing fix):
  Watcher polls docker hub for digest change
  On new digest: emits `INFO: Service ... ( thinx_api ) autoredeploy fired! DIGEST: [ old ] -> [ new ]`
  Expected SLA: ≤120s from new digest to new thinx_api task Running
  Fallback if stalled: `ssh -i ~/.ssh/DOKey2 -p 2020 root@188.166.23.244 "docker service update --force swarmpit_app"` (Rung 1 recovery procedure per AGENTS.md / .planning/runbooks/swarm.md)

Per-package transitive paths (post-fix verification, via `npm ls`):
  follow-redirects:
    Expected: single resolved version ≥1.16.0
    Parents: axios@1.16.1 (+ same nested via @slack/web-api, nano, mailgun.js, @slack/rtm-api)
  lodash:
    Expected: ALL 4 instances at 4.18.1
    Parents: jshint, rollbar > async, winston > async, fs-finder > async
  minimatch:
    Expected: ALL instances at 5.1.9
    Parents: jshint, jshint > cli > glob@11.1.0
  ws:
    Expected: ALL instances at 8.20.1 (top-level + 3 nested)
    Parents: top-level direct, socket.io > engine.io, socket.io-client > engine.io-client, socket.io > socket.io-adapter

GHSA IDs closed by this commit (7 total — for commit body "Alerts closed" reference):
  - GHSA-r4q5-vmmm-2653          (follow-redirects auth-header leak)
  - GHSA-r5fr-rjxr-66jc          (lodash prototype pollution / _.template)
  - GHSA-f23m-r3pf-42rh          (lodash secondary CVE)
  - GHSA-7r86-cg39-jmmj          (minimatch ReDoS)
  - GHSA-23c5-xmqv-rm74          (minimatch secondary)
  - GHSA-3ppc-4f35-3m26          (minimatch tertiary)
  - GHSA-3h5v-q93c-6h6q          (ws — placeholder; verify exact GHSA from `04-DEPENDABOT-PRE.json` for the ws alert; this list is final once Slice 1's triage table is consulted)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the 4 surgical override edits to package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state — lines 97-136 are the overrides block being edited; lines 50-95 are the dependencies block which must NOT be touched)
    - .planning/phases/04-dependency-triage/04-RESEARCH.md (Section "Override block hygiene" L112-L142 — defines the 4 edits and their exact target values; Section "Override side-effects" L201-L259 — per-package risk analysis)
    - .planning/dep-triage.md (Slice 1's triage table — confirms which alerts are blocker-class and will be closed by these 4 edits)
    - AGENTS.md (chai-http v4 lock + 38-override-block context; superagent untouched)
  </read_first>
  <action>
    Make exactly 4 edits to `package.json`. Touch no other file in this task.

    Edit 1 (REMOVE line 115 entirely):
      Pre: line 115 reads `"follow-redirects": "1.15.6",`
      Post: line removed. The next line (`"glob": "11.1.0",`) shifts up. Trailing comma on the prior line (`"flat": "5.0.2",`) stays as-is.
      Why: removing the override lets axios@1.16.1's own `^1.16.0` declaration resolve naturally (1.16.0+ is past the GHSA-r4q5-vmmm-2653 patched version). Future axios updates automatically receive any further follow-redirects security patches.

    Edit 2 (CHANGE line 122 — value only):
      Pre: `"lodash": "4.17.23",`
      Post: `"lodash": "4.18.1",`
      Why: lodash 4.18.0+ closes both GHSA-r5fr-rjxr-66jc and GHSA-f23m-r3pf-42rh; 4.18.1 is the latest minor-patch in the 4.x line; API-stable per upstream changelog and codebase doesn't call `_.template` or `_.unset` directly (verified via grep in research).

    Edit 3 (CHANGE line 124 — value only):
      Pre: `"minimatch": "5.1.0",`
      Post: `"minimatch": "5.1.9",`
      Why: 5.1.9 is the latest patch in the 5.1.x line and covers all 3 GHSAs (`<5.1.8` for two of them, `<5.1.7` for the third). Same-minor bump; minimal API risk; only jshint's CLI exercises it.

    Edit 4 (ADD a new line in the overrides block):
      Insert a new key-value: `"ws": "8.20.1"` immediately after a logical anchor (alphabetical-ish location is the entry around L132-L135; e.g., insert before `"yargs-parser": "20.2.7"` if alphabetical, or at the end of the block before the closing `}`).
      Concrete instruction: maintain valid JSON (trailing comma on the line above the inserted `"ws"` entry if it isn't already the last; no trailing comma on the new `"ws"` entry if it becomes the last entry of the overrides block).
      Why: socket.io's engine.io@6.6.4 and engine.io-client@6.6.3 tilde-lock `~8.17.1` — top-level `ws@^8.20.1` controls only thinx-core.js's direct WebSocket use, not the nested socket.io path. Override propagates 8.20.1 to all three nested instances.

    After all 4 edits, validate JSON:
      `jq . package.json > /dev/null` MUST exit 0 (no parse error).

    Do NOT touch dependencies block, devDependencies block, scripts block, any of the other 34 overrides entries, or any non-overrides field.

    Do NOT run `npm install` in this task — that's Task 2.
  </action>
  <verify>
    <automated>jq . package.json &gt; /dev/null &amp;&amp; jq -e '.overrides | has("ws") and (.ws == "8.20.1") and (.lodash == "4.18.1") and (.minimatch == "5.1.9") and (has("follow-redirects") | not)' package.json &amp;&amp; ! grep -nE '"follow-redirects":\s*"1\.15\.6"' package.json &amp;&amp; git diff --stat package.json | grep -qE 'package\.json' &amp;&amp; ! git diff --stat | grep -qE '(lib/|thinx-core\.js|thinx\.js|spec/|services/)'</automated>
  </verify>
  <acceptance_criteria>
    - `jq . package.json > /dev/null` exits 0 (file is valid JSON post-edit).
    - `jq -r '.overrides.lodash' package.json` returns `4.18.1`.
    - `jq -r '.overrides.minimatch' package.json` returns `5.1.9`.
    - `jq -r '.overrides.ws' package.json` returns `8.20.1`.
    - `jq -e '.overrides | has("follow-redirects") | not' package.json` exits 0 (the follow-redirects override key is absent).
    - `git diff --stat package.json` shows package.json as the ONLY file modified by this task.
    - No changes to `lib/`, `thinx-core.js`, `thinx.js`, `spec/`, or any service submodule.
    - The 34 unrelated entries in the overrides block are byte-identical to their pre-edit state (verify via targeted greps on a sample of unchanged keys: `grep -E '"ajv": "6\.12\.3"' package.json && grep -E '"cookie": "1\.0\.2"' package.json && grep -E '"jose": "4\.11\.2"' package.json` all return matches).
  </acceptance_criteria>
  <done>
    `package.json` reflects exactly the 4 surgical override edits (1 removal + 2 value changes + 1 addition). File parses as valid JSON. No other file modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate package-lock.json via single `npm install`; capture provisional post-fix npm audit counts</name>
  <files>package-lock.json, .planning/phases/04-dependency-triage/04-AUDIT-POST-PROVISIONAL.json, .planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json</files>
  <read_first>
    - package.json (post-Task-1 state — the input to npm install)
    - .planning/phases/04-dependency-triage/04-RESEARCH.md (Section "`npm install` regeneration — free-fix pass" L384-L401 — defines the single-install strategy)
    - .planning/phases/04-dependency-triage/04-AUDIT-PRE-PROD.json (Slice 1's pre-fix runtime-tree baseline — the comparison reference for "did counts drop")
  </read_first>
  <action>
    Run exactly ONE `npm install` invocation, then capture the provisional post-fix audit state. These provisional files inform Task 4's verification but are intentionally not the final post-fix baseline (Slice 3 captures that AFTER deploy + rescan).

    1. `npm install` — runs from repo root with no flags. Will resolve the new override declarations and regenerate `package-lock.json`. Wall-clock budget: ~30-120s depending on cache state.
    2. Confirm lockfile changes are exactly what's expected (no surprise transitive churn):
       - `npm ls follow-redirects` MUST show a single resolved version ≥ `1.16.0`.
       - `npm ls lodash` MUST show all instances at `4.18.1`.
       - `npm ls minimatch` MUST show all instances at `5.1.9`.
       - `npm ls ws` MUST show ALL instances at `8.20.1` (top-level + 3 nested under engine.io, engine.io-client, socket.io-adapter).
    3. Capture provisional audit baselines (these are NOT the final post-fix artifacts — Slice 3 owns the final ones after deploy + Dependabot rescan):
       - `npm audit --json > .planning/phases/04-dependency-triage/04-AUDIT-POST-PROVISIONAL.json 2>/dev/null; true` (full tree; non-zero exit on remaining vulnerabilities is expected)
       - `npm audit --omit=dev --json > .planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json 2>/dev/null; true` (runtime tree — PRIMARY metric)
    4. Sanity-check the provisional runtime-tree count dropped from Slice 1's baseline:
       - `jq '.metadata.vulnerabilities.high' .planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json` MUST equal 0 (or very low — if non-zero, inspect which package remains and decide whether to revert/iterate).
       - If high count is non-zero, this is a red flag — the 4 edits may not have fully propagated. Read the audit JSON's `.vulnerabilities` block to identify the remaining package; consult research before proceeding.
    5. Do NOT run `npm install` a second time in this task. The single install above is the lockfile regeneration. If the verification in step 4 shows unexpected count, the response is to inspect/revert/iterate in a future task — NOT to re-run install.
    6. Do NOT commit yet. Task 3 runs the test suite; Task 4 commits.
  </action>
  <verify>
    <automated>npm ls lodash 2&gt;&amp;1 | grep -qE 'lodash@4\.18\.1' &amp;&amp; npm ls minimatch 2&gt;&amp;1 | grep -qE 'minimatch@5\.1\.9' &amp;&amp; npm ls ws 2&gt;&amp;1 | grep -qE 'ws@8\.20\.1' &amp;&amp; ! npm ls ws 2&gt;&amp;1 | grep -qE 'ws@8\.17\.[0-9]' &amp;&amp; npm ls follow-redirects 2&gt;&amp;1 | grep -qE 'follow-redirects@1\.1[6-9]\.' &amp;&amp; test -s .planning/phases/04-dependency-triage/04-AUDIT-POST-PROVISIONAL.json &amp;&amp; test -s .planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json &amp;&amp; jq -e '.metadata.vulnerabilities.high == 0' .planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json</automated>
  </verify>
  <acceptance_criteria>
    - `package-lock.json` has been modified (verify via `git diff --stat package-lock.json` — large diff expected because all 4 override edits ripple through the lockfile).
    - `npm ls follow-redirects` shows a single resolved version that starts with `1.16` or higher (NOT `1.15.6`).
    - `npm ls lodash` returns `lodash@4.18.1` for ALL instances; no `4.17.23` line remains.
    - `npm ls minimatch` returns `minimatch@5.1.9` for ALL instances; no `5.1.0` line remains.
    - `npm ls ws` returns `ws@8.20.1` for ALL instances (4 expected: top-level + engine.io > ws + engine.io-client > ws + socket.io-adapter > ws); NO `ws@8.17.1` line remains.
    - `.planning/phases/04-dependency-triage/04-AUDIT-POST-PROVISIONAL.json` exists, non-empty, parses as JSON, has `.metadata.vulnerabilities` object.
    - `.planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json` exists, non-empty, parses as JSON.
    - `jq '.metadata.vulnerabilities.high' 04-AUDIT-POST-PROD-PROVISIONAL.json` returns `0` (the primary success metric — runtime-tree high count = zero post-fix).
    - `git status` shows ONLY `package.json`, `package-lock.json`, and the two provisional JSON files as modified/new. No source-code files touched.
  </acceptance_criteria>
  <done>
    Lockfile regenerated; all 4 override changes propagated to all transitive instances; runtime-tree high-severity count is 0. Provisional audit artifacts captured for Task 4's verification. No commit yet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full `npm test` suite locally — pre-push regression gate</name>
  <files></files>
  <read_first>
    - package.json (scripts.test entry — defines the actual test command)
    - .planning/phases/04-dependency-triage/04-RESEARCH.md (Section "CI risk signals to monitor" L423-L439 — defines the spec-failure red flags and recovery actions; Section "Override side-effects" L201-L259 — explains why each edit is low-risk for the test suite)
    - .planning/codebase/STACK.md (Node/test stack context, chai pin documentation)
  </read_first>
  <action>
    Run the full test suite locally, end-to-end, with NO modifications to specs allowed. The test command is whatever `package.json`'s `scripts.test` resolves to (typically `jasmine` driving 17 ZZ-* spec files + non-ZZ specs).

    1. From repo root, run `npm test`. Wall-clock budget: ~5-15 minutes (matches CI typical run).
    2. Watch for the following red-flag spec failures (research-identified — highest probability of regression):
       - `ZZ-AppSessionUserSpec.js` — chai-http v4 round-trip via socket.io. If this fails post-ws-override, the ws 8.17→8.20 bump broke wire-protocol compat. Action: revert Task 1's ws addition; pin ws to 8.18.x or 8.19.x instead; re-run Task 2.
       - `ZZ-RouterPasswordResetSpec.js` — Phase 1's regression spec. Failure here would mean a transitive (likely follow-redirects path via axios in oauth-github or mailgun chain) broke an HTTP path. Action: inspect failing assertion; if the failure is a redirect-behavior change, the follow-redirects 1.15→1.16 bump exposed a latent bug.
       - Any spec touching socket.io worker chain — same diagnosis path as ZZ-AppSessionUserSpec.
    3. On all-green: proceed to Task 4 (commit).
    4. On failure: STOP. Do not commit. Do not push. Inspect the failing spec's output, identify the failing package transitive, decide whether to (a) revert Task 1's specific edit and re-run from Task 2, (b) pin the offending package to a tighter version (e.g., ws 8.18.x), or (c) escalate via checkpoint to the operator. The plan does NOT auto-recover from spec failure — manual judgment is required because the recovery path depends on which spec fails.
    5. Do NOT modify any spec to make it pass. The test suite is the contract; if the override bump breaks a spec, the override is wrong, not the spec.
  </action>
  <verify>
    <automated>npm test 2&gt;&amp;1 | tee /tmp/04-02-test-output.log; TEST_EXIT=$?; grep -qE '([0-9]+ specs?, 0 failures|[0-9]+ passing|all tests passed)' /tmp/04-02-test-output.log; PASS=$?; [ "$TEST_EXIT" = "0" ] &amp;&amp; [ "$PASS" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - Test output indicates 0 failures across all jasmine specs (ZZ-* and non-ZZ).
    - No spec files modified — `git status` post-test shows specs unchanged from Slice 1's commit.
    - `ZZ-AppSessionUserSpec.js` passes (highest-risk spec for ws override side-effect).
    - `ZZ-RouterPasswordResetSpec.js` passes (Phase 1 contract regression gate).
    - If any spec failed: this task remains incomplete; do not advance to Task 4 until the failure is investigated and the override edits are revised in Task 1 + Task 2 re-run.
  </acceptance_criteria>
  <done>
    Local `npm test` is green. The 4 override edits + regenerated lockfile do not break any existing spec. Safe to commit and push.
  </done>
</task>

<task type="auto">
  <name>Task 4: Stage + commit the 2-file change (package.json + package-lock.json) atomically on thinx-staging</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (post-Task-1 state)
    - package-lock.json (post-Task-2 state)
    - .planning/dep-triage.md (Slice 1's Section 1 — to enumerate the 7 GHSA IDs this commit closes in the commit body)
    - .planning/phases/04-dependency-triage/04-AUDIT-POST-PROD-PROVISIONAL.json (counts for commit body)
  </read_first>
  <action>
    Stage only `package.json` + `package-lock.json` and create a single atomic commit on the current branch (`thinx-staging`). Confirm via `git branch --show-current` that you are on `thinx-staging` — if not, ABORT (init context forbids branch switching).

    1. `git add package.json package-lock.json`
    2. Verify staged set with `git status`: ONLY these two files should be staged. If `04-AUDIT-POST-PROVISIONAL.json` or `04-AUDIT-POST-PROD-PROVISIONAL.json` are inadvertently staged, unstage them with `git restore --staged <file>` — those provisional artifacts belong to Slice 3's commit, not this one.
    3. Commit with subject + body via heredoc (preserves formatting):
       Subject: `chore(deps): SEC-DEP-01 - resolve 7 active alerts via overrides`
       Body should contain:
       - "Phase 4 Slice 2 — Dependency Triage blocker fixes."
       - "4 surgical override-block edits in package.json (L97-L136):"
       - "  - REMOVE follow-redirects: 1.15.6 (was forcing axios's safe ^1.16.0 to downgrade)"
       - "  - CHANGE lodash: 4.17.23 -> 4.18.1 (closes GHSA-r5fr-rjxr-66jc, GHSA-f23m-r3pf-42rh)"
       - "  - CHANGE minimatch: 5.1.0 -> 5.1.9 (closes GHSA-7r86-cg39-jmmj, GHSA-23c5-xmqv-rm74, GHSA-3ppc-4f35-3m26)"
       - "  - ADD ws: 8.20.1 (forces engine.io's ~8.17.1 nested tilde-lock upward; closes ws GHSA)"
       - "Provisional npm audit --omit=dev runtime-tree high count: 0 (was 9 pre-fix)."
       - "Verified: `npm test` green locally (17 ZZ-* + non-ZZ jasmine specs)."
       - "Alerts closed (7 total): GHSA-r4q5-vmmm-2653, GHSA-r5fr-rjxr-66jc, GHSA-f23m-r3pf-42rh, GHSA-7r86-cg39-jmmj, GHSA-23c5-xmqv-rm74, GHSA-3ppc-4f35-3m26, plus the ws GHSA from Slice 1's triage table row."
       - "See `.planning/dep-triage.md` Section 2 (fix log — populated by Task 6) for the full closed-alerts breakdown."
    4. Do NOT amend the previous Slice 1 commit. Do NOT use `--no-verify`. Do NOT skip hooks.
    5. Do NOT push yet — Task 5 handles the push so it can be paired with the live CI watch.
  </action>
  <verify>
    <automated>git branch --show-current | grep -qE '^thinx-staging$' &amp;&amp; git log -1 --pretty=%s | grep -qE '^chore\(deps\): SEC-DEP-01' &amp;&amp; git log -1 --stat | grep -qE 'package\.json' &amp;&amp; git log -1 --stat | grep -qE 'package-lock\.json' &amp;&amp; ! git log -1 --stat | grep -qE '(lib/|thinx-core\.js|thinx\.js|spec/|services/)' &amp;&amp; git log -1 --pretty=%B | grep -qE 'GHSA-r4q5-vmmm-2653' &amp;&amp; git log -1 --pretty=%B | grep -qE 'GHSA-r5fr-rjxr-66jc'</automated>
  </verify>
  <acceptance_criteria>
    - On branch `thinx-staging` (no branch switch occurred this slice).
    - Latest commit subject matches `^chore\(deps\): SEC-DEP-01.*overrides$` (or close variant — the key tokens are `chore(deps)`, `SEC-DEP-01`, and "overrides").
    - Commit touches EXACTLY `package.json` + `package-lock.json` — no other files.
    - Commit body mentions all 4 override edits (follow-redirects REMOVE, lodash 4.18.1, minimatch 5.1.9, ws 8.20.1).
    - Commit body lists the 7 GHSA IDs in the "Alerts closed" section.
    - Working tree post-commit contains the 2 provisional audit JSON files as untracked (Slice 3 will commit them or replace them with post-deploy versions).
  </acceptance_criteria>
  <done>
    Single atomic commit on `thinx-staging` containing the 4 override edits + regenerated lockfile. Commit message documents the change, the closed GHSAs, and the provisional post-fix runtime count. Not yet pushed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Push to thinx-staging; watch CI + Swarmpit autoredeploy; verify Phase 1 + 2 contracts on rtm</name>
  <files></files>
  <read_first>
    - .planning/phases/04-dependency-triage/04-RESEARCH.md (Section "CI risk signals to monitor" L423-L439 — defines the wall-clock budgets and red-flag thresholds for the deploy chain)
    - .planning/phases/03-swarm-auto-pull/03-SUMMARY.md (Phase 3's restored autoredeploy fix is load-bearing here; documents the Rung 1 recovery procedure if Swarmpit stalls again)
    - AGENTS.md (deployment flow + swarm host ssh details)
    - .planning/runbooks/swarm.md (Phase 3 runbook for autoredeploy recovery)
  </read_first>
  <action>
    Push the Task 4 commit and observe the deploy chain end-to-end. This task is automated where possible (gh CLI for CI status, docker hub API for digest, curl for the contract probes) but has wall-clock waits that are intrinsic to the pipeline.

    1. `git push origin thinx-staging` — push the single commit.
    2. Capture push timestamp t0 (UTC).
    3. Watch CircleCI: `gh run list --branch thinx-staging --limit 3 --workflow build-api-cloud` (or the exact workflow ID for thinx-device-api). Poll every ~60s until a run for the new commit SHA appears and reaches conclusion. Budget: 15 minutes from t0 to CI green. If CI red: STOP. Read the failing step's log via `gh run view <run-id> --log-failed`; diagnose; revert the override commit on thinx-staging if necessary (`git revert HEAD && git push`); halt this slice and consult with operator.
    4. On CI green at time t1: capture the new image digest. Either via `gh run view <run-id> --log` and grep for the published sha256, or via `curl -s https://hub.docker.com/v2/repositories/thinxcloud/api/tags/latest/ | jq -r '.digest'`.
    5. Watch Swarmpit autoredeploy. Expected SLA: ≤120s from t1 to new thinx_api task Running. Methods (any one suffices):
       - `ssh -i ~/.ssh/DOKey2 -p 2020 root@188.166.23.244 "docker service ps thinx_api --no-trunc --format '{{.Name}} {{.Image}} {{.CurrentState}}' | head -3"` — look for a new task with the new digest in Running state, recent timestamp.
       - `curl -i https://rtm.thinx.cloud/api/v2/spec` — response header should advertise the new image SHA.
    6. On autoredeploy stall (>5 min after CI green with no new task): trigger Rung 1 recovery per `.planning/runbooks/swarm.md`:
       - `ssh -i ~/.ssh/DOKey2 -p 2020 root@188.166.23.244 "docker service update --force swarmpit_app"`
       - Wait 60-120s, re-check service ps. Phase 3 documented this as the load-bearing recovery procedure.
    7. On autoredeploy success at t2: verify the contract smoke tests:
       a. Phase 1 contract: `curl -X POST https://rtm.thinx.cloud/api/v2/password/reset -H 'Authorization: Bearer null' -H 'Content-Type: application/json' -d '{"email":"x@y"}'` — MUST return HTTP 200 with the standard success body shape.
       b. Phase 2 contract (informal — Phase 4 doesn't touch lib/thinx/owner.js so this is just a confidence smoke): tail rtm container logs briefly for any obvious raw-PII emission. Not a blocker for this slice — Phase 2's spec asserts the redaction at code level, not at deploy time.
       c. Optional socket.io builder smoke if reachable: trigger a no-op build via the worker channel if a builder is on the swarm. If services/worker is not reachable (OPS-03 documents its broken autoredeploy), skip this and document in Task 6's bookkeeping.
    8. On contract failure (Phase 1 returns 403 or wrong body): revert the override commit immediately. Use `docker service rollback thinx_api` as the live rollback. Then `git revert HEAD && git push` to roll back the commit. Halt this slice for diagnosis.
    9. On full success (CI green + autoredeploy + Phase 1 200): proceed to Task 6.

    Record observed timestamps and digest values — Task 6's bookkeeping needs them.
  </action>
  <verify>
    <automated>git push origin thinx-staging 2&gt;&amp;1 | tee /tmp/04-02-push.log &amp;&amp; sleep 30 &amp;&amp; CI_STATUS=$(gh run list --branch thinx-staging --limit 1 --json status,conclusion --jq '.[0]'); echo "$CI_STATUS"; CURL_RC=$(curl -s -o /dev/null -w '%{http_code}' -X POST https://rtm.thinx.cloud/api/v2/password/reset -H 'Authorization: Bearer null' -H 'Content-Type: application/json' -d '{"email":"x@y"}'); [ "$CURL_RC" = "200" ]</automated>
  </verify>
  <acceptance_criteria>
    - Commit successfully pushed to `origin/thinx-staging`. `git status -sb` shows up-to-date with origin.
    - CircleCI run for the new commit SHA concludes with `conclusion=success` (green).
    - A new docker image digest is published to `thinxcloud/api:latest` (verify via docker hub API or CI log).
    - Swarmpit autoredeploy fires within 5 minutes of CI green. Either: (a) `docker service ps thinx_api` shows a new task Running on the new digest with recent timestamp, OR (b) Rung 1 recovery was needed and after `docker service update --force swarmpit_app` the autoredeploy rolled.
    - `curl -X POST https://rtm.thinx.cloud/api/v2/password/reset -H 'Authorization: Bearer null' -H 'Content-Type: application/json' -d '{"email":"x@y"}'` returns HTTP 200 (Phase 1 contract preserved).
    - No 502/503 from rtm.thinx.cloud during the rollout window.
    - Observed timestamps recorded for t0 (push), t1 (CI green), t2 (new task Running), and the published image digest — these go into Task 6's bookkeeping.
  </acceptance_criteria>
  <done>
    Override fixes are live on rtm.thinx.cloud. CI green, image rolled, Phase 1 contract intact. The phase's primary metric — runtime-tree zero-high — is provisionally confirmed (final confirmation in Slice 3 after Dependabot rescan).
  </done>
</task>

<task type="auto">
  <name>Task 6: Append fix-log row to .planning/dep-triage.md Section 2; commit the bookkeeping update</name>
  <files>.planning/dep-triage.md</files>
  <read_first>
    - .planning/dep-triage.md (current state — Section 2 has an empty fix-log table from Slice 1; this task appends the first data row)
    - .planning/phases/04-dependency-triage/04-RESEARCH.md (Section "Fix log" L280-L290 — defines the 5-column schema)
    - The Task 4 commit SHA (from `git log -1 --pretty=%H` post-Task-4)
    - The 7 GHSA IDs from Slice 1's triage table (Section 1, blocker-verdict rows)
  </read_first>
  <action>
    Append exactly one data row to Section 2 of `.planning/dep-triage.md` documenting this slice's fix. Schema from research:

    | Commit SHA | Files changed | Override delta | Alerts closed (GHSA IDs) | Verification |
    |------------|---------------|----------------|--------------------------|--------------|

    Row content for this slice:
    - Commit SHA: short SHA from Task 4's commit (`git log -1 --pretty=%h`).
    - Files changed: `package.json, package-lock.json`
    - Override delta: `-follow-redirects: 1.15.6; lodash: 4.17.23 -> 4.18.1; minimatch: 5.1.0 -> 5.1.9; +ws: 8.20.1`
    - Alerts closed (GHSA IDs): comma-separated list of the 7 GHSAs from Slice 1's blocker rows. Get the exact GHSA for ws from Slice 1's triage table row (don't hard-code without verifying — `grep '| ws |' .planning/dep-triage.md` will return the row containing it).
    - Verification: brief — `CI green on <short-sha>; rtm `Bearer null` 200; runtime-tree high=0 per 04-AUDIT-POST-PROD-PROVISIONAL.json; autoredeploy delta=<t2-t1>s` (substituting real observed values from Task 5).

    Then commit only this file:
      1. `git add .planning/dep-triage.md`
      2. `git status` — verify only this one file staged.
      3. Commit subject: `docs(deps): SEC-DEP-01 - fix log row for Slice 2 commit <short-sha>`
      4. Body (heredoc):
         "Documents the Slice 2 override edits in .planning/dep-triage.md Section 2 (Fix log).
          Cross-references commit <short-sha>.
          Slice 3 will append Section 3 (Post-fix baseline) once Dependabot rescan completes."
      5. Push: `git push origin thinx-staging` (small docs-only commit; CI will rebuild but pass trivially).

    Do NOT modify Section 1 (Triage table — Slice 1's deliverable, immutable now).
    Do NOT modify Section 3 (Post-fix baseline — Slice 3's deliverable).
    Do NOT modify Section 4 (Rationale taxonomy — Slice 1's contract).
  </action>
  <verify>
    <automated>awk '/^## Fix log/,/^## Post-fix baseline/' .planning/dep-triage.md | grep -cE '^\| [a-f0-9]{7,} \|' | xargs -I{} test {} -ge 1 &amp;&amp; awk '/^## Fix log/,/^## Post-fix baseline/' .planning/dep-triage.md | grep -qE 'package\.json, package-lock\.json' &amp;&amp; awk '/^## Fix log/,/^## Post-fix baseline/' .planning/dep-triage.md | grep -qE 'follow-redirects.*lodash.*minimatch.*ws' &amp;&amp; git log -1 --pretty=%s | grep -qE 'docs\(deps\): SEC-DEP-01.*fix log' &amp;&amp; git log -1 --stat | grep -qE '\.planning/dep-triage\.md'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/dep-triage.md` Section 2 contains exactly one new data row (header + separator + 1 data row).
    - Data row's Commit SHA column matches `git log` (the Task 4 commit, NOT this Task 6 commit).
    - Override delta column lists all 4 changes (follow-redirects removal + lodash + minimatch + ws).
    - Alerts closed column lists 7 GHSA IDs.
    - Verification column references CI status + rtm contract + runtime-tree high count + autoredeploy delta.
    - Latest commit subject matches `^docs\(deps\): SEC-DEP-01.*fix log`.
    - Latest commit touches ONLY `.planning/dep-triage.md`.
    - Section 1 (Triage table) is byte-identical to Slice 1's commit (no accidental modifications).
  </acceptance_criteria>
  <done>
    Slice 2's fix is fully documented in the dep-triage table. The two commits on thinx-staging (Task 4 + Task 6) plus Slice 1's earlier commit form the complete in-repo paper trail for the override fixes. Slice 3 can now run baseline capture knowing the fix has shipped and been documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This slice ships executable dependency code into the production runtime image. The threat surface is the override changes themselves — specifically, whether the bumped transitive versions introduce unexpected behavior on a hot code path.

| Boundary | Description |
|----------|-------------|
| Override JSON ↔ npm resolver | A typo or invalid semver in package.json overrides will cause `npm install` to fail loudly. Caught by Task 1's `jq` validation + Task 2's `npm ls` post-check. |
| Bumped transitives ↔ runtime hot paths | The 4 affected packages touch: HTTP redirects (follow-redirects via axios in oauth-github, Mailgun, CouchDB I/O via nano), logging (lodash via winston/rollbar), socket.io builder-worker channel (ws via engine.io). |
| New thinx_api docker image ↔ rtm.thinx.cloud users | Swarmpit autoredeploys the new image. Live user traffic hits the new binary. Phase 1 contract probe at Task 5 step 7 is the user-visible regression gate. |
| Developer machine ↔ GitHub (push) + CircleCI (CI) + Docker Hub (image publish) | Pre-existing trust relationships; no new credentials introduced this slice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Tampering | follow-redirects 1.15→1.16 in axios redirect-following code paths (oauth-github callback, Mailgun email send, CouchDB nano I/O) | mitigate | Research confirms 1.16.0 release notes contain only security fix, no API changes. Task 3 runs `npm test` which exercises oauth + mail flows via specs. Task 5 verifies live Phase 1 contract via curl. Rollback path: `docker service rollback thinx_api` + `git revert HEAD`. |
| T-04.2-02 | Tampering | lodash 4.17→4.18 in winston/rollbar/async transitives (every log emission + error tracking) | mitigate | Codebase doesn't call `_.template` or `_.unset` (the affected functions) directly per `grep -r '_.template\|_.unset' lib/ thinx-core.js` returning no hits. Transitive consumers use `_.merge`, `_.get`, `_.cloneDeep` — unaffected by the API change. Task 3's `npm test` will surface any indirect exercise. |
| T-04.2-03 | Tampering | minimatch 5.1→5.1.9 in jshint runtime | accept | jshint runs at lint time only, not at API request time. Same major-minor; patch-level bump within the same minor line. No runtime exposure. Task 3's `npm test` covers indirect exercise. |
| T-04.2-04 | Denial of Service | ws 8.17→8.20 in socket.io builder ⇄ worker channel (`lib/thinx/queue.js:8` + `:94`) | mitigate | ws 8.x patch series is intentionally backward-compatible (engine.io's `~8.17.1` is conservative tilde, not a protocol hard upper bound). HIGHEST verification cost in this slice — the socket.io worker path is production-exercised. Task 5 step 7c verifies build-worker reachability if available. If socket.io regression is observed: revert override + pin to ws 8.18.x or 8.19.x and re-run from Task 2. |
| T-04.2-05 | Tampering | npm install resolves an unintended transitive to a vulnerable version (lockfile churn beyond the 4 intended) | mitigate | Task 2 captures provisional npm audit AFTER install; runtime-tree high count must be 0 (acceptance criterion). If a NEW high-severity surfaces, this slice halts and the diff is inspected. |
| T-04.2-SC | Tampering | npm install (package legitimacy) | mitigate | NO new packages added to dependencies or devDependencies — only `overrides` block edits that pin EXISTING transitive packages to newer versions. All 4 packages (follow-redirects, lodash, minimatch, ws) are well-established, downloaded >50M/week npm packages with established maintainer history. RESEARCH.md verified all 4 target versions via `npm view` (registry source of truth). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages introduced. Package Legitimacy Gate does NOT trigger because the action is "tighten existing pins", not "add new dependency". |
| T-04.2-06 | Elevation of Privilege | Swarmpit autoredeploy fires on a malicious image | accept | Phase 3 verified the autoredeploy chain. Image is built by CircleCI (trusted), pushed to docker hub with the same SHA the CI run records. No new external publish path. Pre-existing trust relationship. |
| T-04.2-07 | Repudiation | Override change not traceable to a specific commit | mitigate | Task 4's commit subject (`chore(deps): SEC-DEP-01 - resolve 7 active alerts via overrides`) + Task 6's fix-log row in dep-triage.md provide bidirectional traceability. The 7 closed GHSA IDs are explicitly enumerated in both the commit body and the fix log. |

**HIGH-severity gate:** T-04.2-04 (ws override on socket.io hot path) is the only LOW-MEDIUM-rated threat in this set. Per RESEARCH.md L242-L246, mitigation cost is verification (Task 3 + Task 5), not avoidance. Disposition: mitigate, not block — research confirms ws 8.x is patch-compatible.

**Slopcheck note:** Slice 2 invokes `npm install` (Task 2) but adds ZERO new packages — only re-pins existing transitives to safer versions. The 4 packages affected (follow-redirects, lodash, minimatch, ws) are NOT [ASSUMED] / [SUS] / [SLOP]; they are the npm ecosystem's most-downloaded packages with established maintainer history (>50M weekly downloads each per npm registry). Package Legitimacy Gate per RESEARCH.md "Package Legitimacy Audit" section does NOT trigger.
</threat_model>

<verification>
- Task 1: `jq -e '.overrides.lodash == "4.18.1" and .overrides.minimatch == "5.1.9" and .overrides.ws == "8.20.1" and (.overrides | has("follow-redirects") | not)' package.json` exits 0.
- Task 2: `npm ls` for each of the 4 packages shows the expected post-fix version at all instances; `04-AUDIT-POST-PROD-PROVISIONAL.json` shows runtime high count = 0.
- Task 3: `npm test` exits 0 with 0 failures.
- Task 4: latest commit on thinx-staging touches only package.json + package-lock.json; commit message lists 4 edits + 7 GHSAs.
- Task 5: push succeeds; CI green; new image deployed via autoredeploy (or Rung 1 recovery); Phase 1 contract `POST /api/v2/password/reset` with Bearer null returns 200; no 502/503 during rollout.
- Task 6: `.planning/dep-triage.md` Section 2 has one fix-log row; bookkeeping commit pushed.
</verification>

<success_criteria>
- **ROADMAP Success Criterion 2 (full):** Every blocker verdict in the triage table has a corresponding fix landed — `package.json` overrides block edited for all 4 fix vectors; `npm audit --omit=dev` runtime-tree high count = 0. ✓ Tasks 1-5.
- **ROADMAP Success Criterion 3 (full):** The `chai-http` v4 lock is respected — no override touches chai-http or superagent; commit diff is package.json + package-lock.json ONLY (no spec migrations). ✓ by construction.
- **ROADMAP Success Criterion 4 (post-fix half — provisional):** Provisional post-fix `npm audit` output captured (04-AUDIT-POST-PROD-PROVISIONAL.json). The final formalized post-fix baseline is Slice 3's responsibility after Dependabot UI rescan. ✓ Task 2.
- **Phase invariant — production stability:** rtm.thinx.cloud remains responsive throughout the rollout; Phase 1 contract (Bearer null → 200) preserved post-deploy. ✓ Task 5.
- **Phase invariant — operational dependency:** Phase 3's restored Swarmpit autoredeploy is exercised end-to-end in Task 5; if Swarmpit stalls, the documented Rung 1 recovery is invoked, NOT a `./restart.sh` fallback. ✓ Task 5.
- **Slice 2 ↔ Slice 3 handoff:** The 2 provisional audit JSONs from Task 2 + the rtm-deployed new image SHA from Task 5 give Slice 3 everything it needs to (a) capture the formal post-fix baselines after Dependabot rescan and (b) compute pre/post deltas for the close-out SUMMARY.
</success_criteria>

<output>
After completing all six tasks (push + autoredeploy + contract verification + fix log commit), create `.planning/phases/04-dependency-triage/04-02-SUMMARY.md` documenting:
  - Task 4 commit SHA (the override fix), Task 6 commit SHA (the fix log row).
  - Observed wall-clock timestamps: t0 (push), t1 (CI green), t2 (new task Running), t2-t1 (autoredeploy delta — compare against Phase 3's 63s baseline).
  - New rtm image digest (sha256:...).
  - Provisional pre/post runtime-tree audit counts (high/moderate from 04-AUDIT-PRE-PROD.json vs 04-AUDIT-POST-PROD-PROVISIONAL.json).
  - Phase 1 contract verification result (200 with body shape match).
  - Whether Rung 1 recovery was needed (yes/no — if yes, document for Phase 3's run-frequency tracking).
  - Whether socket.io builder smoke was performed (yes / skipped because services/worker unreachable per OPS-03).
  - Any unexpected lockfile churn — packages whose versions changed but were not in the 4 explicit override edits (transitive ripple). Slice 3 will assess if any of these introduced a NEW high-severity surface.
</output>
</content>
</invoke>