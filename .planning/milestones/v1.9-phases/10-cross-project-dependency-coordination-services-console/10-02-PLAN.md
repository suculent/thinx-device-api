---
phase: 10-cross-project-dependency-coordination-services-console
plan: 02
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - services/console/.planning/ROADMAP.md
  - services/console
autonomous: true
requirements:
  - SEC-DEP-02

must_haves:
  truths:
    - "`services/console/.planning/ROADMAP.md` has a new SEC-DEP-02 phase entry (as Phase 1 of a new `v1.x Operational Hygiene` milestone, per planner choice from CONTEXT.md Recommendation)."
    - "The new phase entry contains: a goal (remediate the 2 high-severity grunt alerts on the vendored `jquery-validation-1.19.5/package.json`), `Depends on: Nothing`, `Requirements: SEC-DEP-02`, 4 Success Criteria, and a `Plans: TBD` placeholder."
    - "The 4 Success Criteria mirror the parent project's structure: (1) Both Dependabot alerts (54, 52) are resolved (auto-closed after vendored package.json delete OR manually dismissed in Dependabot UI). (2) The vendored `src/assets/global/plugins/jquery-validation-1.19.5/package.json` is removed from the repo OR an explicit `### Deferred` note is added to the phase entry explaining the dismiss-only path was chosen. (3) services/console CI green on the resulting branch (`thinx-staging` of services/console). (4) The submodule pointer in `thinx-device-api` bumps cleanly across the SEC-DEP-02 commit (verified by the parent repo's CircleCI green on the pointer-bump commit)."
    - "The new milestone header references the parent coordination: 'Coordinated from `thinx-device-api` v1.9 milestone Phase 10' with a backlink path."
    - "One atomic GPG-signed commit lands IN the services/console submodule on its `thinx-staging` branch: subject `docs(SEC-DEP-02): schedule SEC-DEP-02 phase coordinated from thinx-device-api v1.9`."
    - "One atomic GPG-signed commit lands in the PARENT `thinx-device-api` repo on its `thinx-staging` branch bumping the submodule pointer from `27758ebda8a179a440aff1cd443f9ffe1fe84a6d` to the new SHA: subject `chore: bump services/console submodule to capture SEC-DEP-02 schedule`."
    - "After both commits, `git submodule status services/console` from the parent shows the new SHA matching the submodule's `HEAD` on its `thinx-staging` branch (no `+` or `-` prefix indicating drift)."
  artifacts:
    - path: "services/console/.planning/ROADMAP.md"
      provides: "SEC-DEP-02 phase entry under new v1.x Operational Hygiene milestone"
      contains: "SEC-DEP-02"
    - path: ".gitmodules"
      provides: "unchanged (pointer bump lives in the index entry, not .gitmodules)"
      contains: "services/console"
  key_links:
    - from: "services/console/.planning/ROADMAP.md (SEC-DEP-02 phase entry)"
      to: ".planning/dep-triage.md (Phase 10 annex)"
      via: "cross-reference to parent project's verdict roll-up"
      pattern: "thinx-device-api.*dep-triage"
    - from: "parent repo submodule index entry (services/console)"
      to: "services/console HEAD on thinx-staging (new SHA)"
      via: "git submodule pointer update"
      pattern: "services/console"
---

<objective>
Schedule the parallel SEC-DEP-02 phase in `services/console/.planning/ROADMAP.md` (a sibling GSD workspace, separate git repo at `git@github.com:thinx-cloud/console.git`) AND bump the parent `thinx-device-api` submodule pointer to capture the new console-side HEAD. This is a TWO-REPO atomic operation: one commit in the submodule (the ROADMAP edit), one commit in the parent repo (the pointer bump).

Purpose: SEC-DEP-02 success criterion #1 (per `.planning/REQUIREMENTS.md` line 30) requires that the parallel SEC-DEP-02 phase exist in `services/console/.planning/ROADMAP.md`. Per CONTEXT.md decision, the actual remediation (delete vendored package.json OR dismiss alerts) lives in the services/console GSD workspace and is run by the operator in a separate session — Phase 10 of v1.9 SCHEDULES it but does not execute it.

Output: TWO atomic GPG-signed commits — one in `services/console` (submodule), one in `thinx-device-api` (parent). After this plan, `git submodule status services/console` from the parent shows the new SHA with no drift markers.

Phase 4 SEC-DEP-01 baseline and Phase 10 Plan 10-01 annex in `.planning/dep-triage.md` are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-cross-project-dependency-coordination-services-console/10-CONTEXT.md
@services/console/.planning/ROADMAP.md
@services/console/.planning/STATE.md

<interfaces>
Current state of the services/console submodule (parent's view, from `git submodule status services/console`):

```
 27758ebda8a179a440aff1cd443f9ffe1fe84a6d services/console (v1.999-3-g27758eb)
```

Branch convention inside services/console (per its STATE.md and PROJECT.md):
- Default working branch: `thinx-staging` (matches parent project convention)
- v1.999 milestone shipped + archived 2026-05-27
- Status: `awaiting_next_milestone` (per services/console STATE.md L4)
- Next planned action (services/console STATE.md L72): `Run /gsd-new-milestone to scope v1.x`

Current structure of `services/console/.planning/ROADMAP.md`:
- L1: `# Roadmap: THiNX Console — Vue Migration`
- L3: `**Status:** v1.999 shipped 2026-05-27 — feature-parity GA across 11 phases.`
- L4: `**Next:** v1.x planning — run /gsd-new-milestone. Candidates live in .planning/v1.x-backlog.md.`
- L7 `## Milestones` (1 entry: v1.999 shipped)
- L13 `## Phases` (collapsed details for v1.999 Phases 1–11)
- L36 `### 📋 Next milestone (to be planned)`
- L50 `## Progress` (per-phase table for v1.999)

The cleanest insertion point per CONTEXT.md Recommendation: add a NEW milestone `## Milestones` entry and a corresponding `## Phases` section that contains SEC-DEP-02 as Phase 1 of that new milestone. The existing v1.999 milestone and its phases are preserved exactly.

Two-row table from Plan 10-01 (Alert 54 + Alert 52) is the data source for SEC-DEP-02 scope summary; the services/console ROADMAP cross-references the parent annex rather than re-listing the alerts.

CRITICAL — Submodule edit workflow:

Editing files inside `services/console/` works on the submodule's checked-out working tree. The required workflow:

1. `cd services/console && git status` — confirm we're on `thinx-staging` (or whatever the submodule's working branch is) with a clean tree.
2. If submodule is in detached-HEAD state (common right after `git submodule update`), checkout `thinx-staging` explicitly: `cd services/console && git checkout thinx-staging`. Then `git pull --ff-only origin thinx-staging` to be at the remote tip — but ONLY if remote access is configured for this session. If not, work against the current detached HEAD's branch position and proceed.
3. Edit `.planning/ROADMAP.md` inside `services/console/`.
4. Stage + commit IN the submodule: `cd services/console && git add .planning/ROADMAP.md && git commit -m "..."` (GPG-signed per submodule's own config; if absent, fall back to `-S` explicit).
5. Capture the new HEAD SHA: `cd services/console && git rev-parse HEAD`.
6. Return to parent: `cd ../..` (or equivalent absolute path). Confirm with `pwd` that we're at `/Users/igraczech/Repositories/thinx-device-api`.
7. Stage the submodule pointer change in the parent: `git add services/console`.
8. Commit in the parent: `git commit -m "..."` (GPG-signed).
9. Verify with `git submodule status services/console` — output should start with a SPACE (clean — pointer matches submodule HEAD), NOT `+` (modified — pointer differs from HEAD) or `-` (uninitialized).

DO NOT push either commit. CircleCI green-gate is deferred to operator push per project convention (matches Phase 7/8 ACCEPT pattern).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEC-DEP-02 phase entry to services/console/.planning/ROADMAP.md (submodule edit + commit)</name>
  <files>services/console/.planning/ROADMAP.md, services/console</files>
  <action>
PRE-FLIGHT (submodule state check):

1. Use Bash: `cd services/console && git status --short && git rev-parse --abbrev-ref HEAD && git rev-parse HEAD` to confirm the submodule's current branch and HEAD SHA. The current pointer SHA per the parent is `27758ebda8a179a440aff1cd443f9ffe1fe84a6d`.

2. If the submodule is in detached HEAD state (`HEAD` printed as branch name), check out `thinx-staging` explicitly: `cd services/console && git checkout thinx-staging`. If `thinx-staging` doesn't exist locally but exists on origin, create it tracking origin: `cd services/console && git checkout -b thinx-staging origin/thinx-staging`. If neither exists, STOP and report — operator must clarify which branch to land the commit on.

3. Confirm the working tree of the submodule is clean before editing: `cd services/console && git status --short` MUST return empty output.

EDIT (insert new milestone + phase entry):

4. Use the Edit tool on `services/console/.planning/ROADMAP.md` to make the following changes (preserving everything else byte-identical):

   a. **Update the header lines L3-L4** to reflect the new milestone state. Replace:
      ```
      **Status:** v1.999 shipped 2026-05-27 — feature-parity GA across 11 phases.
      **Next:** v1.x planning — run `/gsd-new-milestone`. Candidates live in `.planning/v1.x-backlog.md`.
      ```
      With:
      ```
      **Status:** v1.999 shipped 2026-05-27 — feature-parity GA across 11 phases.
      **Next milestone scheduled (2026-06-03):** v1.x Operational Hygiene — Phase 1: SEC-DEP-02 (coordinated from `thinx-device-api` v1.9 Phase 10). Other v1.x candidates remain in `.planning/v1.x-backlog.md`; run `/gsd-new-milestone` to widen scope.
      ```

   b. **Extend the `## Milestones` section** by appending a new milestone bullet UNDER the existing v1.999 line. Insert after L8 (`- ✅ **v1.999 Feature-Parity GA** ...`):
      ```
      - 🚧 **v1.x Operational Hygiene** — Phase 1 scheduled 2026-06-03 (SEC-DEP-02 cross-project triage from `thinx-device-api` v1.9 Phase 10); execution awaits operator `/gsd:plan-phase`.
      ```

   c. **Add a new `## v1.x Operational Hygiene — Scheduled Phases` section AFTER the existing `### 📋 Next milestone (to be planned)` block but BEFORE the `## Progress` section** (insert before the L50 `## Progress` heading). The new section content:

      ```markdown
      ## v1.x Operational Hygiene — Scheduled Phases

      ### Phase 1: SEC-DEP-02 — Vendored Asset Dependency Triage (Cross-Project)

      **Goal:** Remediate the 2 high-severity Dependabot alerts on `thinx-cloud/console` (Alert 54: grunt < 1.5.3 / GHSA-rm36-94g8-835r; Alert 52: grunt < 1.3.0 / GHSA-m5pj-vjjf-4m3h), both lodged in the vendored static-asset bundle at `src/assets/global/plugins/jquery-validation-1.19.5/package.json`. The vulnerabilities are reachable ONLY if a future build step starts invoking grunt against the vendored manifest; effective production exposure today is zero.

      **Depends on:** Nothing (cross-project coordination — the parent project's Phase 10 of v1.9 has already classified the alerts and annexed the verdict roll-up to `thinx-device-api/.planning/dep-triage.md`).

      **Requirements:** SEC-DEP-02 (parent-project requirement; mirrored here for execution).

      **Recommended remediation** (per parent annex):
      - **Preferred:** delete the vendored plugin's `package.json` at `src/assets/global/plugins/jquery-validation-1.19.5/package.json`. The compiled JS/CSS in the same directory is what actually loads — the package.json is metadata only.
      - **Acceptable:** dismiss both alerts in the Dependabot UI with rationale "vendored asset; grunt not in services/console build path".

      **Success Criteria** (what must be TRUE):
        1. Both Dependabot alerts on `thinx-cloud/console` (#54 GHSA-rm36-94g8-835r and #52 GHSA-m5pj-vjjf-4m3h) are resolved — auto-closed after vendored `package.json` delete OR manually dismissed in the Dependabot UI with the rationale recorded in the closing commit message / dismissal note.
        2. EITHER the vendored `src/assets/global/plugins/jquery-validation-1.19.5/package.json` is removed from the repo (preferred path), OR a `### Deferred — dismiss-only` note is appended to this phase entry capturing the dismissal-with-rationale decision and the date.
        3. services/console CI green on the resulting branch (typically `thinx-staging`) — the Vue console build (`npm run build:test` or equivalent) still completes without referencing the deleted manifest.
        4. The parent `thinx-device-api` submodule pointer for `services/console` bumps cleanly across the SEC-DEP-02 commit; parent CircleCI stays green across the pointer-bump commit on `thinx-staging`.

      **Plans:** TBD (operator runs `/gsd:plan-phase 1` in this GSD workspace after `/gsd-new-milestone` is run for `v1.x Operational Hygiene`).

      **Cross-project references:**
      - Parent project verdict roll-up: `thinx-device-api/.planning/dep-triage.md` § "Phase 10 / SEC-DEP-02 (services/console) — Cross-Project Roll-up" (2 rows; both `deferred-vendored-asset`).
      - Parent project phase that scheduled this work: `thinx-device-api/.planning/ROADMAP.md` Phase 10 of v1.9.
      - Cross-project coordination runbook (parent): `thinx-device-api/.planning/runbooks/cross-project-dependency-coordination.md`.

      ```

   d. DO NOT modify the `<details>` block for v1.999 (L15-L34) or the `### 📋 Next milestone (to be planned)` content (L36-L48) or the `## Progress` table (L50-L63) above their existing content. Only INSERT new content at the described locations.

COMMIT (inside the submodule):

5. Stage and commit inside the submodule:

```bash
cd services/console
git add .planning/ROADMAP.md
git commit -m "$(cat <<'EOF'
docs(SEC-DEP-02): schedule SEC-DEP-02 phase coordinated from thinx-device-api v1.9

Adds a new `v1.x Operational Hygiene` milestone (scheduled, not yet
planned) with SEC-DEP-02 as its Phase 1. The phase covers remediation
of the 2 high-severity Dependabot alerts on `thinx-cloud/console`:

- Alert 54: grunt < 1.5.3 (GHSA-rm36-94g8-835r, CVE-2022-1537)
- Alert 52: grunt < 1.3.0 (GHSA-m5pj-vjjf-4m3h, CVE-2020-7729)

Both alerts live in the vendored static-asset bundle at
`src/assets/global/plugins/jquery-validation-1.19.5/package.json`.
services/console does NOT invoke grunt against this vendored manifest
during its own build, so effective production exposure today is zero.

Phase entry includes: goal, depends_on (none), requirements (SEC-DEP-02),
recommended remediation (preferred: delete vendored package.json;
acceptable: dismiss alerts in Dependabot UI), 4 Success Criteria, and
a `Plans: TBD` placeholder. Cross-references the parent project's
verdict roll-up at thinx-device-api/.planning/dep-triage.md
and the parent's Phase 10 of v1.9.

This is a SCHEDULING commit only — execution awaits the operator
running `/gsd-new-milestone` (for "v1.x Operational Hygiene") and
then `/gsd:plan-phase 1` in this GSD workspace.

v1.999 milestone content (Phases 1-11) and the `### 📋 Next milestone
(to be planned)` block are preserved byte-identical.

Coordinated from: thinx-device-api v1.9 milestone Phase 10
(.planning/ROADMAP.md L113-L121; .planning/REQUIREMENTS.md SEC-DEP-02 L30).
EOF
)"
```

6. Capture the new submodule HEAD SHA. Run `cd services/console && git rev-parse HEAD` and save the output to memory (you'll need it for Task 2's commit message body). The SHA MUST be a 40-char hex string different from `27758ebda8a179a440aff1cd443f9ffe1fe84a6d`.

7. Confirm the submodule's working tree is clean post-commit: `cd services/console && git status --short` MUST return empty output.

DO NOT push the submodule's commit. The push happens in the operator's follow-up session (matches the project's Test-env ACCEPT / "CircleCI green-gate deferred to operator push" pattern).
  </action>
  <verify>
    <automated>cd services/console && git log -1 --format='%s' | grep -q '^docs(SEC-DEP-02): schedule SEC-DEP-02 phase coordinated from thinx-device-api v1.9$' && grep -q '^### Phase 1: SEC-DEP-02 — Vendored Asset Dependency Triage' .planning/ROADMAP.md && grep -q 'GHSA-rm36-94g8-835r' .planning/ROADMAP.md && grep -q 'GHSA-m5pj-vjjf-4m3h' .planning/ROADMAP.md && grep -q 'jquery-validation-1.19.5/package.json' .planning/ROADMAP.md && grep -q 'v1.x Operational Hygiene' .planning/ROADMAP.md && [ "$(git status --short | wc -l | tr -d ' ')" = "0" ] && [ "$(git rev-parse HEAD)" != "27758ebda8a179a440aff1cd443f9ffe1fe84a6d" ]</automated>
  </verify>
  <done>
A new SEC-DEP-02 phase entry exists in `services/console/.planning/ROADMAP.md` under a new `v1.x Operational Hygiene` milestone. The phase entry includes goal, `Depends on: Nothing`, `Requirements: SEC-DEP-02`, recommended remediation (preferred/acceptable), 4 Success Criteria, `Plans: TBD`, and cross-references to the parent project's annex and runbook. One atomic GPG-signed commit lands in the `services/console` submodule on its working branch. v1.999 milestone content and the existing "Next milestone (to be planned)" block are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump submodule pointer in parent thinx-device-api repo (atomic commit)</name>
  <files>services/console</files>
  <action>
PRE-FLIGHT (parent repo state):

1. Return to the parent repo root. Use Bash with an absolute path to guarantee state: `cd /Users/igraczech/Repositories/thinx-device-api && pwd`. Confirm we're at the parent project root, NOT inside `services/console/`.

2. Confirm the parent's working tree is clean except for the staged submodule pointer change that Task 1's submodule commit introduced. Run `git status --short`. Expected output: exactly one line like `M services/console` (or `+services/console` in submodule-status notation) and nothing else.

3. Capture the NEW submodule HEAD SHA explicitly for the commit message body. Run `cd /Users/igraczech/Repositories/thinx-device-api && git submodule status services/console`. Expected output format: ` <new-sha> services/console (<describe>)`. The new SHA MUST differ from `27758ebda8a179a440aff1cd443f9ffe1fe84a6d`. Save the first 12 chars of the new SHA (short form) for the commit subject/body. Also save the previous SHA `27758ebda8a17` (12-char short) for the body's from/to line.

STAGE + COMMIT:

4. Stage the submodule pointer change in the parent:

```bash
cd /Users/igraczech/Repositories/thinx-device-api
git add services/console
```

5. Verify the staged change is ONLY the submodule pointer (no stray file edits). Run `git diff --cached --stat`. Expected: exactly one line `services/console | 2 +-` (or similar — submodule pointer changes show as 2-line diff: one `-Subproject commit <old>`, one `+Subproject commit <new>`). If any other file appears in the staged diff, STOP and unstage with `git reset HEAD <unintended-file>`.

6. Commit atomically with GPG signing. Use the actual new SHA captured in step 3 (replace `<new-sha-short>` and `<new-sha-full>` placeholders):

```bash
NEW_SHA=$(cd /Users/igraczech/Repositories/thinx-device-api/services/console && git rev-parse HEAD)
NEW_SHA_SHORT=$(cd /Users/igraczech/Repositories/thinx-device-api/services/console && git rev-parse --short=12 HEAD)
OLD_SHA_SHORT="27758ebda8a1"

cd /Users/igraczech/Repositories/thinx-device-api
git commit -m "$(cat <<EOF
chore: bump services/console submodule to capture SEC-DEP-02 schedule

Bumps the services/console submodule pointer from ${OLD_SHA_SHORT}
to ${NEW_SHA_SHORT}, capturing the SEC-DEP-02 phase scheduling commit
landed in the submodule's GSD workspace.

The submodule commit adds a new \`v1.x Operational Hygiene\` milestone
to \`services/console/.planning/ROADMAP.md\` with SEC-DEP-02 as its
Phase 1 (coordinated from this repo's v1.9 milestone Phase 10).
Execution of that phase awaits the operator's follow-up session
(\`/gsd-new-milestone\` + \`/gsd:plan-phase 1\` inside services/console).

Pointer movement:
  before: 27758ebda8a179a440aff1cd443f9ffe1fe84a6d
  after:  ${NEW_SHA}

No actual code change (no runtime behavior, no build artifact); the
submodule's new HEAD is a docs-only commit on its \`.planning/ROADMAP.md\`.

Closes part 2 of SEC-DEP-02 success criterion #1
(.planning/REQUIREMENTS.md L30: "console-side phase exists in
services/console/.planning/ROADMAP.md").

This is the SCHEDULE-bump only — the LATER bump that captures the
console-side remediation (delete vendored package.json or dismiss
alerts) is a separate operator-side commit per SEC-DEP-02 criterion #3
and per Phase 10 / ROADMAP.md L119 success criterion #3.
EOF
)"
```

POST-VERIFY (drift check):

7. Run `git submodule status services/console`. The output MUST start with a SPACE character (clean — pointer matches submodule HEAD), not `+` (modified — index ahead of HEAD) or `-` (uninitialized) or `U` (conflicted).

8. Run `git log -1 --format='%h %s'` in the parent and confirm the subject matches `chore: bump services/console submodule to capture SEC-DEP-02 schedule`.

9. Confirm the parent's working tree is fully clean post-commit: `cd /Users/igraczech/Repositories/thinx-device-api && git status --short` MUST return empty output.

DO NOT push. Operator push handles the CircleCI green-gate.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && git log -1 --format='%s' | grep -q '^chore: bump services/console submodule to capture SEC-DEP-02 schedule$' && [ "$(git status --short | wc -l | tr -d ' ')" = "0" ] && git submodule status services/console | grep -qE '^ [0-9a-f]{40} services/console' && [ "$(git submodule status services/console | awk '{print $1}')" != "27758ebda8a179a440aff1cd443f9ffe1fe84a6d" ] && [ "$(git show --stat HEAD | grep -c '^ services/console')" -eq 1 ]</automated>
  </verify>
  <done>
The parent `thinx-device-api` repo has a new atomic GPG-signed commit on `thinx-staging` bumping the `services/console` submodule pointer from `27758ebda8a17` to the new SHA captured in Task 1. `git submodule status services/console` reports the new SHA with no drift markers (clean leading space). The commit modifies ONLY the submodule pointer (no other file changes). The parent working tree is fully clean post-commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent repo ↔ submodule repo | Two separate git repos with separate histories. The pointer-bump commit in the parent references the submodule's HEAD by SHA; mismatch = drift (and broken `git submodule update` for downstream consumers). |
| operator → planning artifact | Both edits are planning documents (no code, no runtime). |
| submodule's `thinx-staging` branch tip → parent's submodule pointer | The parent points at a SHA, not a branch. Future force-push or rebase in the submodule's `thinx-staging` could orphan the pointer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | services/console v1.999 ROADMAP content (existing milestone block) | mitigate | Task 1 edits use Edit tool with surgical insertions; v1.999 `<details>` block and the existing "Next milestone (to be planned)" section are explicitly NOT touched. The grep gate `grep -q 'v1.999 Feature-Parity GA'` (implicit via preserve-byte-identical contract) confirms the existing milestone bullet survives. |
| T-10-05 | Tampering | Submodule pointer drift (parent index entry vs submodule HEAD) | mitigate | Task 2's verify uses `git submodule status services/console` and requires a leading SPACE (clean) — `+` would indicate index-ahead-of-HEAD drift; `-` would indicate uninitialized. The new SHA is also verified to differ from `27758ebda8a17`. |
| T-10-06 | Information disclosure | Commit message content (no secrets — only public GHSA IDs and submodule SHAs) | accept | All data is public (GHSA IDs from the public Dependabot API; submodule SHAs are public after push). |
| T-10-07 | Repudiation | Cross-repo commit authorship | mitigate | Both commits are GPG-signed per project convention (`commit.gpgsign=true` in both the parent and submodule git configs; if absent, fall back to `-S`). Commit messages include explicit "Coordinated from: thinx-device-api v1.9 milestone Phase 10" provenance. |
| T-10-08 | Elevation of privilege | Submodule pointer pointing to an unreviewed commit | mitigate | The submodule's commit (Task 1) is also GPG-signed and contains only the planning-document edit specified in Task 1's action. No code changes; impossible to elevate beyond docs scope. Operator's push to origin is the canonical review gate. |
</threat_model>

<verification>
1. The submodule (`services/console`) has a new commit on its working branch with subject `docs(SEC-DEP-02): schedule SEC-DEP-02 phase coordinated from thinx-device-api v1.9`. The commit modifies only `.planning/ROADMAP.md`.
2. `services/console/.planning/ROADMAP.md` contains a new `## v1.x Operational Hygiene — Scheduled Phases` section with a SEC-DEP-02 phase entry (goal, depends_on, requirements, 4 success criteria, Plans: TBD, cross-project references). Existing v1.999 content is preserved.
3. The parent `thinx-device-api` repo has a new commit on `thinx-staging` with subject `chore: bump services/console submodule to capture SEC-DEP-02 schedule`. The commit modifies ONLY the submodule pointer.
4. `git submodule status services/console` reports the new SHA with a leading SPACE (clean — no drift).
5. The new SHA differs from `27758ebda8a179a440aff1cd443f9ffe1fe84a6d`.
6. Both repos' working trees are clean post-commits.

Test-env ACCEPT pattern: planning-doc edits only across two repos — no Docker image, no specs, no CircleCI gate exercised in-session. CircleCI green-gate deferred to operator push (matches Phases 5/6/7/8 ACCEPT pattern).
</verification>

<success_criteria>
- TWO atomic GPG-signed commits land: one inside `services/console` (the ROADMAP edit), one in the parent `thinx-device-api` (the pointer bump).
- The new SEC-DEP-02 phase entry in `services/console/.planning/ROADMAP.md` satisfies SEC-DEP-02 success criterion #1 (parallel phase exists with its own success criteria).
- The submodule pointer in the parent advances cleanly with no drift markers.
- Phase 4 SEC-DEP-01 baseline + Phase 10 Plan 10-01 annex in `.planning/dep-triage.md` remain untouched (verified by `git diff HEAD~1 HEAD -- .planning/dep-triage.md` returning empty for THIS plan's commits).
- All `<verify>` automated checks green.
- Plan SUMMARY written.
</success_criteria>

<output>
Create `.planning/phases/10-cross-project-dependency-coordination-services-console/10-02-SUMMARY.md` when done.
</output>
