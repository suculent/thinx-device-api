---
phase: 10-cross-project-dependency-coordination-services-console
plan: 03
type: execute
wave: 1
depends_on:
  - 10-02
files_modified:
  - .planning/runbooks/cross-project-dependency-coordination.md
autonomous: true
requirements:
  - SEC-DEP-02

must_haves:
  truths:
    - "A new runbook exists at `.planning/runbooks/cross-project-dependency-coordination.md` documenting the cross-project dependency-triage coordination workflow."
    - "The runbook contains 5 sections: (1) When to use this pattern, (2) gh api recipe for Dependabot inventory, (3) Classification verdict glossary (`deferred-vendored-asset` plus the Phase 4 inherited classes), (4) Submodule edit + pointer bump workflow, (5) Post-merge verification checklist."
    - "The runbook references the Phase 10 / SEC-DEP-02 worked example by name (Alert 54 + Alert 52 on `thinx-cloud/console`) and links to `.planning/dep-triage.md` (Phase 10 annex) + `.planning/ROADMAP.md` (Phase 10 of v1.9)."
    - "The runbook's `gh api` recipe section contains the exact command used by Phase 10 (`gh api repos/thinx-cloud/console/dependabot/alerts?state=open&severity=high`) and explains how to substitute the repo slug for future runs."
    - "The runbook's submodule workflow section enumerates the 9-step pre-flight + edit + commit + pointer bump sequence used by Plan 10-02 (matching the workflow's success criteria in this plan's parent ROADMAP entry)."
    - "The runbook lives alongside `swarm.md`, `websocket-handshake.md`, and `managed-logs-redaction.md` in `.planning/runbooks/` and matches their tone/format (Operator-facing recovery procedures style)."
    - "One atomic GPG-signed commit lands on `thinx-staging`: `docs(SEC-DEP-02): cross-project dependency coordination runbook`."
  artifacts:
    - path: ".planning/runbooks/cross-project-dependency-coordination.md"
      provides: "Cross-project dependency-triage coordination runbook (the documented workflow for Phase 10 SEC-DEP-02 and future analogous coordinations)"
      contains: "Cross-Project Dependency Coordination Runbook"
      min_lines: 80
  key_links:
    - from: ".planning/runbooks/cross-project-dependency-coordination.md (worked example)"
      to: ".planning/dep-triage.md (Phase 10 annex)"
      via: "back-reference to the verdict roll-up table"
      pattern: "Phase 10 / SEC-DEP-02"
    - from: ".planning/runbooks/cross-project-dependency-coordination.md (submodule workflow)"
      to: "services/console/.planning/ROADMAP.md (SEC-DEP-02 phase entry)"
      via: "cross-project edit target"
      pattern: "services/console"
    - from: ".planning/runbooks/cross-project-dependency-coordination.md (gh api recipe)"
      to: "thinx-cloud/console Dependabot API"
      via: "alert enumeration command"
      pattern: "gh api repos/.*/dependabot/alerts"
---

<objective>
Create `.planning/runbooks/cross-project-dependency-coordination.md` — a new operator-facing runbook that documents the cross-project dependency-triage coordination workflow used by Phase 10 / SEC-DEP-02 (and applicable to any future analogous coordination between `thinx-device-api` and a sibling submodule project).

Purpose: Phase 10 is the first time we coordinate dependency triage across two GSD-managed repos (`thinx-device-api` + `services/console`). The classification verdict, the cross-repo edit + pointer-bump workflow, and the verification checklist are non-trivial enough to deserve a durable, discoverable runbook. Future coordinations (e.g., `services/worker`, `services/transformer`) will reuse this pattern.

Output: One new runbook file + one atomic GPG-signed commit on `thinx-staging`. The runbook uses the worked example from Phase 10 (Alert 54 + Alert 52 on `thinx-cloud/console`, both classified `deferred-vendored-asset`) as its concrete illustration.

This is a pure docs deliverable — no code, no runtime change, no specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-cross-project-dependency-coordination-services-console/10-CONTEXT.md
@.planning/dep-triage.md
@.planning/runbooks/swarm.md
@.planning/runbooks/websocket-handshake.md
@.planning/runbooks/managed-logs-redaction.md

<interfaces>
Existing runbooks in `.planning/runbooks/` (tone/format reference):

- `swarm.md` (~120 lines): Operator-facing recovery procedures for swarm host. Format: top-level summary + numbered rungs/recipes + verify steps + escalation notes.
- `websocket-handshake.md` (~12k): Reproduction table + operator-side fix instructions + post-fix verification recipe.
- `managed-logs-redaction.md` (~21k): Procedure (snapshot → dry-run → review → apply → sample → compact) + cron recipe + GDPR posture note + rollback.

The new runbook should match this house style:
- H1 title naming the runbook
- Short framing paragraph stating audience (operator) + when to use
- Numbered sections with concrete bash recipes (copy-paste ready)
- Verification checklist at the end
- Cross-references to related runbooks and planning artifacts

Worked example data (from Phase 10):

- Repo slug for Dependabot enumeration: `thinx-cloud/console`
- Concrete command run on 2026-06-03: `gh api 'repos/thinx-cloud/console/dependabot/alerts?state=open&severity=high'`
- Returned alerts: #54 (grunt < 1.5.3, GHSA-rm36-94g8-835r) and #52 (grunt < 1.3.0, GHSA-m5pj-vjjf-4m3h), both at manifest `src/assets/global/plugins/jquery-validation-1.19.5/package.json`
- Classification verdict (both): `deferred-vendored-asset`
- Submodule pointer before pointer-bump: `27758ebda8a179a440aff1cd443f9ffe1fe84a6d`
- Annex location: `.planning/dep-triage.md` § "Phase 10 / SEC-DEP-02 (services/console) — Cross-Project Roll-up" (created by Plan 10-01)
- Submodule edit target: `services/console/.planning/ROADMAP.md` § "v1.x Operational Hygiene — Scheduled Phases" (created by Plan 10-02)
- Parent commit subjects (for the workflow recipe):
  - In submodule: `docs(SEC-DEP-02): schedule SEC-DEP-02 phase coordinated from thinx-device-api v1.9`
  - In parent: `chore: bump services/console submodule to capture SEC-DEP-02 schedule`

Verdict glossary inherited from Phase 4 (`.planning/dep-triage.md` § "Rationale taxonomy"):
- `dev-only-scope` — devDep stripped by Dockerfile L86 `npm install --omit=dev`; no production exploit surface.
- `stale-alert` — installed version is past the alert's vulnerable_version_range; Dependabot will auto-clear on next rescan.
- `chai-http-lock` — fix blocked by AGENTS.md L82-92 (chai-http v5 ESM lock); reserved.
- `transitive-uncontrollable` — vulnerable transitive cannot be overridden without breaking parent.
- `low-severity-high-cost` — low/medium severity AND fix cost is high.
- `mitigated-by-config` — vulnerable surface gated by config not enabled in production.

NEW class introduced by Phase 10 (and now codified in this runbook):
- `deferred-vendored-asset` — package vulnerability lives in a vendored static-asset bundle's `package.json` metadata; the consuming project does NOT invoke the vulnerable package against the vendored manifest during its own build; vulnerable code paths never reached in production runtime.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-project coordination runbook</name>
  <files>.planning/runbooks/cross-project-dependency-coordination.md</files>
  <action>
Create the file `.planning/runbooks/cross-project-dependency-coordination.md` with the following content (matching the house style of `swarm.md`, `websocket-handshake.md`, and `managed-logs-redaction.md`). Use the Write tool.

Required structure (5 numbered sections + framing + verification checklist):

**Header (frontmatter-style, prose):**

- H1 title: `# Cross-Project Dependency Coordination Runbook`
- Framing paragraph: state that this runbook documents the workflow for coordinating Dependabot-alert triage between `thinx-device-api` (the parent monorepo) and a sibling submodule project (e.g., `services/console`, with future candidates `services/worker`, `services/transformer`). Audience: the GSD-driving operator (the developer running `/gsd:plan-phase` and `/gsd:execute-phase` against this repo). When to use: any time Dependabot surfaces an alert on a sibling project whose remediation requires either a planning-doc edit in that submodule's GSD workspace or a coordinated pointer bump in this parent repo.
- "First codified by": Phase 10 of v1.9 (SEC-DEP-02) — see `.planning/ROADMAP.md` Phase 10 and `.planning/phases/10-cross-project-dependency-coordination-services-console/`. The Phase 10 worked example (2 high-severity grunt alerts on `thinx-cloud/console`, both vendored-asset-only) runs through this runbook end-to-end and is referenced throughout.

**Section 1 — When to use this pattern:**

Bulleted list of triggers:
- A Dependabot alert lands on a sibling submodule project (e.g., `services/console`, `services/worker`, `services/transformer`) AND the operator wants to record the classification verdict in the parent's `.planning/dep-triage.md` (the canonical durable triage home for the parent project's planning workspace).
- The remediation work for the alert lives in the sibling project's GSD workspace (each submodule has its own `.planning/` tree) but the SCHEDULING + ROLL-UP belongs in the parent.
- A submodule pointer bump in `thinx-device-api` would otherwise be needed to "capture" the sibling project's resolution commit — and the operator wants that pointer bump to land atomically with a clear paper trail.

Also list anti-patterns / when NOT to use:
- The sibling project is unmanaged (no `.planning/`): do the triage entirely in `.planning/dep-triage.md` of the parent.
- The vulnerability lives in this parent repo's own dependency tree (not the sibling's): use the Phase 4 SEC-DEP-01 pattern in `.planning/dep-triage.md` directly.

**Section 2 — gh api recipe for Dependabot inventory:**

Concrete bash recipe (copy-paste ready):

```bash
# Substitute <owner>/<repo> for the sibling project's slug.
# Example used by Phase 10 / SEC-DEP-02:
gh api 'repos/thinx-cloud/console/dependabot/alerts?state=open&severity=high' \
  --jq '.[] | {number, package: .security_vulnerability.package.name, severity: .security_vulnerability.severity, ghsa: .security_advisory.ghsa_id, manifest: .dependency.manifest_path, scope: .dependency.scope, relationship: .dependency.relationship, vuln_range: .security_vulnerability.vulnerable_version_range}'

# To enumerate ALL open alerts (all severities), drop the severity filter:
gh api 'repos/<owner>/<repo>/dependabot/alerts?state=open' --jq '...'

# To list dismissed alerts (for context on past decisions):
gh api 'repos/<owner>/<repo>/dependabot/alerts?state=dismissed' --jq '.[] | {number, dismissed_reason, dismissed_comment, package: .security_vulnerability.package.name}'
```

Note that the `gh` CLI must be authenticated with `repo` + `security_events` scopes (`gh auth status` to verify). For sibling repos the operator doesn't own, the token needs `security_events` plus org membership / collaborator access.

Worked example output (from 2026-06-03 Phase 10 run):
- 2 alerts returned: #54 (grunt < 1.5.3, GHSA-rm36-94g8-835r, manifest `src/assets/global/plugins/jquery-validation-1.19.5/package.json`, scope `development`) and #52 (grunt < 1.3.0, GHSA-m5pj-vjjf-4m3h, same manifest, scope `development`).

**Section 3 — Classification verdict glossary:**

Subsection `## Rationale taxonomy` reproduced/linked from `.planning/dep-triage.md` § "Rationale taxonomy". Include the FULL closed set so this runbook is self-contained:

- `dev-only-scope`
- `stale-alert`
- `chai-http-lock`
- `transitive-uncontrollable`
- `low-severity-high-cost`
- `mitigated-by-config`
- `deferred-vendored-asset` ← NEW (introduced by Phase 10; included verbatim from `.planning/dep-triage.md` Phase 10 annex Disposition legend)

For each entry, include a 1-sentence definition + a "Future trigger to re-classify" note. The `deferred-vendored-asset` entry MUST include the Phase 10 worked example: "First applied to Alert 54 (GHSA-rm36-94g8-835r) and Alert 52 (GHSA-m5pj-vjjf-4m3h) on `thinx-cloud/console` — both at the vendored `jquery-validation-1.19.5/package.json` bundle. See `.planning/dep-triage.md` § 'Phase 10 / SEC-DEP-02 (services/console) — Cross-Project Roll-up' for the full row entries."

Subsection `## Verdict enum`: list the parent dep-triage.md closed set (`blocker`, `deferred-stale`, `deferred-dev-only`, `deferred-locked`, `deferred-cost`, `deferred-low-sev`) PLUS the new `deferred-vendored-asset`. Note that Phase 10 introduced the new verdict alongside the rationale class (one-to-one mapping for the new class, mirroring how Phase 4 mapped `deferred-stale` to `stale-alert`).

**Section 4 — Submodule edit + pointer bump workflow:**

Numbered procedure (the 9-step canonical sequence used by Plan 10-02, generalized):

```
1. Confirm parent repo working tree is clean:
     cd /path/to/thinx-device-api && git status --short   # must be empty

2. Check the submodule's current branch state:
     cd services/<submodule> && git status --short && git rev-parse --abbrev-ref HEAD && git rev-parse HEAD

3. If the submodule is in detached HEAD state, check out its working branch:
     git checkout thinx-staging                            # or whatever the submodule's working branch is

4. Make the planning-doc edit inside the submodule (typically .planning/ROADMAP.md or .planning/dep-triage.md):
     # Use your editor — the runbook's Section 5 verification gates apply post-edit.

5. Commit IN the submodule (GPG-signed by submodule's own git config):
     cd services/<submodule>
     git add .planning/...
     git commit -m "docs(<REQ-ID>): <scheduling subject>"

6. Capture the new submodule HEAD SHA:
     NEW_SHA=$(git rev-parse HEAD)
     echo "submodule new HEAD: ${NEW_SHA}"

7. Return to parent repo root and stage the pointer update:
     cd ../..   # or absolute path to thinx-device-api root
     pwd        # verify
     git add services/<submodule>

8. Verify ONLY the pointer changed in the parent stage:
     git diff --cached --stat   # expect exactly: services/<submodule> | 2 +-

9. Commit the pointer bump in the parent (GPG-signed):
     git commit -m "chore: bump services/<submodule> submodule to capture <REQ-ID> <description>"

POST-VERIFY:
- git submodule status services/<submodule>   # must start with SPACE (clean — no drift)
- git status --short                          # must be empty
- Neither commit is pushed in-session — operator push handles CircleCI green-gate.
```

Important callouts:
- Submodule commits use the SUBMODULE's git config for signing (GPG key may differ from parent's; verify with `cd services/<submodule> && git config --get commit.gpgsign` before committing if uncertain).
- The pointer-bump commit message MUST cite the submodule's new HEAD SHA in the body (full 40-char hex) so the bump is auditable even if force-push later orphans the SHA in the submodule's remote.
- If the submodule's working branch is rebased/force-pushed AFTER the pointer bump but BEFORE the parent's `thinx-staging` is pushed, the parent's pointer-bump commit becomes a "dangling pointer" — a downstream `git submodule update` will fetch the SHA but it will only resolve if the remote still has the commit reachable from a branch tip. Operator should push the submodule's branch BEFORE pushing the parent's pointer-bump commit (this runbook records the policy; Plan 10-02 defers push entirely to the operator).

**Section 5 — Post-merge verification checklist:**

Checkbox list:
- [ ] `gh api repos/<owner>/<repo>/dependabot/alerts?state=open` shows the alert(s) closed (auto-closed after remediation merges) OR dismissed in UI with rationale matching the verdict.
- [ ] `.planning/dep-triage.md` annex section row(s) for the alert(s) match the verdict in the closing commit / dismissal.
- [ ] Parent CircleCI green on the pointer-bump commit (`thinx-staging` branch).
- [ ] Sibling project CI green on its own resolution commit.
- [ ] `git submodule status services/<submodule>` from the parent reports a leading SPACE (clean, no drift) on every operator machine after `git submodule update --init --recursive`.
- [ ] Phase 10 worked example: SEC-DEP-02 closes when the parent ROADMAP.md Phase 10 Success Criteria #1-4 all true (parallel phase exists in services/console ROADMAP ✓; submodule pointer up-to-date ✓; dep-triage.md annex with verdict roll-up ✓; CircleCI green across the bump ✓).

**Closing — Cross-references:**

- `.planning/dep-triage.md` — canonical triage table for parent project + Phase 10 annex.
- `.planning/ROADMAP.md` Phase 10 of v1.9 — the phase that codified this workflow.
- `.planning/REQUIREMENTS.md` SEC-DEP-02 — requirement spec.
- `.planning/phases/10-cross-project-dependency-coordination-services-console/10-CONTEXT.md` — full context for the Phase 10 worked example.
- `.planning/runbooks/swarm.md`, `.planning/runbooks/websocket-handshake.md`, `.planning/runbooks/managed-logs-redaction.md` — sibling runbooks (same `.planning/runbooks/` collection).
- `AGENTS.md` (parent root) — dependency lock rationale (e.g., chai-http v5 lock) — consult before any v1.x phase touches `package.json` in either repo.

End of runbook.

Verify file contents with grep after writing (see `<verify>` block for the automated checks).
  </action>
  <verify>
    <automated>[ -f .planning/runbooks/cross-project-dependency-coordination.md ] && grep -q '^# Cross-Project Dependency Coordination Runbook' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'deferred-vendored-asset' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'gh api repos/thinx-cloud/console/dependabot/alerts' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'GHSA-rm36-94g8-835r' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'GHSA-m5pj-vjjf-4m3h' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'jquery-validation-1.19.5/package.json' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'git submodule status' .planning/runbooks/cross-project-dependency-coordination.md && grep -q 'Phase 10 / SEC-DEP-02' .planning/runbooks/cross-project-dependency-coordination.md && [ "$(wc -l < .planning/runbooks/cross-project-dependency-coordination.md)" -ge 80 ]</automated>
  </verify>
  <done>
The runbook file `.planning/runbooks/cross-project-dependency-coordination.md` exists with at least 80 lines and contains all 5 required sections (when-to-use, gh api recipe, classification verdict glossary, submodule workflow, post-merge verification checklist). The Phase 10 worked example (Alert 54 + Alert 52, classified `deferred-vendored-asset`, on the vendored jquery-validation bundle) is referenced throughout. Cross-references to `.planning/dep-triage.md`, `.planning/ROADMAP.md` Phase 10, and the other `.planning/runbooks/` files are present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic GPG-signed commit + verify clean tree</name>
  <files>.planning/runbooks/cross-project-dependency-coordination.md</files>
  <action>
1. Confirm only the new runbook file is staged (no stray modifications):

```bash
cd /Users/igraczech/Repositories/thinx-device-api
git status --short
```

Expected: exactly one line `?? .planning/runbooks/cross-project-dependency-coordination.md` (untracked).

2. Stage the runbook and commit atomically with GPG signing:

```bash
git add .planning/runbooks/cross-project-dependency-coordination.md
git commit -m "$(cat <<'EOF'
docs(SEC-DEP-02): cross-project dependency coordination runbook

Adds `.planning/runbooks/cross-project-dependency-coordination.md` — a
new operator-facing runbook documenting the cross-project dependency-
triage coordination workflow first codified by Phase 10 / SEC-DEP-02
of v1.9.

Runbook covers:
  1. When to use this pattern (sibling-submodule Dependabot alerts +
     coordinated parent annex + optional pointer bump).
  2. `gh api repos/<owner>/<repo>/dependabot/alerts` enumeration
     recipe (with the Phase 10 worked-example command verbatim).
  3. Classification verdict glossary — the full Phase 4 rationale
     taxonomy + the new `deferred-vendored-asset` class introduced
     by Phase 10.
  4. Submodule edit + pointer bump workflow (the 9-step canonical
     sequence used by Plan 10-02, generalized for any sibling).
  5. Post-merge verification checklist (Dependabot alert state,
     parent + sibling CI, `git submodule status` drift check).

Worked example referenced throughout: Alert 54 (GHSA-rm36-94g8-835r,
grunt < 1.5.3) and Alert 52 (GHSA-m5pj-vjjf-4m3h, grunt < 1.3.0) on
`thinx-cloud/console`, both at the vendored
`src/assets/global/plugins/jquery-validation-1.19.5/package.json`,
both classified `deferred-vendored-asset`.

Lives alongside `swarm.md`, `websocket-handshake.md`, and
`managed-logs-redaction.md` in `.planning/runbooks/`. Cross-references
the parent annex at `.planning/dep-triage.md`, the parent ROADMAP
Phase 10 entry, and `.planning/REQUIREMENTS.md` SEC-DEP-02.

Closes part 3 of SEC-DEP-02 (operator-facing workflow now durably
documented; future analogous coordinations — `services/worker`,
`services/transformer` — can reuse this runbook verbatim).
EOF
)"
```

3. Verify the commit landed atomically:

```bash
git log -1 --format='%s'                        # must match the subject above
git show --stat HEAD                            # must show exactly 1 file added
git status --short                              # must be empty
```

4. Confirm the file count in `.planning/runbooks/` increased by one (from 3 to 4):

```bash
ls .planning/runbooks/*.md | wc -l              # must be 4
```

DO NOT push. CircleCI green-gate deferred to operator push (Test-env ACCEPT pattern).
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && git log -1 --format='%s' | grep -q '^docs(SEC-DEP-02): cross-project dependency coordination runbook$' && [ "$(git status --short | wc -l | tr -d ' ')" = "0" ] && [ "$(git show --stat HEAD | grep -c '^ .planning/runbooks/cross-project-dependency-coordination.md')" -eq 1 ] && [ "$(ls .planning/runbooks/*.md 2>/dev/null | wc -l | tr -d ' ')" = "4" ]</automated>
  </verify>
  <done>
One atomic GPG-signed commit lands on `thinx-staging` with subject `docs(SEC-DEP-02): cross-project dependency coordination runbook`. The commit adds exactly one file (`.planning/runbooks/cross-project-dependency-coordination.md`). The parent working tree is fully clean post-commit. The `.planning/runbooks/` directory now contains 4 runbooks (swarm, websocket-handshake, managed-logs-redaction, cross-project-dependency-coordination).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → planning artifact | Pure docs deliverable — no code, no runtime, no specs. |
| Phase 4 / Plan 10-01 / Plan 10-02 artifacts → Plan 10-03 runbook | Runbook references and SHOULD NOT modify any of: `.planning/dep-triage.md`, `services/console/.planning/ROADMAP.md`, `.planning/ROADMAP.md`. Enforced by Task 2's `git show --stat HEAD` check (exactly one file added). |
| runbook content → future operator | The runbook will be consulted by operators in future similar coordinations. Inaccurate recipe = real misuse risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information disclosure | Runbook content (public GHSA IDs, public Dependabot API recipe, public submodule SHAs) | accept | All data is public. The `gh api` recipe references the GitHub public API; no secrets in the runbook. |
| T-10-10 | Tampering | Cross-references in the runbook drift out of sync with their targets (e.g., `.planning/dep-triage.md` Phase 10 annex section header) | mitigate | The runbook's cross-references use stable identifiers ("Phase 10 / SEC-DEP-02", "deferred-vendored-asset" rationale class, full file paths). If those targets are renamed in a future phase, a `grep -r 'Phase 10 / SEC-DEP-02' .planning/` will surface the runbook for update. |
| T-10-11 | Repudiation | Runbook authorship | mitigate | Commit is GPG-signed per project convention. Commit subject + body cite Phase 10 of v1.9 as the codifying phase. |
| T-10-12 | Elevation of privilege | Operator misreads the `gh api` recipe and runs it against a repo they don't have access to | accept | The recipe explicitly notes the required `gh auth status` scopes (`repo` + `security_events`). Failure mode is a `401 Bad credentials` from the GitHub API — no privilege elevation, just a failed enumeration. |
</threat_model>

<verification>
1. The file `.planning/runbooks/cross-project-dependency-coordination.md` exists with at least 80 lines.
2. The runbook contains all 5 required sections: when-to-use triggers, `gh api` enumeration recipe, classification verdict glossary (including `deferred-vendored-asset`), submodule edit + pointer bump 9-step workflow, post-merge verification checklist.
3. The Phase 10 worked example (Alert 54 + Alert 52 on `thinx-cloud/console`, vendored `jquery-validation-1.19.5/package.json`, `deferred-vendored-asset`) appears verbatim in the runbook.
4. Cross-references to `.planning/dep-triage.md`, `.planning/ROADMAP.md` Phase 10, `.planning/REQUIREMENTS.md` SEC-DEP-02, and the other three sibling runbooks are present.
5. One atomic GPG-signed commit lands on `thinx-staging` with subject `docs(SEC-DEP-02): cross-project dependency coordination runbook`.
6. The commit modifies exactly one file (the new runbook); no other files touched.
7. `.planning/runbooks/` now contains exactly 4 markdown files.

Test-env ACCEPT pattern: pure docs — no Docker image, no specs, no CircleCI gate exercised in-session. CircleCI green-gate deferred to operator push.
</verification>

<success_criteria>
- `.planning/runbooks/cross-project-dependency-coordination.md` exists with all 5 required sections.
- One atomic GPG-signed commit on `thinx-staging`.
- Phase 4 SEC-DEP-01 baseline, Plan 10-01 annex, and Plan 10-02 submodule edit / pointer bump are untouched by this plan (verified by `git show --stat HEAD` reporting exactly one added file).
- All `<verify>` automated checks green.
- Plan SUMMARY written.
- After all three plans (10-01, 10-02, 10-03) complete: SEC-DEP-02 success criteria #1, #2, and the foundation for #3 (submodule pointer bump for the scheduling commit) are all satisfied; the only remaining SEC-DEP-02 success criterion (#3 the LATER bump after console-side remediation merges, and #4 CircleCI green-gate) is operator-side and out of Phase 10 scope per CONTEXT.md.
</success_criteria>

<output>
Create `.planning/phases/10-cross-project-dependency-coordination-services-console/10-03-SUMMARY.md` when done.
</output>
