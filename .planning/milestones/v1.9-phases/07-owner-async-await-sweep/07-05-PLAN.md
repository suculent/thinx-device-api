---
phase: 07-owner-async-await-sweep
plan: 05
type: execute
wave: 1
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
files_modified:
  - lib/thinx/owner.js
  - spec/jasmine/02-OwnerSpec.js
autonomous: true
requirements:
  - REFACTOR-04
tags:
  - refactor
  - async-await
  - owner
  - password-reset
  - top-5
  - auth-critical
  - phase5-anti-regression
  - sec-pii-01

must_haves:
  truths:
    - "`Owner.password_reset(owner, reset_key, callback)` keeps its 3-argument public signature EXACTLY."
    - "The single caller UNCHANGED: router.user.js:27 (per CONTEXT.md table line 48)."
    - "Plans 07-01, 07-02, 07-03, 07-04 commits MUST have landed first."
    - "**CRITICAL PHASE 5 REFACTOR-02 ANTI-REGRESSION:** The strict-equality comparison `reset_key !== user_reset_key` (Phase 5 fix at line 492 pre-Phase-7; line drifts after Plan 07-01) MUST remain `!==` after this conversion. Plan 07-5's post-commit gate verifies via `grep -nE \"reset_key\\s+!==\\s+user_reset_key\" lib/thinx/owner.js` returning exactly 1 line. ANY drift to `!=` is a REGRESSION of v1.9 Phase 5's REFACTOR-02 fix and MUST be blocked."
    - "The synchronous `missing_reset_key` guard at line 461-464 is preserved."
    - "**SEC-PII-01 preservation:** `alog.log(owner, \"Attempt to reset password with: \" + Util.redactToken(reset_key), \"warning\")` at line 467 (pre-conversion line; drifts post-07-01) MUST remain at the same logical position (before the userlib.view await). Similarly the `console.log(\"ℹ️ [info] Attempting to reset password with key ${Util.redactToken(reset_key)}\")` at line 490 MUST remain before the comparison. These were v1.0 Phase 2 deliverables."
    - "The 4 callback return shapes preserved: `(false, \"missing_reset_key\")`, `(false, err)` (userlib error), `(false, \"user_not_found\")` (empty rows), `(false, \"invalid_reset_key\")` (mismatched key), `(true, { redirectURL: url })` (success)."
    - "A behavior-locking unit test is added: `it('REFACTOR-04 (07-5): Owner.password_reset callback contract preserved for missing_reset_key', ...)` asserting `(false, 'missing_reset_key')` when reset_key is undefined. Safe to run without DB dependency — synchronous guard."
    - "`node --check` clean. Diff scope: Owner.password_reset method body only + spec addition."
    - "One atomic GPG-signed commit on `thinx-staging` with subject `refactor(REFACTOR-04): convert Owner.password_reset to async/await internals`."
  artifacts:
    - path: "lib/thinx/owner.js"
      provides: "Owner.password_reset with async/await internals; public signature preserved; SEC-PII-01 redaction calls preserved; Phase 5 REFACTOR-02 strict-equality preserved."
      contains: "async password_reset(owner, reset_key, callback)"
    - path: "spec/jasmine/02-OwnerSpec.js"
      provides: "Behavior-locking unit test for Owner.password_reset callback contract (REFACTOR-04 07-5)."
      contains: "REFACTOR-04 (07-5)"
  key_links:
    - from: "lib/router.user.js:27"
      to: "lib/thinx/owner.js Owner.password_reset"
      via: "owner.password_reset(owner, reset_key, (success, response) => ...)"
      pattern: "owner\\.password_reset\\("
    - from: "Phase 5 REFACTOR-02 strict-equality fix"
      to: "post-Plan-07-5 Owner.password_reset body"
      via: "literal text reset_key !== user_reset_key (NOT reset_key != user_reset_key)"
      pattern: "reset_key\\s+!==\\s+user_reset_key"
    - from: "SEC-PII-01 (v1.0 Phase 2 redaction)"
      to: "post-Plan-07-5 Owner.password_reset body"
      via: "Util.redactToken(reset_key) appears in alog.log AND console.log"
      pattern: "Util\\.redactToken\\(reset_key\\)"
---

<objective>
Convert `Owner.password_reset(owner, reset_key, callback)` (lines 459-500 pre-conversion) from callback-style to async/await. Preserve the 3-argument signature. Preserve the synchronous `missing_reset_key` guard. **CRITICAL:** preserve the Phase 5 REFACTOR-02 strict-equality fix at the comparison `reset_key !== user_reset_key` (must NOT regress to `!=`). Preserve the two SEC-PII-01 `Util.redactToken(reset_key)` redaction sites (one in `alog.log`, one in `console.log`). Add a behavior-locking unit test for the synchronous missing_reset_key guard.

Purpose: `Owner.password_reset` is the most security-sensitive top-5 method. It is the authentication primitive for the password-reset GET handler (router.user.js:27 → e.g., `/api/v2/password/reset?reset_key=...`). The conversion has two critical anti-regression gates: (1) Phase 5 REFACTOR-02 — the `!=` → `!==` fix landed in v1.9 Phase 5 commit `b0aef15b` MUST survive; (2) SEC-PII-01 — the v1.0 Phase 2 redaction calls MUST stay intact. A regression on either is unacceptable.

Output: Two files modified, one atomic GPG-signed commit on `thinx-staging` with subject `refactor(REFACTOR-04): convert Owner.password_reset to async/await internals`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-owner-async-await-sweep/07-CONTEXT.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md
@.planning/phases/07-owner-async-await-sweep/07-01-PLAN.md
@lib/thinx/owner.js
@spec/jasmine/02-OwnerSpec.js
@spec/jasmine/ZZ-AppSessionUserSpec.js
@spec/jasmine/ZZ-RouterPasswordResetSpec.js
@lib/router.user.js

<interfaces>
Method signature (PRESERVE):
  password_reset(owner, reset_key, callback)

Caller (NOT modified):
  - lib/router.user.js:27 — GET /api/v2/password/reset?reset_key=...&owner=...

Callback shapes (5 total — all preserved):
  - callback(false, "missing_reset_key")    — synchronous guard
  - callback(false, err)                    — userlib.view rejected
  - callback(false, "user_not_found")       — empty body.rows
  - callback(false, "invalid_reset_key")    — strict-mismatch (Phase 5 REFACTOR-02 path)
  - callback(true, { redirectURL: url })    — success

Pre-conversion body (lines 459-500):
  password_reset(owner, reset_key, callback) {
    if (typeof (reset_key) === "undefined") {
      console.log("☣️ [error] Missing reset key.");
      return callback(false, "missing_reset_key");
    }
    // SEC-PII-01: alog.log persists to CouchDB managed_logs - redact reset_key.
    alog.log(owner, "Attempt to reset password with: " + Util.redactToken(reset_key), "warning");
    this.userlib.view("users", "owners_by_resetkey", {
      "key": reset_key,
      "include_docs": true
    }, (err, body) => {
      if (err) {
        console.log("☣️ [error] owners_by_resetkey", err);
        return callback(false, err);
      }
      if (body.rows.length === 0) {
        return callback(false, "user_not_found");
      }
      let user = body.rows[0].doc;
      let user_reset_key = user.reset_key;
      if (typeof (user_reset_key) === "undefined") {
        user_reset_key = null;
      }
      console.log(`ℹ️ [info] Attempting to reset password with key ${Util.redactToken(reset_key)}`);
      if (reset_key !== user_reset_key) {                              // <-- Phase 5 REFACTOR-02 fix (!= → !==)
        console.log("☣️ [error] reset_key does not match");
        callback(false, "invalid_reset_key");
      } else {
        const url = app_config.public_url + "/password.html?reset_key=" + reset_key + "&owner=" + owner;
        callback(true, { redirectURL: url });
      }
    });
  }

Async/await target shape:
  async password_reset(owner, reset_key, callback) {
    if (typeof (reset_key) === "undefined") {
      console.log("☣️ [error] Missing reset key.");
      return callback(false, "missing_reset_key");
    }
    // SEC-PII-01: alog.log persists to CouchDB managed_logs - redact reset_key.
    alog.log(owner, "Attempt to reset password with: " + Util.redactToken(reset_key), "warning");
    let body;
    try {
      body = await this.userlib.view("users", "owners_by_resetkey", {
        "key": reset_key,
        "include_docs": true
      });
    } catch (err) {
      console.log("☣️ [error] owners_by_resetkey", err);
      return callback(false, err);
    }
    if (body.rows.length === 0) {
      return callback(false, "user_not_found");
    }
    let user = body.rows[0].doc;
    let user_reset_key = user.reset_key;
    if (typeof (user_reset_key) === "undefined") {
      user_reset_key = null;
    }
    console.log(`ℹ️ [info] Attempting to reset password with key ${Util.redactToken(reset_key)}`);
    if (reset_key !== user_reset_key) {                                // <-- MUST STAY !== (Phase 5 REFACTOR-02)
      console.log("☣️ [error] reset_key does not match");
      callback(false, "invalid_reset_key");
    } else {
      const url = app_config.public_url + "/password.html?reset_key=" + reset_key + "&owner=" + owner;
      callback(true, { redirectURL: url });
    }
  }

MUST-PRESERVE elements (anti-regression gates):
  1. The synchronous `missing_reset_key` guard — verbatim.
  2. The `alog.log(owner, "Attempt to reset password with: " + Util.redactToken(reset_key), "warning")` call BEFORE the userlib.view — verbatim. The SEC-PII-01 comment above it MUST stay.
  3. The `console.log` "Attempting to reset password with key ${Util.redactToken(reset_key)}" — verbatim (preserves the second SEC-PII-01 redaction site).
  4. **The literal string `reset_key !== user_reset_key`** — Phase 5 REFACTOR-02. MUST remain `!==`, NEVER drift to `!=`.
  5. The `if (typeof (user_reset_key) === "undefined") { user_reset_key = null; }` normalization — verbatim. Without this normalization, a missing reset_key on the DB doc would compare a string against `undefined` rather than `null`, changing behavior.
  6. The success URL construction `app_config.public_url + "/password.html?reset_key=" + reset_key + "&owner=" + owner` — verbatim. Note: the reset_key is intentionally NOT redacted in the URL (it must be the real key for the client to use). This is by design.
  7. All 5 callback (success, response) tuples preserved.
</interfaces>

<behavior>
New test in spec/jasmine/02-OwnerSpec.js (append):

  it("REFACTOR-04 (07-5): Owner.password_reset callback contract preserved for missing_reset_key guard", (done) => {
    // The missing_reset_key guard is synchronous — fires before any userlib call.
    // Safe to run without DB/Redis dependencies, so this test survives the local
    // test-env config-missing ACCEPT scenario.
    user.password_reset("any_owner_id", undefined, (cb_success, cb_response) => {
      expect(cb_success).to.equal(false);
      expect(cb_response).to.equal("missing_reset_key");
      done();
    });
  }, 5000);

Why this test: (1) It locks the callback (success, response) tuple shape via the synchronous guard path. (2) It does NOT exercise the Phase 5 REFACTOR-02 `!==` comparison directly (that requires a real CouchDB doc to compare against) — but the strict-equality survival is enforced by the post-commit grep gate (Task 4 step 2). (3) The existing ZZ-AppSessionUserSpec.js + ZZ-RouterPasswordResetSpec.js (CI-side) exercise the full reset_key flow and would surface any behavioral regression on the strict-mismatch path during the canonical Jasmine green-gate.
</behavior>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check including Phase 5 REFACTOR-02 baseline</name>
  <files>
    - lib/thinx/owner.js (READ ONLY)
  </files>
  <action>
    1. Dependencies: `git log --oneline -20 | grep -cE "REFACTOR-04.*(non-top-5|Owner\.create|Owner\.delete|Owner\.update)"` MUST return 4.
    2. Syntax baseline: `node --check lib/thinx/owner.js` exits 0.
    3. Strict-equality state preserved: `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` returns 0 lines.
    4. **Phase 5 REFACTOR-02 baseline:** `grep -nE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js` MUST return exactly 1 line. This is the Phase 5 fix this plan MUST preserve. If 0, Phase 5 fix is missing — STOP and escalate (something else upstream regressed).
    5. SEC-PII-01 baseline: `grep -c "Util.redactToken(reset_key)" lib/thinx/owner.js` returns at least 2 (one alog.log, one console.log inside password_reset). Plus any other Util.redactToken sites elsewhere in the file.
    6. Locate Owner.password_reset: `grep -n "^\s*password_reset\s*(owner, reset_key, callback)" lib/thinx/owner.js` returns 1 line, NOT yet `async`.
    7. Cumulative async top-5 count: `grep -cE "^\s*async\s+(create|delete|update)\s*\(" lib/thinx/owner.js` returns 3.
    8. set_password not yet converted: `grep -cE "^\s*async\s+set_password\s*\(" lib/thinx/owner.js` returns 0.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && grep -qE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js && test "$(grep -c 'Util.redactToken(reset_key)' lib/thinx/owner.js)" -ge "2" && grep -q "^\s*password_reset\s*(owner, reset_key, callback)" lib/thinx/owner.js && echo "baseline:OK"</automated>
  </verify>
  <done>4 prior commits on staging. Phase 5 REFACTOR-02 baseline confirmed: `reset_key !== user_reset_key` present exactly once. SEC-PII-01 redactToken count >= 2. Owner.password_reset located, still callback-style.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Owner.password_reset to async/await preserving Phase 5 fix and SEC-PII-01 redaction</name>
  <files>
    - lib/thinx/owner.js
  </files>
  <action>
    Apply the canonical transform per <interfaces> "Async/await target shape". Specific rules:

    1. Outer signature: `password_reset(owner, reset_key, callback) {` → `async password_reset(owner, reset_key, callback) {`. PRESERVE the 3-arg order.

    2. **Synchronous guard preserved (lines 461-464):** verbatim.
       ```
       if (typeof (reset_key) === "undefined") {
         console.log("☣️ [error] Missing reset key.");
         return callback(false, "missing_reset_key");
       }
       ```

    3. **SEC-PII-01 alog.log preserved (line 466-467):** verbatim, INCLUDING the comment.
       ```
       // SEC-PII-01: alog.log persists to CouchDB managed_logs - redact reset_key.
       alog.log(owner, "Attempt to reset password with: " + Util.redactToken(reset_key), "warning");
       ```
       This line stays BEFORE the userlib.view await.

    4. **Convert userlib.view to await:** the callback-style `this.userlib.view("users", "owners_by_resetkey", {...}, (err, body) => ...)` becomes:
       ```
       let body;
       try {
         body = await this.userlib.view("users", "owners_by_resetkey", {
           "key": reset_key,
           "include_docs": true
         });
       } catch (err) {
         console.log("☣️ [error] owners_by_resetkey", err);
         return callback(false, err);
       }
       ```
       The `console.log("☣️ [error] owners_by_resetkey", err)` MUST stay in the catch block, verbatim.

    5. **Empty rows check preserved:** verbatim.
       ```
       if (body.rows.length === 0) {
         return callback(false, "user_not_found");
       }
       ```

    6. **user_reset_key normalization preserved (lines 483-488):** verbatim.
       ```
       let user = body.rows[0].doc;
       let user_reset_key = user.reset_key;
       if (typeof (user_reset_key) === "undefined") {
         user_reset_key = null;
       }
       ```

    7. **SEC-PII-01 console.log preserved (line 490):** verbatim.
       ```
       console.log(`ℹ️ [info] Attempting to reset password with key ${Util.redactToken(reset_key)}`);
       ```

    8. **CRITICAL — Phase 5 REFACTOR-02 strict-equality preserved (line 492):**
       ```
       if (reset_key !== user_reset_key) {                  // <-- MUST STAY !== !!!
         console.log("☣️ [error] reset_key does not match");
         callback(false, "invalid_reset_key");
       } else {
         const url = app_config.public_url + "/password.html?reset_key=" + reset_key + "&owner=" + owner;
         callback(true, { redirectURL: url });
       }
       ```
       The `!==` MUST NOT regress to `!=`. ANY drift here is a Phase 5 anti-regression failure. The success URL construction is also verbatim.

    9. **What this task MUST NOT do:**
       - Change the method signature parameter order or names.
       - Remove or relocate the `alog.log` line.
       - Remove or relocate either `Util.redactToken(reset_key)` call.
       - Change `!==` to `!=` (Phase 5 REFACTOR-02 regression).
       - Change the `=== 0` empty-rows check.
       - Change the `=== "undefined"` typeof check.
       - Add a new redaction call (the URL intentionally embeds the unredacted reset_key for the client to use).
       - Add an outer try/catch that swallows the strict-mismatch path (the callback for invalid_reset_key fires inside the success path of the userlib await, NOT inside the catch).
       - Touch any OTHER method (delete, update, create, set_password, etc.).
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && grep -qE "^\s*async\s+password_reset\s*\(owner, reset_key, callback\)\s*\{" lib/thinx/owner.js && grep -qE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js && test "$(grep -c 'Util.redactToken(reset_key)' lib/thinx/owner.js)" -ge "2" && echo "password_reset-converted:OK"</automated>
  </verify>
  <done>Owner.password_reset now `async`. The `reset_key !== user_reset_key` literal STILL present exactly once. Both Util.redactToken(reset_key) calls STILL present. alog.log SEC-PII-01 line preserved with comment. console.log redacted-info line preserved. user_reset_key null-normalization preserved. All 5 callback tuples preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add behavior-locking unit test</name>
  <files>
    - spec/jasmine/02-OwnerSpec.js
  </files>
  <action>
    Append the `it` block from <behavior> at end of existing describe block. Tests the synchronous missing_reset_key guard. Locks the (false, "missing_reset_key") callback contract.
  </action>
  <verify>
    <automated>grep -q "REFACTOR-04 (07-5)" spec/jasmine/02-OwnerSpec.js && node --check spec/jasmine/02-OwnerSpec.js && echo "spec-added:OK"</automated>
  </verify>
  <done>One new `it` block with `REFACTOR-04 (07-5)` label added. Spec syntax clean.</done>
</task>

<task type="auto">
  <name>Task 4: Static validation gates including Phase 5 anti-regression</name>
  <files>
    - lib/thinx/owner.js (READ ONLY)
    - spec/jasmine/02-OwnerSpec.js (READ ONLY)
  </files>
  <action>
    1. Syntax: both files `node --check` exit 0.
    2. **PHASE 5 ANTI-REGRESSION GATE (highest-priority):** `grep -nE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js` MUST return exactly 1 line. If 0, the Phase 5 REFACTOR-02 fix has regressed — STOP IMMEDIATELY and restore the `!==`. If >1, the comparison was duplicated (should never happen).
    3. **`!=` non-regression cross-check:** `grep -nE "reset_key\s+!=\s+user_reset_key" lib/thinx/owner.js` (note: single `=` after `!`) MUST return ZERO lines. If >0, the strict-equality drift has happened — STOP and fix.
    4. **SEC-PII-01 redaction preservation:** `grep -c "Util.redactToken(reset_key)" lib/thinx/owner.js` MUST be >= baseline from Task 1 step 5.
    5. **Signature gate:** `grep -E "^\s*async\s+password_reset\s*\(owner, reset_key, callback\)" lib/thinx/owner.js` returns 1 line.
    6. **General strict-equality state:** `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` returns 0 lines (07-2's gate stays met).
    7. **SEC-PII-01 total count:** `grep -c "alog.log\|Util.redactToken\|Util.redactEmail" lib/thinx/owner.js` >= pre-Phase-7 baseline.
    8. **Diff scope:** `git diff --stat lib/thinx/owner.js` shows hunks limited to the password_reset method body line range + spec addition.
    9. **set_password untouched:** `git diff -U0 lib/thinx/owner.js | grep -E "^[-+]\s*(async\s+)?set_password\s*\("` returns 0 lines.
    10. **Spec gate:** `grep -c "REFACTOR-04 (07-5)" spec/jasmine/02-OwnerSpec.js` returns 1.
    11. **Cumulative async top-5 count:** `grep -cE "^\s*async\s+(create|delete|update|password_reset)\s*\(" lib/thinx/owner.js` returns 4.
    12. Lint (best-effort): `npm run lint 2>&1 | tail -10` — ACCEPT if config-missing.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && node --check spec/jasmine/02-OwnerSpec.js && test "$(grep -cE 'reset_key\s+!==\s+user_reset_key' lib/thinx/owner.js)" = "1" && test "$(grep -cE 'reset_key\s+!=\s+user_reset_key' lib/thinx/owner.js)" = "0" && grep -qE "^\s*async\s+password_reset\s*\(owner, reset_key, callback\)" lib/thinx/owner.js && grep -q "REFACTOR-04 (07-5)" spec/jasmine/02-OwnerSpec.js && test "$(grep -cE '^\s*async\s+(create|delete|update|password_reset)\s*\(' lib/thinx/owner.js)" = "4" && echo "static-gates:OK"</automated>
  </verify>
  <done>All 12 gates pass. Phase 5 REFACTOR-02 fix preserved (exactly 1 `!==`, exactly 0 `!=` literals at the comparison). Both SEC-PII-01 redactToken sites preserved. async password_reset signature exact match. 4-of-5 top-5 methods now async (only set_password remains for 07-6).</done>
</task>

<task type="auto">
  <name>Task 5: Atomic GPG-signed commit on thinx-staging</name>
  <files>
    - lib/thinx/owner.js
    - spec/jasmine/02-OwnerSpec.js
  </files>
  <action>
    1. `git status --short` — only the 2 files modified.
    2. `git add lib/thinx/owner.js spec/jasmine/02-OwnerSpec.js`
    3. `git commit -S` with subject `refactor(REFACTOR-04): convert Owner.password_reset to async/await internals` and body explicitly noting:
       - The Phase 5 REFACTOR-02 anti-regression: the `reset_key !== user_reset_key` strict-equality at line 492 (pre-Phase-7) has been preserved verbatim through the conversion. The Phase 5 fix from commit `b0aef15b` is NOT regressed.
       - SEC-PII-01 preservation: both `Util.redactToken(reset_key)` call sites (alog.log audit and console.log info) preserved at original positions inside the converted method body. The v1.0 Phase 2 SEC-PII-01 redaction contract is intact.
       - Signature preservation: 3-arg (owner, reset_key, callback) order unchanged; single caller router.user.js:27 unmodified.
       - Synchronous missing_reset_key guard preserved.
       - user_reset_key null-normalization preserved.
       - All 5 callback (success, response) tuple shapes preserved.
       - Behavior-locking unit test added: REFACTOR-04 (07-5) — locks the synchronous missing_reset_key callback contract.
       - Validation gates passed: Phase 5 anti-regression grep exact 1 match; `!=` cross-check exact 0 matches; SEC-PII-01 redaction call count >= baseline; node --check clean; strict-equality state across whole file stays at 0 non-strict.
       - Test-env ACCEPT: ZZ-RouterPasswordResetSpec.js + ZZ-AppSessionUserSpec.js are the canonical behavioral gate on CircleCI inside the Docker test image.
       - Refs: REFACTOR-04 (Phase 7); preserves Phase 5 REFACTOR-02 fix at b0aef15b.
    4. Verify GPG: `git log -1 --pretty=%G?` returns `G`.
    5. `git push origin thinx-staging`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^refactor(REFACTOR-04): convert Owner.password_reset to async/await internals$" && git log -1 --pretty=%G? | grep -qE "^(G|U|E)" && echo "commit:OK"</automated>
  </verify>
  <done>One new GPG-signed commit on `thinx-staging` with exact subject; 2 files in diff; Phase 5 REFACTOR-02 anti-regression note documented in commit body; CircleCI triggered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request (GET /api/v2/password/reset?reset_key=...) → router.user.js:27 → Owner.password_reset | Untrusted reset_key from query string crosses into the auth-critical comparison. Phase 5 REFACTOR-02 fixed the string-vs-number coercion attack surface here. |
| Owner.password_reset → CouchDB owners_by_resetkey view | Resolves reset_key to a user doc. View must be consistent with the strict-equality comparison. |
| Owner.password_reset → audit.js (alog.log) | Audit trail of password-reset attempts. SEC-PII-01: reset_key MUST be redacted before persistence to managed_logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Spoofing | Phase 5 REFACTOR-02 anti-regression — drift from `!==` back to `!=` re-opens the string-vs-number coercion attack surface | mitigate | Task 4 step 2 (exact 1 match for `!==`) AND Task 4 step 3 (exact 0 matches for `!=`). DUAL gate ensures no drift in either direction. Subject of Task 5 commit body explicitly documents the anti-regression. |
| T-07-05-02 | Information Disclosure | SEC-PII-01 redaction call (Util.redactToken on reset_key) removed → raw reset_key persisted to CouchDB managed_logs | mitigate | Task 4 step 4 — count of `Util.redactToken(reset_key)` >= baseline (>= 2). Phase 7 CONTEXT.md explicitly lists SEC-PII-01 redaction preservation as a constraint. |
| T-07-05-03 | Tampering | Signature parameter order swap → router.user.js:27 caller passes args wrong → reset_key compared against owner_id or vice versa | mitigate | Task 4 step 5 — exact regex match on `(owner, reset_key, callback)`. The unit test locks the (success, response) tuple shape. |
| T-07-05-04 | Tampering | user_reset_key null-normalization removed → typeof "undefined" compares against string → strict-equality always fails for users without a reset_key | mitigate | Task 2 step 6 — normalization MUST be preserved verbatim. The grep gate in Task 4 doesn't catch this directly; relies on the canonical CI Jasmine green-gate (ZZ-AppSessionUserSpec.js) for behavioral confirmation. |
| T-07-05-05 | Repudiation | Async wrapper swallows the strict-mismatch callback into an outer catch instead of letting it fire — false "userlib error" surfaced instead of "invalid_reset_key" | mitigate | Task 2 step 9 — explicit "Add an outer try/catch that swallows the strict-mismatch path" is FORBIDDEN. The callback for invalid_reset_key fires inside the try-block success path of userlib.view, not inside the catch. Behavioral integrity preserved by the explicit per-step conversion in Task 2. |
| T-07-05-06 | Repudiation | Double-callback — userlib.view error + later success both fire | mitigate | The `return callback(false, err)` inside the catch ensures the catch path is terminal. The success path's branches each `callback(...)` exactly once. |
| T-07-05-07 | Repudiation | Unsigned commit | mitigate | `git commit -S` mandatory. |
| T-07-05-08 | Information Disclosure | reset_key embedded in success URL (`/password.html?reset_key=...`) — appears UNREDACTED in browser history / logs | accept | This is BY DESIGN — the client needs the real key to complete the reset. Out of scope for SEC-PII-01 (which is about server-side log persistence, not URL construction). v1.x candidate to consider token-exchange instead. |
| T-07-05-SC | Tampering | npm/pip/cargo installs | accept | No installs. |
</threat_model>

<verification>
1. `node --check` clean on both modified files.
2. **PHASE 5 ANTI-REGRESSION (CRITICAL):** `grep -nE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js` = exactly 1 line.
3. **`!=` non-regression cross-check:** `grep -nE "reset_key\s+!=\s+user_reset_key" lib/thinx/owner.js` = 0 lines.
4. **SEC-PII-01 redaction:** `grep -c "Util.redactToken(reset_key)" lib/thinx/owner.js` >= baseline (>= 2).
5. Signature: `grep -E "^\s*async\s+password_reset\s*\(owner, reset_key, callback\)" lib/thinx/owner.js` = 1 line.
6. Strict-equality state: `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` = 0 lines.
7. SEC-PII-01 total: count >= pre-Phase-7 baseline.
8. `grep -c "REFACTOR-04 (07-5)" spec/jasmine/02-OwnerSpec.js` = 1.
9. Cumulative async top-5 count: 4 (create, delete, update, password_reset). set_password remains for 07-6.
10. CircleCI green on push (canonical Jasmine ZZ-* gate; ZZ-RouterPasswordResetSpec.js and ZZ-AppSessionUserSpec.js MUST stay green).
11. One GPG-signed commit with exact subject above.
</verification>

<success_criteria>
- Owner.password_reset converted to async/await with preserved 3-arg signature.
- **Phase 5 REFACTOR-02 anti-regression: `reset_key !== user_reset_key` survives the conversion (NEVER regresses to `!=`).**
- **SEC-PII-01: both Util.redactToken(reset_key) call sites preserved at original positions.**
- Synchronous missing_reset_key guard preserved.
- user_reset_key null-normalization preserved.
- All 5 callback tuple shapes preserved.
- Single caller (router.user.js:27) unchanged.
- Behavior-locking unit test added.
- One atomic GPG-signed commit on `thinx-staging`.
- CircleCI triggered.
</success_criteria>

<output>
SUMMARY.md deferred to phase wrap-up per CONTEXT.md.
</output>
