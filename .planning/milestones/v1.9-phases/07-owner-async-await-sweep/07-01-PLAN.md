---
phase: 07-owner-async-await-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/thinx/owner.js
autonomous: true
requirements:
  - REFACTOR-04
tags:
  - refactor
  - async-await
  - owner
  - strict-equality
  - couchdb
  - nano

must_haves:
  truths:
    - "After this commit lands, every NON-top-5 method in lib/thinx/owner.js has its callback-style internals converted to async/await using nano 10's native promise API (no util.promisify)."
    - "Every converted method KEEPS its public signature — `methodName(args, callback)` stays exactly that; the body is `async methodName(args, callback) { try { ... callback(true, x); } catch (err) { callback(false, err); } }`. NO caller in router.*.js, device.js, messenger.js, or requireAdmin.js is modified."
    - "Strict-equality fold lands in this same commit: lines 277 (`api_keys.length == 0`), 515 (`body.rows.length != 1`), 572 (`action_name == \"password_reset\"` and `process.env.ENVIRONMENT == \"test\"`), 646 (`process.env.ENVIRONMENT == \"test\"`), 790 (`keyObj.alias.indexOf(...) == 0`), 930 (`err_u.statusCode == 409`), 1002 (`mesh.mesh_id == newMesh.mesh_id`) — all 7 confirmed by grep — converted to `===`/`!==`. Note: some of these (790, 930, 1002) are inside top-5 methods (`create_default_mqtt_apikey` line 778 → non-top-5; `create` line 805 → top-5 = Plan 07-2; `createMesh` line 975 → non-top-5). Plan 07-1 covers ONLY the non-top-5 ones (277, 515, 572, 646, 790, 1002); the 930 fix inside `create` lands in Plan 07-2."
    - "After this commit, `grep -nE \"[^=!<>]!=[^=]|[^=!<>]==[^=]\" lib/thinx/owner.js` returns exactly the lines inside `create` (line 930-equivalent, post-conversion) — Plan 07-2 will close that one too."
    - "All existing SEC-PII-01 redaction calls (`alog.log(...)`, `Util.redactToken(...)`, `Util.redactEmail(...)`) stay intact in their original positions inside each converted method body."
    - "`node --check lib/thinx/owner.js` exits 0."
    - "The methods touched by this plan are: `mqtt_key` (272), `profile` (302), `process_update` (340, callback-style only — no userlib calls, kept callback-internal), `apply_update` (390), `validate` (440), `password_reset_init` (502), `activate` (541, already `.then/.catch` — convert to await), `atomic` (564), `set_password_reset` (581), `set_password_activation` (620), `create_default_acl` (705), `create_mqtt_access` (726), `create_default_mqtt_apikey` (778), `createMesh` (975), `deleteMeshes` (1026), `listMeshes` (1091), `trackUserLogin` (1115, no callback), `updateLastSeen` (1127, no callback), `addGitHubAccessToken` (1150). The top-5 (`create`, `delete`, `update`, `password_reset`, `set_password`) are EXCLUDED from this commit (Plans 07-2..07-6)."
    - "Local `npm test` ACCEPT pattern carries forward from Phase 5/6: it aborts on missing `/mnt/data/conf/config.json` locally; CI-side Jasmine inside the Docker test image is the canonical behavioral green-gate. Static gates (`node --check`, grep, file diffs) are authoritative locally."
    - "One atomic GPG-signed commit on `thinx-staging` with subject `refactor(REFACTOR-04): convert owner.js non-top-5 methods to async/await + strict-equality sweep`."
  artifacts:
    - path: "lib/thinx/owner.js"
      provides: "All non-top-5 methods converted to async/await internals; 6 of 7 non-strict comparisons (277, 515, 572, 646, 790, 1002) folded to strict; public signatures preserved across every method."
      contains: "async mqtt_key"
  key_links:
    - from: "lib/thinx/owner.js (converted methods)"
      to: "nano 10 native promise API (this.userlib.*)"
      via: "await this.userlib.view(...), await this.userlib.get(...), await this.userlib.atomic(...), await this.userlib.insert(...)"
      pattern: "await this\\.userlib\\."
    - from: "lib/thinx/owner.js (each converted method's try/catch)"
      to: "existing callback contract"
      via: "try { ... callback(true, x); } catch (err) { callback(false, err); }"
      pattern: "try \\{[\\s\\S]*?callback\\([\\s\\S]*?\\} catch"
    - from: "lib/router.auth.js, lib/router.user.js, lib/router.profile.js, lib/router.admin.js, lib/router.google.js, lib/router.github.js, lib/router.mesh.js, lib/router.gdpr.js, lib/thinx/device.js, lib/thinx/messenger.js, lib/middleware/requireAdmin.js"
      to: "lib/thinx/owner.js (unchanged public signatures)"
      via: "callback-style invocations unchanged"
      pattern: "owner\\.(mqtt_key|profile|update|validate|password_reset_init|activate|create_default_acl|create_mqtt_access|create_default_mqtt_apikey|createMesh|deleteMeshes|listMeshes|updateLastSeen|addGitHubAccessToken)"
---

<objective>
Convert the internals of all NON-top-5 methods in `lib/thinx/owner.js` from callback-style to async/await using nano 10's native promise API. Preserve every public method signature so callers in `lib/router.*.js`, `lib/thinx/device.js`, `lib/thinx/messenger.js`, and `lib/middleware/requireAdmin.js` keep their callback-style invocations unchanged. Fold in 6 of the 7 remaining non-strict `==`/`!=` comparisons (lines 277, 515, 572, 646, 790, 1002) in the same commit — the 7th (line 930 inside `Owner.create`) lands in Plan 07-2.

Purpose: REFACTOR-04 calls for the ~74-callback sweep with zero observable behavior change. Plan 07-1 closes ~15 methods in one bisect-friendly atomic commit while the top-5 (`create`, `delete`, `update`, `password_reset`, `set_password`) get their own isolated commits in Plans 07-2..07-6 for surgical revertability. The strict-equality fold piggybacks because each `==`/`!=` lives inside a method this plan is already touching.

Output: One atomic GPG-signed commit on `thinx-staging` modifying ONLY `lib/thinx/owner.js`. Subject: `refactor(REFACTOR-04): convert owner.js non-top-5 methods to async/await + strict-equality sweep`. No spec changes in this plan — behavior-locking unit tests are scoped to the top-5 plans (07-2..07-6) per CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-owner-async-await-sweep/07-CONTEXT.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@lib/thinx/owner.js
@package.json

<interfaces>
<!-- Public method signatures the executor MUST preserve verbatim. -->
<!-- Source: lib/thinx/owner.js (post-Phase-5/6 state). Verified via grep. -->

Non-top-5 methods (signatures unchanged; only internals become async):
- mqtt_key(owner_id, callback)
- profile(owner, callback)
- process_update(body, callback)   // pure data shaper — NO userlib calls; keep as plain non-async
- apply_update(owner, update_key, update_value, callback)
- validate(username, callback)
- password_reset_init(email, callback)
- activate(ac_owner, ac_key, callback)    // already uses .then/.catch — convert to await
- atomic(doc, changes, action_name, callback)
- set_password_reset(rbody, callback)
- set_password_activation(rbody, callback)
- create_default_acl(owner_id, callback)
- create_mqtt_access(owner_id, DEFAULT_APIKEY_NAME, callback)
- create_default_mqtt_apikey(owner_id, callback)
- createMesh(owner, mesh_id, mesh_alias, callback)
- deleteMeshes(owner, mesh_ids, callback)
- listMeshes(owner, callback)
- trackUserLogin(owner_id)               // NO callback — `this.userlib.atomic` is fire-and-forget
- updateLastSeen(doc, repeated = false)  // NO callback — fire-and-forget
- addGitHubAccessToken(owner, token, callback)

Top-5 EXCLUDED from this plan (covered by 07-2..07-6):
- create(body, send_activation, res, callback)   → Plan 07-2
- delete(owner, callback, res)                    → Plan 07-3
- update(owner, body, callback)                   → Plan 07-4 (note: top-5 owner-side; calls process_update + apply_update which ARE in this plan)
- password_reset(owner, reset_key, callback)      → Plan 07-5
- set_password(rbody, callback)                   → Plan 07-6 (orchestrator only — delegates to set_password_reset / set_password_activation which ARE in this plan)

Canonical mechanical transform (per CONTEXT.md, lines 71-89):
Before:
  this.userlib.view("users", "owners_by_resetkey", { key: reset_key, include_docs: true }, (err, body) => {
    if (err) { return callback(false, err); }
    // ...
  });
After:
  try {
    const body = await this.userlib.view("users", "owners_by_resetkey", { key: reset_key, include_docs: true });
    // ...
    callback(true, x);
  } catch (err) {
    callback(false, err);
  }

Nano 10 native promise API: `package.json` pins `"nano": "^10.1.4"`. All `this.userlib.<method>(args)` calls (no callback arg) return a Promise. No util.promisify needed. Verified.

Strict-equality targets in scope for this plan (6 of 7):
- Line 277  (mqtt_key):                       `api_keys.length == 0`                 →  `=== 0`
- Line 515  (password_reset_init):            `body.rows.length != 1`                →  `!== 1`
- Line 572  (atomic):                         `action_name == "password_reset"`      →  `===`
- Line 572  (atomic):                         `process.env.ENVIRONMENT == "test"`    →  `===`
- Line 646  (set_password_activation):        `process.env.ENVIRONMENT == "test"`    →  `===`
- Line 790  (create_default_mqtt_apikey):     `keyObj.alias.indexOf(...) == 0`       →  `=== 0`
- Line 1002 (createMesh):                     `mesh.mesh_id == newMesh.mesh_id`      →  `===`

EXCLUDED from this plan (lands in Plan 07-2):
- Line 930 (create): `err_u.statusCode == 409` → Plan 07-2.

SEC-PII-01 redaction calls that MUST stay intact and in their original positions inside the converted method bodies (do NOT remove or relocate; these were the v1.0 Phase 2 deliverable):
- mqtt_key: none
- profile: none
- apply_update: `alog.log(owner, "Profile update error " + error, "error")`, `alog.log(owner, "Profile update failed.", "error")`, `alog.log(owner, "Profile updated successfully.", abody)`
- password_reset_init: `Util.redactEmail(email)` at the `email_not_found` log
- activate: `Util.redactToken(ac_key)` at the activation log
- atomic: none
- set_password_reset: `Util.redactToken(rbody.reset_key)` at the alog.log
- set_password_activation: none
- create_default_acl: none
- create_mqtt_access: none
- create_default_mqtt_apikey: none
- createMesh: `alog.log(body.owner, "Mesh creation failed.", "error")`, `alog.log(body.owner, "Owner Mesh Created.", "info")`
- deleteMeshes: `alog.log(body.owner, "Mesh deletion failed.", "error")`, `alog.log(body.owner, "Owner Mesh(es) deleted.", "info")`, `alog.log(body.owner, "Owner Mesh(es) delete not successful.", "warning")`
- updateLastSeen: none (but console.log lines stay)
- addGitHubAccessToken: `alog.log(owner, "GitHub Access Token creation failed.", "error")`, `alog.log(owner, "GitHub Token Added.", "info")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification and conversion snapshot</name>
  <files>
    - lib/thinx/owner.js (READ ONLY in this task)
  </files>
  <action>
    Before any edits, capture the pre-conversion state for diff/audit. Run from the repo root:
    1. `wc -l lib/thinx/owner.js` — record line count (expected 1161 pre-conversion).
    2. `grep -c "callback(" lib/thinx/owner.js` — record callback occurrence count (expected ~74).
    3. `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` — record the 7 non-strict comparison lines (277, 515, 572, 646, 790, 930, 1002).
    4. `grep -n "alog.log\|Util.redactToken\|Util.redactEmail" lib/thinx/owner.js` — record the SEC-PII-01 redaction sites (these MUST survive the conversion verbatim).
    5. `grep -n "^\s*\w\+\s*(.*callback" lib/thinx/owner.js | head -40` — record the public method signature lines for the 19 non-top-5 methods (see <interfaces> above for the canonical list).
    6. `node --check lib/thinx/owner.js` MUST exit 0 — record exit code (baseline before conversion).

    No code is written in this task. The grep/wc output is the executor's reference baseline for the diff Task 2 produces.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && echo "baseline:OK"</automated>
  </verify>
  <done>
    All 6 baseline numbers captured. `node --check` exits 0. Executor knows exactly which 19 methods are in scope and which 5 are EXCLUDED (top-5 → other plans).
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert non-top-5 methods to async/await internals + fold 6 strict-equality fixes</name>
  <files>
    - lib/thinx/owner.js
  </files>
  <action>
    Apply the canonical mechanical transform (see <interfaces> "Canonical mechanical transform") to each of the 19 non-top-5 methods listed in <interfaces>. Method-by-method requirements:

    **General rules (apply to every conversion):**
    - The OUTER method signature stays exactly `methodName(args, callback)`. Prefix the function with `async ` — so the body becomes `async methodName(args, callback) { try { ... } catch (err) { ... } }`. NEVER change the parameter list. NEVER drop the `callback` parameter.
    - Each callback-style `this.userlib.X(args, (err, body) => { ... })` becomes `const body = await this.userlib.X(args);` wrapped in a `try { ... } catch (err) { ... }`. The catch block invokes the same `callback(false, err)` (or method-specific error string) the inner callback's err-branch invoked.
    - Each callback-style nested helper call (e.g. `this.create_default_mqtt_apikey(owner_id, (success) => { ... })`) MAY become `const x = await new Promise(resolve => this.create_default_mqtt_apikey(owner_id, (...args) => resolve(args)))` OR — preferred — kept as a callback if the helper is still callback-style at the moment of the call. The cleaner pattern is: methods that this plan ALREADY converts to async/await can be awaited via a small Promise wrapper inside callers that need their result; methods this plan does NOT touch (e.g. `this.apikey.list`, `this.auth.add_mqtt_credentials`, `acl.load`, `acl.commit`) STAY callback-style at their call sites. DO NOT touch `this.apikey.*`, `this.auth.*`, `this.acl.*`, `this.deploy.*`, `mg.messages.create` — those are out-of-scope helpers owned by other modules.
    - All existing SEC-PII-01 redaction calls listed in <interfaces> MUST remain at their original positions inside each method body. Do NOT remove or move `alog.log(...)`, `Util.redactToken(...)`, `Util.redactEmail(...)` calls.
    - All `console.log` lines stay verbatim.
    - The `process_update` method has NO `this.userlib.*` calls — it is a pure data shaper. Leave it as a plain (non-async) function; do not convert. Marked here only because CONTEXT.md listed it.
    - `trackUserLogin` and `updateLastSeen` have no callback parameter; their `this.userlib.atomic(..., (error, ...) => ...)` calls SHOULD become `try { await this.userlib.atomic(...) } catch (error) { console.log(...) }`. The `updateLastSeen` retry-on-conflict path (line 1134-1137) MUST be preserved.

    **Strict-equality fold (6 of 7; line 930 deferred to Plan 07-2):**
    Apply EXACTLY these 6 changes inside the same commit, in the methods being converted:
    - Line 277 (inside `mqtt_key`):                `if (api_keys.length == 0)`                 →  `if (api_keys.length === 0)`
    - Line 515 (inside `password_reset_init`):     `if (body.rows.length != 1)`                →  `if (body.rows.length !== 1)`
    - Line 572 (inside `atomic`):                  `(action_name == "password_reset")`         →  `(action_name === "password_reset")`
    - Line 572 (inside `atomic`):                  `(process.env.ENVIRONMENT == "test")`       →  `(process.env.ENVIRONMENT === "test")`
    - Line 646 (inside `set_password_activation`): `(process.env.ENVIRONMENT == "test")`       →  `(process.env.ENVIRONMENT === "test")`
    - Line 790 (inside `create_default_mqtt_apikey`): `keyObj.alias.indexOf(DEFAULT_APIKEY_NAME) == 0` → `=== 0`
    - Line 1002 (inside `createMesh`):             `mesh.mesh_id == newMesh.mesh_id`           →  `===`

    Line 930 (`err_u.statusCode == 409` inside `Owner.create`) is EXCLUDED from this plan. It is the `Owner.create` body and lands in Plan 07-2 alongside that method's async/await conversion.

    **Method-by-method conversion notes** (one per method):
    - **mqtt_key (272-300):** outer becomes `async mqtt_key`. Inner `this.apikey.list(owner_id, (api_keys) => { ... })` STAYS callback-style (`this.apikey` is out-of-scope). Inside the callback, the nested `this.create_default_mqtt_apikey(owner_id, (zuccess, key) => ...)` call: since Plan 07-1 ALSO converts `create_default_mqtt_apikey`, you may keep the callback-style call at this call site (downstream callbacks still work — the converted method still INVOKES the callback). NO functional change beyond adding `async` to the outer method and folding `=== 0` at line 277.
    - **profile (302-337):** wrap `this.userlib.get(owner, ...)` in try/await. Preserve the legacy console flatten comment (lines 318-323 in source) verbatim — these comments document Issue E from v1.0 Phase 1.
    - **process_update (340-387):** NO `this.userlib.*` calls. Leave unchanged. Method stays non-async.
    - **apply_update (390-408):** convert nested `this.userlib.get` → `this.userlib.atomic` chain to two sequential `await` calls in a single try/catch. Preserve all 3 `alog.log` calls in their original positions.
    - **validate (440-457):** convert `this.userlib.view` to await. Preserve the `xerr` handling — note the existing code does `if (xerr !== null)` after the original callback fires, which is callback-style noise; in the await form this becomes a try/catch where the catch invokes `callback(false)` and the try block validates `db_body` (same `db_body.rows.count > 1` → callback(false) edge case).
    - **password_reset_init (502-539):** convert `this.userlib.view` to await. The original `if (err !== null)` branch becomes the catch. Inside the try, fold line 515 to `!== 1`. Preserve `Util.redactEmail(email)` log. Final call `this.resetUserWithKey(user, email, callback)` — `resetUserWithKey` is NOT in <interfaces> (line 182) and is OUT-OF-SCOPE for Plan 07-1; the call stays callback-passthrough.
    - **activate (541-562):** already uses `.then/.catch` — convert to `try { await this.userlib.view(...); ... } catch (err) { ... }`. Preserve `Util.redactToken(ac_key)` log.
    - **atomic (564-579):** convert `this.userlib.atomic` to await. Fold both `==` at line 572 to `===`. The success branch's `(action_name === "password_reset") && (process.env.ENVIRONMENT === "test")` test-env special-case MUST be preserved verbatim — this is the test-mode hook the existing `02-OwnerSpec.js` exercises.
    - **set_password_reset (581-618):** convert `this.userlib.view` to await. The existing `this.atomic(userdoc._id, changes, "password_reset", callback)` call at line 615 STAYS as a callback-style call (passing the original `callback` straight through to the awaited helper — works fine because `this.atomic` is also converted by this plan and still invokes its callback). Preserve `Util.redactToken(rbody.reset_key)` log.
    - **set_password_activation (620-654):** convert `this.userlib.view` to await. Fold line 646 to `===`. The inner `this.atomic(userdoc._id, changes, "activation", (success, response) => ...)` STAYS callback-style at this call site (same reason as set_password_reset).
    - **create_default_acl (705-724):** outer becomes `async`. The `new ACL(this.redis, owner_id)` and `acl.load(() => ...)` + `acl.addTopic(...)` + `acl.commit(() => ...)` calls STAY callback-style (ACL is out-of-scope). Only the outer wrapper changes to enable `await` at call sites.
    - **create_mqtt_access (726-776):** This method has complex compensating logic (5s timeout via `setTimeout`, `settled` flag, revoke-on-throw). DO NOT simplify. The outer becomes `async`. The inner `this.apikey.create(owner_id, DEFAULT_APIKEY_NAME, (success, object) => ...)` call STAYS callback-style. The `this.auth.add_mqtt_credentials(...)` and `this.create_default_acl(...)` calls also STAY callback-style (the latter is converted by this plan but still has a callback signature). The compensation guard (lines 747-774) — `settled` flag, `setTimeout` timer, `try/catch` around `add_mqtt_credentials` — MUST be preserved EXACTLY. This is the 2026-05-31 OOM-incident fix (see quick task 260531-n72 in STATE.md).
    - **create_default_mqtt_apikey (778-798):** convert `this.apikey.list` — WAIT, this is `this.apikey.list` (out-of-scope), so it STAYS callback-style. The outer method becomes `async`. Fold line 790 `=== 0`. The `this.create_mqtt_access(owner_id, DEFAULT_APIKEY_NAME, callback)` call stays callback-passthrough.
    - **createMesh (975-1024):** convert outer `this.userlib.get` AND inner `this.userlib.atomic` to two sequential awaits in one try/catch. Fold line 1002 to `===`. Preserve both `alog.log` calls.
    - **deleteMeshes (1026-1088):** convert outer `this.userlib.get` to await. The `acl.load(() => { ... })` block STAYS callback-style (ACL out-of-scope). Inside the acl.load callback, convert `this.userlib.atomic` to await. Preserve all 3 `alog.log` calls.
    - **listMeshes (1091-1109):** convert `this.userlib.get` to await.
    - **trackUserLogin (1115-1125):** no callback parameter. Convert `this.userlib.atomic` to `try { await ... } catch (error) { console.log(...) }`. Keep the final `alog.log(owner_id, "OAuth2 User logged in...")` call.
    - **updateLastSeen (1127-1144):** no callback parameter. Convert `this.userlib.atomic` to await, preserving the retry-on-conflict logic (the `error.toString().indexOf("conflict")` branch at line 1134). The recursive `this.updateLastSeen(doc, true)` call STAYS — that's the retry primitive.
    - **addGitHubAccessToken (1150-1160):** convert `this.userlib.atomic` to await. Preserve both `alog.log` calls.

    **What this task MUST NOT touch:**
    - `Owner.create` (805-969)        — Plan 07-2
    - `Owner.delete` (681-703)        — Plan 07-3
    - `Owner.update` (410-432)        — Plan 07-4
    - `Owner.password_reset` (459-500) — Plan 07-5
    - `Owner.set_password` (656-679)   — Plan 07-6
    - `sendMail` (88), `sendGDPRExpirationEmail24` (101), `sendGDPRExpirationEmail168` (120), `sendResetEmail` (139), `resetUserWithKey` (182), `sendActivationEmail` (195), `saveAvatar` (264), `avatar` / `avatar_path` helpers — all OUT OF Phase 7 scope (mailgun helpers; not in REFACTOR-04's call-graph spot-check list).
    - Any `this.apikey.*`, `this.auth.*`, `this.deploy.*`, `mg.messages.create`, `new ACL(...).load/.commit/.addTopic/.prune` calls.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && grep -c "^\s*async\s\+\(mqtt_key\|profile\|apply_update\|validate\|password_reset_init\|activate\|atomic\|set_password_reset\|set_password_activation\|create_default_acl\|create_mqtt_access\|create_default_mqtt_apikey\|createMesh\|deleteMeshes\|listMeshes\|addGitHubAccessToken\)" lib/thinx/owner.js | grep -q "^1[5-9]\|^2[0-9]" || (echo "expected 15-19 async method declarations"; exit 1)</automated>
  </verify>
  <done>
    `node --check lib/thinx/owner.js` exits 0. At least 15 non-top-5 methods now have `async ` prefix. All 5 top-5 method bodies (create, delete, update, password_reset, set_password) are UNTOUCHED (verify by `git diff --stat` showing only the non-top-5 method line ranges). All 6 strict-equality fixes (277, 515, 572[x2], 646, 790, 1002) are applied. Line 930 inside `Owner.create` is still `==` (Plan 07-2 will close it). All SEC-PII-01 redaction calls (alog.log/redactToken/redactEmail) are still present at their original positions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Static validation — node check, strict-equality grep, callback-survival grep</name>
  <files>
    - lib/thinx/owner.js (READ ONLY)
  </files>
  <action>
    Run the static validation gates that lock in the must-haves locally (CI is the canonical Jasmine green-gate per the test-env ACCEPT pattern). Execute from repo root:

    1. **Syntax gate:** `node --check lib/thinx/owner.js` MUST exit 0. If it errors, the conversion has a syntax bug — fix and re-verify.

    2. **Strict-equality count gate:**
       `grep -v '^\s*//' lib/thinx/owner.js | grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" | grep -v statusCode`
       This MUST return ZERO lines after Plan 07-1. The `grep -v statusCode` filter excludes line 930 inside `Owner.create` (deferred to Plan 07-2). The `grep -v '^\s*//'` filter avoids counting comments.
       Alternative explicit form (preferred for the commit verify trail):
       `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js`
       MUST return exactly 1 line — and that line MUST be the line containing `err_u.statusCode == 409` (post-conversion line number drifts; identify by content, not number). If MORE than 1 line is returned, the fold missed a target.

    3. **Public-signature preservation gate:** Confirm every non-top-5 method still has its callback parameter:
       `grep -nE "^\s*async\s+(mqtt_key|profile|apply_update|validate|password_reset_init|activate|atomic|set_password_reset|set_password_activation|create_default_acl|create_mqtt_access|create_default_mqtt_apikey|createMesh|deleteMeshes|listMeshes|addGitHubAccessToken)\s*\(" lib/thinx/owner.js`
       Each returned line MUST end in `, callback)` or `, callback) {` (or `, DEFAULT_APIKEY_NAME, callback)` for create_mqtt_access). If any line is missing `callback`, the conversion broke the public signature — STOP and fix.

    4. **Top-5 untouched gate:** `git diff -U0 lib/thinx/owner.js | grep -E "^[-+]\s*(async\s+)?(create|delete|update|password_reset|set_password)\s*\("` MUST return zero lines. The top-5 method signatures must not appear in the diff. If they do, the conversion accidentally touched a top-5 method — STOP and revert that hunk.

    5. **SEC-PII-01 survival gate:**
       `grep -c "alog.log\|Util.redactToken\|Util.redactEmail" lib/thinx/owner.js`
       MUST return a count >= the pre-conversion count from Task 1 step 4. If lower, a redaction call was deleted — STOP and restore.

    6. **Lint gate (optional, may fail with config-load issue):**
       `npm run lint 2>&1 | tee /tmp/lint.log; tail -5 /tmp/lint.log`
       If lint errors are limited to the touched lines, they MUST be addressed. If lint cannot run locally (config dependency), record as ACCEPT and rely on CircleCI.

    7. **Callback-style residue check (informational):**
       `grep -nE "this\.userlib\.\w+\([^)]*=>" lib/thinx/owner.js`
       Lines returned MUST be ONLY inside the top-5 methods (`create`, `delete`, `update`, `password_reset`, `set_password`) — those are not yet converted. If a converted method still has `this.userlib.X(args, (err, ...) =>` syntax, the conversion is incomplete for that method — STOP and finish it.

    Document each gate's pass/fail in the commit message body or in the eventual SUMMARY.md.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && test "$(grep -nE '[^=!<>]!=[^=]|[^=!<>]==[^=]' lib/thinx/owner.js | wc -l | tr -d ' ')" = "1" && grep -nE '[^=!<>]!=[^=]|[^=!<>]==[^=]' lib/thinx/owner.js | grep -q statusCode && echo "static-gates:OK"</automated>
  </verify>
  <done>
    All 5 hard gates pass: (1) node --check exits 0, (2) exactly 1 non-strict comparison remains and it's the line 930 `statusCode == 409` inside `Owner.create` (deferred to 07-2), (3) every non-top-5 async method ends with `, callback)` in its signature, (4) `git diff` shows zero touches to the 5 top-5 method signatures, (5) SEC-PII-01 redaction call count is >= baseline. Gate 6 (lint) is best-effort. Gate 7 (callback residue) returns only top-5 method lines.
  </done>
</task>

<task type="auto">
  <name>Task 4: Atomic GPG-signed commit on thinx-staging</name>
  <files>
    - lib/thinx/owner.js
  </files>
  <action>
    Create ONE atomic GPG-signed commit on `thinx-staging` (the project's working branch per AGENTS.md). Sequence:

    1. Confirm working tree state:
       `git status --short` — only `lib/thinx/owner.js` should be modified. If other files are dirty, STOP and ask.

    2. Stage:
       `git add lib/thinx/owner.js`

    3. Commit (HEREDOC form, GPG-signed by default per PROJECT.md "GPG-sign commits is the project default"):
       ```
       git commit -S -m "$(cat <<'EOF'
       refactor(REFACTOR-04): convert owner.js non-top-5 methods to async/await + strict-equality sweep

       Convert internals of 15+ non-top-5 methods in lib/thinx/owner.js from
       callback-style to async/await using nano 10's native promise API.
       Preserve every public method signature — callers in router.*.js,
       device.js, messenger.js, requireAdmin.js are unmodified.

       Methods converted (all signatures preserved):
         mqtt_key, profile, apply_update, validate, password_reset_init,
         activate, atomic, set_password_reset, set_password_activation,
         create_default_acl, create_mqtt_access, create_default_mqtt_apikey,
         createMesh, deleteMeshes, listMeshes, trackUserLogin, updateLastSeen,
         addGitHubAccessToken.

       Top-5 methods (create, delete, update, password_reset, set_password)
       are NOT touched by this commit — they land in Plans 07-2..07-6 for
       bisect-friendly isolation.

       Strict-equality fold (6 of 7 non-strict comparisons in owner.js):
         line 277  (mqtt_key)                    api_keys.length == 0    -> === 0
         line 515  (password_reset_init)         body.rows.length != 1   -> !== 1
         line 572  (atomic)                      action_name == "..."    -> === (x2 on same line)
         line 646  (set_password_activation)     ENVIRONMENT == "test"   -> ===
         line 790  (create_default_mqtt_apikey)  keyObj.alias.indexOf == -> === 0
         line 1002 (createMesh)                  mesh.mesh_id ==         -> ===
       Line 930 (statusCode == 409 inside Owner.create) is deferred to
       Plan 07-2 and folds into that method's conversion commit.

       SEC-PII-01 redaction calls (alog.log, Util.redactToken,
       Util.redactEmail) preserved verbatim at original positions per the
       v1.0 Phase 2 contract.

       Validation:
         - node --check lib/thinx/owner.js  exits 0
         - grep -nE '[^=!<>]!=[^=]|[^=!<>]==[^=]' lib/thinx/owner.js
           returns exactly 1 line (the deferred line 930 inside Owner.create)
         - public signatures verified — every async method ends with ", callback)"
         - top-5 method signatures unchanged in git diff
         - SEC-PII-01 call count >= baseline

       Test-env ACCEPT: local `npm test` aborts on missing
       /mnt/data/conf/config.json; CI-side Jasmine inside the Docker test
       image is the canonical behavioral green-gate. Pattern matches
       Phases 5 and 6.

       Refs: REFACTOR-04 (Phase 7); closes deferred owner.js strict-equality
       sweep from Phase 5 REFACTOR-02 (06b of 7; final equal fix lands in 07-2).
       EOF
       )"
       ```

    4. Verify the commit:
       `git log -1 --pretty=fuller --show-signature lib/thinx/owner.js`
       MUST show `gpg: Good signature` (or equivalent) AND the subject line above.

    5. Push to remote:
       `git push origin thinx-staging`
       This triggers CircleCI; the canonical Jasmine `ZZ-*` green-gate runs inside the Docker test image (per CI config, NOT locally).

    Notes:
    - GPG-sign is mandatory unless the user explicitly granted a per-session unsigned authorization (none active for this session per STATE.md / PROJECT.md).
    - Do NOT amend or force-push. If the commit needs a fix, create a follow-up commit.
    - Do NOT merge to master/main from this plan — that's the milestone close-out, not a per-phase action.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^refactor(REFACTOR-04): convert owner.js non-top-5 methods to async/await + strict-equality sweep$" && git log -1 --pretty=%G? | grep -qE "^(G|U|E)" && echo "commit:OK"</automated>
  </verify>
  <done>
    One new atomic commit exists on `thinx-staging` with the exact subject line above. The commit is GPG-signed (verified by `git log --show-signature` or `git log --pretty=%G?` returning `G` for good signature). `lib/thinx/owner.js` is the only file in the diff. CircleCI was triggered by the push; the canonical Jasmine green-gate runs inside the Docker test image.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nano (CouchDB driver) → owner.js | Driver returns Promise (nano 10 native API); owner.js consumes via await. Boundary moves error surface from callback-2nd-arg to thrown exception. |
| owner.js → router.*.js / device.js / messenger.js / requireAdmin.js | Callback contract preserved at the boundary. Boundary is INTERNAL to the codebase; no external trust change. |
| owner.js → audit.js (alog.log) | Sensitive PII flows through alog.log. Boundary unchanged by this plan; SEC-PII-01 redaction calls preserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | owner.js public signature drift | mitigate | Static gate Task 3 step 3 — `grep -nE "^\s*async\s+(method)\s*\(" lib/thinx/owner.js` MUST show every signature still ending in `, callback)`. Top-5 method signature untouched-gate at Task 3 step 4. |
| T-07-02 | Information Disclosure | SEC-PII-01 redaction call removed during conversion | mitigate | Task 3 step 5 — count of `alog.log|Util.redactToken|Util.redactEmail` post-conversion MUST be >= baseline from Task 1 step 4. CONTEXT.md "SEC-PII-01 redaction preservation" constraint. |
| T-07-03 | Denial of Service | Promise-rejection unhandled inside async wrapper crashes the worker | mitigate | Each converted method's body is wrapped in `try { await ... callback(true, x); } catch (err) { callback(false, err); }`. Catch clause is mandatory; no `await` outside try/catch. |
| T-07-04 | Tampering | Subtle behavior drift in atomic() test-mode hook | mitigate | The `(action_name === "password_reset") && (process.env.ENVIRONMENT === "test")` branch at line 572 (post-conversion) MUST be preserved bit-for-bit. The existing `02-OwnerSpec.js` reset-flow test depends on this hook. Listed in Task 2 method-by-method notes. |
| T-07-05 | Tampering | 2026-05-31 OOM-incident fix in create_mqtt_access overwritten | mitigate | The compensation guard (settled flag + 5s setTimeout + revoke-on-throw) at lines 747-774 MUST be preserved EXACTLY (per Task 2 create_mqtt_access notes). This is the live production fix from quick task 260531-n72. |
| T-07-06 | Repudiation | Unsigned commit | mitigate | `git commit -S` is mandatory; verify via `git log --pretty=%G?` returning `G`. PROJECT.md default. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Pure code refactor of an existing module. No package legitimacy gate required. |
</threat_model>

<verification>
Phase-level criteria for Plan 07-1 (post-commit):

1. `node --check lib/thinx/owner.js` exits 0.
2. `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` returns exactly 1 line (the deferred `err_u.statusCode == 409` inside `Owner.create`; Plan 07-2 closes it).
3. `grep -c "async \(mqtt_key\|profile\|apply_update\|validate\|password_reset_init\|activate\|atomic\|set_password_reset\|set_password_activation\|create_default_acl\|create_mqtt_access\|create_default_mqtt_apikey\|createMesh\|deleteMeshes\|listMeshes\|addGitHubAccessToken\)" lib/thinx/owner.js` returns at least 15.
4. `grep -E "^\s*(async\s+)?(create|delete|update|password_reset|set_password)\s*\(" lib/thinx/owner.js` returns 5 lines, NONE of which contain `async ` (top-5 methods are NOT yet converted).
5. SEC-PII-01 redaction call count `grep -c "alog.log\|Util.redactToken\|Util.redactEmail" lib/thinx/owner.js` is >= the Task-1 baseline.
6. CircleCI green on the merge to `thinx-staging` (canonical Jasmine `ZZ-*` green-gate inside the Docker test image). Local `npm test` ACCEPT per Phase 5/6.
7. One GPG-signed commit on `thinx-staging` with subject `refactor(REFACTOR-04): convert owner.js non-top-5 methods to async/await + strict-equality sweep`.
</verification>

<success_criteria>
- 19 non-top-5 method internals converted to async/await; 17 method signatures gain `async ` prefix (process_update / trackUserLogin counted but stay non-async; trackUserLogin has no callback so just wraps userlib.atomic in try/catch).
- 6 of 7 non-strict comparisons folded to strict; the 7th (line 930 `statusCode == 409`) is deferred to Plan 07-2.
- Zero changes to the 5 top-5 method bodies (create, delete, update, password_reset, set_password).
- Zero changes to SEC-PII-01 redaction calls.
- Zero changes to any file other than `lib/thinx/owner.js`.
- `node --check` clean.
- One atomic GPG-signed commit on `thinx-staging` with the exact subject above.
- CircleCI run triggered.
</success_criteria>

<output>
Create `.planning/phases/07-owner-async-await-sweep/07-01-SUMMARY.md` per the phase-close pattern (deferred to phase wrap-up per CONTEXT.md "atomic-commit pattern + SUMMARY per plan deferred to phase wrap-up"). Optionally produce a minimal SUMMARY now if the execute-plan workflow requires it; otherwise the phase-close orchestrator will produce all 6 SUMMARYs together.
</output>
