---
phase: 07-owner-async-await-sweep
plan: 06
type: execute
wave: 1
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
  - 07-05
files_modified:
  - lib/thinx/owner.js
  - spec/jasmine/02-OwnerSpec.js
autonomous: true
requirements:
  - REFACTOR-04
tags:
  - refactor
  - async-await
  - owner
  - set-password
  - top-5
  - auth-critical
  - phase-close

must_haves:
  truths:
    - "`Owner.set_password(rbody, callback)` keeps its 2-argument public signature EXACTLY. This is the method named `password_set` in the ROADMAP.md success criterion 3 — CONTEXT.md confirmed (line 64) the code identifier is `set_password` while the roadmap uses `password_set`. Both refer to the method at line 656 of pre-Phase-7 owner.js."
    - "All 5 plans (07-01 through 07-05) commits MUST have landed first."
    - "Owner.set_password is an ORCHESTRATOR — it has no `this.userlib.*` calls of its own. It validates rbody and delegates to either `set_password_reset` or `set_password_activation` (both converted by Plan 07-01) based on which key (`reset_key` or `activation`) is present."
    - "The synchronous `password_mismatch` guard (lines 658-664) is preserved verbatim."
    - "The delegation logic preserved: `if (Util.isDefined(rbody.reset_key))` → `this.set_password_reset(rbody, callback)`; `if (Util.isDefined(rbody.activation))` → `this.set_password_activation(rbody, callback)`; fallthrough → `callback(false, \"invalid_change_flow-\" + JSON.stringify(rbody))`."
    - "Both SEC-PII-01 `Util.redactToken` console.log lines (line 668 for reset_key, line 674 for activation) are preserved at original positions."
    - "A behavior-locking unit test is added: `it('REFACTOR-04 (07-6): Owner.set_password callback contract preserved for password_mismatch', ...)` asserting `(false, 'password_mismatch')` when rbody.password !== rbody.rpassword. Safe to run without DB dependency — synchronous guard."
    - "`node --check` clean. Diff scope: Owner.set_password method body only + spec addition."
    - "**This is the PHASE 7 CLOSE-OUT plan.** After Plan 07-6 lands, ALL top-5 methods are async, ALL non-strict comparisons are folded, and the phase's REFACTOR-04 requirement is met. The phase-close orchestrator (run separately, NOT in this plan) will produce 6 SUMMARY.md files and update STATE.md/ROADMAP.md to mark Phase 7 complete."
    - "One atomic GPG-signed commit on `thinx-staging` with subject `refactor(REFACTOR-04): convert Owner.set_password to async/await internals`."
  artifacts:
    - path: "lib/thinx/owner.js"
      provides: "Owner.set_password with async/await internals; public signature preserved; orchestrator delegation intact."
      contains: "async set_password(rbody, callback)"
    - path: "spec/jasmine/02-OwnerSpec.js"
      provides: "Behavior-locking unit test for Owner.set_password callback contract (REFACTOR-04 07-6)."
      contains: "REFACTOR-04 (07-6)"
  key_links:
    - from: "Owner.set_password orchestrator"
      to: "Owner.set_password_reset (async, callback-style at call site)"
      via: "this.set_password_reset(rbody, callback) when rbody.reset_key defined"
      pattern: "this\\.set_password_reset\\("
    - from: "Owner.set_password orchestrator"
      to: "Owner.set_password_activation (async, callback-style at call site)"
      via: "this.set_password_activation(rbody, callback) when rbody.activation defined"
      pattern: "this\\.set_password_activation\\("
    - from: "spec/jasmine/02-OwnerSpec.js (new it block)"
      to: "lib/thinx/owner.js Owner.set_password"
      via: "instance call with rbody.password !== rbody.rpassword — asserts (false, 'password_mismatch')"
      pattern: "REFACTOR-04 \\(07-6\\)"
---

<objective>
Convert `Owner.set_password(rbody, callback)` (lines 656-679 pre-conversion) from callback-style to async/await. This is an ORCHESTRATOR method — no userlib calls of its own; it validates rbody and delegates to `set_password_reset` (when rbody.reset_key is defined) OR `set_password_activation` (when rbody.activation is defined). Both delegated methods were converted by Plan 07-01 but kept their callback-style public signatures. The conversion adds the `async` keyword for file-wide consistency and preserves all 3 callback shapes + 2 SEC-PII-01 redaction log lines.

After Plan 07-6 lands, REFACTOR-04 is met: all top-5 methods converted, zero non-strict comparisons remain, every public method signature preserved, all SEC-PII-01 redaction calls intact. The phase-close orchestrator (separate workflow) will produce SUMMARYs and update STATE.md/ROADMAP.md.

Purpose: Owner.set_password is the entry point for the password-set route (the second half of the reset/activation flow). Auth-critical: a mis-delegation could silently route a reset attempt to the activation handler or vice versa, breaking flows.

Output: Two files modified, one atomic GPG-signed commit on `thinx-staging` with subject `refactor(REFACTOR-04): convert Owner.set_password to async/await internals`. This is the Phase 7 close-out commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-owner-async-await-sweep/07-CONTEXT.md
@.planning/phases/07-owner-async-await-sweep/07-01-PLAN.md
@lib/thinx/owner.js
@spec/jasmine/02-OwnerSpec.js
@lib/router.user.js

<interfaces>
Method signature (PRESERVE):
  set_password(rbody, callback)

Note: ROADMAP.md success criterion 3 refers to `password_set` — that is the same method as `set_password` in code (CONTEXT.md line 64 confirms; the researcher / planner pass resolved this open question with default = code identifier `set_password`).

Caller (single, per CONTEXT.md): not explicitly listed in the line-table — the method is called from router.user.js as part of the POST /api/v2/password/set flow. Search confirms: `grep -n "set_password" lib/router.user.js` should locate the caller. The caller is NOT modified by this plan.

Pre-conversion body (lines 656-679):
  set_password(rbody, callback) {
    if (
      (!Util.isDefined(rbody.password)) ||
      (!Util.isDefined(rbody.rpassword)) ||
      (rbody.password !== rbody.rpassword)
    ) {
      return callback(false, "password_mismatch");
    }
    // Password Reset
    if (Util.isDefined(rbody.reset_key)) {
      console.log("ℹ️ [info] Resetting password " + Util.redactToken(rbody.reset_key) + " using set_password_reset...");
      return this.set_password_reset(rbody, callback);
    }
    // User Activation
    if (Util.isDefined(rbody.activation)) {
      console.log("ℹ️ [info] Searching " + Util.redactToken(rbody.activation) + " using set_password_activation...");
      return this.set_password_activation(rbody, callback);
    }
    return callback(false, "invalid_change_flow-" + JSON.stringify(rbody));
  }

Async/await target shape (minimal — just add async):
  async set_password(rbody, callback) {
    if (
      (!Util.isDefined(rbody.password)) ||
      (!Util.isDefined(rbody.rpassword)) ||
      (rbody.password !== rbody.rpassword)
    ) {
      return callback(false, "password_mismatch");
    }
    // Password Reset
    if (Util.isDefined(rbody.reset_key)) {
      console.log("ℹ️ [info] Resetting password " + Util.redactToken(rbody.reset_key) + " using set_password_reset...");
      return this.set_password_reset(rbody, callback);
    }
    // User Activation
    if (Util.isDefined(rbody.activation)) {
      console.log("ℹ️ [info] Searching " + Util.redactToken(rbody.activation) + " using set_password_activation...");
      return this.set_password_activation(rbody, callback);
    }
    return callback(false, "invalid_change_flow-" + JSON.stringify(rbody));
  }

MUST-PRESERVE:
  1. The 3-condition mismatch guard (password undefined, rpassword undefined, OR password !== rpassword) — verbatim. The `!==` here is NOT the Phase 5 REFACTOR-02 fix (that was a different `!=` at line 492); this `!==` was already strict pre-Phase-7.
  2. Both SEC-PII-01 `Util.redactToken` console.log lines (rbody.reset_key + rbody.activation).
  3. The delegation order: reset_key first, activation second. Reversing the order would route an rbody with BOTH set differently — accept-as-locked, the original order wins.
  4. The fallthrough `invalid_change_flow-` callback with JSON.stringify(rbody) — verbatim. Note: rbody may contain password / rpassword in plain text. The JSON.stringify here is an existing SEC-PII-01 gap (passwords appear in this fallback log). This is OUT-OF-Phase-7 scope — DO NOT modify. v1.x candidate to track. (Same observation made in Phase 2 SEC-PII-01; this site is the residual.)
  5. All 4 callback shapes: `(false, "password_mismatch")`, delegated `(success, response)` from set_password_reset, delegated `(success, response)` from set_password_activation, `(false, "invalid_change_flow-...")`.

Like Owner.update, set_password has no userlib calls — work is delegated. The `async` keyword is added for consistency, NOT because the method awaits anything internally. The minimal form is preferred.
</interfaces>

<behavior>
New test in spec/jasmine/02-OwnerSpec.js (append):

  it("REFACTOR-04 (07-6): Owner.set_password callback contract preserved for password_mismatch", (done) => {
    // The password_mismatch guard is synchronous — fires before any delegate.
    // Safe to run without DB/Redis dependencies, so this test survives the local
    // test-env config-missing ACCEPT scenario.
    user.set_password({ password: "abc", rpassword: "xyz" }, (cb_success, cb_response) => {
      expect(cb_success).to.equal(false);
      expect(cb_response).to.equal("password_mismatch");
      done();
    });
  }, 5000);
</behavior>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight check — all 5 prior commits + baselines</name>
  <files>
    - lib/thinx/owner.js (READ ONLY)
  </files>
  <action>
    1. **All 5 prior commits landed:** `git log --oneline -20 | grep -cE "REFACTOR-04.*(non-top-5|Owner\.create|Owner\.delete|Owner\.update|Owner\.password_reset)"` MUST return 5. If <5, the prior plans have not all executed — STOP.
    2. Syntax baseline: `node --check lib/thinx/owner.js` exits 0.
    3. Strict-equality state preserved: `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` returns 0 lines.
    4. Phase 5 REFACTOR-02 still preserved: `grep -cE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js` returns exactly 1.
    5. Locate Owner.set_password: `grep -n "^\s*set_password\s*(rbody, callback)" lib/thinx/owner.js` returns 1 line, NOT yet `async`.
    6. SEC-PII-01 redaction baseline: `grep -c "Util.redactToken" lib/thinx/owner.js` >= baseline; note specifically that lines like `Util.redactToken(rbody.reset_key)` and `Util.redactToken(rbody.activation)` MUST be present (the two SEC-PII-01 redactions inside set_password).
    7. Cumulative async top-5 count BEFORE this plan: `grep -cE "^\s*async\s+(create|delete|update|password_reset)\s*\(" lib/thinx/owner.js` returns 4 (07-2..07-5).
    8. Identify the caller of set_password: `grep -n "set_password" lib/router.user.js | head -5` — record the line(s) for the Task 5 commit message body.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && test "$(git log --oneline -20 | grep -cE 'REFACTOR-04.*(non-top-5|Owner\.create|Owner\.delete|Owner\.update|Owner\.password_reset)')" -ge "5" && grep -q "^\s*set_password\s*(rbody, callback)" lib/thinx/owner.js && test "$(grep -cE 'reset_key\s+!==\s+user_reset_key' lib/thinx/owner.js)" = "1" && echo "baseline:OK"</automated>
  </verify>
  <done>All 5 prior commits confirmed. Phase 5 anti-regression preserved. Owner.set_password located, still callback-style. SEC-PII-01 redactToken sites present.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Owner.set_password (orchestrator) to async — minimal form</name>
  <files>
    - lib/thinx/owner.js
  </files>
  <action>
    Apply the minimal transform per <interfaces> "Async/await target shape":

    1. Outer signature: `set_password(rbody, callback) {` → `async set_password(rbody, callback) {`. PRESERVE the 2-arg order.

    2. **Mismatch guard preserved (lines 658-664):** verbatim.
       ```
       if (
         (!Util.isDefined(rbody.password)) ||
         (!Util.isDefined(rbody.rpassword)) ||
         (rbody.password !== rbody.rpassword)
       ) {
         return callback(false, "password_mismatch");
       }
       ```
       Note: the `!==` here was strict pre-Phase-7 (NOT the Phase 5 REFACTOR-02 fix). It STAYS strict. The grep gate in Task 4 step 3 ensures no drift.

    3. **Reset-path delegation preserved (lines 667-670):** verbatim.
       ```
       // Password Reset
       if (Util.isDefined(rbody.reset_key)) {
         console.log("ℹ️ [info] Resetting password " + Util.redactToken(rbody.reset_key) + " using set_password_reset...");
         return this.set_password_reset(rbody, callback);
       }
       ```
       set_password_reset is async (Plan 07-01) but its callback-style public signature lets this call site stay callback-passthrough. The `return` ensures only one delegate fires.

    4. **Activation-path delegation preserved (lines 672-676):** verbatim.
       ```
       // User Activation
       if (Util.isDefined(rbody.activation)) {
         console.log("ℹ️ [info] Searching " + Util.redactToken(rbody.activation) + " using set_password_activation...");
         return this.set_password_activation(rbody, callback);
       }
       ```

    5. **Fallthrough callback preserved (line 678):** verbatim.
       ```
       return callback(false, "invalid_change_flow-" + JSON.stringify(rbody));
       ```

    6. **What MUST NOT change:**
       - Method signature parameter list.
       - Either `Util.redactToken(...)` SEC-PII-01 call.
       - The order of the two delegation branches (reset_key first, activation second).
       - Any console.log text string.
       - The `!==` in the mismatch guard.
       - Any code outside set_password (the rest of the file stays as Plans 07-01..07-05 left it).
       - The fallback `JSON.stringify(rbody)` — IF the SEC-PII-01 gap concern is raised (rbody contains plaintext password in this fallback log), it is OUT-OF-Phase-7-SCOPE. Track as v1.x candidate; do NOT fix in this plan.

    Like Owner.update (Plan 07-4), this conversion is minimal — just `async` on the signature. The method does no awaiting; the value is file-wide consistency + the behavior-locking unit test.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && grep -qE "^\s*async\s+set_password\s*\(rbody, callback\)\s*\{" lib/thinx/owner.js && grep -q "Util.redactToken(rbody.reset_key)" lib/thinx/owner.js && grep -q "Util.redactToken(rbody.activation)" lib/thinx/owner.js && grep -q "invalid_change_flow-" lib/thinx/owner.js && echo "set_password-converted:OK"</automated>
  </verify>
  <done>Owner.set_password is now `async set_password(rbody, callback) {`. Mismatch guard intact (`!==` preserved). Both delegate-and-return branches intact in original order. Both Util.redactToken SEC-PII-01 sites intact. invalid_change_flow- fallback intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add behavior-locking unit test</name>
  <files>
    - spec/jasmine/02-OwnerSpec.js
  </files>
  <action>
    Append the `it` block from <behavior> at end of existing describe block. Tests the synchronous password_mismatch guard. Locks the (false, "password_mismatch") callback contract.
  </action>
  <verify>
    <automated>grep -q "REFACTOR-04 (07-6)" spec/jasmine/02-OwnerSpec.js && node --check spec/jasmine/02-OwnerSpec.js && echo "spec-added:OK"</automated>
  </verify>
  <done>One new `it` block with `REFACTOR-04 (07-6)` label added. Spec syntax clean.</done>
</task>

<task type="auto">
  <name>Task 4: Static validation gates — Phase 7 close-out gates</name>
  <files>
    - lib/thinx/owner.js (READ ONLY)
    - spec/jasmine/02-OwnerSpec.js (READ ONLY)
  </files>
  <action>
    These are the **Phase 7 close-out gates** — they assert REFACTOR-04 is fully met after this commit:

    1. **Syntax:** both files `node --check` exit 0.

    2. **Signature gate:** `grep -E "^\s*async\s+set_password\s*\(rbody, callback\)" lib/thinx/owner.js` returns 1 line.

    3. **Mismatch-guard preservation:** `grep -q "rbody.password !== rbody.rpassword" lib/thinx/owner.js` exits 0 (the strict-equality in the guard is preserved).

    4. **PHASE 5 REFACTOR-02 ANTI-REGRESSION (still met):** `grep -cE "reset_key\s+!==\s+user_reset_key" lib/thinx/owner.js` returns exactly 1.

    5. **Cumulative strict-equality state:** `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` returns ZERO lines. This is the Phase 7 cumulative goal-backward truth #2 — zero non-strict comparisons remain in `lib/thinx/owner.js`.

    6. **SEC-PII-01 redaction preservation:** `grep -c "alog.log\|Util.redactToken\|Util.redactEmail" lib/thinx/owner.js` >= pre-Phase-7 baseline.

    7. **All 5 top-5 methods now async — PHASE 7 GOAL-BACKWARD TRUTH #1 MET:**
       `grep -cE "^\s*async\s+(create|delete|update|password_reset|set_password)\s*\(" lib/thinx/owner.js` returns exactly 5.

    8. **No remaining callback-style `this.userlib.*(args, (err, body) => ...)` patterns inside the top-5 method bodies — PHASE 7 GOAL-BACKWARD TRUTH #3 MET:**
       For the top-5 methods specifically, scan their bodies and confirm no nested `this.userlib.X(args, (err...) =>` syntax remains. This is best confirmed by inspection: the converted methods should use `try { await this.userlib.X(args); } catch (err) { ... }` pattern exclusively for direct userlib calls.

    9. **All public method signatures preserved — PHASE 7 GOAL-BACKWARD TRUTH #4 MET:** `git log -p --since="Phase 7 start" lib/router.auth.js lib/router.user.js lib/router.profile.js lib/router.admin.js lib/router.google.js lib/router.github.js lib/router.mesh.js lib/router.gdpr.js lib/thinx/device.js lib/thinx/messenger.js lib/middleware/requireAdmin.js | wc -l` returns 0. NO caller file was touched by Phase 7. Alternative: `git diff HEAD~6..HEAD -- lib/router.*.js lib/thinx/device.js lib/thinx/messenger.js lib/middleware/requireAdmin.js | wc -l` MUST be 0.

    10. **Phase 5 REFACTOR-02 anti-regression survival — PHASE 7 GOAL-BACKWARD TRUTH #5 MET:** Gate 4 already confirmed.

    11. **SEC-PII-01 redaction preservation — PHASE 7 GOAL-BACKWARD TRUTH #6 MET:** Gate 6 already confirmed.

    12. **Behavior-locking tests count:** `grep -c "REFACTOR-04 (07-" spec/jasmine/02-OwnerSpec.js` returns exactly 5 (one per top-5 plan: 07-2..07-6) — PHASE 7 GOAL-BACKWARD TRUTH #8 MET.

    13. **Six atomic commits ordered: 07-1 → 07-2 → 07-3 → 07-4 → 07-5 → 07-6 — PHASE 7 GOAL-BACKWARD TRUTH #9 MET:**
       `git log --oneline -10 | grep -cE "refactor\(REFACTOR-04\):"` returns exactly 6 (assuming Plan 07-6's commit is the most recent; check via `git log -1 --pretty=%s | grep -q set_password`).

    14. Lint (best-effort): ACCEPT if config-missing.
  </action>
  <verify>
    <automated>node --check lib/thinx/owner.js && node --check spec/jasmine/02-OwnerSpec.js && test "$(grep -cE '^\s*async\s+(create|delete|update|password_reset|set_password)\s*\(' lib/thinx/owner.js)" = "5" && test "$(grep -cE '[^=!<>]!=[^=]|[^=!<>]==[^=]' lib/thinx/owner.js)" = "0" && test "$(grep -cE 'reset_key\s+!==\s+user_reset_key' lib/thinx/owner.js)" = "1" && test "$(grep -c 'REFACTOR-04 (07-' spec/jasmine/02-OwnerSpec.js)" = "5" && echo "phase-close-gates:OK"</automated>
  </verify>
  <done>All 14 Phase 7 close-out gates pass. All 5 top-5 methods async. Zero non-strict comparisons. Phase 5 REFACTOR-02 preserved. SEC-PII-01 preserved. 5 behavior-locking unit tests in 02-OwnerSpec.js. REFACTOR-04 fully met. Ready for phase-close orchestrator.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic GPG-signed commit — Phase 7 close-out commit</name>
  <files>
    - lib/thinx/owner.js
    - spec/jasmine/02-OwnerSpec.js
  </files>
  <action>
    1. `git status --short` — only the 2 files modified.
    2. `git add lib/thinx/owner.js spec/jasmine/02-OwnerSpec.js`
    3. `git commit -S` with subject `refactor(REFACTOR-04): convert Owner.set_password to async/await internals` and body documenting:
       - Minimal conversion (orchestrator method — no userlib calls; work delegated to set_password_reset / set_password_activation).
       - Signature preservation: 2-arg (rbody, callback); single caller in router.user.js (note caller line from Task 1 step 8).
       - Synchronous password_mismatch guard preserved with strict-equality `!==`.
       - Both SEC-PII-01 `Util.redactToken` sites preserved.
       - Both delegation branches preserved in original order (reset_key first, activation second).
       - Fallback `invalid_change_flow-` callback preserved (note: rbody serialization in this fallback contains plaintext password — SEC-PII-01 residual gap, tracked as v1.x candidate, OUT-OF-Phase-7-scope).
       - Behavior-locking unit test added: REFACTOR-04 (07-6) — locks the password_mismatch callback contract.
       - **PHASE 7 CLOSE-OUT:** This is the 6th and final REFACTOR-04 commit. After this commit:
         - All 5 top-5 methods (`create`, `delete`, `update`, `password_reset`, `set_password`) are async.
         - Zero non-strict comparisons remain in `lib/thinx/owner.js`.
         - Phase 5 REFACTOR-02 fix (`reset_key !== user_reset_key`) preserved through all 6 commits.
         - All SEC-PII-01 redaction calls preserved.
         - 5 behavior-locking unit tests in `spec/jasmine/02-OwnerSpec.js` (one per top-5 method).
         - Zero caller files modified — no public signature break on any of router.*.js, device.js, messenger.js, requireAdmin.js.
       - Test-env ACCEPT: local `npm test` aborts on missing /mnt/data/conf/config.json; CircleCI inside the Docker test image is the canonical Jasmine ZZ-* green-gate.
       - The phase-close orchestrator (separate workflow, NOT this plan) will produce 6 SUMMARY.md files (07-01-SUMMARY.md through 07-06-SUMMARY.md), update STATE.md, and mark Phase 7 complete in ROADMAP.md.
       - Refs: REFACTOR-04 (Phase 7); 7-of-7 strict-equality sweep + 5-of-5 top-5 async conversion + Phase 5 REFACTOR-02 preserved.
    4. Verify GPG: `git log -1 --pretty=%G?` returns `G`.
    5. `git push origin thinx-staging` — triggers CircleCI canonical Jasmine green-gate run.

    Notes:
    - DO NOT amend or force-push.
    - DO NOT run the phase-close orchestrator from this plan — that is a separate workflow (`/gsd:complete-phase 07` or equivalent).
    - DO NOT merge to master/main — that is milestone close-out.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^refactor(REFACTOR-04): convert Owner.set_password to async/await internals$" && git log -1 --pretty=%G? | grep -qE "^(G|U|E)" && test "$(git log --oneline -10 | grep -cE 'refactor\(REFACTOR-04\):')" -ge "6" && echo "phase-7-close-commit:OK"</automated>
  </verify>
  <done>Sixth atomic GPG-signed commit on `thinx-staging` with exact subject. 2 files in diff (lib/thinx/owner.js, spec/jasmine/02-OwnerSpec.js). 6 REFACTOR-04 commits visible in recent history. CircleCI triggered. Phase 7 REFACTOR-04 work is code-complete; phase-close orchestrator runs next.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request (POST /api/v2/password/set) → router.user.js → Owner.set_password | Untrusted rbody (password, rpassword, reset_key or activation) crosses into the validation guard. |
| Owner.set_password → Owner.set_password_reset OR Owner.set_password_activation | Orchestrator-to-delegate dispatch. Wrong delegation breaks flows (silent). |
| Owner.set_password → console.log with Util.redactToken | SEC-PII-01 redaction at the dispatch decision points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | Mismatch guard `!==` accidentally weakened to `!=` | mitigate | Task 4 step 3 — exact grep `rbody.password !== rbody.rpassword`. The unit test asserts the (false, "password_mismatch") tuple. |
| T-07-06-02 | Tampering | Delegation order swapped (activation before reset_key) — silent behavioral change for rbody with both keys set | mitigate | Task 2 step 3-4 — explicit order preservation. CI Jasmine green-gate (ZZ-RouterPasswordResetSpec.js) catches behavioral drift on real rbody inputs. |
| T-07-06-03 | Information Disclosure | SEC-PII-01 `Util.redactToken` removed at either dispatch site | mitigate | Task 4 step 6 — SEC-PII-01 count >= baseline. Task 2 step 6 explicitly preserves both. |
| T-07-06-04 | Information Disclosure | invalid_change_flow- fallback's JSON.stringify(rbody) leaks plaintext password to console.log | accept | Pre-existing SEC-PII-01 residual gap; OUT-OF-Phase-7-scope. Tracked as v1.x candidate. Note in commit body so phase-close SUMMARY records the residual. |
| T-07-06-05 | Repudiation | Double-callback if both reset_key and activation branches fire | mitigate | Both delegation branches use `return this.set_password_X(...)` — preserved. The `return` ensures terminal dispatch. |
| T-07-06-06 | Repudiation | Unsigned commit | mitigate | `git commit -S` mandatory. |
| T-07-06-07 | Tampering | Phase 5 REFACTOR-02 anti-regression — check across all 6 plans | mitigate | Task 4 step 4 cumulative check — Phase 5 fix (`reset_key !== user_reset_key`) STILL present after all 6 commits. |
| T-07-06-SC | Tampering | npm/pip/cargo installs | accept | No installs across all 6 plans. |
</threat_model>

<verification>
Phase 7 cumulative goal-backward verification (all 9 truths from CONTEXT.md must be met after Plan 07-6):

1. `node --check lib/thinx/owner.js` exits 0.
2. `grep -nE "[^=!<>]!=[^=]|[^=!<>]==[^=]" lib/thinx/owner.js` returns 0 lines.
3. No remaining callback-style `this.userlib.*(args, (err, body) => ...)` patterns in the top-5 method bodies.
4. Every public method preserved its signature; zero caller files modified by Phase 7.
5. Phase 5 REFACTOR-02 fix (`reset_key !== user_reset_key`) still present (exact 1 grep match).
6. All SEC-PII-01 redaction calls intact (`alog.log`, `Util.redactToken`, `Util.redactEmail` count >= pre-Phase-7 baseline).
7. CircleCI Jasmine ZZ-* suite green (canonical gate inside Docker test image).
8. 5 behavior-locking unit tests in `spec/jasmine/02-OwnerSpec.js` (REFACTOR-04 (07-2), (07-3), (07-4), (07-5), (07-6)).
9. Six atomic GPG-signed commits on `thinx-staging`, ordered: 07-1 → 07-2 → 07-3 → 07-4 → 07-5 → 07-6.
</verification>

<success_criteria>
- Owner.set_password converted to async (minimal — orchestrator, no userlib calls).
- Mismatch guard with strict-equality preserved.
- Both SEC-PII-01 redaction call sites preserved.
- Both delegation branches preserved in original order.
- Single caller (router.user.js) unchanged.
- Behavior-locking unit test added.
- One atomic GPG-signed commit on `thinx-staging` — the 6th and final REFACTOR-04 commit.
- CircleCI triggered.
- Phase 7 REFACTOR-04 work is code-complete: 9/9 goal-backward truths met.
</success_criteria>

<output>
SUMMARY.md deferred to phase wrap-up. After Plan 07-6's commit lands and CircleCI confirms green, the phase-close orchestrator (separate workflow, e.g. `/gsd:complete-phase 07`) produces 6 SUMMARY.md files (07-01-SUMMARY.md through 07-06-SUMMARY.md), updates STATE.md to mark Phase 7 complete, updates ROADMAP.md to mark REFACTOR-04 verified, and may issue a milestone-level summary.
</output>
