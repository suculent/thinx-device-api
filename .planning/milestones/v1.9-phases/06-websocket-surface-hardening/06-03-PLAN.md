---
phase: 06-websocket-surface-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/runbooks/websocket-handshake.md
autonomous: true
requirements:
  - SEC-WS-01
tags:
  - security
  - websocket
  - runbook
  - nginx
  - edge-routing
  - operator-action

must_haves:
  truths:
    - "`.planning/runbooks/websocket-handshake.md` exists in the canonical runbook directory."
    - "The runbook names the root cause: rtm.thinx.cloud edge nginx routes `/api/*` to the Express upstream but does NOT route `/<owner>` (WebSocket endpoint shape per AGENTS.md:52-53)."
    - "The runbook contains the 7-row request/response reproduction table from CONTEXT.md (with the `curl -sI` probe + response distinction proving Express was reached for `/api/*` but NOT for `/<owner>`)."
    - "The runbook names the operator-side action: SSH to `root@188.166.23.244 -p2020`, run `nginx -T` to inspect current config, add a `location` block for `/<owner>(/<timestamp>)?` proxying to the Express WebSocket upstream with `proxy_set_header Upgrade $http_upgrade` and `proxy_set_header Connection \"Upgrade\"`."
    - "The runbook tags the work as `deferred to edge-redesign` (per the requirement's option (b) in REQUIREMENTS.md:27)."
    - "The runbook documents a post-fix verification: re-run the same `curl -sI` probes against `/suculent` and confirm the response is 401 (Express auth rejection) or 101 (successful upgrade) — anything but the bare nginx 404."
    - "NO code change in this plan — the fix lives on the swarm host, outside this repo."
  artifacts:
    - path: ".planning/runbooks/websocket-handshake.md"
      provides: "Operator-side runbook for the rtm.thinx.cloud WebSocket-handshake 404 root cause + the nginx config edit that resolves it."
      contains: "SEC-WS-01"
      min_lines: 80
  key_links:
    - from: ".planning/runbooks/websocket-handshake.md"
      to: "swarm host (`root@188.166.23.244 -p2020`) nginx config for `rtm.thinx.cloud`"
      via: "operator-side `nginx -T` inspection + a new `location` block adding the WebSocket-endpoint route"
      pattern: "location"
    - from: ".planning/runbooks/websocket-handshake.md"
      to: "REQUIREMENTS.md SEC-WS-01"
      via: "deferred-to-edge-redesign tag"
      pattern: "deferred to edge-redesign"
---

<objective>
Document the SEC-WS-01 root cause and the operator-side fix in `.planning/runbooks/websocket-handshake.md`. CONTEXT.md established that the rtm.thinx.cloud handshake-404 is NOT a bug in this repo's code — Express was reached for `/api/*` probes (helmet CSP + CORS headers present in the response) but NOT for `/<owner>` probes (bare `Server: nginx` 404, no helmet headers). The edge nginx config routes `/api/*` to the Express upstream but lacks a route for the WebSocket endpoint shape `/<owner>` and `/<owner>/<timestamp>` (per `AGENTS.md:52-53`). The fix is an operator-side nginx config edit on the swarm host (`root@188.166.23.244 -p2020`), tagged `deferred to edge-redesign` per the requirement's option (b).

This plan produces ONLY the runbook. Per CONTEXT.md, a code-side change for SEC-WS-01 would mask the actual issue and create false hope — REFACTOR-03 (Plan 06-01) is a SEPARATE real code gap that needed fixing on its own merits, but it is not the rtm 404 root cause.

Purpose: Give the operator a complete, reproducible action item for the swarm-side fix and a post-fix verification recipe. Close SEC-WS-01 in this repo with the option (b) "documented + tagged deferred" path.

Output: A new file `.planning/runbooks/websocket-handshake.md` containing the root-cause statement, the 7-row reproduction table from CONTEXT.md, the operator-side action items, the post-fix verification recipe, and the deferred-to-edge-redesign tag. One atomic GPG-signed commit: `docs(SEC-WS-01): rtm.thinx.cloud WebSocket handshake 404 runbook (edge nginx routing gap)`.

**Wave 1 placement rationale:** This plan touches NO code and NO files outside `.planning/runbooks/` — it is parallel-safe with Plan 06-01 (which touches `thinx-core.js` + a new spec). Plan 06-02 depends on this runbook existing (or creates a stub if it does not); the recommended execution order is 06-01 + 06-03 in parallel (Wave 1), then 06-02 (Wave 2). The orchestrator MAY schedule 06-03 before 06-02 to avoid the fallback-stub path in 06-02 Task 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-websocket-surface-hardening/06-CONTEXT.md
@.planning/runbooks/swarm.md
@AGENTS.md

<interfaces>
<!-- Canonical runbook format (mirror .planning/runbooks/swarm.md) + the SEC-WS-01 source material. -->

From `AGENTS.md` § "Websocket Findings" (lines 44-55):
- Console-side fix landed (`gulpfile.js` now derives the WS URL from `API_BASEURL`).
- Local rebuilt output uses `wss://rtm.thinx.cloud/<owner>` and `wss://rtm.thinx.cloud/<owner>/<timestamp>`.
- Quote (line 55): "if live still returns `404` after serving the corrected bundle, the remaining bug is server-side websocket routing or upgrade handling on `rtm.thinx.cloud`, not CSP and not the console host selection".

From CONTEXT.md (SEC-WS-01 section, the 7-row reproduction table captured 2026-06-02T21:10-21:11Z):

| Request | Response | Headers | Diagnosis |
|---------|----------|---------|-----------|
| `GET https://rtm.thinx.cloud/` | `HTTP/2 200` | static landing | nginx routes `/` to a static-content path |
| `GET https://rtm.thinx.cloud/api/v2/users` + WS upgrade | `HTTP/2 404` | helmet CORS + `default-src 'none'` + `access-control-allow-origin: https://rtm.thinx.cloud` | Express IS reached (CSP+CORS headers prove it); 404 because the GET verb isn't routed |
| `GET https://rtm.thinx.cloud/api/githook` | `HTTP/2 404` | helmet CORS | Express IS reached; 404 because GET on POST-only route |
| `GET https://rtm.thinx.cloud/test` (no upgrade) | `HTTP/2 404` | `server: nginx`, no helmet | Express NOT reached — bare nginx default 404 |
| `GET https://rtm.thinx.cloud/test` + WS upgrade headers | `HTTP/2 404` | `server: nginx` | Same — edge nginx returns 404 before forwarding |
| `GET https://rtm.thinx.cloud/test` + WS upgrade + HTTP/1.1 forced | `HTTP/1.1 404 Not Found` | `Server: nginx` | Not HTTP/2 specific — protocol fallback yields same edge 404 |
| `GET https://rtm.thinx.cloud/suculent` (real owner) + WS upgrade + Cookie | `HTTP/1.1 404 Not Found` | `Server: nginx` | The actual production WS endpoint shape — edge nginx returns 404 before app sees it |

From CONTEXT.md (SEC-WS-01 Decision block):
- Filename: `.planning/runbooks/websocket-handshake.md` (under `.planning/runbooks/` per Phase 9's pattern of extending the runbook set).
- Action items: root cause statement, operator action with `nginx -T` + the missing `location` block, deferred-to-edge-redesign tag, post-fix verification.
- The expected nginx location pattern (illustrative — operator may choose a different regex to avoid conflict with `/api/*` and `/static/*`):
  ```
  location ~ ^/[^/]+(/.*)?$ {
      proxy_pass http://thinx-api-upstream;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "Upgrade";
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
  }
  ```
  The runbook should present this as ILLUSTRATIVE — the operator validates against the current nginx config via `nginx -T` before committing.

From `.planning/runbooks/swarm.md` (canonical runbook format reference):
- Top-level `# <Topic>` heading.
- Optional intro paragraph.
- `## Symptom` section (what the operator sees that triggers consulting this runbook).
- `## Root Cause` section.
- `## Reproduction` section (curl/ssh recipe).
- `## Recovery` / `## Action` section (the operator-side fix).
- `## Verification` section (post-fix probe).
- `## Reference` section (links to PLAN/SUMMARY/CONTEXT for traceability).

Production console deploy details (from `AGENTS.md:9-19`):
- Production console URL: `https://rtm.thinx.cloud/`
- Swarm SSH: `ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020`
- Swarm config path: `/mnt/gluster/deployment/swarm`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the WebSocket-handshake runbook with the SEC-WS-01 root cause + reproduction + operator action + verification</name>
  <files>.planning/runbooks/websocket-handshake.md</files>
  <read_first>
    - .planning/phases/06-websocket-surface-hardening/06-CONTEXT.md (SEC-WS-01 section — the 7-row reproduction table is the canonical source; copy it verbatim)
    - .planning/runbooks/swarm.md (canonical runbook format reference)
    - AGENTS.md lines 9-19 (swarm SSH details) and lines 44-55 (Websocket Findings — the console-side fix + the server-side gap statement)
    - .planning/REQUIREMENTS.md line 27 (SEC-WS-01 — the option (a) reproduce+fix vs option (b) document+defer-to-edge choice; CONTEXT.md locks option (b))
  </read_first>
  <action>
    Create the file `.planning/runbooks/websocket-handshake.md` with the following structure. Use Markdown level-2 headings (`##`) for the sections. The file is the canonical operator-side reference for the rtm.thinx.cloud WebSocket-handshake 404 issue.

    **Required sections, in order:**

    1. **Heading + intro:**
       ```
       # WebSocket Handshake Runbook

       Operational runbook for WebSocket-related issues on `rtm.thinx.cloud`. Currently documents SEC-WS-01 (rtm handshake 404 — edge nginx routing gap). Plan 06-02 (SEC-COOKIE-01) appends a rollback section to this file.
       ```

    2. **Symptom:**
       ```
       ## Symptom

       The Vue console at `https://rtm.thinx.cloud/app` cannot establish a WebSocket connection. Browser DevTools shows the upgrade request to `wss://rtm.thinx.cloud/<owner>` (or `wss://rtm.thinx.cloud/<owner>/<timestamp>`) returning HTTP 404 instead of the expected 101 Switching Protocols.

       Console-side debugging confirms the bundle is correct (per `AGENTS.md:44-55` — `services/console/src/gulpfile.js` derives the WS URL from `API_BASEURL`, and local rebuilds verify `wss://rtm.thinx.cloud/<owner>`). The 404 persists after deploying a corrected bundle.
       ```

    3. **Root Cause:**
       ```
       ## Root Cause

       The rtm.thinx.cloud edge nginx config routes `/api/*` to the Express upstream (thinx-device-api) but does NOT have a `location` block matching the WebSocket endpoint shape `/<owner>` and `/<owner>/<timestamp>`. The request never reaches Express, so the Express `this.server.on('upgrade', ...)` handler at `thinx-core.js:459` is never invoked. The 404 is the bare nginx default response.

       This is a reproducible edge-routing gap, NOT a bug in this repo's code. AGENTS.md originally guessed "Traefik" as the edge layer, but the response headers (`Server: nginx`, no helmet/CSP) show the actual edge is nginx-fronted.
       ```

    4. **Reproduction:**
       ```
       ## Reproduction

       Captured 2026-06-02T21:10-21:11Z via `curl -sI`. The Express CSP/CORS header presence/absence distinguishes Express-reached vs nginx-only responses.

       <... insert the 7-row table from CONTEXT.md verbatim, including the header row ...>

       Key distinction: rows 2-3 show helmet+CORS headers (Express reached); rows 4-7 show only `Server: nginx` with no helmet headers (Express NOT reached).
       ```

       The reproduction table MUST be copied VERBATIM from CONTEXT.md. The executor MUST NOT rewrite or summarize the rows — the exact request/response shapes are evidence.

    5. **Operator Action:**
       ```
       ## Operator Action

       Tagged: **deferred to edge-redesign** (per SEC-WS-01 requirement option (b), REQUIREMENTS.md:27).

       The fix lives on the swarm host, NOT in this repo.

       1. **SSH to the swarm host:**
          ```
          ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020
          ```
          (Per AGENTS.md:18.)

       2. **Inspect the current nginx config** (the rtm.thinx.cloud server block):
          ```
          nginx -T 2>&1 | grep -A 50 "server_name rtm.thinx.cloud"
          ```
          Identify the existing `location` blocks. Confirm that `location /api/` (or `location ~ ^/api/`) exists and that `location /` (or similar) handles the static landing. Confirm there is NO existing `location` block matching the WebSocket-endpoint shape `/<owner>(/<timestamp>)?`.

       3. **Add the missing `location` block** for the WebSocket endpoint shape, BEFORE the catch-all `location /` block. The block must:
          - Match `/<owner>` and `/<owner>/<timestamp>` (path segments do not start with `api` or `static`).
          - Proxy to the Express WebSocket upstream (the same upstream the existing `location /api/` block uses).
          - Set the Upgrade + Connection headers required by the WebSocket handshake.
          - Set the X-Forwarded-* headers consistent with the rest of the rtm.thinx.cloud server block.

          Illustrative block (operator validates against current upstream name and surrounding directives before committing):
          ```nginx
          location ~ ^/[^/]+(/.*)?$ {
              proxy_pass http://thinx-api-upstream;
              proxy_http_version 1.1;
              proxy_set_header Upgrade $http_upgrade;
              proxy_set_header Connection "Upgrade";
              proxy_set_header Host $host;
              proxy_set_header X-Real-IP $remote_addr;
              proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
              proxy_set_header X-Forwarded-Proto $scheme;
          }
          ```
          The regex MUST be ordered AFTER `/api/` and `/static/` location blocks so the more-specific routes win. If nginx-on-this-host uses named-capture or different upstream-block conventions, adapt accordingly.

       4. **Test the config without applying** (catches typos):
          ```
          nginx -t
          ```
          MUST return `nginx: configuration file ... test is successful` before proceeding.

       5. **Reload nginx:**
          ```
          systemctl reload nginx
          ```
          (Or `nginx -s reload` if the host does not use systemd.)
       ```

    6. **Verification:**
       ```
       ## Verification

       Re-run the same `curl -sI` probes from the Reproduction table. The success criterion is that `/suculent` (or any owner-shaped path) is NO LONGER a bare nginx 404. Acceptable post-fix responses:

       - **`401 Unauthorized`** — Express reached, app rejected the upgrade (no valid `x-thx-core` session cookie). Response headers include helmet CSP + CORS — same shape as the `/api/v2/users` probe in row 2 of the reproduction table.
       - **`101 Switching Protocols`** — Express reached, upgrade succeeded (valid session cookie present). This is the success path the Vue console exercises.

       Either response confirms the edge-routing gap is closed. A continued `Server: nginx` 404 means the new `location` block did not match — debug with `nginx -T | grep -B2 -A10 "location"` and confirm the regex.

       Probe recipe (run from any internet-connected workstation):
       ```
       curl -sI -H "Connection: upgrade" -H "Upgrade: websocket" -H "Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==" -H "Sec-WebSocket-Version: 13" -H "Cookie: x-thx-core=test" https://rtm.thinx.cloud/suculent
       ```
       Compare the `Server:` header and the presence/absence of helmet `Content-Security-Policy:` and `Access-Control-Allow-Origin:` headers against the reproduction table.

       Post-fix Vue console smoke: reload `https://rtm.thinx.cloud/app` in a fresh browser session, log in, open DevTools Network tab, filter on WS, confirm the WS subscribe handshake returns 101.
       ```

    7. **Reference:**
       ```
       ## Reference

       - `.planning/phases/06-websocket-surface-hardening/06-CONTEXT.md` — the SEC-WS-01 decision block + the canonical 7-row reproduction table (this runbook's evidence source).
       - `.planning/phases/06-websocket-surface-hardening/06-03-PLAN.md` — the plan that produced this runbook.
       - `.planning/REQUIREMENTS.md` SEC-WS-01 (line 27) — the requirement, with both option (a) "reproduce+fix here" and option (b) "document+defer-to-edge" paths. CONTEXT.md locks option (b).
       - `AGENTS.md` § "Websocket Findings" (lines 44-55) — the original surfacing of the issue + the console-side fix (server-side gap remained).
       - `.planning/runbooks/swarm.md` — adjacent runbook covering swarm autoredeploy recovery; this runbook follows the same format.
       ```

    The file is plain Markdown — no YAML frontmatter required for runbooks (mirror `.planning/runbooks/swarm.md`). Verify with `wc -l` that the file is ≥ 80 lines (the content above expands well past that floor with the reproduction table included).
  </action>
  <verify>
    <automated>test -f .planning/runbooks/websocket-handshake.md && grep -c "SEC-WS-01" .planning/runbooks/websocket-handshake.md</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/runbooks/websocket-handshake.md` exists.
    - `grep -c "SEC-WS-01" .planning/runbooks/websocket-handshake.md` returns ≥ 2.
    - `grep -c "deferred to edge-redesign" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
    - `grep -c "188.166.23.244" .planning/runbooks/websocket-handshake.md` returns ≥ 1 (the swarm host is named).
    - `grep -c "nginx -T" .planning/runbooks/websocket-handshake.md` returns ≥ 1 (the operator action references the canonical inspection command).
    - `grep -c "proxy_set_header Upgrade" .planning/runbooks/websocket-handshake.md` returns ≥ 1 (the illustrative nginx block includes the Upgrade-header directive).
    - `grep -c "rtm.thinx.cloud/suculent" .planning/runbooks/websocket-handshake.md` returns ≥ 1 (the actual production-shaped probe row of the table is present; this is row 7 of the reproduction table and the verification probe).
    - `grep -c "101 Switching Protocols" .planning/runbooks/websocket-handshake.md` returns ≥ 1 (the success-criterion response is named).
    - `grep -c "401" .planning/runbooks/websocket-handshake.md` returns ≥ 1 (the acceptable Express-rejection response is named).
    - `wc -l .planning/runbooks/websocket-handshake.md | awk '{print $1}'` returns a value ≥ 80.
    - **Reproduction table integrity:** all 7 rows of the CONTEXT.md table are present. Each row references either a curl-style request, a response code, headers, or a diagnosis. Spot-check: `grep -c "Express IS reached" .planning/runbooks/websocket-handshake.md` returns ≥ 1; `grep -c "Express NOT reached" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
  </acceptance_criteria>
  <done>
    - The runbook exists, names the root cause, contains the 7-row reproduction table, names the operator action with concrete `nginx -T` + `location` block guidance, names the post-fix verification (curl probe + Vue console smoke), and tags the work `deferred to edge-redesign`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit SEC-WS-01 runbook as one atomic GPG-signed commit</name>
  <files>.planning/runbooks/websocket-handshake.md</files>
  <read_first>
    - .planning/runbooks/websocket-handshake.md (post-Task-1 state)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-01-PLAN.md Task 3 (canonical commit pattern from Phase 5)
  </read_first>
  <action>
    Stage and commit the SEC-WS-01 runbook as one atomic GPG-signed commit, following the atomic-per-requirement Phase 5 pattern.

    Commit subject: `docs(SEC-WS-01): rtm.thinx.cloud WebSocket handshake 404 runbook (edge nginx routing gap)`

    Commit body: explain (1) the root cause — rtm edge nginx routes `/api/*` but not `/<owner>(/<timestamp>)?` to the Express upstream; (2) the reproduction evidence — the 7-row curl probe table captured 2026-06-02T21:10-21:11Z, with the helmet/CSP-header presence/absence distinguishing Express-reached vs edge-nginx-only responses; (3) the operator action — SSH to `root@188.166.23.244 -p2020`, run `nginx -T`, add the missing `location` block; (4) the deferred-to-edge-redesign tag — per SEC-WS-01 requirement option (b); (5) why this is documentation, not code — the request never reaches Express, so a code-side change would mask the actual gap. Reference: "Phase 6 — WebSocket Surface Hardening".

    Stage exactly the one file (`git add .planning/runbooks/websocket-handshake.md` — NOT `git add -A`). If Plan 06-02 Task 3 has APPENDED to this file already (Wave 2 ordering), the SEC-COOKIE-01 rollback section is part of THIS commit ONLY IF that other plan has not yet committed; otherwise the file's state in `git diff --staged` should show ONLY the SEC-WS-01 sections. Verify via `git diff --staged .planning/runbooks/websocket-handshake.md | head -5` before committing — if the diff contains SEC-COOKIE-01 content, the execution order has slipped; STOP and surface the conflict so the operator can re-sequence.

    GPG-sign by default. Do NOT push in this plan.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -q "^docs(SEC-WS-01): rtm.thinx.cloud WebSocket handshake 404 runbook (edge nginx routing gap)$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s'` returns exactly `docs(SEC-WS-01): rtm.thinx.cloud WebSocket handshake 404 runbook (edge nginx routing gap)`.
    - `git log -1 --stat` shows exactly one file changed (`.planning/runbooks/websocket-handshake.md`), with insertions ≥ 80 / deletions = 0 (the file is created in this commit).
    - `git log -1 --show-signature 2>&1 | grep -iE "Good signature|gpg:"` returns a "Good signature" line.
    - `git status --short` is empty.
    - The commit contains NO code changes — `git log -1 --name-only` lists only the runbook file.
  </acceptance_criteria>
  <done>
    - SEC-WS-01 lands as one atomic, GPG-signed commit touching exactly the runbook file.
    - Working tree is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser / wscat) → edge nginx | Untrusted upgrade requests cross here. The edge nginx config decides which paths are forwarded to the Express upstream. Currently `/api/*` is forwarded; `/<owner>(/<timestamp>)?` is NOT. |
| Edge nginx → Express upstream | Internal boundary on the swarm host. The Express WS upgrade handler at `thinx-core.js:459` only runs if nginx forwards the request. |
| Documentation → operator action | The runbook is the input to a manual swarm-side action. Trust is in the operator's execution of the documented `nginx -T` + reload steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Denial of Service | rtm.thinx.cloud WebSocket endpoint (edge nginx routing gap) | mitigate-by-runbook | The runbook documents the root cause + operator-side fix with a concrete `location` block and verification recipe. The fix itself lives on the swarm host; this repo's contribution is the documentation that drives the operator action. Tag: `deferred to edge-redesign` per the requirement's option (b). |
| T-06-11 | Tampering | `.planning/runbooks/websocket-handshake.md` content | accept | New file; no existing content overwritten. GPG-signed commit + atomic-per-requirement pattern keeps blast radius isolated. |
| T-06-12 | Information Disclosure | Runbook contents (e.g., swarm SSH details, `188.166.23.244`, port 2020, the upstream hint) | accept | All disclosed details are already in `AGENTS.md` (parent root, repo-checked-in) and in `.planning/runbooks/swarm.md`. The runbook does not add any new sensitive disclosure. Note: the runbook does NOT include the actual SSH key or any secret material. |
| T-06-13 | Repudiation | Operator action audit trail | mitigate | The runbook's `## Operator Action` section names the exact commands the operator runs (`ssh ...`, `nginx -T`, `nginx -t`, `systemctl reload nginx`). The shell history on the swarm host provides the audit trail. Defense-in-depth: the post-fix verification (`curl -sI` probes against `/suculent`) gives an externally-observable confirmation that the change landed. |
| T-06-14 | Defense-in-Depth Note | Edge routing gap | accept | The edge nginx routing gap is logged as a defense-in-depth weakness pending operator action. The downstream code-side raw-socket close handler in Plan 06-01 (REFACTOR-03) hardens the Express WS upgrade handler against mid-flight aborts independently — both items together harden the WS surface even before the edge fix lands. |

T-06-10 is the primary mitigate-by-runbook disposition. T-06-14 captures the defense-in-depth observation called out in the planning brief.
</threat_model>

<verification>
- `.planning/runbooks/websocket-handshake.md` exists.
- `grep -c "SEC-WS-01" .planning/runbooks/websocket-handshake.md` returns ≥ 2.
- `grep -c "deferred to edge-redesign" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
- `grep -c "188.166.23.244" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
- `grep -c "nginx -T" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
- `grep -c "proxy_set_header Upgrade" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
- `grep -c "101 Switching Protocols" .planning/runbooks/websocket-handshake.md` returns ≥ 1.
- `wc -l .planning/runbooks/websocket-handshake.md | awk '{print $1}'` returns ≥ 80.
- The 7-row reproduction table is present verbatim from CONTEXT.md (spot-check: `grep -c "Express IS reached" ...` returns ≥ 1; `grep -c "Express NOT reached" ...` returns ≥ 1).
- One atomic GPG-signed commit on the current branch (`docs(SEC-WS-01): rtm.thinx.cloud WebSocket handshake 404 runbook (edge nginx routing gap)`) touching exactly the runbook file.
- **No code changes in this plan** — `git log -1 --name-only` lists only the runbook.
- **Local test-environment limitation:** N/A. This plan is documentation-only; no specs run.
</verification>

<success_criteria>
- ROADMAP Phase 6 success criterion 2: `wscat` handshake from a fresh Vue session against `rtm.thinx.cloud` returns `101 Switching Protocols` ... if not code-fixable from this repo, a runbook documents the upstream-Traefik condition with reproduction steps. ✓ (Runbook documents the rtm edge-nginx routing gap with the full 7-row reproduction table and the operator-side fix.)
- REQUIREMENTS.md SEC-WS-01 validation criteria (a, b): handshake returns 101 (deferred to operator-side fix; runbook is the input) ✓; runbook documents the failure mode if it can't be code-fixed here ✓.
- Goal-backward must_have #5 (runbook exists with reproduction + operator action + deferred-to-edge-redesign tag). ✓
</success_criteria>

<output>
Create `.planning/phases/06-websocket-surface-hardening/06-03-SUMMARY.md` when done.
</output>
