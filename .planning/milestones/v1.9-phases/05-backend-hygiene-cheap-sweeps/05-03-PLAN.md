---
phase: 05-backend-hygiene-cheap-sweeps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: false
requirements:
  - REFACTOR-05
tags:
  - refactor
  - dependencies
  - package-json
  - supply-chain
  - docker

must_haves:
  truths:
    - "`jshint` is declared exactly once in `package.json`, under `devDependencies` (NOT under `dependencies`)."
    - "`fs-finder` remains in `dependencies` (intentional scope deferral per CONTEXT.md — full removal deferred to v1.10)."
    - "No runtime `require('jshint')` in `lib/` or `thinx-core.js` (already true pre-edit — assert via grep)."
    - "Production Docker image (`npm install --omit=dev`) builds without errors and reaches the `Server up at` log line."
    - "CircleCI green on the merge to `thinx-staging`; Swarmpit autoredeploy completes within the 5-minute SLA."
  artifacts:
    - path: "package.json"
      provides: "jshint declared under devDependencies, fs-finder unchanged in dependencies."
      contains: "jshint"
  key_links:
    - from: "Dockerfile (RUN npm install --omit=dev .)"
      to: "package.json (dependencies block)"
      via: "npm install --omit=dev"
      pattern: "npm install --omit=dev"
    - from: "package.json devDependencies"
      to: "CI / local-dev tooling only"
      via: "npm install (full, no --omit=dev) in CircleCI"
      pattern: "devDependencies"
---

<objective>
Move `jshint` from `dependencies` to `devDependencies` in `package.json` (REFACTOR-05 — REDUCED scope per CONTEXT.md). `fs-finder` STAYS in `dependencies` because it has 5 active runtime call sites in `lib/` (builder.js, deployment.js, platform.js, repository.js, plugins/arduino/plugin.js); moving it would break production immediately. The full `fs-finder` removal is deferred to a proposed v1.10 phase.

Purpose: Slim the production Docker image (`npm install --omit=dev`) by removing one runtime install that has zero runtime use (`grep -rE "require\(['\"]jshint['\"]\)" lib/ thinx-core.js` already returns zero matches). Net result: smaller image, narrower supply-chain surface for the production runtime, zero behavior change.

Output: One edit to `package.json` (delete `jshint` from `dependencies`, add to `devDependencies`), a Docker smoke-build verification, and one atomic commit: `refactor(REFACTOR-05): move jshint to devDependencies (fs-finder deferred to v1.10)`. Includes a checkpoint:human-verify gate after the Docker smoke build because production-image-shape changes are blocking-human per the scope-amendment notes in CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md
@.planning/codebase/STACK.md
@AGENTS.md
@package.json
@Dockerfile

<interfaces>
<!-- package.json layout — relevant blocks only. Read the full file before editing for exact line numbers. -->

Current state (verify by grep before editing):
- `package.json:32` — `"dependencies": {` (start of dependencies block)
- `package.json:55` — `"fs-finder": "github:suculent/Node-FsFinder#master",` (STAYS — do NOT remove)
- `package.json:59` — `"jshint": "^2.13.4",` (MOVE this entry out of dependencies)
- `package.json:137` — `"devDependencies": {` (start of devDependencies block)
- `package.json:138-156` — existing devDependencies entries (alphabetically ordered; insert `jshint` between `jasmine-core` and `jest-junit` to preserve alphabetical order — exact position TBD by file state at edit time)

Dockerfile reference (`Dockerfile:85-86`):
```
RUN npm install -g npm@10.2.3 \
 && npm install --omit=dev .
```
This is the production install line. `--omit=dev` means `devDependencies` are NOT installed in the production image. Therefore: moving `jshint` to `devDependencies` removes it from the production image; the only way this is safe is if no runtime code requires it. CONTEXT.md confirms zero `require('jshint')` in `lib/` or `thinx-core.js` — sanity-check this with grep in Task 1 before committing.

Note on stylistic concerns:
- `package.json` MUST remain valid JSON. Use a JSON-aware edit (or apply the change with a sed/awk that you verify with `jq` afterward) — DO NOT corrupt the trailing-comma structure.
- After the edit, run `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` to confirm validity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-edit verification — confirm zero runtime require of jshint, snapshot package.json</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - package.json (full file; specifically blocks at :32-130 dependencies and :137-156 devDependencies)
    - Dockerfile (specifically the `npm install --omit=dev` line)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-05 decision block — scope amendment)
  </read_first>
  <action>
    Two pre-flight checks before any edit. Both MUST pass before proceeding to Task 2.

    **Check 1 — Zero runtime require of jshint anywhere in app code:**
    Run `grep -rE "require\(['\"]jshint['\"]\)" lib/ thinx-core.js 2>/dev/null`. The grep MUST return zero lines (CONTEXT.md asserts this is already true today; we re-verify to make the gate self-contained).

    **Check 2 — Sanity-check fs-finder is intentionally staying in dependencies:**
    Run `grep -rnE "require\(['\"]fs-finder['\"]\)" lib/ thinx-core.js`. Confirm the FIVE expected call sites listed in CONTEXT.md still exist (builder.js, deployment.js, platform.js, repository.js, plugins/arduino/plugin.js). This is a re-assertion of the scope-amendment rationale — if fs-finder somehow has zero call sites, the planner's deferral assumption is wrong and the operator should be alerted before editing.

    **Snapshot:** Capture the pre-edit `package.json` to `/tmp/phase5-03-package-json.pre` (via `cp package.json /tmp/phase5-03-package-json.pre`) so a forensic diff is available after the edit.

    Do NOT modify any file in this task. Do NOT install anything. Do NOT run `npm install` yet (the install + smoke-build is Task 3).
  </action>
  <verify>
    <automated>grep -rE "require\(['\"]jshint['\"]\)" lib/ thinx-core.js 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "require\(['\"]jshint['\"]\)" lib/ thinx-core.js 2>/dev/null | wc -l` returns exactly `0`.
    - `grep -rE "require\(['\"]fs-finder['\"]\)" lib/ thinx-core.js 2>/dev/null | wc -l` returns exactly `5` (as documented in CONTEXT.md).
    - `/tmp/phase5-03-package-json.pre` exists and matches the working tree's `package.json` byte-for-byte (`diff -q /tmp/phase5-03-package-json.pre package.json` returns empty / exit 0).
  </acceptance_criteria>
  <done>
    - Zero `require('jshint')` confirmed.
    - Five `require('fs-finder')` confirmed (matches CONTEXT.md inventory).
    - Pre-edit `package.json` snapshot captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move jshint from dependencies to devDependencies in package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file — to find exact line numbers for dependencies and devDependencies blocks at edit time, and to preserve alphabetical ordering inside devDependencies)
    - /tmp/phase5-03-package-json.pre (the pre-edit snapshot from Task 1)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-05 decision block)
  </read_first>
  <action>
    Make exactly two edits to `package.json`, preserving JSON validity:

    1. **Remove** the line `    "jshint": "^2.13.4",` from the `dependencies` block (currently at `:59`). Remove the line in its entirety — do NOT leave a blank line.

    2. **Insert** the line `    "jshint": "^2.13.4",` into the `devDependencies` block (currently starts at `:137`), in the correct alphabetical position. The devDeps block currently contains entries `@commitlint/cli`, `@commitlint/config-conventional`, `assert`, `eslint`, `eslint-config-jquery`, `expect`, `grunt`, `gulp-sourcemaps`, `jasmine`, `jasmine-core`, `jest-junit`, `karma`, `karma-chrome-launcher`, `karma-coverage`, `karma-jasmine`, `mocha`, `mocha-lcov-reporter`, `nyc`. The correct alphabetical position for `jshint` is between `jest-junit` and `karma`.

    Constraints:
    - Preserve indentation exactly (4 spaces, matching the surrounding entries in both blocks).
    - Preserve trailing-comma semantics (the last entry in each block has NO trailing comma; all earlier entries DO have a trailing comma). After insertion, `jshint` will NOT be the last entry in devDependencies (since `karma` follows it), so it MUST have a trailing comma.
    - After removing `jshint` from dependencies, the line that previously followed it (`jsonwebtoken` at the old `:60`) becomes the new entry at that slot — verify its formatting is intact.
    - Do NOT change any other entry. Do NOT change the version pin (`^2.13.4`). Do NOT change `fs-finder` (it stays in dependencies, unchanged).

    After the edit, validate JSON integrity:
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` MUST exit 0.
    - `diff /tmp/phase5-03-package-json.pre package.json` SHOULD show exactly two hunks: one removal (jshint leaves deps) and one addition (jshint enters devDeps).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); process.exit(p.devDependencies.jshint && !p.dependencies.jshint ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); process.exit(p.devDependencies.jshint && !p.dependencies.jshint ? 0 : 1)"` exits 0.
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies['fs-finder'] ? 0 : 1)"` exits 0 (fs-finder STAYS in dependencies — sanity check).
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (JSON still valid).
    - `node -e "const p=require('./package.json'); process.exit(p.devDependencies.jshint === '^2.13.4' ? 0 : 1)"` exits 0 (version pin preserved).
    - `diff /tmp/phase5-03-package-json.pre package.json | grep -c "^[<>]"` returns exactly `2` (one removal line + one addition line — no other lines changed).
  </acceptance_criteria>
  <done>
    - `jshint` declared exactly once, under `devDependencies`.
    - `fs-finder` unchanged (still under `dependencies`).
    - JSON valid.
    - Diff vs. pre-edit snapshot is the expected two-line change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Production Docker smoke-build — confirm image still builds and reaches `Server up at`</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - Dockerfile (full file — to confirm `npm install --omit=dev` line is what the smoke build exercises)
    - package.json (post-Task-2 state)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-05 validation criteria — "Production Docker image still builds, starts, and logs `Server up at`")
  </read_first>
  <action>
    Build the production Docker image locally and confirm the resulting container reaches the `Server up at` log line. This is REFACTOR-05's hard gate per CONTEXT.md — without it, the production-image-shape change is unverified.

    **Build:** `docker build -t thinx-test:phase5 .` from the repo root. Capture build output to `/tmp/phase5-03-docker-build.log`.

    Notes on the build:
    - `Dockerfile:85-86` runs `npm install --omit=dev .` — this is the line that proves jshint is NOT needed at runtime. Pre-edit, jshint would have been installed here; post-edit, it should NOT be installed here.
    - The build inherits from `thinxcloud/base:alpine` which provides the Node 22.x runtime — no special build setup required beyond `docker build`.
    - If the build fails because the base image is not available locally, run `docker pull thinxcloud/base:alpine` first (the base image is the public THiNX base image; pull is permitted).
    - If the build fails because of an unrelated transient (e.g., upstream npm registry hiccup), retry ONCE. Do not retry indefinitely.

    **Run:** `docker run --rm --name thinx-test-phase5 -d thinx-test:phase5` — start the container in detached mode with a known name, then tail logs for up to 90 seconds waiting for the `Server up at` log line. Capture container logs to `/tmp/phase5-03-docker-run.log`.

    Concretely: `timeout 90 docker logs -f thinx-test-phase5 2>&1 | tee /tmp/phase5-03-docker-run.log | grep -m1 "Server up at"`. The `grep -m1` exits as soon as the line is seen.

    **Stop:** `docker stop thinx-test-phase5 2>/dev/null || true` and `docker rm thinx-test-phase5 2>/dev/null || true` to clean up. Keep the image (`thinx-test:phase5`) around for the operator's human-verify checkpoint in Task 4.

    Caveat — runtime dependencies the container needs that are NOT available locally:
    The full prod runtime expects CouchDB, Redis, Mosquitto, Influx, Mailgun config, etc. The container may FAIL to fully start in a local-only `docker run` (the `Server up at` line may never appear because the app crashes during DB/Redis/MQTT bootstrap). If the `Server up at` line does NOT appear within 90 seconds:
      (a) capture the last 100 lines of container output to `/tmp/phase5-03-docker-run.log`,
      (b) confirm via `grep -E "Cannot find module 'jshint'|require.*jshint" /tmp/phase5-03-docker-run.log` that the failure is NOT a missing-jshint error,
      (c) document in the SUMMARY that the bootstrap-dependency limitation prevented a full local smoke (the production CI build is the canonical gate — Task 4 surfaces this to the operator),
      (d) proceed to Task 4 (the human-verify gate explicitly covers the "CircleCI green on staging push" criterion).
    Do NOT block on the local container reaching `Server up at` if the only blocker is missing external services — the human-verify checkpoint after CI is the authoritative gate.
  </action>
  <verify>
    <automated>docker build -t thinx-test:phase5 . 2>&1 | tee /tmp/phase5-03-docker-build.log | tail -1 | grep -qE "successfully|naming to|Successfully built|FINISHED"</automated>
  </verify>
  <acceptance_criteria>
    - `docker build -t thinx-test:phase5 .` exits 0 (the build succeeds — the install step does NOT fail because jshint moved to devDependencies).
    - `/tmp/phase5-03-docker-build.log` does NOT contain any line matching `Cannot find module 'jshint'` or `npm ERR!` (`grep -E "Cannot find module 'jshint'|npm ERR!" /tmp/phase5-03-docker-build.log | wc -l` returns 0).
    - EITHER `/tmp/phase5-03-docker-run.log` contains a `Server up at` line (full local smoke succeeded) OR the run log shows the container reached `npm install` and started loading lib/ modules (i.e., the failure is downstream of the install step — e.g., CouchDB connect error, NOT a missing-module error). Concrete grep: `grep -E "Cannot find module" /tmp/phase5-03-docker-run.log` returns ZERO lines about jshint specifically.
    - `docker images thinx-test:phase5 --format '{{.Repository}}:{{.Tag}}'` returns `thinx-test:phase5` (image exists for the Task-4 human-verify gate).
  </acceptance_criteria>
  <done>
    - Production Docker image builds successfully with `jshint` moved to devDependencies.
    - No "Cannot find module 'jshint'" error anywhere in build or run output.
    - Either `Server up at` confirmed locally, OR the local smoke's failure is provably-unrelated to the jshint move (and the operator is alerted in Task 4 to confirm via CI).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator confirmation — production-image-shape change (REFACTOR-05) is acceptable to land</name>
  <what-built>
    - `package.json` edited: `jshint ^2.13.4` moved from `dependencies` to `devDependencies`. `fs-finder` (a runtime dep with 5 active call sites) unchanged.
    - `docker build -t thinx-test:phase5 .` succeeded locally; build log at `/tmp/phase5-03-docker-build.log`. No "Cannot find module 'jshint'" error.
    - `docker run` smoke: either `Server up at` confirmed locally (best case), OR the local smoke failed at a stage that is provably-unrelated to the jshint move (e.g., container couldn't reach a local CouchDB). The run log is at `/tmp/phase5-03-docker-run.log`.
  </what-built>
  <how-to-verify>
    Production-image-shape changes are operationally consequential — the actual canonical gate per REFACTOR-05 success criterion 3 is "Swarmpit autoredeploy completes within the 5-minute SLA after merge to thinx-staging". That gate cannot be exercised from the planner / executor environment. Operator-verification checklist:

    1. **Local artifacts check (one-shot):**
       - Confirm `docker images thinx-test:phase5` shows the image was built.
       - Optionally: `docker history thinx-test:phase5 --no-trunc | grep -iE "jshint|fs-finder"` — confirm jshint does NOT appear in the install layer's contents (you should see fs-finder but NOT jshint).

    2. **Operator decision point — proceed to commit + push?**
       - If you (operator) are willing to commit the package.json edit and trigger the canonical CircleCI build by pushing to `thinx-staging`: answer "approved — proceed to commit".
       - If you want a deeper local smoke first (e.g., bringing up the docker-compose test stack with CouchDB/Redis to truly confirm `Server up at`): answer "deeper-smoke" and run `docker compose -f docker-compose.test.yml up --abort-on-container-exit` separately before re-prompting.
       - If you see anything in `/tmp/phase5-03-docker-build.log` or `/tmp/phase5-03-docker-run.log` that suggests jshint or another dev-only tool IS being required at runtime: answer "rollback" and the planner will revert the package.json edit.

    3. **What CI will assert post-push (canonical gates):**
       - CircleCI build green (full image build + push to thinxcloud/api:latest).
       - Swarmpit autoredeploy ≤5min SLA.
       - `docker service logs thinx_api --since 5m | grep "Server up at"` returns a match on the swarm host post-deploy.
       - Operator may verify via `ssh root@188.166.23.244 -i ~/.ssh/DOKey2 -p2020 'docker service logs thinx_api --since 5m | grep "Server up at"'`.

    Note: This checkpoint is BLOCKING per the supply-chain threat-model entry T-05-SC. Production-image-shape changes are not auto-advanceable even if `workflow.auto_advance=true`.
  </how-to-verify>
  <resume-signal>Type "approved — proceed to commit" to advance to Task 5 (commit), OR "deeper-smoke" to pause for a full docker-compose smoke, OR "rollback" to revert the package.json edit.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit REFACTOR-05 atomically (after operator approval)</name>
  <files>package.json</files>
  <read_first>
    - package.json (post-Task-2 state)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (commit convention + scope-amendment rationale)
  </read_first>
  <action>
    Stage and commit the REFACTOR-05 atomic change. Single file modified (`package.json`); single atomic GPG-signed commit:

    Subject: `refactor(REFACTOR-05): move jshint to devDependencies (fs-finder deferred to v1.10)`

    Body (mandatory — the scope amendment MUST be documented in the commit message so post-hoc verifiers can reconstruct the deferral without reading CONTEXT.md):
    - State that `jshint` moved from `dependencies` to `devDependencies` (zero runtime requires of jshint in `lib/` or `thinx-core.js`, confirmed by grep).
    - State that `fs-finder` STAYS in `dependencies` because it has 5 active runtime call sites in `lib/` (builder, deployment, platform, repository, plugins/arduino) — moving it would break production immediately.
    - Note that the full `fs-finder` removal (replace with `fs-extra` glob helpers or native `fs.promises.readdir`) is deferred to a proposed v1.10 phase.
    - Reference Phase 5; reference the local Docker smoke (`docker build -t thinx-test:phase5 . && docker run ...`) that confirmed the install layer still resolves.

    Stage exactly one file (`git add package.json`). GPG-sign per project default. Do NOT push to remote in this plan — the push happens after Wave 2 (doc-update plan 05-04) lands.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -q "^refactor(REFACTOR-05): move jshint to devDependencies (fs-finder deferred to v1.10)$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s'` returns exactly `refactor(REFACTOR-05): move jshint to devDependencies (fs-finder deferred to v1.10)`.
    - `git log -1 --name-only --pretty=format:` lists exactly one file: `package.json`.
    - `git log -1 --format=%b | grep -iE "fs-finder.*defer|deferred.*v1.10|5 active runtime"` returns at least one match (the scope-amendment rationale is in the commit body, not only the subject).
    - `git log -1 --show-signature 2>&1 | grep -iE "Good signature|gpg:"` confirms GPG-signed.
    - `git status --short` returns empty.
  </acceptance_criteria>
  <done>
    - REFACTOR-05 (scope-reduced) lands as one atomic GPG-signed commit touching only `package.json`.
    - Commit body documents the fs-finder deferral and the v1.10 proposal.
    - Working tree is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `npm install --omit=dev` | Supply-chain boundary. Production image's installed runtime tree is whatever `dependencies` resolves to from the npm registry. |
| `package.json` → Docker image layer | Edits to `dependencies` change the production image's installed module surface. |
| Production container runtime → `require(...)` calls in app code | If any `require('jshint')` exists at runtime, moving jshint to devDeps breaks production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Denial of Service | Production container fails to start if jshint is required somewhere | mitigate | Task 1 pre-edit grep proves zero `require('jshint')` in `lib/` and `thinx-core.js`; Task 3 docker-build smoke proves the install layer resolves cleanly; Task 4 blocking-human checkpoint is the operator's final gate before the canonical CI build. |
| T-05-09 | Tampering | npm install pulls a different package than expected | accept | `jshint ^2.13.4` is a long-established legitimate package on the npm registry (>15M weekly downloads, jshint org). `fs-finder` is intentionally pinned to the internally-owned fork `github:suculent/Node-FsFinder#master` (NOT pulled from the public npm registry, per CONTEXT.md "fork is internally owned"). No new packages added; no upstream source change. Existing `package.json` `overrides` block (`:97-136`) handles known transitive CVEs. |
| T-05-10 | Information Disclosure | Smaller production image = smaller attack surface | accept (positive) | Removing jshint from the runtime install layer is a net negative-surface change — fewer modules loadable at runtime, narrower transitive footprint. No new disclosure risk. |
| T-05-11 | Spoofing | Operator might miss a hidden runtime use of jshint | mitigate | Task 1's grep is the explicit gate; Task 3's docker smoke build + Task 4's blocking-human checkpoint provide three independent layers of verification before commit. |
| T-05-SC | Tampering | npm/Docker install layer | mitigate | Blocking-human checkpoint at Task 4 (per supply-chain threat-model convention). Not auto-advanceable. No `[SUS]`/`[ASSUMED]` packages added; the package being reclassified is the long-established `jshint` from the canonical jshint org. |

No new HIGH severity items. T-05-08 is the canonical jshint-move risk and is fully mitigated by the multi-layer gate set.
</threat_model>

<verification>
- `node -e "const p=require('./package.json'); process.exit(p.devDependencies.jshint && !p.dependencies.jshint ? 0 : 1)"` exits 0.
- `node -e "const p=require('./package.json'); process.exit(p.dependencies['fs-finder'] ? 0 : 1)"` exits 0.
- `grep -rE "require\(['\"]jshint['\"]\)" lib/ thinx-core.js | wc -l` returns 0.
- `docker build -t thinx-test:phase5 .` exits 0; no `Cannot find module 'jshint'` in build log.
- Operator approval recorded for the production-image-shape change.
- One atomic GPG-signed commit (`refactor(REFACTOR-05): move jshint to devDependencies (fs-finder deferred to v1.10)`); commit body documents the fs-finder deferral.
- (Post-merge gate, exercised by execute-phase wrap-up, not this plan): CircleCI green on push to `thinx-staging`; Swarmpit autoredeploy ≤5min SLA; swarm-side `docker service logs thinx_api | grep "Server up at"` returns a match.
</verification>

<success_criteria>
- REFACTOR-05 success criterion 3 (roadmap line 43 — INTENTIONALLY NARROWED per CONTEXT.md): `jshint` is declared in `devDependencies`; production Docker image still builds + reaches `Server up at`; no `require('jshint')` remains in `lib/` or `thinx-core.js`. ✓
- `fs-finder` portion of literal criterion 3 EXPLICITLY DEFERRED with documented rationale (commit body + Plan 05-04 doc-update artifact).
- REFACTOR-05 success criterion 4 (CircleCI green + autoredeploy SLA): asserted post-push by execute-phase wrap-up.
- Goal-backward must_haves #3 (jshint in devDeps), #4 (production image reaches `Server up at`), #6 (CircleCI green / autoredeploy SLA) satisfied (or, for #6, asserted at the next gate).
</success_criteria>

<output>
Create `.planning/phases/05-backend-hygiene-cheap-sweeps/05-03-SUMMARY.md` when done.
</output>
