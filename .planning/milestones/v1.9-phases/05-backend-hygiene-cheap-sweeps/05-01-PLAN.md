---
phase: 05-backend-hygiene-cheap-sweeps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thinx-core.js
autonomous: true
requirements:
  - REFACTOR-01
tags:
  - refactor
  - express
  - trust-proxy
  - thinx-core

must_haves:
  truths:
    - "Exactly one `app.set('trust proxy', ...)` call exists in thinx-core.js."
    - "The surviving call is the allowlist form: `app.set('trust proxy', ['loopback', '127.0.0.1'])` (preserves observed behavior — the later allowlist call has been winning all along)."
    - "Express still resolves req.ip correctly via the loopback proxy (Traefik fronts the app on loopback in the swarm topology)."
    - "Jasmine session/auth specs remain green (no behavior regression detectable from a public route)."
  artifacts:
    - path: "thinx-core.js"
      provides: "Single canonical trust proxy declaration with an explanatory comment near the surviving call site."
      contains: "trust proxy"
  key_links:
    - from: "thinx-core.js (single trust proxy call site)"
      to: "Express session/IP-derivation middleware"
      via: "app.set('trust proxy', ['loopback', '127.0.0.1'])"
      pattern: "trust proxy"
---

<objective>
Collapse the two competing `app.set('trust proxy', ...)` calls in `thinx-core.js` down to a single canonical site (REFACTOR-01). Today the file calls `app.set("trust proxy", 1)` at `:300` and then `app.set('trust proxy', ['loopback', '127.0.0.1'])` at `:422` — the second call wins because it executes after, so the observed behavior is the allowlist form. This plan deletes the earlier number-of-proxies call (`:300`) and keeps the allowlist call (`:422`) with an explanatory comment.

Purpose: Eliminate the duplicate-config foot-gun without changing observable IP-derivation behavior on any public route. Matches the "no change to observed IP-derivation behavior" guardrail in REFACTOR-01.

Output: A single-line deletion at `thinx-core.js:300` plus a one-line clarifying comment near the surviving `:422` call. One atomic commit: `refactor(REFACTOR-01): single trust-proxy source — allowlist form`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@AGENTS.md
@thinx-core.js

<interfaces>
<!-- Express trust-proxy contract — for context only, no implementation changes required outside the two call sites. -->
Relevant Express API:
- `app.set('trust proxy', value)` — values: number (count of proxies), string ('loopback'), array of CIDR/IP/named addresses, or function. The allowlist form `['loopback', '127.0.0.1']` trusts requests whose immediate hop is loopback — correct for the Traefik-fronted swarm topology.

Current state in thinx-core.js (verify by grep before editing — see read_first):
- `thinx-core.js:300` — `app.set("trust proxy", 1);` (DELETE this line)
- `thinx-core.js:422` — `app.set('trust proxy', ['loopback', '127.0.0.1']);` (KEEP this line; add the explanatory comment immediately above it)

Surrounding context at :300 is a DI block (`app.builder = builder; app.queue = queue;`) and a no-op `require('path');` — the trust-proxy line is structurally unrelated to its neighbours and safe to remove without touching anything else.

Surrounding context at :422 is the `app.use('/static', ...)` static-mount line immediately before, and the WebSocket-server block immediately after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the duplicate trust-proxy call at thinx-core.js:300 and add an explanatory comment near :422</name>
  <files>thinx-core.js</files>
  <read_first>
    - thinx-core.js (full file; specifically read lines :295-310 and :415-430 before editing)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (REFACTOR-01 decision block)
  </read_first>
  <action>
    Make two edits to `thinx-core.js`, both per the REFACTOR-01 decision in CONTEXT.md:

    1. **Delete** the line `app.set("trust proxy", 1);` at `thinx-core.js:300`. Remove the line entirely (do not leave a blank placeholder; do not comment it out). The line is in a DI block — its removal is structurally inert and does NOT affect the lines above (`app.builder = builder; app.queue = queue;`) or below (`require('path');`).

    2. **Add** a single explanatory comment line immediately above the surviving `app.set('trust proxy', ['loopback', '127.0.0.1']);` call at (current) `thinx-core.js:422`. The comment MUST be a JavaScript `//` line comment (not a block comment) and MUST capture two facts: (a) this is the single source of truth for the `trust proxy` setting, (b) the allowlist form is chosen because Traefik fronts the app on loopback in the swarm topology. Exact comment text:

    `// REFACTOR-01: single source of truth for trust-proxy; allowlist form chosen because Traefik fronts the app on loopback in the swarm topology.`

    Do NOT touch any other line in `thinx-core.js`. Do NOT reformat surrounding code. Do NOT remove the unrelated `require('path');` no-op at the old `:302` (that's flagged separately in CONTEXT.md "Deferred Ideas — package.json audit" as a v1.10 candidate, OUT OF Phase 5 scope).
  </action>
  <verify>
    <automated>grep -c "trust proxy" thinx-core.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "trust proxy" thinx-core.js` returns exactly `1` (was 2 before the edit).
    - `grep -n "trust proxy" thinx-core.js` shows a single match whose RHS is `['loopback', '127.0.0.1']` — confirm by inspecting the matched line; verify there is no remaining occurrence with the value `1` or any number-of-proxies form.
    - `grep -n "REFACTOR-01: single source of truth for trust-proxy" thinx-core.js` returns exactly one match, and that match's line number is immediately above (line N where the trust-proxy line is N+1) the surviving `app.set('trust proxy', ['loopback', '127.0.0.1']);` call.
    - `node --check thinx-core.js` exits 0 (syntax still valid).
  </acceptance_criteria>
  <done>
    - The line `app.set("trust proxy", 1);` no longer exists anywhere in `thinx-core.js`.
    - The line `app.set('trust proxy', ['loopback', '127.0.0.1']);` still exists, preceded by the REFACTOR-01 comment.
    - File parses (`node --check`) clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the Jasmine session/auth/reset specs to confirm zero behavioral regression on req.ip-derived flows</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - thinx-core.js (lines :295-310, :415-430 — post-edit state from Task 1)
    - spec/jasmine/ZZ-AppSession.js
    - spec/jasmine/ZZ-AppSessionUserSpec.js
    - spec/jasmine/ZZ-RouterPasswordResetSpec.js
  </read_first>
  <action>
    Run the Jasmine session/auth/reset spec set to confirm the trust-proxy dedup did not change any req.ip-derived behavior. The full Jasmine suite is too heavy for a smoke run; the targeted set below covers the session middleware that consumes the trust-proxy setting plus the reset_key flow that touches the same `thinx-core.js` block.

    Run, from the repository root, exactly these specs:
    - `spec/jasmine/ZZ-AppSession.js`
    - `spec/jasmine/ZZ-AppSessionUserSpec.js`
    - `spec/jasmine/ZZ-RouterPasswordResetSpec.js`

    Use the test runner the project already uses (`npx jasmine --config=spec/support/jasmine.json --filter=...` if the project supports `--filter`, OR temporarily configure the runner via the standard spec-helper environment). Do NOT install new dev dependencies. Do NOT modify `spec/support/jasmine.json`. If the runner does not support per-file filtering in this codebase, run `npm test` and verify the three named specs pass within the full suite output.

    If any of the three specs report a failure that grep-confirms originates from `req.ip` resolution, cookie / session middleware, or rate-limit attribution: STOP, do NOT proceed, report the failing assertion verbatim (so the operator can audit whether the older `trust proxy: 1` was actually doing something distinct under test). Otherwise, proceed.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/phase5-01-jasmine.log | grep -E "ZZ-AppSession|ZZ-RouterPasswordReset" | grep -Eiv "passed|spec passed" | grep -Ei "fail" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The three named specs all complete without a failing assertion. Concretely: `grep -E "ZZ-AppSession|ZZ-RouterPasswordReset" /tmp/phase5-01-jasmine.log | grep -iE "fail|error"` returns zero lines that represent a Jasmine failure (note: ignore lines that contain `fail` only as part of a passing assertion's expected-value string).
    - `node --check thinx-core.js` exits 0 (re-asserted post-test, in case any teardown wrote to the file).
    - The file's single trust-proxy declaration is still the allowlist form: `grep -n "trust proxy" thinx-core.js` returns a single line whose RHS is `['loopback', '127.0.0.1']`.
  </acceptance_criteria>
  <done>
    - Session/auth/reset spec set green; no behavioral regression detected.
    - `thinx-core.js` post-edit state preserved through the test run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Stage and commit the REFACTOR-01 atomic change</name>
  <files>thinx-core.js</files>
  <read_first>
    - thinx-core.js (post-Task-1 state)
    - .planning/phases/05-backend-hygiene-cheap-sweeps/05-CONTEXT.md (commit-message convention — recommendation is one atomic commit per REFACTOR-NN)
  </read_first>
  <action>
    Stage and commit the REFACTOR-01 edit as a single atomic GPG-signed commit. Per CONTEXT.md, each requirement lands as its own atomic commit (recommended) to keep blast radius isolated and rollback surgical.

    Commit message (use HEREDOC for correct formatting):

    `refactor(REFACTOR-01): single trust-proxy source — allowlist form`

    Body: explain that the duplicate `app.set("trust proxy", 1)` at the old `:300` was deleted; the surviving call is the allowlist form at `:422` which has been the effective setting all along (the later call wins). Note that this matches the Traefik-on-loopback swarm topology. Reference the phase: "Phase 5 — Backend Hygiene — Cheap Sweeps".

    Only stage `thinx-core.js` (`git add thinx-core.js` — NOT `git add -A`). GPG-sign by default per project policy.

    Do NOT push to remote in this plan — the push happens once all three Wave 1 plans land and Wave 2 (doc-update) is committed. (Optional: defer the push to the execute-phase wrap-up.)
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -q "^refactor(REFACTOR-01): single trust-proxy source — allowlist form$"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s'` returns exactly `refactor(REFACTOR-01): single trust-proxy source — allowlist form`.
    - `git log -1 --stat` shows exactly one file changed (`thinx-core.js`), with insertions ≥ 1 and deletions ≥ 1 (one line removed, one comment added — net could be 0 or +1 depending on whether a blank line was preserved).
    - `git log -1 --show-signature 2>&1 | grep -iE "Good signature|gpg:"` returns a "Good signature" line (or, if the operator has granted a per-session unsigned authorization for this phase, that authorization is recorded in STATE.md — but the default is GPG-signed).
    - `git status --short` returns empty (no other staged or unstaged changes leaked into this commit).
  </acceptance_criteria>
  <done>
    - REFACTOR-01 lands as one atomic, GPG-signed commit touching only `thinx-core.js`.
    - Working tree is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Traefik → Express (HTTP request) | Untrusted user input crosses here; trust-proxy setting determines which `X-Forwarded-*` header chain Express honors when populating `req.ip`. |
| Express → session / rate-limit middleware | `req.ip` derived above feeds session-cookie attribution, rate-limit bucketing, and audit-log IP fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | `app.set('trust proxy', ...)` in `thinx-core.js` | mitigate | Keep the allowlist form `['loopback', '127.0.0.1']` — narrower than `'trust proxy': 1` because it only honors `X-Forwarded-For` when the immediate hop is loopback. Number-form trusts blindly; the allowlist form rejects spoofed `X-Forwarded-For` from non-loopback peers. Net effect of this plan: no widening of spoofing surface (already running the allowlist form in practice — the deletion REMOVES the more-permissive number-form earlier call, which was being overwritten anyway). |
| T-05-02 | Repudiation | Session middleware / audit-log IP attribution | accept | Behavioral parity is the explicit guardrail — the post-state matches the pre-state (the second call has been winning all along). No new audit-attribution drift. Jasmine session/auth specs (Task 2) cover the regression surface. |
| T-05-03 | Tampering | `thinx-core.js` source | accept | Single-line deletion + single-line comment addition. No new dependencies; no transitive supply-chain exposure introduced. `node --check` and a pinned spec set cover the source-integrity gate. |

No HIGH severity items. Disposition for all three is consistent with the "no observable behavior change" guardrail in REFACTOR-01.
</threat_model>

<verification>
- `grep -c "trust proxy" thinx-core.js` returns 1.
- The single match's value is `['loopback', '127.0.0.1']` (allowlist form).
- A REFACTOR-01 comment precedes the surviving call.
- `node --check thinx-core.js` exits 0.
- Jasmine session/auth/reset spec set green.
- One atomic GPG-signed commit on the current branch (`refactor(REFACTOR-01): single trust-proxy source — allowlist form`).
</verification>

<success_criteria>
- REFACTOR-01 success criterion 1 (roadmap line 41): `grep -n "trust proxy" thinx-core.js` returns exactly one canonical call. ✓
- Jasmine session/auth specs unchanged & green. ✓
- Per-requirement validation in REQUIREMENTS.md (REFACTOR-01.a, .b): grep returns 1, session/auth specs green. ✓
- Goal-backward must_have #1 (exactly one trust-proxy call with allowlist value) satisfied. ✓
</success_criteria>

<output>
Create `.planning/phases/05-backend-hygiene-cheap-sweeps/05-01-SUMMARY.md` when done.
</output>
