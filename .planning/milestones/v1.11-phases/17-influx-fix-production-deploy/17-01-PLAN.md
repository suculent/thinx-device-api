---
phase: 17-influx-fix-production-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/runbooks/swarm.md
  - .planning/phases/17-influx-fix-production-deploy/deploy-pre.txt
  - .planning/phases/17-influx-fix-production-deploy/deploy-post.txt
autonomous: false
requirements:
  - OPS-EXEC-03

must_haves:
  truths:
    - "thinx_api on production runs the pipeline-5266 :latest image (contains influx fix 9b6d931c), task Running"
    - "docker service logs thinx_api shows no 'found BADSTRING' lines after deploy"
    - "Vue dashboard check-in/build stats read non-zero/updating values (owner-tag fix live)"
    - "thinx_api remains co-located with thinx_mosquitto on the core node (MQTT not broken)"
    - "Phase 17 execution annex appended to .planning/runbooks/swarm.md with operator initials, UTC, pre/post digests, node placement, BADSTRING grep, dashboard verdict"
  artifacts:
    - path: ".planning/runbooks/swarm.md"
      provides: "Phase 17 / OPS-EXEC-03 execution annex"
      contains: "OPS-EXEC-03"
    - path: ".planning/phases/17-influx-fix-production-deploy/deploy-pre.txt"
      provides: "pre-deploy baseline probe output (image digest, node, BADSTRING present)"
    - path: ".planning/phases/17-influx-fix-production-deploy/deploy-post.txt"
      provides: "post-deploy verification output (new digest, node unchanged, BADSTRING absent)"
    - path: ".planning/phases/17-influx-fix-production-deploy/17-01-SUMMARY.md"
      provides: "OPS-EXEC-03 closure record"
---

<objective>
Deploy the already-committed, CI-green influx stats fix (9b6d931c) to production by forcing the swarm to roll thinx_api to the current thinxcloud/api:latest image (pipeline 5266), while keeping thinx_api co-located with thinx_mosquitto on the core node, then verify the BADSTRING log spam is gone and dashboard numbers are correct.

This is an OPERATOR-GATED phase: the production commands are run by the operator over SSH. The orchestrator prepares the runbook, pauses at the deploy checkpoint, and resumes on the operator's signal to record verification + annex.
</objective>

<context>
@.planning/phases/17-influx-fix-production-deploy/17-CONTEXT.md
@.planning/runbooks/swarm.md
</context>

<tasks>

<task type="checkpoint" tdd="false">
  <name>Task 1: Pre-deploy baseline probe (operator)</name>
  <files>.planning/phases/17-influx-fix-production-deploy/deploy-pre.txt</files>
  <action>
    Operator runs over SSH and saves output to deploy-pre.txt:

    1. Current image digest thinx_api is running:
       docker service inspect thinx_api --format '{{.Spec.TaskTemplate.ContainerSpec.Image}}'
    2. Which node thinx_api runs on + confirm it is the same node as thinx_mosquitto (the core node):
       docker service ps thinx_api --no-trunc --format '{{.Node}} {{.CurrentState}} {{.Image}}' | head -3
       docker service ps thinx_mosquitto --no-trunc --format '{{.Node}} {{.CurrentState}}' | head -1
    3. Confirm thinx_api placement constraint (if any) pins it to core:
       docker service inspect thinx_api --format '{{.Spec.TaskTemplate.Placement.Constraints}}'
    4. Baseline: BADSTRING spam is present BEFORE the fix:
       docker service logs thinx_api --since 15m 2>&1 | grep -c "found BADSTRING"
  </action>
  <acceptance_criteria>
    - deploy-pre.txt records the current (pre-influx) image digest
    - deploy-pre.txt records the node thinx_api runs on AND confirms it equals the thinx_mosquitto node
    - deploy-pre.txt records the BADSTRING occurrence count (expected > 0, confirming the bug is live)
  </acceptance_criteria>
</task>

<task type="checkpoint" tdd="false">
  <name>Task 2: Force-rollout thinx_api to :latest (operator) — CO-LOCATION GUARDRAIL</name>
  <files>(production swarm — no repo file)</files>
  <action>
    PRECONDITION the orchestrator must confirm before handing this to the operator: Phases 15/16 are NOT pushed (origin/thinx-staging must NOT contain the Phase 15/16 commits), so :latest is still the influx-only image. Do NOT git push during this phase.

    Operator runs:
       docker service update --force thinx_api
    Wait ~60-120s for the new task to boot.

    If thinx_api does NOT have a constraint pinning it to core and the scheduler moved it to micro, reschedule it back to core before proceeding (placement constraint update or drain micro). A thinx_api task on micro is a FAILED deploy (breaks MQTT).

    Rollback if a regression appears:
       docker service rollback thinx_api
  </action>
  <acceptance_criteria>
    - docker service update --force thinx_api completed; a new task is Running
    - thinx_api task image digest now matches Hub's :latest (the influx-fix image)
  </acceptance_criteria>
</task>

<task type="checkpoint" tdd="false">
  <name>Task 3: Post-deploy verification (operator)</name>
  <files>.planning/phases/17-influx-fix-production-deploy/deploy-post.txt</files>
  <action>
    Operator runs and saves output to deploy-post.txt:

    1. New digest + Running + node placement (MUST be the same core node as pre-deploy / thinx_mosquitto):
       docker service ps thinx_api --no-trunc --format '{{.Node}} {{.CurrentState}} {{.Image}}' | head -3
    2. BADSTRING gone (expect 0):
       docker service logs thinx_api --since 10m 2>&1 | grep -c "found BADSTRING"
    3. MQTT alive (no connack-timeout spam):
       docker service logs thinx_api --since 10m 2>&1 | grep -ci "connack\|mqtt.*timeout"
    4. Dashboard numbers: load the Vue dashboard (or curl /api/v2/stats with a valid session) and confirm check-in/build counts are non-zero / updating (not stale 0).
  </action>
  <acceptance_criteria>
    - deploy-post.txt shows thinx_api Running on the SAME core node as before (co-location intact)
    - deploy-post.txt shows BADSTRING count = 0
    - deploy-post.txt shows no new connack/MQTT-timeout spam
    - dashboard check-in numbers verified non-zero/updating by the operator
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Annex + SUMMARY (orchestrator, after operator reports results)</name>
  <files>.planning/runbooks/swarm.md, .planning/phases/17-influx-fix-production-deploy/17-01-SUMMARY.md</files>
  <action>
    Append a "Phase 17 / OPS-EXEC-03 — Influx fix production deploy" section to .planning/runbooks/swarm.md recording: operator initials, UTC timestamp, pre/post image digests, the node thinx_api ran on before/after (co-location confirmation), BADSTRING grep before→after, MQTT check, and the dashboard verdict. Reference deploy-pre.txt / deploy-post.txt.

    Write 17-01-SUMMARY.md with the OPS-EXEC-03 closure: what was deployed, the verification matrix, and any deviation. Mark REQUIREMENTS.md OPS-EXEC-03 [x]. Commit the annex + SUMMARY + REQUIREMENTS + probe files.
  </action>
  <acceptance_criteria>
    - .planning/runbooks/swarm.md contains an OPS-EXEC-03 annex with operator initials + UTC + digests + node placement + BADSTRING before/after
    - 17-01-SUMMARY.md exists with the verification matrix
    - REQUIREMENTS.md OPS-EXEC-03 marked complete
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Operator workstation → production swarm (SSH) | Deploy commands run as root on 188.166.23.244 |
| Docker Hub :latest → swarm thinx_api | The image the swarm pulls; must be the pipeline-5266 influx image, not a rebuilt one |

## STRIDE Register
| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-17-01 | Denial of Service | thinx_api ↔ mosquitto co-location | mitigate | Pre/post node-placement check; a move to micro is a failed deploy (breaks server-side MQTT connack) — reschedule back to core |
| T-17-02 | Tampering | :latest content | mitigate | Confirm Phases 15/16 are unpushed so :latest is the influx-only image; do not push during the phase |
| T-17-03 | Availability | force-rollout restart | accept | Brief thinx_api restart (~60-120s); rollback via docker service rollback if regression |

## Residual Risk
A ~1-2 min thinx_api restart during rollout (accepted). Phases 15/16 remain undeployed by design.
</threat_model>

<success_criteria>
Phase 17 complete when: influx-fix image is live on thinx_api (Running), BADSTRING log spam is 0, dashboard numbers are correct/updating, thinx_api stayed co-located with mosquitto on core, MQTT is alive, and the swarm.md annex + SUMMARY + REQUIREMENTS update are committed.
</success_criteria>

<output>
Create .planning/phases/17-influx-fix-production-deploy/17-01-SUMMARY.md when done (after operator reports verification results).
</output>
