---
phase: 12-code-side-closure-helpers
phase_id: 12-code-side-closure-helpers
plan: 3
plan_id: 12-03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/thinx/audit-ttl-probe.js
  - thinx-core.js
  - spec/jasmine/ZZ-AuditTTLEvictionSpec.js
autonomous: true
requirements:
  - OBS-02
requirements_addressed:
  - OBS-02
user_setup: []

must_haves:
  truths:
    - "New module lib/thinx/audit-ttl-probe.js mirrors Phase 11 cert-probe.js EXACTLY at the module level: PURE / READ-ONLY / NO-LOG / NEVER-throws (per D-01)"
    - "Module body contains NO `console.log` / `console.warn` / `rollbar` calls (caller emits WARN per D-08; module purity per D-01)"
    - "Module signature is `async function probeTtlEviction({ couchdbUri, dbName, graceMs, timeoutMs })` returning `{ ok, oldestExpiredId, staleByDays, message }` (per D-02; mirrors cert-probe result-shape)"
    - "All error paths return a result object — module NEVER throws (per D-02 cert-probe inheritance)"
    - "GRACE_MS default is 7 days; overridable via `app_config.audit_ttl_grace_days` at the caller (per D-05)"
    - "Timeout is 5000ms fixed (NOT configurable) (per D-06)"
    - "On timeout the probe returns `{ ok: null, message: 'probe skipped: ...' }` (per D-06)"
    - "Probe uses a NEW short-lived `nano(db_uri).use(prefix + 'managed_logs')` instance — does NOT import audit.js's `loglib` (per D-03)"
    - "Probe query uses a Mango `find` selector matching `expire_at < (Date.now() - GRACE_MS)` (per D-04 — Mango chosen over the design-doc-dependent `_design/cleanup` view because that view may not be installed yet on production)"
    - "Result `ok` semantics: `true` = no warning, `false` = emit WARN at caller, `null` = probe skipped (CouchDB unreachable, timeout, malformed URI) (per D-02)"
    - "WARN format at the caller is `console.log('⚠️ [warning] ' + probeResult.message)` (verbatim mirror of cert-probe line 215, per D-08)"
    - "WARN message body includes a doc-prefix-redacted `_id` (first 8 chars + ellipsis) plus the stale-by-days delta (per D-08; 8-char depth shared with OBS-01 D-21 redaction)"
    - "Caller wire-in in thinx-core.js is FIRE-AND-FORGET Promise (non-blocking; no `await`) (per D-07)"
    - "Caller wire-in is positioned right after the cert-probe call site at thinx-core.js:~216 (additive 5 lines; zero existing-behavior change) (per D-07 / D-12)"
    - "Caller failure path (rejected Promise) is non-fatal and emits NOTHING — `.catch(_e => { /* non-fatal */ })` keeps dev/test boots clean (per D-07)"
    - "NO Rollbar wiring — matches Phase 11 cert-probe stance (per D-09)"
    - "Fixture-based spec `spec/jasmine/ZZ-AuditTTLEvictionSpec.js` covers 4 branches: no-warn / single-warn / timeout / CouchDB-error (per D-10)"
    - "Spec uses synthetic doc objects ONLY — NO live CouchDB (per D-10)"
    - "Spec file uses `ZZ-` naming convention (integration spec touching live audit-DB infrastructure, even though mocked) (per D-11)"
    - "Compatibility constraint: the 5-line additive call site does NOT change any existing behavior in thinx-core.js (per D-12; cert-probe additive pattern at lines 211-216 is the literal template)"
  artifacts:
    - path: "lib/thinx/audit-ttl-probe.js"
      provides: "DETECT-only startup probe for audit-log TTL eviction drift"
      min_lines: 80
      contains: "async function probeTtlEviction"
    - path: "thinx-core.js"
      provides: "5-line additive fire-and-forget call site right after the cert-probe call site"
      contains: "audit-ttl-probe"
    - path: "spec/jasmine/ZZ-AuditTTLEvictionSpec.js"
      provides: "Fixture-based unit spec — 4 branches covered (no-warn / single-warn / timeout / CouchDB-error)"
      min_lines: 100
      contains: "probeTtlEviction"
  key_links:
    - from: "lib/thinx/audit-ttl-probe.js"
      to: "lib/thinx/cert-probe.js"
      via: "STRUCTURAL TEMPLATE — same PURE/READ-ONLY/NO-LOG/NEVER-throws contract"
      pattern: "PURE.*READ-ONLY.*NEVER"
    - from: "thinx-core.js (~line 217)"
      to: "lib/thinx/audit-ttl-probe.js"
      via: "require + fire-and-forget Promise"
      pattern: "audit-ttl-probe"
    - from: "lib/thinx/audit-ttl-probe.js"
      to: "lib/thinx/audit.js _buildRecord (expire_at field)"
      via: "Probe interrogates the `expire_at` field this method writes (90-day default)"
      pattern: "expire_at"
---

<objective>
OBS-02 — Land a DETECT-only startup probe that warns when the oldest live `managed_logs` doc has `expire_at < Date.now() - GRACE_MS` (default 7 days). The probe is a new PURE module `lib/thinx/audit-ttl-probe.js` (structurally mirroring Phase 11 `cert-probe.js` EXACTLY) wired additively into `thinx-core.js` right after the existing cert-probe call site at ~line 216, plus a fixture-based unit spec at `spec/jasmine/ZZ-AuditTTLEvictionSpec.js`.

Purpose: Surface CouchDB-side TTL eviction drift early so the operator can investigate. The v1.9 Phase 9 SEC-PII-02 forward-TTL writes `expire_at` on every audit record (90-day default per `_retentionDays`); this probe verifies CouchDB is actually evicting at the boundary. Analogous to THINX-CERT-CHECK-01 — DETECT-only, no auto-fix.

Output: 1 new module + 1 additive call-site edit + 1 new spec, plus a single GPG-signed commit on `thinx-staging`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-code-side-closure-helpers/12-CONTEXT.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- The STRUCTURAL TEMPLATE the new module mirrors (Phase 11). -->

From lib/thinx/cert-probe.js lines 1-38 (the module-header doc shape to MIRROR EXACTLY per D-01):
- Required prose: "DETECT-only", "PURE, READ-ONLY, NO-LOG probe", "caller decides whether to emit a warning", "NEVER throws — all error paths return a well-formed result object"
- Required hard-guarantee enumeration block (numbered 1, 2, 3 — verifiable by grep)
- Required "Allowlist duplication note" style sub-block (OBS-02 equivalent: "Self-contained CouchDB client — does NOT import audit.js loglib, per D-03")

From lib/thinx/cert-probe.js result-shape (the SHAPE PATTERN to mirror per D-02):
- cert-probe returns: `{ ok, leafIssuer, caContains, message }`
- audit-ttl-probe MIRRORS this with: `{ ok, oldestExpiredId, staleByDays, message }`
- where `ok === true` → no warning, `ok === false` → emit WARN at caller, `ok === null` → probe skipped (CouchDB unreachable, timeout, malformed URI)

From thinx-core.js:211-216 (the LITERAL caller pattern to mirror per D-07 / D-12):
```javascript
// THINX-CERT-CHECK-01: DETECT-only probe — emits a WARN when ca.pem does not contain the leaf's LE intermediate issuer.
const certProbe = require('./lib/thinx/cert-probe');
const probeResult = certProbe.probeCaFreshness(app_config.ssl_cert, app_config.ssl_ca);
if (!probeResult.ok) {
  console.log(`⚠️ [warning] ${probeResult.message}`);
}
```

From lib/thinx/audit.js:42-50 (the `expire_at` field shape — what the probe is interrogating):
```javascript
const retentionDays = this._retentionDays();
const expire_at = new Date(mtime.getTime() + retentionDays * 24 * 60 * 60 * 1000);
return {
  "message": message, "owner": owner, "date": mtime,
  "flags": Array.isArray(flag) ? flag : [flag],
  "expire_at": expire_at  // ← OBS-02 probe queries against this field
};
```

From lib/thinx/database.js + lib/thinx/globals.js (existing accessors — confirmed in audit.js:5-9):
- `const Globals = require("./globals.js"); const prefix = Globals.prefix();`
- `const Database = require("./database.js"); const db_uri = new Database().uri();`
- `Globals` is ALREADY in scope at thinx-core.js:44 (`const Globals = require("./lib/thinx/globals.js");`) — wire-in can reference it directly without a new top-level require
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write lib/thinx/audit-ttl-probe.js (PURE module mirroring cert-probe.js)</name>
  <files>lib/thinx/audit-ttl-probe.js</files>
  <read_first>
    - lib/thinx/cert-probe.js (the STRUCTURAL TEMPLATE — read all 195 lines; module-header doc lines 1-38 are required reading; the result-shape returns at lines 118-192 show the per-error-path return pattern)
    - lib/thinx/audit.js (lines 1-65 — `_buildRecord` writes `expire_at` field as a `Date` object; probe queries against the same field)
    - lib/thinx/database.js (Database class exposes `.uri()` — the probe's CouchDB URI resolution is via the `couchdbUri` parameter passed by the caller)
    - lib/thinx/globals.js (`Globals.prefix()` — DB-name prefix, e.g., `""` or `"test_"`; used for the `prefix + "managed_logs"` DB name)
    - .planning/phases/12-code-side-closure-helpers/12-CONTEXT.md (D-01 through D-12 — the locked OBS-02 invariants)
    - .planning/milestones/v1.9-phases/11-build-cert-hygiene/11-CONTEXT.md (Phase 11 D-01 / D-02 rationale: PURE module + "Allowlist duplication note"; OBS-02 inherits the same posture)
  </read_first>
  <behavior>
    - Test 1 (no-warn): synthetic find response with `docs: []` → result `{ ok: true, oldestExpiredId: null, staleByDays: null, message: null }`.
    - Test 2 (single-warn): synthetic find response with one doc whose `expire_at` is past `Date.now() - GRACE_MS` → result `{ ok: false, oldestExpiredId: <8-char-prefix>..., staleByDays: <positive number>, message: starts with "audit-log TTL eviction drift" }`.
    - Test 3 (timeout): mocked nano returns a never-resolving Promise → after ~250ms (using a low timeoutMs override for test speed) the probe returns `{ ok: null, message: "probe skipped: CouchDB query timed out at 250ms" }`.
    - Test 4 (CouchDB error): mocked nano `.find` rejects with `new Error("ECONNREFUSED")` → probe returns `{ ok: null, message: "probe skipped: ECONNREFUSED" }`.
    - Module NEVER throws — every error path returns a well-formed result object.
  </behavior>
  <action>
    Create `lib/thinx/audit-ttl-probe.js` as a NEW PURE module that mirrors cert-probe.js EXACTLY at the structural level. Required concrete shape:

    1. **Header doc comment** — must mirror cert-probe.js lines 1-38 structurally and contain these identifier strings (used by grep gates):
       - `"OBS-02 — Startup audit-log TTL eviction probe (DETECT-only)"`
       - `"PURE, READ-ONLY, NO-LOG probe"` — also `"NEVER throws"` as separate verbatim string
       - `"Mirrors lib/thinx/cert-probe.js structural template"` (acknowledgement of inheritance per D-01)
       - `"Probes the expire_at field written by lib/thinx/audit.js _buildRecord"` (provenance per D-02)
       - `"Self-contained CouchDB client — does NOT import audit.js loglib"` (per D-03; "Allowlist duplication note" equivalent)
       - Hard guarantees enumeration block mirroring cert-probe.js lines 20-29:
         - 1. Module body contains NO `console.log`, `console.warn`, or `rollbar` calls (verifiable by grep)
         - 2. Module uses ONLY `nano(...).use(...).find(...)` — never `.insert`, `.bulk`, `.destroy`, etc.
         - 3. `probeTtlEviction` NEVER throws — all error paths return a well-formed result object

    2. **Requires** (top of file, after the header comment):
       - `const nano = require("nano");` (already a runtime dep — see audit.js:9, notifier.js:10)

    3. **Constants**:
       - `const DEFAULT_GRACE_MS = 7 * 24 * 60 * 60 * 1000;` (7 days per D-05; verbatim arithmetic)
       - `const DEFAULT_TIMEOUT_MS = 5000;` (5s fixed per D-06; verbatim literal)

    4. **Helper `redactDocId(id)`** (per D-08 — 8-char prefix + ellipsis; matches OBS-01 D-21 redaction depth):
       - Signature: `function redactDocId(id) { ... }`
       - Behavior: if `id` is not a non-empty string, return `"<unknown>"`; else return `id.slice(0, 8) + "..."`.

    5. **Helper `withTimeout(promise, ms)`** (per D-06 — 5s timeout via `Promise.race`):
       - Signature: `function withTimeout(promise, ms) { ... }`
       - Behavior: returns `Promise.race([promise, timeoutPromise])` where `timeoutPromise` rejects with `new Error("__OBS_02_TIMEOUT__")` after `ms` milliseconds. Uses `setTimeout` + a `.finally` `clearTimeout` to avoid leaking the timer.
       - Sentinel error message: literal `"__OBS_02_TIMEOUT__"` — caller pattern-matches on this to distinguish timeout from other errors.

    6. **Main function `async function probeTtlEviction({ couchdbUri, dbName, graceMs, timeoutMs })`** per D-02:
       - Param defaults: `graceMs = graceMs || DEFAULT_GRACE_MS;`, `timeoutMs = timeoutMs || DEFAULT_TIMEOUT_MS;`
       - Guard: if `!couchdbUri || !dbName` → return `{ ok: null, oldestExpiredId: null, staleByDays: null, message: "probe skipped: missing couchdbUri or dbName" }`.
       - Step (a): construct `const db = nano(couchdbUri).use(dbName);` inside a `try/catch` (nano constructor can throw on malformed URI) — on catch return `{ ok: null, oldestExpiredId: null, staleByDays: null, message: "probe skipped: " + (e && e.message ? e.message : String(e)) }`.
       - Step (b): compute `const expiredBefore = Date.now() - graceMs;`.
       - Step (c): query the oldest expired-but-live doc using a Mango find (per D-04 — Mango chosen because the SEC-PII-02 `_design/cleanup` view may not be installed yet on production CouchDB):
         - Selector: `{ expire_at: { "$lt": new Date(expiredBefore).toISOString() } }`
         - Sort: `[{ expire_at: "asc" }]`
         - Limit: 1
         - Fields: `["_id", "expire_at"]` (minimal payload — avoids returning the full audit-log doc which could contain PII shapes)
         - Wrap the `db.find(...)` Promise in `withTimeout(queryPromise, timeoutMs)`.
       - Step (d): the `try/catch` around the `await withTimeout(...)`:
         - If caught error has `message === "__OBS_02_TIMEOUT__"` → return `{ ok: null, oldestExpiredId: null, staleByDays: null, message: "probe skipped: CouchDB query timed out at " + timeoutMs + "ms" }` (verbatim prefix per D-06).
         - Else → return `{ ok: null, oldestExpiredId: null, staleByDays: null, message: "probe skipped: " + (e && e.message ? e.message : String(e)) }` (graceful per D-06).
       - Step (e): parse the response (post-await):
         - If `!response || !response.docs || response.docs.length === 0` → return `{ ok: true, oldestExpiredId: null, staleByDays: null, message: null }` (no-warn case).
         - Else: extract `const doc = response.docs[0];`, compute `const expireAtMs = new Date(doc.expire_at).getTime();`, `const staleByDays = Math.floor((Date.now() - graceMs - expireAtMs) / (24 * 60 * 60 * 1000));`, build `const oldestExpiredId = redactDocId(doc._id);`, `const graceDays = Math.floor(graceMs / (24 * 60 * 60 * 1000));`, return `{ ok: false, oldestExpiredId, staleByDays, message: "audit-log TTL eviction drift: oldest expired-but-live doc _id=" + oldestExpiredId + " is stale by " + staleByDays + " days beyond GRACE_MS=" + graceDays + "d" }` (warn case per D-08).

    7. **Exports** (last line): `module.exports = { probeTtlEviction, redactDocId, DEFAULT_GRACE_MS, DEFAULT_TIMEOUT_MS };` (helpers exported for the spec to assert against without re-mocking nano).

    Locked constraints (verbatim — do NOT deviate):
    - NO `console.log` / `console.warn` / `rollbar` calls anywhere in the module body (per D-01 / D-08 / D-09). Comments and strings may mention these tokens for documentation, but executable code must not call them.
    - NO `nano.insert`, no `nano.bulk`, no `nano.destroy`, no `fs.writeFile*` — only `nano(...).use(...).find(...)` (read-only per D-01).
    - Module NEVER throws — every error path returns a result object (per D-02).
    - Signature matches D-02 EXACTLY: `async function probeTtlEviction({ couchdbUri, dbName, graceMs, timeoutMs })` → `{ ok, oldestExpiredId, staleByDays, message }`.
    - `ok` semantics: `true` = no warning, `false` = emit WARN, `null` = probe skipped (matches cert-probe per D-02).
    - GRACE_MS default 7 days; overridable via the caller-passed `graceMs` parameter (the caller in thinx-core.js reads `app_config.audit_ttl_grace_days` per D-05).
    - Timeout 5000ms fixed (per D-06).
    - Self-contained nano client per D-03 (no import of audit.js loglib).
    - Mango find (not view-based) per D-04.
    - Doc-ID redaction = first 8 chars + ellipsis (per D-08; matches OBS-01 D-21 depth).
    - NO Rollbar (per D-09).
  </action>
  <verify>
    <automated>node --check lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "async function probeTtlEviction" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "console\.log("</automated>
    <automated>grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "console\.warn("</automated>
    <automated>grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "rollbar"</automated>
    <automated>grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "\.insert("</automated>
    <automated>grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "\.bulk("</automated>
    <automated>grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "\.destroy("</automated>
    <automated>grep -c "OBS-02" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "PURE" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "NEVER throws" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "DEFAULT_GRACE_MS = 7 \* 24 \* 60 \* 60 \* 1000" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "DEFAULT_TIMEOUT_MS = 5000" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "probe skipped: CouchDB query timed out at " lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "audit-log TTL eviction drift" lib/thinx/audit-ttl-probe.js</automated>
    <automated>grep -c "module.exports = { probeTtlEviction" lib/thinx/audit-ttl-probe.js</automated>
    <automated>wc -l lib/thinx/audit-ttl-probe.js | awk '{print ($1 >= 80) ? "ok" : "short"}'</automated>
  </verify>
  <acceptance_criteria>
    - `node --check lib/thinx/audit-ttl-probe.js` exits 0
    - `grep -c "async function probeTtlEviction" lib/thinx/audit-ttl-probe.js` = 1 (signature present per D-02)
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "console\.log("` = 0 (no logging in executable code per D-01; comment-stripped grep avoids false positives on header prose)
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "console\.warn("` = 0 (per D-01)
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "rollbar"` = 0 (no Rollbar per D-09)
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "\.insert("` = 0 (READ-ONLY per D-01)
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "\.bulk("` = 0 (READ-ONLY per D-01)
    - `grep -v '^[[:space:]]*\*\|^[[:space:]]*//' lib/thinx/audit-ttl-probe.js | grep -c "\.destroy("` = 0 (READ-ONLY per D-01)
    - `grep -c "OBS-02" lib/thinx/audit-ttl-probe.js` ≥ 1 (requirement ID present in header)
    - `grep -c "PURE" lib/thinx/audit-ttl-probe.js` ≥ 1 (per D-01)
    - `grep -c "NEVER throws" lib/thinx/audit-ttl-probe.js` ≥ 1 (per D-02)
    - `grep -c "DEFAULT_GRACE_MS = 7 \* 24 \* 60 \* 60 \* 1000" lib/thinx/audit-ttl-probe.js` = 1 (per D-05)
    - `grep -c "DEFAULT_TIMEOUT_MS = 5000" lib/thinx/audit-ttl-probe.js` = 1 (per D-06)
    - `grep -c "probe skipped: CouchDB query timed out at " lib/thinx/audit-ttl-probe.js` = 1 (verbatim per D-06)
    - `grep -c "audit-log TTL eviction drift" lib/thinx/audit-ttl-probe.js` = 1 (per D-08 warn body)
    - `grep -c "Mango\|find(" lib/thinx/audit-ttl-probe.js` ≥ 1 (Mango find approach per D-04)
    - `grep -c "audit.js loglib\|loglib" lib/thinx/audit-ttl-probe.js` ≤ 1 (only doc-comment reference allowed; no actual import per D-03)
    - `grep -c "module.exports = { probeTtlEviction" lib/thinx/audit-ttl-probe.js` = 1
    - `wc -l lib/thinx/audit-ttl-probe.js` ≥ 80 (per must_haves.artifacts.min_lines)
  </acceptance_criteria>
  <done>
    `lib/thinx/audit-ttl-probe.js` exists, syntactically clean, mirrors cert-probe.js structural template per D-01, exports `probeTtlEviction` with the locked signature per D-02. Module is PURE / READ-ONLY / NO-LOG / NEVER-throws (verified by comment-stripped grep gates). Self-contained nano client per D-03, Mango find per D-04, 7-day GRACE_MS default per D-05, 5000ms timeout per D-06, 8-char doc-ID redaction per D-08, no Rollbar per D-09.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire 5-line additive fire-and-forget call into thinx-core.js</name>
  <files>thinx-core.js</files>
  <read_first>
    - thinx-core.js lines 40-50 (confirm `const Globals = require("./lib/thinx/globals.js");` is already in scope at line 44 — needed so the wire-in can reference `Globals.prefix()` without a new import)
    - thinx-core.js lines 200-220 (the existing cert-probe call site at lines 211-216 — the LITERAL pattern to mirror per D-07 / D-12)
    - lib/thinx/audit-ttl-probe.js (the freshly-written module from Task 1 — confirms the export signature `{ probeTtlEviction, redactDocId, DEFAULT_GRACE_MS, DEFAULT_TIMEOUT_MS }`)
    - lib/thinx/database.js (Database class — `new Database().uri()` is the standard URI accessor; matches audit.js:8 + notifier.js:9)
    - .planning/phases/12-code-side-closure-helpers/12-CONTEXT.md (D-07 + D-12 — additive 5-line call site, FIRE-AND-FORGET, non-blocking)
  </read_first>
  <action>
    Edit `thinx-core.js` exactly once. Locate the cert-probe call site at lines 211-216 (currently ending with the closing brace of `if (!probeResult.ok) { console.log(...); }`). Insert the OBS-02 wire-in IMMEDIATELY AFTER line 216 and BEFORE line 218 (`let caCert = read(app_config.ssl_ca, 'utf8');`).

    Exact lines to insert (5 functional lines + 1 header comment line; additive; fire-and-forget per D-07; NO `await`):

    ```
              // OBS-02: DETECT-only probe — emits a WARN when the oldest live managed_logs doc is stale-expired beyond GRACE_MS (default 7 days).
              const auditTtlProbe = require('./lib/thinx/audit-ttl-probe');
              const _Database_obs02 = require('./lib/thinx/database');
              const _graceDays = (app_config && typeof app_config.audit_ttl_grace_days === 'number' && app_config.audit_ttl_grace_days > 0) ? app_config.audit_ttl_grace_days : 7;
              auditTtlProbe.probeTtlEviction({ couchdbUri: new _Database_obs02().uri(), dbName: Globals.prefix() + 'managed_logs', graceMs: _graceDays * 24 * 60 * 60 * 1000 }).then(r => { if (r.ok === false) console.log('⚠️ [warning] ' + r.message); }).catch(_e => { /* non-fatal */ });
    ```

    Locked constraints (verbatim per D-07 / D-08 / D-12):
    - **Position:** IMMEDIATELY after the cert-probe call site's closing brace (after line 216, before the `let caCert = ...` line). Use the same indentation as the surrounding code (14 spaces — visible from cert-probe call at line 212).
    - **FIRE-AND-FORGET:** NO `await`. The `.then(...)` chain handles the result; the `.catch(_e => { /* non-fatal */ })` swallows any rejection so dev/test boots stay clean (per D-07).
    - **WARN format:** `console.log('⚠️ [warning] ' + r.message)` — verbatim mirror of cert-probe line 215 (per D-08).
    - **app_config guard:** `app_config.audit_ttl_grace_days` is read via a defensive guard (per D-05); falls back to 7 days if missing/invalid (`undefined`, non-number, zero, or negative).
    - **DB name:** `Globals.prefix() + 'managed_logs'` — matches `audit.js:9` (`loglib = require("nano")(db_uri).use(prefix + "managed_logs")`).
    - **Globals scope:** `Globals` is already imported at thinx-core.js:44 (confirmed by `read_first`); no new top-level require needed for it.
    - **Variable naming:** Local-scope `_` prefix (`_Database_obs02`, `_graceDays`) avoids colliding with the surrounding closure's identifiers.
    - **ZERO behavior change** to any existing line (per D-12). The block is purely additive.

    Do NOT touch any other line in `thinx-core.js`. Do NOT refactor the existing cert-probe call. Do NOT change the SSL block. Do NOT add a top-level `require` (the require is intentionally inside the call block — same pattern as `certProbe` at line 212).
  </action>
  <verify>
    <automated>node --check thinx-core.js</automated>
    <automated>grep -c "audit-ttl-probe" thinx-core.js</automated>
    <automated>grep -c "OBS-02:" thinx-core.js</automated>
    <automated>grep -c "auditTtlProbe.probeTtlEviction" thinx-core.js</automated>
    <automated>grep -c "app_config.audit_ttl_grace_days" thinx-core.js</automated>
    <automated>grep -c "Globals.prefix() + 'managed_logs'" thinx-core.js</automated>
    <automated>grep -cE "\.then\(r => \{ if \(r\.ok === false\) console\.log\('⚠️ \[warning\] ' \+ r\.message\); \}\)" thinx-core.js</automated>
    <automated>grep -c "audit-ttl-probe.*await" thinx-core.js</automated>
    <automated>awk '/OBS-02: DETECT-only/,/let caCert = /' thinx-core.js | wc -l</automated>
    <automated>grep -B5 "auditTtlProbe.probeTtlEviction" thinx-core.js | grep -c "await"</automated>
    <automated>node -e "const src = require('fs').readFileSync('thinx-core.js', 'utf8'); const m = src.match(/auditTtlProbe\.probeTtlEviction\([\s\S]{0,500}\)/); process.exit(m && /await/.test(m[0]) ? 1 : 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check thinx-core.js` exits 0
    - `grep -c "audit-ttl-probe" thinx-core.js` = 1 (single insertion; module referenced exactly once)
    - `grep -c "OBS-02:" thinx-core.js` ≥ 1 (header comment present)
    - `grep -c "auditTtlProbe.probeTtlEviction" thinx-core.js` = 1 (call site present)
    - `grep -c "app_config.audit_ttl_grace_days" thinx-core.js` = 1 (per D-05)
    - `grep -c "Globals.prefix() + 'managed_logs'" thinx-core.js` = 1 (DB name matches audit.js:9 pattern, per D-03)
    - `grep -cE "\.then\(r => \{ if \(r\.ok === false\) console\.log\('⚠️ \[warning\] ' \+ r\.message\); \}\)" thinx-core.js` = 1 (fire-and-forget WARN handler per D-07 + D-08)
    - `grep -c "audit-ttl-probe.*await" thinx-core.js` = 0 (no `await` on the same line — fire-and-forget per D-07; same-line grep — see Node-based check below for the multi-line guarantee)
    - **POSITIONAL gate (BLOCK-04 tightening)**: `awk '/OBS-02: DETECT-only/,/let caCert = /' thinx-core.js | wc -l` equals exactly **8** lines (1 OBS-02 header comment + 5 functional wire-in lines + 1 blank separator + 1 `let caCert = read(app_config.ssl_ca, 'utf8');` line; per D-07 / D-12 positional lock right after the cert-probe call site at lines 211-216). If the inserted block ends up a different size due to formatting, the executor must adjust the OBS-02 wire-in until this count is exactly 8. This range starts at the OBS-02 header comment and ends at the next existing line (`let caCert = ...` at the original line 218), which precisely bounds the insertion. This positional gate REPLACES the prior loose `THINX-CERT-CHECK-01..OBS-02` range gate (which allowed the impossible "OBS-02 lands INSIDE cert-probe block" range).
    - **PRECEDING-CONTEXT await gate (BLOCK-04 tightening)**: `grep -B5 "auditTtlProbe.probeTtlEviction" thinx-core.js | grep -c "await"` equals **0** — no `await` keyword in the 5 lines preceding the probe call (per D-07 fire-and-forget). This catches the "executor accidentally wrapped the call in `await (...)` on a preceding line" failure mode that the same-line grep above misses.
    - **MULTI-LINE await Node check (BLOCK-04 tightening)**: `node -e "const src = require('fs').readFileSync('thinx-core.js', 'utf8'); const m = src.match(/auditTtlProbe\.probeTtlEviction\([\s\S]{0,500}\)/); process.exit(m && /await/.test(m[0]) ? 1 : 0)"` exits **0** (no `await` keyword found within 500 characters of the `auditTtlProbe.probeTtlEviction(` substring — catches `await` placed on a line BEFORE the call within the same expression, e.g., `const _r = await
  auditTtlProbe.probeTtlEviction(...)`. Same-line grep misses this; the Node check confirms it across newlines; enforces D-07 fire-and-forget non-blocking semantic).
    - `git diff thinx-core.js | grep -cE "^[+]" | xargs -I{} echo {}` count of `+` lines is exactly 6 (one header comment + five functional lines; additive only per D-12) — executor verifies via `git diff thinx-core.js`
  </acceptance_criteria>
  <done>
    `thinx-core.js` has a 6-line additive insertion (1 comment + 5 functional lines) immediately after the cert-probe call site at lines 211-216 (per D-07 / D-12). The wire-in is FIRE-AND-FORGET (no `await`, per D-07), reads `app_config.audit_ttl_grace_days` with a 7-day fallback per D-05, uses `Globals.prefix() + 'managed_logs'` DB name matching audit.js:9 (per D-03), and emits the WARN via `console.log('⚠️ [warning] ' + r.message)` verbatim per D-08. Zero existing-line modifications per D-12.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write spec/jasmine/ZZ-AuditTTLEvictionSpec.js (fixture-based, 4 branches)</name>
  <files>spec/jasmine/ZZ-AuditTTLEvictionSpec.js</files>
  <read_first>
    - lib/thinx/audit-ttl-probe.js (the SUT — read after Task 1 commit; spec asserts against `probeTtlEviction` exports)
    - spec/jasmine/ZZ-CertProbeSpec.js (Phase 11 precedent IF it exists in the tree — fixture-based, 4 `it()` blocks pattern; if not present, fall back to the structural pattern in TESTING.md "Test Structure")
    - .planning/codebase/TESTING.md (sections "Test Structure", "Mocking", "Anti-patterns to avoid", "Common Patterns: Console markers" — no sinon/jest; inline-object mock pattern; `🚸 [chai]` markers)
    - .planning/phases/12-code-side-closure-helpers/12-CONTEXT.md (D-10 + D-11 — fixture-based 4-branch spec; ZZ- prefix per the integration-suite naming pattern; "Claude's Discretion: synthetic CouchDB doc objects inline OR in spec/fixtures/audit-ttl/" — planner picks INLINE, no static fixture files needed because the objects are trivially small)
  </read_first>
  <behavior>
    - Test 1 (no-warn): mocked `nano(...).use(...).find(...)` resolves to `{ docs: [] }` → `probeTtlEviction` returns `{ ok: true, oldestExpiredId: null, staleByDays: null, message: null }`.
    - Test 2 (single-warn): mocked find resolves to `{ docs: [{ _id: "deadbeef-cafe-1234-...-fullhash", expire_at: new Date(Date.now() - 30*24*60*60*1000).toISOString() }] }` with a default 7d GRACE → `result.ok === false`, `result.oldestExpiredId === "deadbeef..."`, `result.staleByDays >= 1`, `result.message` contains `"audit-log TTL eviction drift"`.
    - Test 3 (timeout): mocked find returns a never-resolving Promise (`new Promise(() => {})`); spec calls `probeTtlEviction({..., timeoutMs: 250})` → result `{ ok: null, message: "probe skipped: CouchDB query timed out at 250ms" }`. Spec timeout for this `it()` is `}, 5000);` (well under the Jasmine 30s default cap, well above the 250ms probe timeout).
    - Test 4 (CouchDB error): mocked find rejects with `new Error("ECONNREFUSED")` → result `{ ok: null, message: "probe skipped: ECONNREFUSED" }`.
  </behavior>
  <action>
    Create `spec/jasmine/ZZ-AuditTTLEvictionSpec.js` (per D-10 + D-11 — `ZZ-` prefix matches the integration-spec naming convention).

    Mock pattern (planner discretion per CONTEXT.md "Claude's Discretion") — use `require.cache` substitution to swap `nano` BEFORE requiring `audit-ttl-probe.js`. This mirrors the RedactSlackSpec.js pattern used in Plan 12-02 (consistency across the phase). Concrete shape:

    1. Top-level requires + nano-mock factory:
       ```
       const nanoPath = require.resolve("nano");
       let mockFindBehavior = null; // set per-test: function() returning Promise OR throwing
       const _origNanoExports = require.cache[nanoPath] ? require.cache[nanoPath].exports : require("nano");
       require.cache[nanoPath] = {
         id: nanoPath,
         filename: nanoPath,
         loaded: true,
         exports: function nanoMock(_uri) {
           return {
             use: (_dbName) => ({
               find: (_query) => {
                 if (typeof mockFindBehavior !== "function") {
                   return Promise.resolve({ docs: [] });
                 }
                 try { return mockFindBehavior(); } catch (e) { return Promise.reject(e); }
               }
             })
           };
         }
       };
       const chai = require("chai");
       const expect = require("chai").expect;
       const probe = require("../../lib/thinx/audit-ttl-probe");
       ```

    2. `describe("OBS-02 audit-log TTL eviction probe", function () { ... })` body:
       - `beforeAll(() => { console.log("🚸 [chai] >>> running OBS-02 audit-log TTL eviction spec"); });` (per D-35 marker convention)
       - `afterAll(() => { console.log("🚸 [chai] <<< completed OBS-02 audit-log TTL eviction spec"); });`
       - `beforeEach(() => { mockFindBehavior = null; });`
       - 4 `it()` blocks matching the 4 behaviors above (per D-10).

    3. Test 1 — no-warn (synchronous-style with done):
       ```
       it("returns ok:true when no expired-live docs exist", function (done) {
         mockFindBehavior = () => Promise.resolve({ docs: [] });
         probe.probeTtlEviction({ couchdbUri: "http://stub:5984", dbName: "managed_logs" })
           .then((r) => {
             expect(r.ok).to.equal(true);
             expect(r.oldestExpiredId).to.equal(null);
             expect(r.staleByDays).to.equal(null);
             expect(r.message).to.equal(null);
             done();
           });
       }, 30000);
       ```

    4. Test 2 — single-warn:
       ```
       it("returns ok:false with redacted _id + staleByDays when an expired-live doc is found", function (done) {
         const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString();
         mockFindBehavior = () => Promise.resolve({ docs: [{ _id: "deadbeef-cafe-1234-5678-fullhash", expire_at: thirtyDaysAgo }] });
         probe.probeTtlEviction({ couchdbUri: "http://stub:5984", dbName: "managed_logs" })
           .then((r) => {
             expect(r.ok).to.equal(false);
             expect(r.oldestExpiredId).to.equal("deadbeef...");
             expect(r.staleByDays).to.be.at.least(1);
             expect(r.message.indexOf("audit-log TTL eviction drift")).to.equal(0);
             expect(r.message.indexOf("deadbeef...")).to.be.greaterThan(0);
             done();
           });
       }, 30000);
       ```

    5. Test 3 — timeout:
       ```
       it("returns ok:null with timeout message when CouchDB does not respond", function (done) {
         mockFindBehavior = () => new Promise(() => { /* never resolves */ });
         probe.probeTtlEviction({ couchdbUri: "http://stub:5984", dbName: "managed_logs", timeoutMs: 250 })
           .then((r) => {
             expect(r.ok).to.equal(null);
             expect(r.message).to.equal("probe skipped: CouchDB query timed out at 250ms");
             done();
           });
       }, 5000);
       ```

    6. Test 4 — CouchDB error:
       ```
       it("returns ok:null with graceful skip message on CouchDB error", function (done) {
         mockFindBehavior = () => Promise.reject(new Error("ECONNREFUSED"));
         probe.probeTtlEviction({ couchdbUri: "http://stub:5984", dbName: "managed_logs" })
           .then((r) => {
             expect(r.ok).to.equal(null);
             expect(r.message).to.equal("probe skipped: ECONNREFUSED");
             done();
           });
       }, 30000);
       ```

    Locked constraints:
    - File path: `spec/jasmine/ZZ-AuditTTLEvictionSpec.js` (`ZZ-` prefix per D-11).
    - No sinon, no jest — only inline `require.cache` substitution per TESTING.md "Anti-patterns to avoid".
    - All 4 tests in the same `describe("OBS-02 audit-log TTL eviction probe", ...)` block per D-10.
    - `🚸 [chai]` markers per D-35 / TESTING.md.
    - Per-`it()` timeout = `}, 30000);` for 3 tests, `}, 5000);` for the timeout test (5s is sufficient because the probe timeoutMs is overridden to 250ms; 30s would unnecessarily slow the suite on CI).
    - Discoverable by Jasmine via `spec/jasmine/*[sS]pec.js` glob.
    - Spec consumes ZERO live infrastructure (no CouchDB, no Redis, no MQTT) per D-10. NO `bootstrap.thx` required.
    - Synthetic doc shapes are INLINE (no `spec/fixtures/audit-ttl/` directory needed — planner discretion per CONTEXT.md, simpler given the trivial corpus size).
  </action>
  <verify>
    <automated>node --check spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "describe(\"OBS-02 audit-log TTL eviction probe\"" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "^  it(" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "require.cache\[nanoPath\]" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "🚸 \[chai\] >>> running OBS-02 audit-log TTL eviction spec" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "🚸 \[chai\] <<< completed OBS-02 audit-log TTL eviction spec" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "probe.probeTtlEviction" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "require('sinon')" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "require('jest" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "bootstrap.thx" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "probe skipped: CouchDB query timed out at 250ms" spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
    <automated>grep -c "deadbeef..." spec/jasmine/ZZ-AuditTTLEvictionSpec.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check spec/jasmine/ZZ-AuditTTLEvictionSpec.js` exits 0
    - `grep -c "describe(\"OBS-02 audit-log TTL eviction probe\"" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 1
    - `grep -c "^  it(" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 4 (four `it()` blocks per D-10)
    - `grep -c "require.cache\[nanoPath\]" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 1 (nano-mock pattern present per D-10)
    - `grep -c "🚸 \[chai\] >>> running OBS-02 audit-log TTL eviction spec" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 1
    - `grep -c "🚸 \[chai\] <<< completed OBS-02 audit-log TTL eviction spec" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 1
    - `grep -c "probe.probeTtlEviction" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` ≥ 4 (one call per `it()` minimum)
    - `grep -c "require('sinon')" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 0 (no sinon per TESTING.md)
    - `grep -c "require('jest" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 0 (no jest per TESTING.md)
    - `grep -c "bootstrap.thx" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 0 (NO live infrastructure per D-10)
    - `grep -c "probe skipped: CouchDB query timed out at 250ms" spec/jasmine/ZZ-AuditTTLEvictionSpec.js` = 1 (timeout-branch assertion verbatim, per D-06)
    - `grep -c "deadbeef..." spec/jasmine/ZZ-AuditTTLEvictionSpec.js` ≥ 1 (8-char redaction verified per D-08)
    - File path is `spec/jasmine/ZZ-AuditTTLEvictionSpec.js` (per D-11 `ZZ-` prefix)
    - `wc -l spec/jasmine/ZZ-AuditTTLEvictionSpec.js` ≥ 100 (per must_haves.artifacts.min_lines)
  </acceptance_criteria>
  <done>
    `spec/jasmine/ZZ-AuditTTLEvictionSpec.js` exists, syntactically clean, contains 4 `it()` blocks covering the no-warn / single-warn / timeout / CouchDB-error branches per D-10. Uses `require.cache` nano substitution (matching plan 12-02's RedactSlackSpec pattern). No sinon/jest. No `bootstrap.thx` per D-10. ZZ- prefix per D-11. Discoverable by Jasmine.
  </done>
</task>

<task type="auto">
  <name>Task 4: GPG-sign atomic commit (3 files)</name>
  <files>lib/thinx/audit-ttl-probe.js, thinx-core.js, spec/jasmine/ZZ-AuditTTLEvictionSpec.js</files>
  <read_first>
    - .planning/phases/12-code-side-closure-helpers/12-CONTEXT.md (D-42 GPG-sign default)
    - .planning/STATE.md (commit-message style — recent: `40c755e0 fix(worker): syncing submodule improvements`, `474995ec docs(req): mark THINX-CERT-CHECK-01 complete (deferred mark from SDK auto-update)`)
  </read_first>
  <action>
    Stage exactly these 3 files and create one GPG-signed commit on `thinx-staging`:
    - `lib/thinx/audit-ttl-probe.js`
    - `thinx-core.js`
    - `spec/jasmine/ZZ-AuditTTLEvictionSpec.js`

    Commit message (HEREDOC):
    `feat(OBS-02): audit-log TTL eviction startup probe (DETECT-only)`

    Body summarizes:
    - new lib/thinx/audit-ttl-probe.js — PURE / READ-ONLY / NO-LOG / NEVER-throws (mirrors Phase 11 cert-probe.js template per D-01)
    - 5-line additive fire-and-forget call site in thinx-core.js right after the cert-probe call site per D-07
    - GRACE_MS 7d default (overridable via app_config.audit_ttl_grace_days), 5s timeout fixed
    - WARN format mirrors cert-probe line 215: `⚠️ [warning] <message>`
    - doc-ID redaction = first 8 chars + ellipsis per D-08
    - NO Rollbar per D-09
    - spec/jasmine/ZZ-AuditTTLEvictionSpec.js — 4 fixture-based `it()` blocks (no-warn / single-warn / timeout / CouchDB-error) per D-10
    - addresses OBS-02

    Do NOT stage anything else (no SUMMARY.md yet — that's a separate post-execution artifact). Do NOT use `--no-verify`. Do NOT amend.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -c "feat(OBS-02)"</automated>
    <automated>git log -1 --show-signature 2>&amp;1 | grep -cE "gpg: Signature|Good signature"</automated>
    <automated>git diff HEAD~1 --name-only | sort | tr '\n' ',' | grep -c "lib/thinx/audit-ttl-probe\.js,spec/jasmine/ZZ-AuditTTLEvictionSpec\.js,thinx-core\.js,"</automated>
    <automated>git diff HEAD~1 --name-only | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `^feat\(OBS-02\): `
    - `git log -1 --show-signature 2>&1` contains a `gpg:` line (signature present per D-42)
    - `git diff HEAD~1 --name-only` lists exactly 3 files: `lib/thinx/audit-ttl-probe.js`, `spec/jasmine/ZZ-AuditTTLEvictionSpec.js`, `thinx-core.js` (per D-38 file-touch matrix)
    - No file outside that triple is touched (verifies parallel-safety with plans 12-01 and 12-02, per D-39)
  </acceptance_criteria>
  <done>
    Single atomic GPG-signed commit `feat(OBS-02): audit-log TTL eviction startup probe (DETECT-only)` lands on `thinx-staging`. Exactly 3 files in the commit per D-38 / D-39 / D-42. Zero overlap with plans 12-01 and 12-02 file lists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| THiNX bootstrap → CouchDB managed_logs (read) | The probe issues exactly one Mango `find` against `managed_logs`; no writes, no design-doc dependencies |
| audit-ttl-probe module → caller (thinx-core.js) | The probe returns a result object; the caller decides whether to emit a WARN. PURE contract per D-01 |
| Probe result → console stdout (via caller) | The caller emits `⚠️ [warning] <message>` — the message contains a doc-ID prefix (8 chars) and a stale-by-days delta. No PII exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | Doc _id in WARN message | mitigate | Doc-ID redaction = first 8 chars + ellipsis per D-08 (matches OBS-01 D-21 redaction depth); spec test 2 asserts the redacted form `deadbeef...` is in the message and the full ID is NOT |
| T-12-03-02 | Information Disclosure | CouchDB connection URI in module logs | mitigate | Module is NO-LOG per D-01 (verified by comment-stripped grep gates); the URI is consumed only as input to `nano()` and never serialized into a log line or result.message |
| T-12-03-03 | Information Disclosure | Mango find returns full audit doc | mitigate | Mango query specifies `fields: ["_id", "expire_at"]` — limits the response payload to the minimum needed for the warn computation; even if a doc's `message` field contains a PII shape (the very thing SEC-PII-02 redacts), the probe never sees it |
| T-12-03-04 | Denial of Service | CouchDB hangs during probe | mitigate | 5000ms fixed timeout per D-06; on timeout the probe returns `{ ok: null }` and the caller's `.catch(_e => { /* non-fatal */ })` ensures the boot sequence is not blocked (per D-07 FIRE-AND-FORGET semantics) |
| T-12-03-05 | Denial of Service | Probe rejection blocks boot | mitigate | Caller uses fire-and-forget Promise per D-07 — `await` is forbidden; spec test 3 + grep gate `grep -c "audit-ttl-probe.*await" thinx-core.js` = 0 enforces this at the source level |
| T-12-03-06 | Tampering | Probe writes to managed_logs | mitigate | Module is READ-ONLY per D-01; comment-stripped grep gates verify NO `.insert(` / `.bulk(` / `.destroy(` calls. Mango `find` is the only nano operation invoked |
| T-12-03-07 | Repudiation | WARN message logged but ignored | accept | DETECT-only stance per D-01 / D-09; surfacing the WARN to Slack / Rollbar / Prometheus is deferred to v1.11 (see CONTEXT.md "Deferred Ideas: Probe metric/log to Prometheus or InfluxDB") |
| T-12-03-08 | Spoofing | Mocked nano in spec leaks into runtime | mitigate | `require.cache` substitution is scoped to the spec file only; production `require("nano")` in audit-ttl-probe.js resolves the real module at module load time, before any spec runs |
| T-12-03-SC | Tampering | npm install (no new deps in this plan) | mitigate | This plan adds ZERO new npm deps — `nano` is already in `package.json` (used widely: audit.js:9, notifier.js:10, etc.). No package legitimacy audit required for this plan. |

Severity assessment: T-12-03-01 (doc-ID exposure) is the load-bearing threat, mitigated by D-08 8-char redaction + spec assertion. T-12-03-04 / T-12-03-05 (DoS via probe hang) are mitigated by D-06 5s timeout + D-07 fire-and-forget. All threats `low` severity, none reach `high`. Plan is NOT blocked by the security gate.

No npm/pip/cargo install tasks in this plan — Package Legitimacy Gate does not apply.
</threat_model>

<verification>
After all 4 tasks complete:

1. **Static gates (local, per D-41 Test-env ACCEPT):**
   - `node --check lib/thinx/audit-ttl-probe.js` exits 0
   - `node --check thinx-core.js` exits 0
   - `node --check spec/jasmine/ZZ-AuditTTLEvictionSpec.js` exits 0
   - All `<acceptance_criteria>` grep gates pass for all 4 tasks
   - Comment-stripped grep gates confirm NO logging / NO mutation / NO Rollbar in the probe module body
   - `git log -1 --show-signature` shows the GPG signature

2. **Canonical green-gate (CI per D-41):**
   - CircleCI Jasmine inside the Docker test image runs `spec/jasmine/ZZ-AuditTTLEvictionSpec.js` PASS as part of the `ZZ-*` integration-spec set
   - All 4 `it()` blocks pass
   - thinx-core.js boots without blocking on the probe (FIRE-AND-FORGET per D-07 ensures any probe rejection is non-fatal during the CI boot)

3. **Production-readiness checks:**
   - On a CouchDB with no expired-live docs → no WARN emitted at boot (clean boot)
   - On a CouchDB with a stale-expired doc → exactly one WARN line `⚠️ [warning] audit-log TTL eviction drift: oldest expired-but-live doc _id=<8chars>... is stale by <N> days beyond GRACE_MS=7d`
   - On unreachable CouchDB → probe silently skips, no WARN, no boot block
</verification>

<success_criteria>
- OBS-02 ROADMAP requirement closed:
  - (a) Fixture-based unit spec covers no-warn / single-warn / timeout / CouchDB-error → 4 `it()` blocks per D-10
  - (b) Startup with no expired-live docs emits no warning → spec test 1 + module returns `ok: true` (caller skips WARN, per D-02)
  - (c) Startup with a stale-expired doc beyond GRACE_MS emits a single WARN naming the doc-prefix-redacted `_id` + stale-by-days delta → spec test 2 + caller emits `console.log('⚠️ [warning] ' + r.message)` per D-08
  - (d) Probe is timeout-bounded (5s fixed per D-06), non-fatal (caller `.catch` swallows per D-07), does NOT block boot (fire-and-forget per D-07) → grep gates `audit-ttl-probe.*await` = 0
- Module mirrors Phase 11 cert-probe.js EXACTLY at the module level per D-01 — verified by grep gates on `PURE`, `NEVER throws`, no `console.log`, no `.insert/.bulk/.destroy`, no `rollbar`
- Self-contained nano client per D-03; Mango find per D-04; 7-day GRACE per D-05; 5s timeout per D-06
- 5-line additive call site at thinx-core.js per D-07 / D-12 — verified by `grep -c "audit-ttl-probe" thinx-core.js` = 1
- Single GPG-signed commit lands on `thinx-staging` per D-42
- Zero file overlap with plans 12-01 and 12-02 (verifies D-38 / D-39 parallel-safety):
  - 12-01: `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js`
  - 12-02: `scripts/redact-managed-logs.js`, `spec/jasmine/RedactSlackSpec.js`, `.planning/REQUIREMENTS.md`
  - 12-03: `lib/thinx/audit-ttl-probe.js`, `thinx-core.js`, `spec/jasmine/ZZ-AuditTTLEvictionSpec.js`
</success_criteria>

<output>
Create `.planning/phases/12-code-side-closure-helpers/12-03-SUMMARY.md` when done.
</output>
