---
phase: 12-code-side-closure-helpers
phase_id: 12-code-side-closure-helpers
plan: 1
plan_id: 12-01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js
autonomous: true
requirements:
  - TEST-WS-01
requirements_addressed:
  - TEST-WS-01
user_setup: []

must_haves:
  truths:
    - "New Jasmine spec exists at spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js (per D-26)"
    - "Spec uses the `ws` package's `new WebSocket(url)` client (NOT chai-http, per AGENTS.md:82-92 LOCK) (per D-27)"
    - "Spec uses shared bootstrap via `require('../helpers/bootstrap')` and reads `bootstrap.thx` (per D-28)"
    - "Spec does NOT call `thx.server.close()` — bootstrap.js owns server lifecycle (per D-28, D-33)"
    - "Spec contains exactly 2 positive `it()` blocks: `/<envi.oid>` and `/<envi.oid>/<Date.now()>` (per D-29)"
    - "Port is discovered via `bootstrap.thx.server.address().port` (per D-30)"
    - "Positive assertion is `ws.on('open', ...)` firing within 5s → `expect(true).to.equal(true); done();` (per D-31)"
    - "Each async `it()` ends with `}, 30000);` per TESTING.md convention (per D-34)"
    - "Cleanup via `ws.close()` in `afterEach` (per D-33)"
    - "Console markers `🚸 [chai] >>> running WebSocket Handshake (rtm) spec` / `🚸 [chai] <<< completed WebSocket Handshake (rtm) spec` present (per D-35)"
    - "Spec header comment documents the regression target at `thinx-core.js:466` (`server.on('upgrade')` registration) (per D-32)"
    - "Negative-case proof is IMPLICIT via timeout — no separate negative `it()` block (per D-32)"
    - "Owner ID comes from `envi.oid` in `spec/_envi.json` — spec does NOT mutate that fixture (per D-36)"
    - "Spec covers in-process upgrade handler only; no live rtm-edge probe in this phase (deferred to Phase 13 OPS-EXEC-01, per D-37)"
  artifacts:
    - path: "spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js"
      provides: "WebSocket handshake CI smoke probe — regression coverage for SEC-WS-01 in-process upgrade handler"
      min_lines: 60
      contains: "ws.on('open'"
  key_links:
    - from: "spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js"
      to: "spec/helpers/bootstrap.js"
      via: "require('../helpers/bootstrap')"
      pattern: "require\\('\\.\\./helpers/bootstrap'\\)"
    - from: "spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js"
      to: "thinx-core.js:466 (server.on('upgrade'))"
      via: "implicit handshake against bootstrap.thx.server"
      pattern: "thinx-core\\.js:466"
---

<objective>
TEST-WS-01 — Land a Jasmine spec that exercises the rtm-style `/<owner>(/<timestamp>)?` WebSocket upgrade against the in-process THiNX server so any future regression of the `server.on('upgrade')` handler at `thinx-core.js:466` surfaces at CI rather than user-report.

Purpose: Give Phase 13's swarm-host nginx fix (OPS-EXEC-01) CI regression coverage from day one. The actual rtm-edge handshake remains the operator-runbook concern; this spec covers the in-process upgrade handler only.

Output: 1 new spec file `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` plus a single GPG-signed commit on `thinx-staging`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-code-side-closure-helpers/12-CONTEXT.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Existing exports / fixture fields the spec consumes. -->

From spec/helpers/bootstrap.js (module.exports):
```javascript
// bootstrap.thx is a THiNX instance whose `.server` is an http.Server
// already bound on an ephemeral port (ENVIRONMENT === "test" → port 0).
// bootstrap owns the `afterAll` that closes `thx.server`; consumers MUST NOT close it.
module.exports = { thx /* set asynchronously in beforeAll */ };
```

From spec/_envi.json:
```json
{ "oid": "07cef9718edaad79b3974251bb5ef4aedca58703142e8c4c48c20f96cda4979c", ... }
```

From thinx-core.js (regression-target surface):
```javascript
// line 417: this.server = http.createServer(app).listen(listenPort, "0.0.0.0", function () { ... });
// line 466: this.server.on('upgrade', function (request, socket, head) { ... });
// line 484: socket.write('HTTP/1.1 401 Unauthorized\r\n\r\n'); // unauthorized branch
// line 500: wss.handleUpgrade(request, socket, head, function (ws) { ... });
```

The `ws` npm package is already a runtime dep (used at thinx-core.js:274 — `var WebSocket = require("ws");`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</name>
  <files>spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</files>
  <read_first>
    - spec/helpers/bootstrap.js (the spec shares this bootstrap; never call `thx.server.close()`)
    - spec/_envi.json (read `envi.oid` — used as the `<owner>` URL segment; do NOT mutate)
    - spec/jasmine/ZZ-WebSocketLifecycleSpec.js (Phase 6 precedent for the bootstrap usage + 30000ms timeout idiom; note its raw-socket approach — this spec DIVERGES by using the `ws` client)
    - thinx-core.js (lines 460-516 only — the WebSocket upgrade handler; line 466 is the regression target referenced in the spec header)
    - .planning/codebase/TESTING.md (sections "Bootstrap", "Test Structure", "Common Patterns: Console markers" — verify the `🚸 [chai] >>>` marker idiom and the `}, 30000);` per-`it()` timeout)
    - .planning/phases/12-code-side-closure-helpers/12-CONTEXT.md (D-26 through D-37 — locked spec shape)
  </read_first>
  <behavior>
    - Test 1 (positive): `it("GET /<owner> (WebSocket upgrade)", ...)` opens `ws://127.0.0.1:<port>/<envi.oid>` and `expect(true).to.equal(true); done();` inside `ws.on('open', ...)` within an internal 5s timer; jasmine timeout `}, 30000);`.
    - Test 2 (positive): `it("GET /<owner>/<timestamp> (rtm-style WebSocket upgrade)", ...)` opens `ws://127.0.0.1:<port>/<envi.oid>/<Date.now()>` with the same assertion + cleanup pattern.
    - Implicit negative-case proof: if `thinx-core.js:466` (`server.on('upgrade')`) is removed, the `open` event never fires and the spec times out at 30000ms → spec FAILS. Documented in the file header.
    - Cleanup: `ws.close()` in `afterEach`. Bootstrap owns `thx.server` lifecycle — do NOT close it.
  </behavior>
  <action>
    Create a new Jasmine spec file at the path above. Required structure (concrete identifiers per D-26..D-37):

    Header doc comment must explicitly reference:
    - "TEST-WS-01 — WebSocket handshake CI smoke probe (regression coverage for SEC-WS-01)"
    - The regression target: `thinx-core.js:466` (`this.server.on('upgrade', function (request, socket, head) { ... })`)
    - The implicit-negative-case proof: "If the upgrade handler at thinx-core.js:466 is removed, this spec times out at 30000ms → FAIL"
    - The chai-http v4 LOCK rationale: "Uses `ws` package because chai-http v4 lacks WebSocket upgrade support (AGENTS.md:82-92 LOCK)"
    - The Phase 5 Test-env ACCEPT pattern: "Local `npm test` aborts on missing `/mnt/data/conf/config.json`; canonical green-gate is Jasmine inside the CircleCI Docker test image"

    Requires (top of file): `const bootstrap = require('../helpers/bootstrap');`, `const chai = require('chai');`, `const expect = require('chai').expect;`, `const WebSocket = require('ws');`, `const envi = require('../_envi.json');`.

    `describe("WebSocket Handshake (rtm)", function () { ... })` body:
    - Top-level `let thx;` and `let openSockets = [];`
    - `beforeAll((done) => { console.log("🚸 [chai] >>> running WebSocket Handshake (rtm) spec"); thx = bootstrap.thx; done(); });` (no timeout arg — synchronous after bootstrap.thx is already set)
    - `afterAll(() => { console.log("🚸 [chai] <<< completed WebSocket Handshake (rtm) spec"); });` — do NOT call `thx.server.close()`.
    - `afterEach(() => { while (openSockets.length) { const s = openSockets.pop(); try { s.close(); } catch (_e) {} } });`
    - Helper inside the describe: `function getPort() { const addr = thx.server && thx.server.address(); return addr && addr.port ? addr.port : null; }`
    - `it("GET /<owner> (WebSocket upgrade)", function (done) { const port = getPort(); if (!port) { done(); return; } const url = "ws://127.0.0.1:" + port + "/" + envi.oid; const ws = new WebSocket(url); openSockets.push(ws); const openTimer = setTimeout(() => { try { ws.close(); } catch (_e) {} done(new Error("WebSocket 'open' event did not fire within 5000ms — regression at thinx-core.js:466 server.on('upgrade')")); }, 5000); ws.on('open', () => { clearTimeout(openTimer); expect(true).to.equal(true); done(); }); ws.on('error', (_err) => { /* swallow — open or timeout decides */ }); }, 30000);`
    - `it("GET /<owner>/<timestamp> (rtm-style WebSocket upgrade)", function (done) { ... });` — identical body shape; URL is `"ws://127.0.0.1:" + port + "/" + envi.oid + "/" + Date.now()`.

    All identifiers locked verbatim per D-26..D-37. Do NOT add a third negative `it()` block (per D-32). Do NOT introduce sinon/jest/promises beyond what is shown (per TESTING.md "Anti-patterns to avoid"). Do NOT lower the 30000ms timeout (per TESTING.md "Patterns enforced"). Do NOT change `console.log` to anything else for the markers (per TESTING.md "Common Patterns: Console markers"). Spec file MUST be GPG-signed in the final commit (per D-42).

    **bootstrap.thx contract preservation (WARN-03)** — Add a top-of-file doc comment IMMEDIATELY AFTER the header doc block (before the requires) with this exact verbatim text (case-sensitive — the grep gate is case-insensitive but the canonical form is mixed case):

    ```
    // IMPORTANT: do not move the require('../helpers/bootstrap') into a function body.
    // bootstrap.thx is asynchronously populated by bootstrap.js's beforeAll, which is
    // registered AT REQUIRE-TIME by Jasmine. Moving the require() into a nested scope
    // (beforeAll body, helper function, dynamic import, etc.) unregisters bootstrap's
    // beforeAll for this spec → `thx` stays null → handshake test fails silently with
    // "WebSocket 'open' event did not fire within 5000ms" but for the WRONG reason.
    // The top-level `require('../helpers/bootstrap')` is load-bearing.
    ```

    This comment guards against a future maintainer refactoring the require into a function body without realizing bootstrap.thx's registration semantics. The grep gate `grep -ci "do not move the require" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` must return ≥ 1.
  </action>
  <verify>
    <automated>node --check spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>test -f spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "ws.on('open'" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "}, 30000);" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "🚸 \[chai\] >>> running WebSocket Handshake (rtm) spec" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "🚸 \[chai\] <<< completed WebSocket Handshake (rtm) spec" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "thinx-core.js:466" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "require('../helpers/bootstrap')" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "envi.oid" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -c "thx.server.close" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
    <automated>grep -ci "do not move the require" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` exits 0
    - `test -f spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` exits 0 (per D-26 spec-path lock)
    - `grep -c "ws.on('open'" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` ≥ 2 (one per positive `it()` block, per D-29 / D-31)
    - `grep -c "}, 30000);" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` ≥ 2 (one per `it()` block, per D-34)
    - `grep -c "🚸 \[chai\] >>> running WebSocket Handshake (rtm) spec" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` = 1 (per D-35)
    - `grep -c "🚸 \[chai\] <<< completed WebSocket Handshake (rtm) spec" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` = 1 (per D-35)
    - `grep -c "thinx-core.js:466" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` ≥ 1 (header documents regression target, per D-32)
    - `grep -c "require('../helpers/bootstrap')" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` = 1 (per D-28)
    - `grep -c "envi.oid" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` ≥ 2 (used in both positive `it()` blocks, per D-36)
    - `grep -c "thx.server.close" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` = 0 (bootstrap owns server lifecycle per D-28/D-33)
    - `grep -c "require('chai-http')" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` = 0 (uses `ws` client per D-27)
    - `grep -c "new WebSocket(" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` ≥ 2 (per D-27)
    - File contains exactly 2 `it(` blocks (no negative-case block per D-32)
    - `grep -ci "do not move the require" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` ≥ 1 (bootstrap.thx contract guard comment present per WARN-03 — protects against future maintainer moving the top-level require() into a function body and silently breaking the Jasmine beforeAll registration order)
    - No `wss://rtm.thinx.cloud` reference or live-rtm probe code (per D-37; live rtm probe deferred to Phase 13 OPS-EXEC-01)
  </acceptance_criteria>
  <done>
    File `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` exists, syntactically clean, contains 2 positive `it()` blocks per D-29, uses `ws` client per D-27, reuses `bootstrap.thx` per D-28, documents `thinx-core.js:466` regression target per D-32, ends each `it()` with `}, 30000);` per D-34, emits `🚸 [chai]` markers per D-35. No mutation of `spec/_envi.json` (per D-36). CircleCI green-gate is canonical for the spec PASS (per D-41).
  </done>
</task>

<task type="auto">
  <name>Task 2: GPG-sign atomic commit</name>
  <files>spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js</files>
  <read_first>
    - .planning/phases/12-code-side-closure-helpers/12-CONTEXT.md (D-42 GPG-sign default)
    - .planning/STATE.md (recent commit-message style — `40c755e0 fix(worker): syncing submodule improvements`, `2b3ce898 docs(SEC-DEP-02): mark Phase 10 alerts RESOLVED in dep-triage.md annex`)
  </read_first>
  <action>
    Stage only `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js`. Create exactly one GPG-signed commit on `thinx-staging` with commit message:

    `test(TEST-WS-01): add ZZ-WebSocketHandshakeRtmSpec.js in-process upgrade smoke probe`

    Body (HEREDOC) summarizes:
    - 2 positive `it()` blocks against `bootstrap.thx.server.address().port` using the `ws` client
    - implicit negative-case proof via 30000ms timeout — regression target is `thinx-core.js:466` `server.on('upgrade')`
    - CI green-gate is canonical per Phase 5 Test-env ACCEPT
    - addresses TEST-WS-01

    Do NOT stage anything else. Do NOT use `--no-verify`. Do NOT amend an existing commit. Verify `git log -1 --show-signature` shows the signature.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -c "test(TEST-WS-01)"</automated>
    <automated>git log -1 --show-signature 2>&amp;1 | grep -cE "gpg: Signature|Good signature"</automated>
    <automated>git diff HEAD~1 --name-only | grep -c "spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js"</automated>
    <automated>git diff HEAD~1 --stat | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` matches `^test\(TEST-WS-01\): `
    - `git log -1 --show-signature 2>&1` contains a `gpg:` line (signature present per D-42)
    - `git diff HEAD~1 --name-only` lists exactly one file: `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` (per D-38 file-touch matrix)
    - No file outside `spec/jasmine/` is touched by this commit (per D-39 parallel-safety)
  </acceptance_criteria>
  <done>
    Single atomic GPG-signed commit `test(TEST-WS-01): add ZZ-WebSocketHandshakeRtmSpec.js in-process upgrade smoke probe` lands on `thinx-staging`. Only `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` is in the commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → in-process Express/WS server | Trusted; test code exercises the same process — no external network surface, no auth boundary crossed |
| spec → `spec/_envi.json` | Read-only consumption of a static test fixture; no PII (Jára Cimrman is the fictional test identity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | spec/_envi.json | mitigate | Spec is read-only against the fixture — TESTING.md "Anti-patterns to avoid" forbids mutation; verified by `grep -c "envi.oid =" spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` = 0 |
| T-12-01-02 | Denial of Service | thx.server | accept | Spec opens up to 2 WS connections per test run against an in-process server bound to 127.0.0.1 ephemeral port; `afterEach` closes all open sockets; no risk to production surface |
| T-12-01-03 | Information Disclosure | console logs | accept | Spec emits only the locked `🚸 [chai]` markers + Jasmine assertion output; no PII, no credentials, no doc IDs |

Severity assessment: pure test code; no production threat surface introduced. All threats `low` severity, all mitigated by the locked invariants in `must_haves.truths` and verified by the grep gates in `<acceptance_criteria>`.

No `high`-severity threats identified — plan is not blocked by the security gate.

No npm/pip/cargo install tasks in this plan; the Package Legitimacy Gate does not apply (the `ws` package is already a runtime dep at thinx-core.js:274).
</threat_model>

<verification>
After both tasks complete:

1. **Static gates (local, per D-41 Test-env ACCEPT):**
   - `node --check spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` exits 0
   - All `<acceptance_criteria>` grep gates pass
   - `git log -1 --show-signature` shows the GPG signature

2. **Canonical green-gate (CI per D-41):**
   - CircleCI Jasmine inside the Docker test image runs the spec PASS as part of the `ZZ-*` integration set
   - The spec's PASS on `thinx-staging` is the closure evidence for TEST-WS-01 validation criteria (b) ("the spec passes in CI on `thinx-staging`")

3. **Negative-case proof (documented, not executed):**
   - The implicit-negative-case proof at `thinx-core.js:466` is documented in the spec header — no separate test step needed per D-32
</verification>

<success_criteria>
- TEST-WS-01 ROADMAP requirement closed:
  - (a) Spec FAILS when `server.on('upgrade')` at thinx-core.js:466 is removed → implicit via 30000ms timeout per D-32
  - (b) Spec PASSES in CI on `thinx-staging` → CircleCI Jasmine green-gate
  - (c) Spec runs in-process only — no swarm-side resource touched → verified by file content (only `ws://127.0.0.1:<port>/...` URLs); live rtm-edge probe deferred per D-37
- Spec follows TESTING.md conventions: `bootstrap.thx` (per D-28), 30000ms timeouts (per D-34), `🚸 [chai]` markers (per D-35), no chai-http (per LOCK / D-27), no fixture mutation (per D-36)
- Single GPG-signed commit lands on `thinx-staging` per D-42
- No file outside `spec/jasmine/ZZ-WebSocketHandshakeRtmSpec.js` is modified — zero file overlap with plans 12-02 and 12-03 (verifies D-38 / D-39 parallel-safety)
</success_criteria>

<output>
Create `.planning/phases/12-code-side-closure-helpers/12-01-SUMMARY.md` when done.
</output>
