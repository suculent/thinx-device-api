{
	"_id": "_design/users",
	"language": "javascript",
	"views": {
		"owners_by_username": {
			"map": "function (doc) { if(doc.username) { emit (doc.username, doc.password); } }"
		},
		"owners_by_email": {
			"map": "function (doc) { if(doc.email) { emit (doc.username, doc); } }"
		}
	}
}
